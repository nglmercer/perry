# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**NOTE**: Keep this file concise. Detailed changelogs live in CHANGELOG.md.

## Project Overview

Perry is a native TypeScript compiler written in Rust that compiles TypeScript source code directly to native executables. It uses SWC for TypeScript parsing and LLVM for code generation.

**Current Version:** 0.5.603


## TypeScript Parity Status

Tracked via the gap test suite (`test-files/test_gap_*.ts`, 28 tests). Compared byte-for-byte against `node --experimental-strip-types`. Run via `/tmp/run_gap_tests.sh` after `cargo build --release -p perry-runtime -p perry-stdlib -p perry`.

**Last full sweep:** run `./run_parity_tests.sh` for the current snapshot — the gap-suite top-line has shifted with recent landings; see open issues #447 (nested-async hang masking `test_async`), #448 (`*[Symbol.iterator]()` class hang), #449 (`new.target` → NaN), #450 (`defineProperty` accessor `this`), #451 (`test_gap_json_advanced` SIGSEGV), and #452 (`Array.fromAsync` / Proxy/Reflect / JSON ordering). Several of these are pre-existing bugs that the parity skip-list and gap-suite output truncation hid from earlier numbers.

**Known categorical gaps**: lookbehind regex (Rust `regex` crate), `console.dir`/`console.group*` formatting, lone surrogate handling (WTF-8).

## Workflow Requirements

**IMPORTANT:** Follow these practices for every code change made directly on `main` (maintainer workflow):

1. **Update CLAUDE.md**: Add 1-2 line entry in "Recent Changes" for new features/fixes
2. **Increment Version**: Bump patch version (e.g., 0.5.48 → 0.5.49)
3. **Commit Changes**: Include code changes and CLAUDE.md updates together

### External contributor PRs

PRs from outside contributors should **not** touch `[workspace.package] version` in `Cargo.toml`, the `**Current Version:**` line in `CLAUDE.md`, or add a "Recent Changes" entry. The maintainer bumps the version and writes the changelog entry at merge time — usually by rebasing the PR branch and amending. This avoids the patch-version collisions that happen when Perry's `main` ships several commits while a PR is in review (each on-main commit bumps the version; a PR that bumped to the same patch on day 1 is already behind by merge day). Contributors just write code; let the maintainer fold in the metadata last.

## Build Commands

```bash
cargo build --release                          # Build all crates
cargo build --release -p perry-runtime -p perry-stdlib  # Rebuild runtime (MUST rebuild stdlib too!)
cargo test --release --workspace \
  --exclude perry-ui-ios --exclude perry-ui-tvos --exclude perry-ui-watchos \
  --exclude perry-ui-visionos --exclude perry-ui-android --exclude perry-ui-windows \
  --exclude perry-ui-gtk4   # Run tests (exclude cross-host UI crates on macOS)
cargo run --release -- file.ts -o output && ./output    # Compile and run TypeScript
cargo run --release -- file.ts --print-hir              # Debug: print HIR
```

## Architecture

```
TypeScript (.ts) → Parse (SWC) → AST → Lower → HIR → Transform → Codegen (LLVM) → .o → Link (cc) → Executable
```

| Crate | Purpose |
|-------|---------|
| **perry** | CLI driver (parallel module codegen via rayon) |
| **perry-parser** | SWC wrapper for TypeScript parsing |
| **perry-types** | Type system definitions |
| **perry-hir** | HIR data structures (`ir.rs`) and AST→HIR lowering (`lower.rs`) |
| **perry-transform** | IR passes (closure conversion, async lowering, inlining) |
| **perry-codegen** | LLVM-based native code generation |
| **perry-runtime** | Runtime: value.rs, object.rs, array.rs, string.rs, gc.rs, arena.rs, thread.rs |
| **perry-stdlib** | Node.js API support (mysql2, redis, fetch, fastify, ws, etc.) |
| **perry-ui** / **perry-ui-macos** / **perry-ui-ios** / **perry-ui-tvos** | Native UI (AppKit/UIKit) |
| **perry-jsruntime** | JavaScript interop via QuickJS |

## NaN-Boxing

Perry uses NaN-boxing to represent JavaScript values in 64 bits (`perry-runtime/src/value.rs`):

```
TAG_UNDEFINED = 0x7FFC_0000_0000_0001    BIGINT_TAG  = 0x7FFA (lower 48 = ptr)
TAG_NULL      = 0x7FFC_0000_0000_0002    POINTER_TAG = 0x7FFD (lower 48 = ptr)
TAG_FALSE     = 0x7FFC_0000_0000_0003    INT32_TAG   = 0x7FFE (lower 32 = int)
TAG_TRUE      = 0x7FFC_0000_0000_0004    STRING_TAG  = 0x7FFF (lower 48 = ptr)
```

Key functions: `js_nanbox_string/pointer/bigint`, `js_nanbox_get_pointer`, `js_get_string_pointer_unified`, `js_jsvalue_to_string`, `js_is_truthy`

**Module-level variables**: Strings stored as F64 (NaN-boxed), Arrays/Objects as I64 (raw pointers). Access via `module_var_data_ids`.

## Garbage Collection

Generational mark-sweep GC in `crates/perry-runtime/src/gc.rs` (default since v0.5.237 / Phase D). Two regions in the per-thread arena: nursery (`ARENA`, fills with new allocations, swept on minor GC) and old-gen (`OLD_ARENA`, holds tenured/evacuated objects). Conservative stack scan + precise shadow-stack roots + 9 registered scanners. Write barriers populate a remembered set so minor GC can avoid retracing the old-gen. Two-bit aging (`HAS_SURVIVED` / `TENURED`) promotes nursery survivors after 2 minor cycles; the C4b evacuation pass moves non-pinned tenured objects into old-gen with full reference rewriting. Idle nursery blocks observed empty for 2 GC cycles are `dealloc`'d back to the OS (C4b-δ, v0.5.235), and the next-trigger calc is hard-capped at the initial threshold (64 MB) so >90%-freed step-doubling can't blow up peak occupancy (C4b-δ-tune, v0.5.236). Triggers on arena block allocation (1 MB blocks since v0.5.196), malloc count threshold, or explicit `gc()` call. 8-byte GcHeader per allocation.

**Escape hatches**: `PERRY_GEN_GC=0`/`off`/`false` reverts to full mark-sweep (bisection only). `PERRY_GEN_GC_EVACUATE=1` enables the copying evacuation pass (default OFF — complete and correctness-safe but adds work that's a no-op on workloads where nothing tenures). `PERRY_WRITE_BARRIERS=1` opts into codegen-emitted write barriers (default OFF — barrier emission has its own perf cost; the runtime barrier always exists). `PERRY_GC_DIAG=1` prints per-cycle diagnostics.

## Threading (`perry/thread`)

Single-threaded by default. `perry/thread` provides:
- **`parallelMap(array, fn)`** / **`parallelFilter(array, fn)`** — data-parallel across all cores
- **`spawn(fn)`** — background OS thread, returns Promise

Values cross threads via `SerializedValue` deep-copy. Each thread has independent arena + GC. Results from `spawn` flow back via `PENDING_THREAD_RESULTS` queue, drained during `js_promise_run_microtasks()`.

## Native UI (`perry/ui`)

Declarative TypeScript compiles to AppKit/UIKit calls. Handle-based widget system (1-based i64 handles, NaN-boxed with POINTER_TAG). `--target ios-simulator`/`--target ios`/`--target tvos-simulator`/`--target tvos` for cross-compilation.

**To add a new widget** — change 4 places:
1. Runtime: `crates/perry-ui-macos/src/widgets/` — create widget, `register_widget(view)`
2. FFI: `crates/perry-ui-macos/src/lib.rs` — `#[no_mangle] pub extern "C" fn perry_ui_<widget>_create`
3. Codegen: `crates/perry-codegen/src/codegen.rs` — declare extern + NativeMethodCall dispatch
4. HIR: `crates/perry-hir/src/lower.rs` — only if widget has instance methods

## Compiling npm Packages Natively (`perry.compilePackages`)

Configured in `package.json`:
```json
{ "perry": { "compilePackages": ["@noble/curves", "@noble/hashes"] } }
```
First-resolved directory cached in `compile_package_dirs`; subsequent imports redirect to the same copy (dedup).

## Known Limitations

- **No runtime type checking**: Types erased at compile time. `typeof` via NaN-boxing tags. `instanceof` via class ID chain.
- **No shared mutable state across threads**: No `SharedArrayBuffer` or `Atomics`.

## Common Pitfalls & Patterns

### NaN-Boxing Mistakes
- **Double NaN-boxing**: If value is already F64, don't NaN-box again. Check `builder.func.dfg.value_type(val)`.
- **Wrong tag**: Strings=STRING_TAG, objects=POINTER_TAG, BigInt=BIGINT_TAG.
- **`as f64` vs `from_bits`**: `u64 as f64` is numeric conversion (WRONG). Use `f64::from_bits(u64)` to preserve bits.

### LLVM Type Mismatches
- Loop counter optimization produces i32 — always convert before passing to f64/i64 functions
- Constructor parameters always f64 (NaN-boxed) at signature level

### Async / Threading
- Thread-local arenas: JSValues from tokio workers invalid on main thread
- Use `spawn_for_promise_deferred()` — return raw Rust data, convert to JSValue on main thread
- Async closures: Promise pointer (I64) must be NaN-boxed with POINTER_TAG before returning as F64

### Cross-Module Issues
- ExternFuncRef values are NaN-boxed — use `js_nanbox_get_pointer` to extract
- Module init order: topological sort by import dependencies
- Optional params need `imported_func_param_counts` propagation through re-exports

### Closure Captures
- `collect_local_refs_expr()` must handle all expression types — catch-all silently skips refs
- Captured string/pointer values must be NaN-boxed before storing, not raw bitcast
- Loop counter i32 values: `fcvt_from_sint` to f64 before capture storage

### Handle-Based Dispatch
- TWO systems: `HANDLE_METHOD_DISPATCH` (methods) and `HANDLE_PROPERTY_DISPATCH` (properties)
- Both must be registered. Small pointer detection: value < 0x100000 = handle.

### objc2 v0.6 API
- `define_class!` with `#[unsafe(super(NSObject))]`, `msg_send!` returns `Retained` directly
- All AppKit constructors require `MainThreadMarker`

## Recent Changes

One-liners only — full detail in CHANGELOG.md.

- **v0.5.603** — Refs #519 / #421 (hono fetch chain — Web Fetch API typed-parameter native-instance registration): the v0.5.601 IMPLICIT_THIS fix unblocked hono's `RegExpRouter.match` chain, but the next blocker downstream — `app.fetch(req)` reading `req.url` inside hono's compiled `fetch` arrow — surfaced because `Request` / `Response` / `Headers` parameter type annotations weren't registered as native instances at HIR lowering. Without the registration, `r.url` on a typed `(r: Request)` parameter went through the generic-object-property-get fallback which tried to interpret the runtime handle id (a small integer like `1.0`) as an ObjectHeader pointer — `unbox_to_i64` of `1.0` returns 0, so `js_object_get_field_by_name(NULL, "url")` returned undefined, and downstream `url.indexOf("/")` threw `Cannot read properties of undefined (reading 'indexOf')`. Fix is one block in each of `crates/perry-hir/src/lower/expr_function.rs` (arrow params, line 67+) and `crates/perry-hir/src/lower_decl.rs` (function-decl params, line 292+): when a parameter's `Type::Named` matches `"Request"` / `"Response"` / `"Headers"`, call `ctx.register_native_instance` with the same module/class strings the local-init paths in `destructuring.rs` use for `const r = new Request(…)`. After registration, codegen's `Request.url` / `Response.status` / `Headers.get` static dispatches at `lower_call.rs:3503+` fire and route directly to `js_request_get_url(handle)` etc. — the runtime handle reaches the getter as a raw f64, where `handle as usize` correctly recovers the registry id. End-to-end smoke verified with the minimal repro `function takeReq(r: Request): string { return r.url }` — pre-fix returned `undefined`, post-fix returns the URL. Hono itself is still blocked because its compiled JS (via compilePackages) has TypeScript types stripped, so `(request) =>` (no annotation) doesn't trigger the registration; proper hono support needs a runtime-side handle-detection path (separate, larger followup — likely NaN-boxing handle returns with POINTER_TAG or detecting raw small-integer values as handles in the property-get dispatcher). Parity sweep: 210 pass / 3 known fails / 13 skipped (98.5%, no regressions vs main).

- **v0.5.602** — **Closes #509** (parallel-codegen race producing colliding object files): two rayon codegen workers compiling sibling modules within the same microsecond ended up writing to the same `/tmp/perry_llvm_<pid>_<nanos>.ll` path. Both overwrote the same `.ll`, both `clang -c` invocations compiled the same IR, both read back the same `.o` bytes — leaving sibling `.o` files (e.g. `consumer_type_only.o` and `consumer_value.o`) with one module's symbols stamped onto the other's filename. Linker then rejected the unresolved cross-module symbols (`_perry_fn_..._consumeTypeOnly`, `_fixtures_issue_446_pkg_consumer_type_only_ts__init`). Surfaced after v0.5.589 (closure rest registry shifted codegen timing into the failure window); the underlying race had been latent since `compile_ll_to_object` was written. Root cause in `crates/perry-codegen/src/linker.rs::compile_ll_to_object`: the temp-path nonce was `SystemTime::now().duration_since(UNIX_EPOCH).as_nanos()`, but macOS `SystemTime` resolves to microseconds — so two threads racing the path construction within ~1µs produced byte-identical `nanos` values. Fix: new module-level `static TEMP_NONCE_COUNTER: AtomicU64 = AtomicU64::new(0)` is `fetch_add(1, Relaxed)`-incremented on every call and mixed into the path alongside the existing pid + wall-clock nanos. The atomic counter guarantees in-process uniqueness regardless of clock resolution; pid + wall nanos preserve cross-process distinctness for two `perry` invocations sharing `/tmp`. New regression test `temp_nonce_counter_is_unique_across_concurrent_calls` spawns 16 threads × 16 fetches and asserts all 256 values are distinct. With the fix, `test_issue_446_import_type_method_typeof` and `test_issue_310_namespace_reexport` (the two `known_failures.json` entries tracking this race) link cleanly and produce correct output; both removed from the skip list.

- **v0.5.601** — **Closes #519** (hono `app.fetch()` chain unblocked: cross-instance method-style calls now bind `this` correctly): `instance.fn(args)` where `fn` is a closure-typed class field — hono's `RegExpRouter { match = match; }` (where `match` is the imported function declaration from matcher.js) — was reaching its function body with `this = 0` because perry's closure ABI takes `(closure_ptr, arg0, …)` with no `this` slot. The function body's `this.buildAllMatchers()` then fell through to issue #510's catch-all `(number).buildAllMatchers is not a function`. Three coordinated fixes:

  **(1)** New thread-local `IMPLICIT_THIS` (`crates/perry-runtime/src/object.rs::31+`, `Cell<u64>` initialized to `TAG_UNDEFINED`) plus `js_implicit_this_get/set(value: f64) -> f64` extern "C" helpers. `set` returns the previous value so callers can save+restore around the binding. Defaults to undefined, matching JS strict-mode top-level semantics.

  **(2)** Codegen for `Expr::This` (`crates/perry-codegen/src/expr.rs::4067`) — when the lexical `this_stack` is empty (top-level module code, top-level function declarations, or non-arrow function expressions without `captures_this`), call `js_implicit_this_get` instead of returning the 0.0 sentinel. Class methods and arrow closures with `captures_this` keep their existing `this_slot`-based path unchanged. Runtime decls in `runtime_decls.rs::2024+` so the LLVM module declares the symbol.

  **(3)** Two complementary call-site bindings save+set+call+restore IMPLICIT_THIS around the dispatch:
    - `crates/perry-runtime/src/object.rs::js_native_call_method` field-scan path (around line 4197-4250): when the receiver is an object whose `keys_array` contains a closure-valued field matching the method name, save IMPLICIT_THIS, set it to the receiver, call the closure via `js_native_call_value`, restore. Covers the dispatch path for any-typed receivers + Map/Set field access.
    - `crates/perry-codegen/src/lower_call.rs` closure-call fallback (around line 2270): when the callee shape is `recv.method(args)` (a `PropertyGet`) and we're emitting `js_closure_call<N>`, also emit `js_implicit_this_set(recv)` before the call and `js_implicit_this_set(prev)` after. Skips `Expr::GlobalGet` / `Expr::NativeModuleRef` / `Expr::ExternFuncRef` receivers since those aren't user objects whose identity should propagate as `this`. Direct function calls (`fn(args)`) leave IMPLICIT_THIS untouched.

  **Follow-on fix** in the same release: `js_native_call_method`'s string-receiver dispatch arm (`crates/perry-runtime/src/object.rs::4082+`) was missing `match` / `matchAll` / `search`. Hono's matcher.js uses `path2.match(matcher[0])` where `matcher[0]` is a regex-typed `Expr::Member` access — perry's HIR-level `Expr::StringMatch` optimistic lowering only fires when the regex arg is a literal or a static `RegExp`-typed `Ident`, so the `Member` form fell through to the dynamic dispatch and hit the issue #510 catch-all (`(string).match is not a function`). Added the three method arms routing through `crate::regex::js_string_match{,_all}` and `js_string_search_regex` — same dispatch shape the HIR-level path uses.

  End-to-end smoke verified: hono's `app.router.match('GET', '/')` chain now runs through `SmartRouter.match` → `RegExpRouter.match` (the imported `match` function with `this` correctly bound to the RegExpRouter instance) → `this.buildAllMatchers()` → returns the empty-match `[ [], [] ]` (correct shape; route registration is a separate downstream concern). `await app.fetch(req)` progresses past the previous TypeError; the next blocker downstream is `Cannot read properties of undefined (reading 'indexOf')` inside the dispatch chain — separate followup, not part of #519. The minimal repro `class Foo { match = plainFn; } const f = new Foo(); f.match()` correctly observes `this === f` (was `this = 0` pre-fix). Parity sweep: 210 pass / 3 known fails / 13 skipped (98.5%, no regressions vs main; the 3 fails are pre-existing `known_failures.json` entries). The runtime-only IMPLICIT_THIS approach intentionally doesn't enforce JS spec for nested non-method calls (`f()` inside an outer method-style call still sees the outer's `this` since direct calls don't reset IMPLICIT_THIS) — that's an incremental gap the codegen could close by emitting `js_implicit_this_set(undefined)` on direct-call sites if a future test demands it; today's pre-fix behavior was "return 0.0" for all such cases, so any test that depended on the looser observed behavior keeps working.

- **v0.5.600** — Closes #461 (bare `_pull` symbol — final residual link error from the Effect repro): a `const f = (...) => …` arrow declaration inside a function body wasn't pre-registered as a local before its initializer was lowered, so any nested closure inside that initializer that referenced `f` couldn't find it via `lookup_local` and fell through to `lookup_imported_func`, which returned the namespace import shadowed by the local — Effect's `internal/stream.ts` has `import * as pull from "./stream/pull.js"` at the top AND `const pull = (state, ...) => { … }` declared inside three exported functions; nested arrows like `onSuccess: (chunk) => { … return pull(...) … }` lowered the inner `pull(...)` call to `Expr::ExternFuncRef { name: "pull" }`, which `lower_call.rs::ExternFuncRef`'s "Native library functions" fallback then emitted as a direct LLVM call to the bare symbol `@pull`, surfacing at link time as `Undefined symbol: _pull, referenced from _perry_closure_…__N` (~7 closures across stream.ts). Function declarations (`function f() {}`) already pre-register their local id BEFORE lowering the body (`lower_decl.rs:3138`), so self-recursive `function fac(n) { return n < 2 ? 1 : n * fac(n-1) }` works — `const fac = (n) => …` didn't get the same treatment. Fix is one block in `crates/perry-hir/src/destructuring.rs::lower_var_decl_with_destructuring`'s `Pat::Ident` arm just before the existing `lower_expr(init)` call: detect `Some(ast::Expr::Arrow(_)) | Some(ast::Expr::Fn(_))` init shape and `define_local(name, ty)` ahead-of-time when the local isn't already module-pre-registered or in scope; pass the resulting LocalId through to the `Stmt::Let` so the slot the closure stores into is the same slot the closure body reads from. Gated on function-expr init only — `const x = x + 1` (TDZ violation) and other non-function inits keep their original behavior since they DO need to reach a runtime error rather than silently self-reference. Closure body doesn't execute until call time, so by the time the body's `LocalGet(id)` reads the slot, the Let stmt has stored the closure into it. End-to-end smoke verified: 4-line minimal repro (`import * as pull from "./pull"; export const outer = (x) => { const pull = (state) => { … pull(...) … }; return pull(1); }` + a main that calls `outer(10)` / `outer(20)`) compiled cleanly post-fix and returned `101` / `101` matching Node; the original simpler shape (3 inner closures each calling `pull(N)`) returned `11 / 12 / 13` matching Node. Direct self-recursion (`const fac = (n) => n < 2 ? 1 : n * fac(n-1)`) returned `120` / `3628800` for `fac(5)` / `fac(10)`. Function-expression init `const fib = function(n) { … }` returned `55` for `fib(10)`. Effect's `bun add effect && perry compile test_minimal.ts`: `_pull` no longer appears in the linker's undefined-symbol list (was the v0.5.525 holdout — Order/Union/Emit landed in v0.5.525, `_pull` was the carved-out followup); remaining undefined symbols are all `_perry_fn_node_modules_effect_src_{Cron,Duration,Graph}_ts__*` cross-module references unrelated to #461 (Cron/Duration/Graph modules don't get their `_init` symbols emitted — separate issue). Parity sweep: 209 pass / 3 known fails / 1 known compile-fail / 13 skipped (98.5%, identical to main); the 3 mismatches and 1 compile-fail are pre-existing entries in `test-parity/known_failures.json`. The fix should also benefit any pre-bundled npm package that uses the `var X = (...) => …; export { X };` pattern where `X` is recursive AND a same-named module is namespace-imported elsewhere — the Effect surface has dozens of such shapes.

- **v0.5.599** — **Closes #518** (async class method returns empty array when awaiting a method-call result on a scalar-replaced object literal): `class Wrapper { async fromMethod() { const cur = makeCursor(); const arr = await cur.toArray(); return arr; } }` returned `[]` (length 0) under `perry run`; the identical body in a free `async function fromFree()` returned the full array. Inserting any non-return use of `arr` between the `await` and `return arr` (e.g. `Array.isArray(arr)`) made the bug disappear, which had Ralph suspecting an async-resume-frame slot drop, but the actual culprit was upstream in the inliner→scalar-replacement→method-dispatch interaction. Root cause: `makeCursor()` is `alwaysinline` and small, so the inliner replaces `cur = makeCursor()` with `cur = { toArray: <closure> }` — a literal object init. `crates/perry-codegen/src/collectors.rs::collect_non_escaping_object_literals` then identifies `cur` as a non-escaping object literal candidate (its only access is `cur.toArray`, a known field), and `stmt.rs` swaps the heap-allocated object for per-field stack allocas. But `cur.toArray()` does NOT lower as a direct closure call — `lower_call.rs::2158+` routes it through `js_native_call_method(receiver, "toArray", args)` because `cur`'s static type isn't a known class. The dispatcher takes `cur` itself as the `this` receiver, which `lower_expr(LocalGet(cur))` loads from the dummy slot the scalar-replacement path leaves uninitialized (`stmt.rs:302-303` allocates a slot but never stores the object's value into it — there IS no object value, only per-field allocas). The receiver becomes uninitialized stack memory; `js_native_call_method` returns `NULL_OBJECT_BYTES` for unknown receivers; the legacy busy-wait await in the async-class-method codegen path treats the non-promise result as a settled value and returns it; the outer caller sees a sentinel array-shaped pointer with `length=0`. The "non-return use forces materialisation" symptom was the escape analysis correctly observing a use that DIDN'T fit the safe-property-access pattern (e.g. `Array.isArray(arr)` — `LocalGet(arr_id)` falls into the `Expr::LocalGet` arm at `collectors.rs:5819` which marks the candidate escaped) and falling back to the heap-allocated path. The bug shows up only in async class methods because async free functions go through `transform_async_to_generator` (`crates/perry-transform/src/async_to_generator.rs:78`, only iterates `module.functions`, never class methods), which restructures the body into a generator state machine where the awaited result flows through `__gen_sent` — a different code path that doesn't hit this mis-optimization. Class methods skip the transform and use the legacy direct busy-wait await path where the bug fires. Fix: extend `check_object_literal_escapes_in_expr` (and its `CallSpread` sibling) at `crates/perry-codegen/src/collectors.rs::5855+` to detect the `Call { callee: PropertyGet { object: LocalGet(id), .. } }` pattern at the call site and mark `id` as escaped — the receiver-in-method-position is a use the scalar-replacement path can't safely handle since it can't reconstruct the object's identity for `js_native_call_method`. Bare property reads (`x = cur.field` in arithmetic / value position) keep the scalar-replacement fast path because they recurse into `Expr::PropertyGet` which evaluates to the field value directly, never the object identity. Tested four reproductions: the original `await cur.toArray()` repro from the issue (full + minimal `return await cur.toArray()` form), `await Promise.resolve([...])` directly (already worked, kept working), and `await makeArr()` for free-function callees (already worked through `js_promise_value` resolution, not this path). Parity sweep: 210 pass / 3 known fails / 0 compile fails / 13 skipped (98.5%, no regressions vs main; the 3 fails are pre-existing `known_failures.json` entries). All workspace cargo tests pass.

- **v0.5.598** — Closes #512: the auto-generated `.d.ts` (`docs/api/perry.d.ts`) and markdown reference (`docs/src/api/reference.md`) used to render every stdlib function as `(...args: any[]): any`, so editors and `tsc` accepted obviously wrong calls — `bcrypt.hash(123, "salt")` passed clean despite the runtime expecting a string for the first arg. Three coordinated fixes: **(1)** `crates/perry-api-manifest/src/lib.rs` grows two public types — `enum TypeSpec { String, Number, Bool, BigInt, Buffer, Handle, Void, Any }` mirrors the param/return-type vocabulary in `docs/src/native-libraries/manifest-v1.md` so the in-tree manifest and external `perry.nativeLibrary` manifests share one model, and `enum ParamSpec { Named { name, ty, optional }, Rest { name, ty } }` carries one slot of a method's signature. `ApiEntry` gains `params: &'static [ParamSpec]` + `returns: TypeSpec`; existing const-fn helpers (`method` / `property` / `class`) default both to `&[]` / `Any`, and a new `method_sig` helper takes the typed shape so backfilled rows stay terse. **(2)** `crates/perry-api-manifest/src/entries.rs` backfills 110 module-level (no-receiver, no class_filter) rows from `NATIVE_MODULE_TABLE`. The mapping is `NA_STR → String`, `NA_F64`/`NA_PTR`/`NA_JSV → Any` (these slots accept arbitrary NaN-boxed JSValues — narrowing them would lie), `NA_VARARGS → Rest(Any)`; returns map `NR_STR → String`, `NR_BIGINT → BigInt`, `NR_PTR`/`NR_F64 → Any` (Promise-typed and handle returns vary too widely to claim a concrete type), `NR_I32`/`NR_VOID → Void`. Hand-curated overrides for the rows where the dispatch table's `NA_F64` actually means "a string is what the user passes" — `bcrypt.hash` / `bcrypt.compare` / `argon2.hash` / `argon2.verify` / `jsonwebtoken.{sign,verify,decode}` / `cron.{validate,schedule,describe}` / `validator.is*` / `uuid.{v4,v1,v7,validate}` / `nanoid.nanoid` get tightened to `string` first-args, and `validator.is*` / `cron.validate` / `uuid.validate` get `boolean` returns. Hand-listed entries (crypto / os / path / process / classes / properties / perry/*) stay on the `&[] / Any` default since their dispatch goes through custom `Expr::*` HIR variants whose param types vary per call site — separate followup. **(3)** `crates/perry-api-manifest/src/emit.rs::emit_dts` renders real signatures via a new `render_signature(entry)` helper that walks `entry.params` + `entry.returns`. The fallback shape is preserved: when `params` is empty AND `returns` is `Any`, the emitter still prints `(...args: any[]): any` so un-typed entries don't regress. Two new tests gate the behavior — `dts_bcrypt_hash_has_real_signature` asserts the bcrypt block contains `export function hash(password: string` and does NOT contain the loose `(...args: any[])` fallback; `dts_uuid_v4_has_no_args` asserts `uuid.v4` renders as `(): string`. **(4)** `crates/perry-codegen/tests/manifest_consistency.rs` gains a parallel `manifest_param_counts_match_dispatch_table` test that walks the auto-derivable rows and asserts the manifest's `params.len()` matches the dispatch table's args arity, AND that no manifest entry tries to NARROW an `NA_STR` slot onto something other than `String` / `Any` (which would let `tsc` accept calls codegen would mis-coerce). Narrowing in the OTHER direction (`NA_F64`/`PTR`/`JSV` slot declared as `String`) is allowed since those FFI paths accept any NaN-boxed JSValue. The test uses the new `arg_kinds` / `ret_kind` fields on `NativeMethodRef` (returned as opaque `"NA_STR"` / `"NR_F64"` tag strings so perry-api-manifest doesn't have to depend on perry-codegen's internal enums). **Acceptance test verified end-to-end**: `bcrypt.hash(123, "salt")` against the regenerated `docs/api/perry.d.ts` now produces `error TS2345: Argument of type 'number' is not assignable to parameter of type 'string'`, exit 1; the same file accepts the correct `bcrypt.hash("password123", 10)` cleanly. Docs regenerated via `scripts/regen_api_docs.sh` so the api-docs-drift CI gate stays green. **Deferred followups**: instance methods (has_receiver=true) and class-filtered rows still hang off `[key: string]: any;` on the bare class — narrowing those needs HIR work to thread receiver types into the manifest. Hand-curated entries (crypto.subtle, os.cpus, path.join, ...) still render with the loose fallback; tightening them is a per-entry pass that depends on what real signatures the runtime functions accept.

- **v0.5.600** — **Closes #513** (followup to #463): API manifest now enumerates every entry in `NATIVE_MODULES` and every routed binding in `well_known_bindings.toml`, so the unimplemented-API gate flips into strict mode for every module Perry advertises support for. Pre-#513 coverage was **397 entries across 45 modules**; post-#513 is **638 entries across 64 modules** — the gap audit found ~20 modules with zero entries (`fs`, `util`, `stream`, `child_process`, `tty`, `http`, `https`, `axios`, `node-fetch`, `bignumber.js`, `node-cron`, `perry/ui`, `perry/system`, `perry/i18n`, `perry/updater`, `perry/media`, `perry/plugin`, `perry/widget`, plus the well-known aliases `redis` / `date-fns` / `streams` / `rate-limiter-flexible` / `fetch` that weren't even registered as native modules). The pre-#513 gate at `crates/perry-hir/src/lower/expr_member.rs:519` is `module_has_any_entries(M) && module_has_symbol(M, prop).is_none()` — modules with zero entries silently fell through to the old permissive behaviour, so e.g. `axios.foo` compiled cleanly and returned `undefined` at runtime, the same class of bug Justin originally hit on `crypto.subtle` in the #455 / #463 thread. Two coordinated changes ship the fix: **(1)** `crates/perry-api-manifest/src/entries.rs` — backfill of ~250 manifest entries covering the gap modules. perry/ui, perry/system, perry/i18n, perry/updater, perry/media are auto-derivable from the matching `PERRY_*_TABLE` in `crates/perry-dispatch/src/lib.rs` (mechanical; the parallel `every_dispatch_entry_has_manifest_counterpart` test guards against drift in either direction now). perry/plugin entries from `PERRY_PLUGIN_TABLE` in `crates/perry-codegen/src/lower_call.rs`. The Node-builtin gap modules (`fs` / `util` / `stream` / `child_process` / `tty` / `buffer` / `url`) have method/class entries reflecting what perry-runtime + perry-stdlib actually implement — `fs` covers the sync surface lowered to `Expr::Fs*` plus the async wrappers + stream constructors; `util` covers `inspect` / `format` / `promisify` / `callbackify` / `deprecate` / `inherits` / `isDeepStrictEqual` plus the TextEncoder / TextDecoder classes; `stream` covers Readable / Writable / Duplex / Transform / PassThrough plus `pipeline` / `finished`; `child_process` covers `exec` / `execSync` / `execFile` / `execFileSync` / `spawn` / `spawnSync` / `fork`; `tty` covers `isatty` + ReadStream / WriteStream classes. The HTTP-client trio (`axios` / `node-fetch` / `http` / `https`) covers the standard verb methods plus class constructors. The well-known aliases (`redis` / `date-fns` / `bignumber.js` / `node-cron` / `streams` / `rate-limiter-flexible` / `fetch`) get entries that mirror their canonical-module surface so `import { Redis } from 'redis'` works the same way as `import { Redis } from 'ioredis'`. **(2)** `crates/perry-codegen/tests/manifest_consistency.rs` — two new reverse-direction drift tests. `every_native_module_has_at_least_one_manifest_entry` walks `NATIVE_MODULES` and asserts each one has at least one manifest entry (allow-list: `dotenv/config`, side-effect-only sub-path with no value binding); `every_well_known_binding_has_manifest_entry` parses `crates/perry/well_known_bindings.toml` and asserts each routed module name has manifest coverage. Both tests fail loudly with the missing module names + the fix recipe, so adding a future native module without manifest entries breaks CI before the PR ships. **(3)** `crates/perry-hir/tests/unimplemented_api_check.rs` — new `every_supported_module_rejects_bogus_member` parity sweep that compiles `import * as m from "<module>"; const x = m.__perry_known_bogus_member_513__;` for every entry in `NATIVE_MODULES` and asserts the R005 / #463 error fires (skip list: `dotenv/config` + the external `tursodb` / `iroh` bindings whose value-binding shape doesn't trigger the gate in the isolated HIR test). The previously-permissive `module_with_no_manifest_entries_is_permissive` test that documented the pre-#513 fall-through is replaced with `supported_module_with_unknown_member_is_rejected`, asserting the new strict behaviour. End-to-end smoke verified: `import * as fs from "fs"; const x = fs.bogusMethod;` errors with `R005`-style message naming the offending property; `import * as ui from "perry/ui"; ui.bogusWidget;` errors identically; legitimate calls like `fs.writeFileSync(p, "ok")` + `path.join("/tmp", "x")` continue to compile + run unchanged. `docs/src/api/reference.md` (now 638 entries / 64 modules) and `docs/api/perry.d.ts` regenerated through `scripts/regen_api_docs.sh` — the `api-docs-drift` CI gate from v0.5.560 keeps these in sync going forward. Parity sweep clean: no regressions vs main (the manifest backfill is additive — modules previously in strict mode keep their existing entries; modules previously in permissive mode flip strict only for `module.unknown_property` shapes, which no parity test legitimately exercises). Closes the credibility gap from #513's "v0.5.585 release implies completeness it doesn't have yet" framing — `--print-api-manifest` now genuinely reflects the supported surface, and a user on `async_hooks` / `dgram` / etc. gets the same #463 compile-time error every other module produces.

- **v0.5.597** — **Closes #511** (followup to #462): `x.foo()` where `x` is `undefined` or `null` now throws `TypeError: Cannot read properties of <undefined|null> (reading '<method>')` and exits 1, instead of silently no-op'ing and exiting 0. Audit for #511's "all uncaught throws should exit non-zero" concern: `js_throw_type_error_property_access`, `js_throw_type_error_not_a_function`, and `exception::js_throw` already call `std::process::exit(1)` on uncaught — and the issue's exact repro (`obj.foo` read on undefined) already exits 1 via the v0.5.526 helper. Hole found: `Call { callee: PropertyGet }` in `crates/perry-codegen/src/lower_call.rs::2208` shortcuts straight to `js_native_call_method(recv, method, ...)` without re-evaluating the receiver through `Expr::PropertyGet`, so the codegen-side nullish gate (`crates/perry-codegen/src/expr.rs::3320+`, issue #462) never fires for the call form. Fix: new arm at `crates/perry-runtime/src/object.rs::js_native_call_method` (between the `match method_name { "toString"/"bind"/"push"/... }` block and the existing `primitive_kind` Issue #510 catch-all) — when `jsval.is_undefined() || jsval.is_null()` and no earlier dispatch fired, call `js_throw_type_error_property_access(is_null, method_name_ptr, method_name_len)` (the same helper #462 / v0.5.526 uses, so message format and exit-code path are identical). The check intentionally lives AFTER the toString/bind/push/pop/length match arms — Perry's existing Perry-ism of `undefined.toString()` → `"undefined"` / `null.toString()` → `"null"` continues to work unchanged (Node throws there too, but a wider tightening would break unrelated callers; this fix surfaces only the catch-all typo case `x.foo()` that previously exited 0). Try/catch interaction inherits `js_throw_type_error_property_access`'s pre-existing `process::exit(1)` direct-call (the helper bypasses the `js_throw` longjmp path even in the v0.5.526 PropertyGet shape — separate, pre-existing limitation tracked outside this issue). End-to-end smoke: `const x: any = undefined; x.foo()` → `TypeError: Cannot read properties of undefined (reading 'foo')`, exit 1; `const y: any = null; y.bar` → unchanged (existing #462 path); `undefined.toString()` → `"undefined"` (Perry-ism preserved); user-level `throw new Error("kaboom")` without try/catch → `Uncaught exception: Error: kaboom`, exit 1 (existing `exception::js_throw` path); legitimate method calls on real objects unchanged. Out-of-scope but related (separate followups): `arr[0]` / `arr[0]()` on undefined still silently returns `undefined` / no-ops via `js_dyn_index_get` and the IndexGet call shortcut — those need an analogous nullish gate in `Expr::IndexGet` lowering and a runtime-side check; not blocking #511 since the issue's stated repro and most-common typo case (method call on undefined) are now fixed. Parity sweep: 209 pass / 3 known fails / 1 known compile fail / 13 skipped (98.5%, no regressions vs main).

- **v0.5.596** — Refs #519 (partial — vtable `this` NaN-boxing): `call_vtable_method` in `crates/perry-runtime/src/object.rs` was bit-casting the raw pointer `this: i64` directly to f64, producing a subnormal float (no NaN-box tag). The dispatched method body's `this` parameter then arrived as a "number" — every nested method call inside the body fell through to the issue #510 catch-all and threw `TypeError: (number).<method> is not a function`. The fix wraps the raw pointer with `POINTER_TAG` (via `JSValue::pointer(ptr).bits()`) before casting to f64 when the bits look like a raw pointer (`0 < bits ≤ 0x0000_FFFF_FFFF_FFFF`); already-NaN-boxed values pass through unchanged. End-to-end with `app.router.match('GET', '/')`: the OUTER call now correctly dispatches to SmartRouter.match with a real instance pointer as `this`, but the INNER `router.match(method, path)` call inside SmartRouter's body (where `router` is `routers[i]` from a class field) still trips a different path that doesn't reach `call_vtable_method` — only the outer dispatch is unblocked, hono's `app.fetch(req)` still throws. The remaining cross-module class-method dispatch issue is tracked in #519. Parity: 208 pass / 3 fail / 2 compile-fail / 13 skipped — the 2 compile-fails are the parallel-codegen race (#509) which gets timing-shifted by every codegen-touching commit; the 3 mismatches are the long-standing pre-existing knowns. Updated `test-parity/known_failures.json` to add `test_issue_310_namespace_reexport` alongside `test_issue_446_import_type_method_typeof` for #509's tracking.

- **v0.5.595** — Closes #515: a class method named `with` with ≥2 params was getting folded into `Expr::ArrayWith` (the ES2023 `Array.prototype.with(idx, value)` builtin), so `obj.with(a, b)` on a `class Demo { with(a, b): this { … return this; } }` returned a typed-array index-replace result instead of dispatching to the user method — `result === obj` came back `false`, `result.val` was `undefined`, and `obj2.with(3, 4).get()` returned a subnormal float as the runtime read garbage out of an array header it didn't have. The 1-param form worked (`Array.prototype.with` requires ≥2 args, so the `args.len() >= 2` gate excluded it) and the user noted "if I change the method name other than `with` it will work" — both clues that the bug was a method-name collision in HIR lowering. Fix is one line in `crates/perry-hir/src/lower/expr_call.rs:4029` — gate the `"with"` arm on `!recv_is_class` (the same predicate the sibling `reduce`/`map`/`filter`/`forEach`/`find`/`findIndex` arms already use to bail when the receiver is a user-defined class instance, a `new ClassName()`, or `this`). The other two `"with"` folds in the same file are already safely scoped: the line-2879 arm sits inside an `is_not_string + !is_user_class_instance` outer guard, the line-3592 arm only fires for inline array literals (`[1,2,3].with(0, 'x')`). The line-3414 imported-variable arm doesn't have `recv_is_class` in scope and the issue's repro doesn't hit it; left as-is. End-to-end smoke against the issue's exact 5-line repro: all 5 lines match expected output (`result === obj: true`, `result.val: 3`, `obj.val: 3`, `chain: 7`, `1-param chain: 10`); `Array.prototype.with` continues to fold correctly on local arrays + inline literals (`[1,2,3,4,5].with(2, 99)` → `[1,2,99,4,5]`, `arr.with(-1, 42)` → `[1,2,3,4,42]`). Parity sweep: 210 pass / 3 known fails / 13 skipped (98.5%, no regressions vs main). The one-line guard happened to land in the eabd514c (#514) commit's working tree as a side effect of the rebase that produced v0.5.594; this commit records the proper attribution + version stamp for #515.

- **v0.5.594** — Closes #514 (followup to v0.5.589/v0.5.591 hono progress): `s[0]` / `s.at(-1)` on a parameter typed `any` that holds a string now correctly returns the indexed character instead of the byte_len-as-subnormal-f64. The pre-fix IndexGet fallback emitted `arr_handle + 8 + idx*8` direct memory reads unconditionally — when the runtime value was a NaN-boxed `*StringHeader`, the offset 8 read landed on the string's `byte_len` field instead of an element slot, returning the LENGTH as a subnormal f64 (e.g. `'/'[0]` → `5e-324` = bit pattern `1` = length of `'/'`). Hono's `mergePath = (base, sub, ...rest) => …` triggered this — every `base?.[0] === "/" ? "" : "/"` check inside its template literal returned the byte length of `base` instead of the first character, so route paths arrived as `////api` instead of `/api`. Three coordinated fixes: **(1)** new runtime `js_dyn_index_get(value, index)` helper in `crates/perry-runtime/src/value.rs` — strings route to `js_string_char_at`, everything else uses the same `raw_ptr + 8 + idx*8` direct-read offset hack the existing IndexGet fallback uses (load-bearing for Object-with-numeric-keys / TypedArrays / Records, per the comment at lower_call.rs's num_idx branch). LAZY_ARRAY / FORWARDED arrays (issue #233 async-fn param handoff) chase through `js_array_get_f64` to materialize the chain. **(2)** Codegen `crates/perry-codegen/src/expr.rs::IndexGet` adds a new branch BEFORE the existing array fast path: when the receiver's static type is genuinely unknown (`Type::Any` / `Type::Unknown` / no recorded type) AND the index is numeric, emit `js_dyn_index_get`. The narrow gate keeps existing TypedArray, Object-with-numeric-keys, and class-instance fast paths intact (avoids the regression that broader gating caused on `test_gap_typed_arrays` / `test_edge_enums_const` / `test_edge_iteration` in the first iteration). **(3)** `crates/perry-runtime/src/object.rs::js_native_call_method` adds a string-method dispatch arm BEFORE the issue #510 primitive-call TypeError catch-all — `s.at(-1)` / `s.charAt(0)` / `s.charCodeAt(0)` / `s.slice(start[, end])` / `s.toString()` / `s.valueOf()` on `(s: any)` parameters route to the matching `js_string_*` helper instead of triggering #510's "(string).at is not a function" throw. **(4)** `crates/perry-hir/src/lower/expr_call.rs` gates the optimistic `Expr::ArrayAt` emission on a statically-known array type (`Type::Array(_)` or TypedArray) — `at` is shared between `String.prototype` and `Array.prototype`, so for `(s: any).at(-1)` perry can't tell which one the user means; the gate falls through to the runtime tower's tag-aware dispatch. Other array methods (`map` / `filter` / `find` / etc.) keep their optimistic emission since they're array-only. End-to-end smoke: `(s: any) => s[0]` with `s = '/foo'` returns `'/'` (was `2e-323`); hono's `mergePath('/', '/api')` returns `/api` (was `////api`); `app.routes` registers entries with correct paths `/` and `/api` (were `[object Object]` two iterations ago). Next blocker for hono compile end-to-end is `await app.fetch(req)` throwing `TypeError: Cannot read properties of undefined (reading 'length')` inside the dispatch chain — separate followup.
- **v0.5.593** — **Closes #510** (followup to #462): calling a method on a primitive whose name doesn't resolve to a real method now throws `TypeError: <kind>.<prop> is not a function` instead of silently no-op'ing. Three coordinated fixes: **(1)** `crates/perry-codegen/src/lower_string_method.rs:650` — the unknown-method catch-all on string-typed receivers (hit when `s: any = "hello"` then `s.lengt()` — codegen refines `Any` to `String` via `refined_ty`, and `is_string_expr` routes through `lower_string_method`) was returning `Ok(recv_box)`, evaluating the call to the receiver itself ("hello") and continuing execution. Now emits `js_throw_type_error_not_a_function("string", "<prop>")` + `unreachable`. **(2)** `crates/perry-runtime/src/object.rs::js_native_call_method` catch-all — when receiver is a non-string primitive (number/int32/bool/bigint), throws via the same helper instead of returning `NULL_OBJECT_BYTES`. Real-object receivers keep the null-object fallback (separate concern; many call paths use this dispatcher as a generic shortcut and rely on the silent return). **(3)** `crates/perry-codegen/src/lower_call.rs::skip_native` — added `Expr::NativeModuleRef(_)` to the skip predicate alongside `Expr::GlobalGet(_)`. `NativeModuleRef` lowers to literal `0.0`, which the runtime catch-all would now classify as `(number)` and throw on. Without this, unsupported `fs.METHOD()` / `crypto.METHOD()` calls (which fall through their dedicated codegen arms to `lower_call`) would regress from silent-no-op to TypeError — caught while testing by `test_gap_node_fs`'s `fs.rmSync(tmpDir, { recursive: true, force: true })` (no perry handler exists for `rmSync` yet). New `js_throw_type_error_not_a_function(kind_ptr, kind_len, prop_ptr, prop_len)` runtime helper in `error.rs` + extern decl in `runtime_decls.rs` (signature `void(ptr, i64, ptr, i64)` — diverges, declared as void-return for LLVM). Property *reads* on primitives still return undefined per Node's auto-boxed prototype-lookup semantics — the throw fires only on the call site. End-to-end smoke: `s.lengt()` → `TypeError: (string).lengt is not a function`, exit 1; `(42).foo()` → `TypeError: (number).foo is not a function`; `true.bar()` → `TypeError: (boolean).bar is not a function`. Legitimate `s.toUpperCase()` / `s.split(",")` / `n.toFixed(2)` etc. unchanged. Parity sweep: 208 pass / 3 known fails / 13 skipped (97.6%, no regressions vs main).

- **v0.5.592** — Closes #471 (BSON encoder corruption / OP_MSG truncation on ≥1.5KB inserts): `Record<number, T>` writes via `obj[i] = v` were corrupting unrelated heap allocations, surfacing in `@perryts/mongodb` as `insertMany(50)` producing on-the-wire BSON docs that contained only `_id`. Root cause: codegen's `Expr::IndexSet` numeric-key fallback (`crates/perry-codegen/src/expr.rs`) emitted an inline `obj_handle + 8 + idx*8` store under the assumption that the receiver had an `ArrayHeader` (8-byte header) layout — which holds for arrays but breaks for plain objects whose `ObjectHeader` is 24 bytes followed by `max(field_count, 8)` inline slots. For `idMap[22] = v` on a freshly-allocated `{}`, the inline write at offset 184 overflowed the ~88-byte allocation and trampled the keys_array pointer of the next object the arena had handed out (`prepared[0]` in the @perryts/mongodb repro). The clobbered keys_array pointed into the StringHeader of a recently-allocated `idMap` value, so `Object.keys(prepared[0])` returned `length=0` while `keys.join(',')` read the StringHeader's length+data bytes interpreted as ArrayHeader+f64 elements (the load-bearing "0.000…2121995791" subnormal in the diagnostic output). The BSON encoder saw `keys.length=0`, emitted `<int32=5><0x00>` for that doc — a 5-byte empty BSON document — and mongod accepted it with only the auto-generated `_id`. Fix: new runtime helpers `js_object_set_index_polymorphic` + `js_object_get_index_polymorphic` in `crates/perry-runtime/src/object.rs` dispatch by GC type — `GC_TYPE_ARRAY` / `GC_TYPE_LAZY_ARRAY` route through `js_array_set_f64_extend` / `js_array_get_f64` (preserving forwarding-chain follow + lazy-array materialize + per-kind buffer/typed-array stores), `GC_TYPE_OBJECT` / `GC_TYPE_CLOSURE` stringify the integer index (with a fast-path for the integer case avoiding the Display allocator) and delegate to `js_object_set_field_by_name` / `js_object_get_field_by_name`. Codegen's IndexSet/IndexGet numeric fallbacks (`expr.rs:2823+` set, `2207+` get) now call these helpers instead of doing inline `obj+8+idx*8` stores/loads — the previous "load-bearing for Object-with-numeric-keys storage" comment was load-bearing only by coincidence (object slot 0 happened to live at offset 24 = ObjectHeader, but slot N landed at offset 24+N*8 not 8+N*8 — the inline path silently misaligned for any N≠2). Both helpers handle negative indices by stringifying through `to_string()` so `obj[-1]` keeps its JS `obj["-1"]` semantics. Parity sweep with the fix: 206 pass / 3 known fails / 3 known compile fails / 13 skipped (98.5%) — REGAINS 3 tests that were silently wrong before (`test_edge_enums_const`, `test_edge_iteration`, `test_gap_typed_arrays` were each masking the same ObjectHeader-as-ArrayHeader inline-write bug). End-to-end smoke verified against the issue's `@perryts/mongodb` repro: `insertMany(50)` against mongod 7.0 now returns a sample doc with all 7 fields (`_id,i,salt,label,tags,score,nested`); the prior `_id`-only output is gone for n=50 and n=1000. The same fix incidentally restores correct behavior for any `Record<number, T>` workload — `mongoose`-style `{ [seq: number]: cached }` caches, batch-id lookup tables, sparse arrays accessed through index types — that previously appeared to work for small N but corrupted neighboring heap allocations once `idx ≥ 7`.

- **v0.5.591** — Hono compile end-to-end progress (followup to #493): cross-module `Call { callee: ExternFuncRef }` for an imported VARIABLE binding (`var mergePath = (b, s, ...r) => …` exported from another module) now correctly fetches the closure value via the zero-arg getter and dispatches through `js_closure_callN` — previously the call site emitted `perry_fn_<src>__<name>(args)` directly, treating the GETTER as if it were the function body, and the getter ignored the args and returned the closure handle as the call's result. `mergePath('/', '/foo')` returned a function reference (`typeof === 'function'`) instead of a string. Two coordinated changes: **(1)** `crates/perry/src/commands/compile.rs::1869` now adds EVERY exported-object name to `imported_vars` (not just primitives) — closures-assigned-to-const are in BOTH `exported_objects` AND `exported_func_param_counts`, but their `perry_fn_<src>__<name>` symbol IS the getter, not the function body. The new `is_function_decl` exclusion keeps `function foo(){}` decls out (their `perry_fn_<…>` IS the body). **(2)** `crates/perry-codegen/src/lower_call.rs::ExternFuncRef` Call path checks `imported_vars.contains(name)` BEFORE the standard `perry_fn_<src>__<name>(args)` direct-call dispatch — when present, calls the getter with 0 args, treats the result as a closure value, then dispatches through `js_closure_callN`. The runtime's closure-rest registry (issue #493) bundles trailing args correctly when the closure has `...rest`. End-to-end smoke: hono's `app.get('/', cb)` now registers routes with actual string paths (vs `[object Object]` pre-fix); paths still show extra leading slashes (`////` instead of `/`) — that's a separate template-literal + optional-chaining bug, not blocking. Parity: 208 pass / 3 fail / 1 compile-fail / 13 skipped (no regressions; the lone compile-fail is `test_issue_446_import_type_method_typeof` — a pre-existing parallel-codegen race in perry-codegen surfaces as identical output bytes for `consumer_type_only.o` and `consumer_value.o`, tracked as #509). known_failures.json updated to point at the new tracking issue. Refs #421 / #420 / #493.

- **v0.5.590** — Closes #490: cross-platform system tray icon API. New `trayCreate` / `traySetIcon` / `traySetTooltip` / `trayAttachMenu` / `trayOnClick` / `trayDestroy` exports on `perry/ui`, dispatched through a new block of rows in `PERRY_UI_TABLE` (`crates/perry-dispatch/src/lib.rs`) so the LLVM, JS, and WASM codegen backends pick them up automatically without per-backend match arms. **macOS** (`crates/perry-ui-macos/src/tray.rs`, ~210 LOC) wraps `NSStatusItem` from `NSStatusBar.system`; `trayAttachMenu` reuses the existing `NSMenu` storage from `menu.rs` via a new `pub fn get_menu_for_tray` accessor and calls `NSStatusItem.setMenu:` so left-click auto-pops the menu the user built with `menuCreate` / `menuAddItem` (no manual `TrackPopupMenu`-equivalent needed). Tooltip routes through the button's `toolTip` since `NSStatusItem.setToolTip` is deprecated since 10.10. Click handler is a `define_class!` `PerryTrayClickTarget : NSObject` mirroring the existing `PerryMenuItemTarget` pattern — per-tray `target` + `Sel::register(c"trayClicked:")` action, with `js_nanbox_get_pointer` + `js_closure_call0` to invoke the JS closure. New `objc2-app-kit` features `NSStatusBar` / `NSStatusItem` / `NSStatusBarButton` added to `crates/perry-ui-macos/Cargo.toml`. **Windows** (`crates/perry-ui-windows/src/tray.rs`, ~420 LOC) uses `Shell_NotifyIconW(NIM_ADD/MODIFY/DELETE)` with `WM_USER + 200` as the callback message; the existing main-window `WndProc` in `app.rs` gains a `WM_PERRY_TRAY` arm that forwards `(wparam, lparam)` to `tray::handle_callback_message`, which dispatches `WM_LBUTTONUP` to the user's `onClick` and `WM_RBUTTONUP` through `SetForegroundWindow + GetCursorPos + TrackPopupMenu(TPM_RETURNCMD | TPM_RIGHTBUTTON | TPM_LEFTALIGN | TPM_BOTTOMALIGN, ...)` into the existing `menu::dispatch_menu_item` path (so `menuAddItem` callbacks fire identically to widget context-menu items). Icons via `LoadImageW(LR_LOADFROMFILE | LR_DEFAULTCOLOR)` at `SM_CXSMICON`/`CY` — PNG and ICO both work on Vista+; empty path → `LoadIconW(NULL, IDI_APPLICATION)`. Menu lookup is at click time (not attach time) so `menuClear` + rebuild between attach and click correctly shows the new menu. New `pub fn get_hmenu(menu_handle)` in `menu.rs` exposes the HMENU. Multi-tray multiplex through one `WM_USER + 200` message; `uID` disambiguates. Slot reuse policy matches macOS (destroyed slots stay occupied, `alive = false` flag for set_*-after-destroy no-ops). **Linux/GTK4** (`crates/perry-ui-gtk4/src/tray.rs`, ~520 LOC) uses pure-Rust `ksni = "0.3"` (StatusNotifierItem / KSNI over D-Bus) gated under `cfg(target_os = "linux")` — works on KDE Plasma, GNOME-with-`appindicator`-extension, XFCE, Cinnamon, MATE, Budgie, LXQt out of the box; vanilla GNOME without the extension keeps the service alive but the icon doesn't render and a one-line warning logs at create time. KSNI's `Tray` trait is poll-based (it asks for state on each property fetch), so dynamic state lives in an `Arc<Mutex<TrayState>>` the impl reads on every property fetch; updates push via `Handle::update(|_| ()).await` to broadcast a DBus property-change. `activate(&mut self)` callbacks fire on a tokio worker thread, so closure invocation marshals back to the GTK main thread via `glib::MainContext::default().invoke(move || js_closure_call0(...))` — same pattern `location.rs` already uses. New `pub(crate) fn snapshot_menu(menu_handle)` in `menu.rs` walks the existing GTK menu storage and produces a `Vec<MenuItemSnapshot>` (StandardItem / Separator / SubMenu shapes) the tray module converts to KSNI's `MenuItem` enum at attach time — single source of truth, no parallel menu registry. Icons decoded via `gtk4::gdk_pixbuf::Pixbuf` to ARGB32-network-byte-order (KSNI's wire format). `ksni 0.3.4` + transitives recorded in `Cargo.lock`; `ksni`'s zbus + tokio deps are already pulled by `mpris-server`, so no new top-level link cost. **iOS / tvOS / visionOS / watchOS / Android** ship 6 `extern "C" { 0 }` no-op stubs (no system-tray concept on these platforms). **HarmonyOS** auto-stubs via `crates/perry-runtime/build.rs`'s `PERRY_UI_TABLE` walk under `--features ohos-napi` — build.rs detects the new dispatch entries and emits no-op shims at compile time, no manual change. **Web** target's `crates/perry-codegen-js/src/emit.rs` continues to fall through to `ui_method_to_runtime` before its per-backend extras, so the 6 new `perry_ui_tray_*` symbols would resolve via `web_runtime.js` if a stub is added there (currently unimplemented; browser tabs have no tray equivalent so a `console.warn` shim is the natural future implementation). End-to-end smoke verified on macOS: `test_tray.ts` (`trayCreate("")` + `traySetTooltip` + `trayOnClick` + `menuCreate` / `menuAddItem` / `menuAddSeparator` / `trayAttachMenu`) compiles cleanly with `perry test_tray.ts -o test_tray`, runs without crash, the placeholder "●" status item appears in the menu bar with the registered tooltip, and `nm libperry_ui_macos.a | grep _perry_ui_tray` shows all 6 expected symbols defined as `T`. TS surface added to `types/perry/ui/index.d.ts` with full per-platform behavior contract documented inline (which platforms render, which fall back to no-op, what icon formats each backend accepts). Parity sweep: 206 pass / 3 fail / 3 compile-fail / 13 skipped — 98.5%, no regressions vs main (the 3 mismatches and 3 compile-fails are pre-existing entries in `test-parity/known_failures.json`).

- **v0.5.589** — Closes #493 (closure rest-param bundling now happens at runtime via a func_ptr → fixed_arity registry, fixing dynamic-dispatch call sites where codegen can't statically bundle): a closure stored as an instance property (`this.cb = (...args) => …` inside a constructor) was unreachable through perry's HIR-level rest-bundling pass — the call site `obj.cb(a, b, c)` resolves through `js_native_call_method` → field scan → `js_native_call_value` → `js_closure_callN`, none of which knew the closure declared `...rest`. The closure body was compiled with K+1 fixed params expecting the rest array as the K+1-th arg, but the call passed N positional args, so the rest param ended up bound to the first positional arg as a scalar (e.g. `args = "a"` instead of `["a","b","c"]`, `args.length = 1`). Hono's `app.get('/', cb)` route registration silently no-op'd because `args.forEach((handler) => this.#addRoute(...))` couldn't iterate over `"a"`. Three coordinated fixes: **(1)** new runtime side-table `CLOSURE_REST_REGISTRY: thread_local!<HashMap<func_ptr, fixed_arity>>` with public `js_register_closure_rest(func_ptr, fixed_arity)` + private `lookup_closure_rest`, plus a `dispatch_rest_bundled(closure, func_ptr, args, fixed_arity)` helper that bundles trailing args into a fresh JS array via `js_array_alloc` + `js_array_push_f64` and dispatches by K to the appropriate `extern "C" fn(*const ClosureHeader, f64×K, f64) -> f64` transmute (K=0..7 supported, > 7 falls through to undefined — same upper bound the static-bundling path enforces); **(2)** every `js_closure_callN` (N=0..6 hot path) now consults the registry between the BOUND_METHOD check and the existing transmute — when found, dispatches through `dispatch_rest_bundled`; **(3)** codegen emits `js_register_closure_rest(@perry_closure_<modprefix>__<fid>, <fixed_arity>)` at module init from `emit_string_pool`, walking the `closure_rest_params` map (already built from `closures.filter(p.is_rest)` at codegen.rs:1607). The static-bundling path in `lower_call.rs::Expr::LocalGet` was REMOVED — letting the runtime do it avoids the double-bundle that produced `[[1,2,3]]` instead of `[1,2,3]` in `let f = (...n) => …; f(1,2,3)` (caught by `test_edge_rest_spread_defaults` regressing in the first iteration). FuncRef calls keep their static-bundling because they don't go through `js_closure_callN`. Plus a small followup: `js_native_call_method`'s array-method dispatch tower learned `forEach` (was previously `map`/`filter`/`slice` only) so `args.forEach(cb)` for any-typed receivers actually iterates instead of falling through to undefined. Parity sweep: 209 pass / 3 known fails / 13 skipped (was 204 / 8 / 13) — `test_edge_rest_spread_defaults` and the 4 net tests (now passing because the echo-server fixture starts cleanly under the rebuilt runtime) all moved into the pass column. End-to-end smoke: `app.get('/', (c) => c.text('hi'))` now correctly increments `app.routes.length` to 1; the next blocker for hono compile-end-to-end is the route's `path` field reading as `[object Object]` instead of `"/"` (mergePath / Object.assign-on-this interaction — separate followup).

- **v0.5.588** — Closes #507 (perry-ext-net `Handle::current()` panic — LTO dead-strip CONTEXT statics): outbound TCP via `net.createConnection` panicked with "there is no reactor running" because perry-stdlib (rebuilt by auto-optimize in `target/perry-auto-<hash>/release/`) and perry-ext-net (built once into the workspace `target/release/`) had **independent tokio compilations** with different mangled hashes on `tokio::runtime::context::CONTEXT`. Both staticlibs ended up in the final binary as DISTINCT TLS variables — perry-stdlib's runtime entered one CONTEXT, perry-ext-net's `Handle::current()` read the other (empty) one. Confirmed empirically with `nm`: pre-fix perry-ext-net.a defined CONTEXT with hash `ee5072d8cc321e54`, perry-stdlib.a referenced hash `b7834470a65f466f`. Fix: `optimized_libs::build_optimized_libs` now adds `-p <crate>` to the auto-optimize cargo invocation for any tokio-using ext binding (`net`, `ws`, `http`, `https`, `axios`, `node-fetch`, `fetch`, `fastify`, `mongodb`, `pg`, `mysql2`, `mysql2/promise`, `ioredis`, `redis`, `nodemailer`), so cargo unifies tokio across them within ONE invocation in ONE target-dir. The rebuilt `lib<name>.a` lives next to `libperry_stdlib.a` in the auto-optimize target-dir; both reference the same mangled CONTEXT symbol; the linker dedups; one TLS variable in the final binary; `Handle::current()` works. CPU-only wrappers (bcrypt, argon2, sharp, dotenv, …) are unchanged — they have no own-crate tokio dep, route through perry-stdlib's `spawn_blocking` shim, and continue to use the workspace `target/release/lib*.a` directly. New `binding_needs_shared_tokio(module: &str) -> bool` allowlist gates which bindings get the rebuild treatment; 3 unit tests cover net inclusion, CPU-only exclusion, and unknown-module default. Existing `std::hint::black_box` + `eprintln!` defensive scaffolding in `crates/perry-ext-net/src/lib.rs::spawn_socket_runner` (v0.5.578-v0.5.581 attempts) left in place as a safety net + diagnostic for any future path that bypasses the auto-optimize flip. End-to-end smoke verified: the issue's `net.createConnection(127.0.0.1:80)` repro now fires `'error' { Connection refused }` cleanly and exits 0 instead of panicking with "there is no reactor running"; with a listening server on a free port, `'connect'` fires correctly and the socket round-trips. Unblocks `@perryts/mongodb` benchmark + every other wrapper that sits on top of perry-ext-net (mongodb, postgres, mysql, http clients, fastify back-fetch).

- **v0.5.587** — Closes #458 / #491: WAV recording (`audioSetOutputFilename` / `audioStartRecording` / `audioStopRecording`) is now implemented on every UI backend. The original PR #458 shipped working impls only for Linux (PulseAudio capture in perry-ui-gtk4) and Web (MediaRecorder in perry-codegen-js); the seven other platform crates were `pub fn ... {}` stubs and four of them (`perry-ui-{ios,tvos,visionos,windows}/src/lib.rs`) didn't even compile because the FFI wrappers referenced `crate::string_header::StringHeader` — a path that only exists in the macOS UI crate (which keeps a local `StringHeader` copy to avoid pulling perry-runtime into its link). Other UI crates depend on perry-runtime directly and need `perry_runtime::string::StringHeader`. This release replaces every stub and fixes the broken path. Per-platform integration: macOS / iOS / tvOS / visionOS append channel-0 samples inside the AVAudioEngine `installTapOnBus` callback when `RECORDING.load(Relaxed)` is set; sample rate captured live from `outputFormatForBus:` so the WAV header matches whatever the input device negotiates (typically 48kHz). watchOS uses the same AVAudioEngine pattern with a slightly simpler audio.rs and rewrites the watchOS `lib.rs` FFI wrappers, which were `_filename_ptr` (param-ignored) no-ops in the original PR — they now route through the existing `str_from_header` helper into `audio::set_output_filename` / `start_recording` / `stop_recording`. Android appends inside the existing AudioRecord JNI read loop in the spawned capture thread (sample rate fixed at 48kHz to match the AudioRecord constructor's `sample_rate` arg). Windows appends channel-0 samples inside the WASAPI capture loop, honoring the existing `is_float` / `bits_per_sample` branch and the WASAPI mix format's live sample rate; uses a per-packet `Vec<f32>` so the existing per-sample loop only does the lock-extend once. HarmonyOS needs no code change: `crates/perry-runtime/build.rs` walks `PERRY_SYSTEM_TABLE` (which #458 added the three new rows to) and auto-generates no-op stubs under `--features ohos-napi` — the harmonyos `.so` link-resolves and calls become silent no-ops, by design (no perry-ui-harmonyos crate exists; the harvest model rewrites widget trees into ArkUI source). Recording state is module-level statics: `RECORDING: AtomicBool` + `RECORDED_SAMPLES: Mutex<Vec<f32>>` + `OUTPUT_FILENAME: Mutex<String>` + `RECORDING_SAMPLE_RATE: AtomicU64` (bits-of-f64). Non-recording capture pays only a relaxed atomic load per buffer. Single 16-bit-mono-PCM WAV writer per crate (header + clamp-to-i16 samples; 0 dBFS = 1.0 → i16::MAX). Doc-tests fence-lint also flagged 2 untagged \`\`\`ts fences in `docs/src/native-libraries/authoring-guide.md` (illustrative `my-bindings` PDF parser snippets that can't compile-test against a fictional package); tagged with \`\`\`typescript,no-test to opt out — proper `{{#include}}` fixture work tracked separately as #494. Also folds in a cargo fmt --all sweep that picked up pre-existing drift in 5 unrelated files (codegen.rs / expr.rs / ioredis.rs / unroll.rs / gtk4 audio.rs). Co-authored with #458 contributor Lebei (lebei2046@gmail.com); their original PulseAudio Linux + MediaRecorder Web implementations + `PERRY_SYSTEM_TABLE` dispatch entries + TypeScript surface declarations + media playback `file://` URI fix all carry forward unchanged.
- **v0.5.586** — Closes #485 (cross-module subclass field initializers + arrow class fields installed in parent ctor body): a subclass that `extends` an imported parent now correctly inherits the parent's class fields AND any `this.X = arrow` assignments in the parent's constructor body, fixing the hono `app.fetch is not a function` blocker carved out for #421. Three orthogonal fixes had to land together. **(1) `Expr::SuperCall` now falls through to `imported_class_ctors`** when `parent_class.constructor` is `None` — previously perry silently dropped `super(...)` for imported parents, so the parent's ctor body (where hono-base installs `get`/`post`/`use`/`fetch` etc. on `this`) never ran. The new branch declares the cross-module ctor symbol (`<source_prefix>__<class>_constructor`), pads optional args with `TAG_UNDEFINED`, and emits `call_void` — same dispatch shape `lower_new`'s no-own-constructor fallback at `lower_call.rs:2720` already used. The standalone ctor itself runs `apply_field_initializers_recursive_pub` (codegen.rs:3071), so calling it from `super()` automatically inherits the parent's arrow-class-field initializers (`request = (...) => …` / `fetch = (…) => …` / `routes = []` / `_basePath = "/"`). **(2) `exported_classes` now indexes `export { Local as Exported }` renames** in `compile.rs` — without this, `export { Hono as HonoBase }` registered the source class only under `(path, "Hono")`, so the importing side's `(path, "HonoBase")` lookup missed and `imported_classes` skipped the entry entirely. The new sweep at `compile.rs:1734+` walks `hir_module.exports` for `Export::Named { local, exported }` where `local != exported`, finds the matching local class, and inserts an alias entry under the `exported` name. **(3) Both the local-class-with-imported-parent path AND the imported-class-stub-with-imported-parent path in `codegen.rs` now walk through `imported_class_stubs` when building `packed_keys` and computing `total_field_count`** — local Sub extends imported HBase no longer collapses to zero parent-field contribution, and imported Sub stubs in the entry module also pull HBase's fields into their inline-slot count. Without this, allocations on the importing side reserved 0 inline slots for `Sub` and the parent's cross-module ctor's `this.field = …` writes overflowed the object header — `f.field` reads on the importing side returned `undefined` even though the parent's ctor symbol "ran" successfully. The transitive-class-closure loop in `compile.rs:3249+` also gained a `parent_refs` chain (alongside the existing `field_types` walk) so importing a subclass auto-pulls the parent class into `imported_classes`, with `local_alias` set when the child's `parent_name` differs from the source class's `class.name` (e.g. `Sub.extends_name = "HBase"` while the source class is named `"Base"`). End-to-end smoke verified: a 4-file repro (handcl/base.js declares `Base` with arrow class fields + ctor-body `this.X = …`; handcl/index.js does `class Sub extends HBase` with `super(options)`; main.ts does `new Sub()`) now correctly observes all parent fields + arrow methods on the instance. With hono itself: `new Hono()` instances now have `get`/`post`/`put`/`delete`/`options`/`patch`/`all`/`on`/`use` as functions, `routes` as an empty Array, `_basePath` as `"/"`. Routes still register as zero on `app.get('/', cb)` because the `#addRoute` private method's body has untested edge cases — separate followup. Parity sweep: 204 pass / 8 known fails / 13 skipped (96.2%) — no regressions; the 8 failures are all pre-existing entries in `test-parity/known_failures.json` (3 edge-case parity gaps + 5 net-test environmental fails on macOS without an echo fixture server). Unblocks the hono 30-line acceptance test (#486) and `@hono/perry-server` adapter (#487) groundwork.

- **v0.5.585** — Refs #139: floating-point fast-math is now opt-in, not the default. The per-instruction LLVM `reassoc + contract` FMF flags that `block.rs` previously emitted on every `fadd`/`fsub`/`fmul`/`fdiv`/`frem`/`fneg` are now gated on a new `--fast-math` CLI flag (also `PERRY_FAST_MATH=1` env var, also `"perry": { "fastMath": true }` in package.json — CLI > env > package.json, last wins). Default OFF means Perry produces bit-exact f64 output with Node on most FP-heavy code; the `scripts/fp_fuzz.mjs` differential fuzzer drops from ~30% divergence with both flags on to ~6% with neither (the residual 6% is from the LLVM SLP vectorizer at -O3, not gated by per-instruction FMFs — separate followup). Performance impact measured on M-series ARM64: tight `sum += constant` loops slow down ~7x without the flags (96ms vs 13ms on a 100M-iter loop), but realistic FP code (10M-element dot product, 10M-element array reduction) shows zero measurable change — the flag's win is narrow even where it exists, because data-dependent reductions can't fully vectorize without `reassoc` AND Node's V8 vectorizes them too. Plumbing: new `pub static FAST_MATH: AtomicBool` in `crates/perry-codegen/src/block.rs` set once per build at `compile_module` entry from the new `CompileOptions::fast_math` field; `fmf_prefix()` returns `"reassoc contract "` or `""`. Cache key in `compile/object_cache.rs::compute_object_cache_key` gains a `fmath` field — without this, toggling the CLI flag would silently serve stale `.o` bytes from `.perry-cache/objects/` and the flag would appear to do nothing (the original investigation hit exactly this bug). New unit test `key_changes_with_fast_math_flag` gates the regression. The new `block.rs::fadd_emits_expected_ir_default` test covers the no-flag emission; the fast-math path is covered end-to-end by `scripts/fp_fuzz.mjs`. New docs page `docs/src/cli/fast-math.md` (linked from SUMMARY.md under CLI Reference) explains the flag's full behavior contract — what it enables (`reassoc + contract`), what it deliberately does NOT enable (`nnan/ninf/nsz/arcp/afn` — would break NaN-boxing, the v0.2-era `-ffast-math` revert at commit b5a8c83f explained), the speed table (7x sum-loop, ~0% on realistic code), the divergence numbers (94/100 default, ~70/100 with --fast-math, fp_fuzz seed=200 count=100), and the cache-invalidation note. linker.rs comment also updated to reflect that `-fno-math-errno` is the only clang FP flag we pass — the per-instruction FMFs come from block.rs and are now opt-in, not "every fast-math effect comes from the per-instruction FMFs emitted here" as the previous comment claimed (the previous comment also overstated the perf benefit of the flags by quoting the sum-loop number as universal). Closes the credibility critique from the original issue thread (the v0.5.584-and-earlier defaults were technically non-spec-conformant on f64 arithmetic with no opt-out path; users who want bit-exact-with-Node now get it for free, users who want the headline benchmark numbers add `--fast-math` and accept the divergence trade).

- **v0.5.584** — Closes #484: rest-parameter bundling for class method dispatch. `class Builder { with<T>(id, ...args: T extends void ? [] | [void] : [T]) }` (or any `...args` rest pattern on a class method) crashed at runtime with `TypeError: Cannot read properties of undefined (reading 'length')` because the codegen-side method dispatch table only tracked `method_param_counts: HashMap<(class, method), usize>` — no rest-parameter flag. Call sites padded missing args with `TAG_UNDEFINED` (issue #235 path) but didn't bundle trailing args into a `js_array_alloc(n)` rest array. The conditional rest type from the issue's repro was a red herring — even `...args: any[]` would have shown the same gap, except no test exercised it on the static-class-dispatch path. Free-standing functions had this right via `func_signatures.has_rest` since v0.5.0; class methods didn't. Fix is mechanical: parallel `method_has_rest: HashMap<(class, method), bool>` populated alongside `method_param_counts` in `compile_module`, threaded through `CrossModuleCtx` + `FnCtx`, and consulted in `lower_call.rs`'s static-dispatch + virtual-override arm to emit the same `js_array_alloc + js_array_push_f64` sequence the freestanding-call path already uses. After fix: `b.with(1)` correctly passes `args=[]` (length 0) to the method body. Conditional rest types compile through unchanged because the `is_rest` bit propagates from HIR's pattern-shape detection (`Pat::Rest(_)`), independent of the type annotation.

- **v0.5.583** — Post-v0.5.582 release fix-forward 2: populated `test-parity/known_failures.json` with the 8 currently-failing tests (3 pre-existing parity gaps + 5 net tests that pass locally with the echo fixture server but fail on Linux CI without it). Each entry has a brief reason + tracking note. Without this list the `Tests` workflow's parity job rejected the v0.5.582 tag at the "Check for new failures" step, blocking the release-packages publish gate. v0.5.581 + v0.5.582's tags remain published but their assets / npm publish are blocked behind this gate; v0.5.583 unblocks the gate so the release pipeline can complete cleanly. No code changes — pure CI-config fix.

- **v0.5.582** — Post-v0.5.581 release fix-forward: cargo fmt drift (52 files) reformatted in lockstep, and Cargo.lock synced to current workspace version. Lint job in `Tests` workflow rejected v0.5.581's tag with formatting check; this bump re-runs the gating workflows on a clean tree. Compile-smoke job's fastify integration test (`./scripts/run_fastify_tests.sh`) reveals a separate fastify reactor panic ("there is no reactor running") on hyper's accept loop — same LTO-strips-tokio-context shape that perry-ext-net hit in v0.5.578-580, but the `Handle::try_current` + `std::hint::black_box` workaround that worked for perry-ext-net DOESN'T work for fastify (hyper's larger surface gives LTO more to dead-strip). Tracked as a v0.6.0 followup; programs that import fastify will hit this panic at server-start time. Recommended workaround for fastify users today: use perry-stdlib's bundled fastify by setting `PERRY_DISABLE_WELL_KNOWN=1` env var on `perry compile`. Other tests in compile-smoke unaffected.

- **v0.5.581** — Refs #421/#420 (issue #91 dispatch via HANDLE_METHOD_DISPATCH for perry-ext-net): the `Map.get` / struct-field / wrapper-function receivers where codegen lost the static type fall through to `js_native_call_method` → `dispatch_handle_method` → `is_net_socket_handle` → `dispatch_net_socket`. Both predicates were `#[cfg(feature = "net")]`-gated in perry-stdlib, so the well-known flip stripped them entirely. perry-ext-net's Map.get'd sockets silently dispatched to undefined (issue #91 regression). Fixes: (a) perry-ext-net exposes `js_ext_net_is_socket_handle(handle) -> i32` extern; (b) perry-stdlib's `dispatch_handle_method` flips the cfg gate from `feature = "net"` to `feature = "bundled-net"` (perry-stdlib's own implementation) and adds a parallel arm under `not(bundled-net) AND external-net-pump` that calls `js_ext_net_is_socket_handle` + a new `dispatch_external_net_socket` helper; (c) the helper declares `js_net_socket_write` / `_end` / `_destroy` / `_on` / `_method_connect` / `_upgrade_tls` extern "C" — perry-ext-net already provides these. Also strengthened the LTO black-box in perry-ext-net's `spawn_socket_runner` (eprintln-on-error + black_box on JoinHandle) since the v0.5.580 single black-box on `try_current()` regressed when binary size shifted from 3.4MB to 2.4MB. Net parity recovery: 5 fails → 1 fail (`test_net_min` / `test_net_socket` / `test_issue_422_socket_connect` / `test_sock_write_map` PASS; `test_net_upgrade_tls` still needs TLS test fixture server, environmental, not in scope).

- **v0.5.580** — Refs #421/#420 (perry-ffi `alloc_buffer` routes through extern `js_buffer_alloc` symbol): `data` events from perry-ext-net delivered Buffers that arrived in user code as `[object Object]` instead of working as Node Buffers — `buf.toString('utf8')` returned `"[object Object]"`. Root cause: `perry_ffi::alloc_buffer` called `perry_runtime::buffer::buffer_alloc` (a Rust function that gets monomorphized into perry-ext-net's archive). The runtime's small-buffer slab + `BUFFER_REGISTRY` are `thread_local!` statics — perry-ext-net's monomorphized copy had its OWN private slab, separate from the main thread's slab that perry-runtime's `is_registered_buffer` dispatch path checks. Allocations from external wrappers landed in perry-ext-net's slab; the dispatch on the main thread checked the runtime's slab and didn't find them → fell through to generic-object dispatch → toString returned the default `[object Object]`. Fix: `perry_ffi::alloc_buffer` now routes through the stable `extern "C" fn js_buffer_alloc(size, fill)` symbol — single shared entry point in libperry_runtime.a, all wrappers land in the SAME thread-local slab the dispatch checks. Also addressed `Handle::current()` LTO dead-strip via `std::hint::black_box(tokio::runtime::Handle::try_current())` (release-mode LTO without it strips perry-ext-net's tokio CONTEXT statics → spawn closure panics with "no reactor running"). Plus added `"type": "module"` to perry's `package.json` so node's `--experimental-strip-types` doesn't print the MODULE_TYPELESS warning that was diffing parity output. Net parity recovery: 5/5 → 3/5 fail → 2/5 fail (`test_net_min`, `test_net_socket`, `test_issue_422_socket_connect` pass; `test_net_upgrade_tls` needs TLS test server, `test_sock_write_map` hits issue #91 dispatch — Map.get'd socket dispatches through `HANDLE_METHOD_DISPATCH` → perry-ext-net doesn't expose `dispatch_net_socket` symbol yet. Both followups, not blocking).

- **v0.5.579** — Refs #421/#420 (net event-firing fix — pump registration + LTO black-box): the v0.5.578 reactor fix made `test_net_min` not crash but the 'connect' event still didn't fire because (a) perry-stdlib's `js_stdlib_process_pending` was `#[cfg]`-gated off (the v0.5.572 well-known flip stripped `feature = "net"` so the pump-call site disappeared); (b) the spawn-blocking shim wasn't calling `ensure_pump_registered()`, so perry-runtime's `STDLIB_PUMP_FN` stayed null and `js_run_stdlib_pump` was a no-op even when stdlib was linked; (c) release-mode LTO was dead-stripping perry-ext-net's tokio CONTEXT statics, so `Handle::current()` from inside the spawned closure panicked with "no reactor running" even after the perry-stdlib runtime correctly entered the context. Three cooperating fixes:

  1. `perry-ffi_spawn_blocking` + `perry_ffi_spawn_blocking_with_reactor` shims now call `async_bridge::ensure_pump_registered()` before scheduling — registers `js_stdlib_process_pending` with perry-runtime's `STDLIB_PUMP_FN` slot so subsequent `js_run_stdlib_pump` invocations actually drain.
  2. perry-stdlib gains a new `external-net-pump` feature; the cfg gate around the net pump call site (in `js_stdlib_process_pending`) flips from `feature = "net"` to `any(feature = "bundled-net", feature = "external-net-pump")`. `optimized_libs::build_optimized_libs` activates the new feature when the well-known flip strips `bundled-net`. Pump-call site is preserved exactly when perry-ext-net is linked.
  3. perry-ext-net's `spawn_socket_runner` adds a guard `let _check = tokio::runtime::Handle::try_current();` before `Handle::current()` — keeps the LTO pass from dead-stripping perry-ext-net's tokio CONTEXT statics. Without it, release-mode LTO strips the tokio thread-local definitions and `Handle::current()` panics inside the spawned closure even though perry-stdlib's runtime entered the context. Added a comment explaining the gotcha.

  After fix: `test_net_min` runs to completion with 'connect fired' visible; perry-ext-net's full event-pump pipeline (push_event → notify_main_thread → js_run_stdlib_pump → js_stdlib_process_pending → js_net_process_pending → listener callback) is wired end-to-end. Remaining 4 net parity tests still diff on Buffer-as-object formatting + missing test-fixture servers (separate, smaller issues — not blocking #421's deeper hono dispatch chain that v0.5.577 unblocked).

- **v0.5.578** — Refs #421/#420 (net regression fix — tokio features + reactor context): the v0.5.571 perry-ext-net port introduced 5 parity regressions (`test_net_min`, `test_net_socket`, `test_net_upgrade_tls`, `test_issue_422_socket_connect`, `test_sock_write_map`) — all crashed with "there is no reactor running, must be called from the context of a Tokio 1.x runtime" or "Cannot start a runtime from within a runtime". Two cooperating bugs:

  1. **Tokio feature mismatch** — perry-stdlib's `tokio` was declared with the reduced feature set `["rt-multi-thread", "sync", "time", "net", "macros", "io-util"]` while perry-ext-net pulled tokio with `"full"` from the workspace. When `auto-optimize` rebuilds perry-stdlib in a separate target dir (per-program feature set), cargo can't unify them — two distinct tokio compilations end up linked, each with its own thread-local runtime context. perry-stdlib's `runtime().spawn()` ran the closure on its own runtime; perry-ext-net's `tokio::runtime::Handle::current()` looked at perry-ext-net's tokio statics and saw nothing. Fixed by flipping perry-stdlib to `tokio = { features = ["full"] }` so both halves share the same compilation.

  2. **`Handle::block_on` from inside an async task** — once the runtime context unified, `handle.block_on(fut)` panicked with "Cannot start a runtime from within a runtime" because the spawn_blocking_with_reactor shim wraps the closure in `runtime().spawn(async {...})`. Fixed by adding `perry_ffi::spawn_blocking_with_reactor` (companion to `spawn_blocking` — runs the closure inside an async block on the multi-thread runtime so the closure body has full reactor + handle access) plus switching perry-ext-net's `spawn_socket_runner` to use `handle.spawn(fut)` (detach onto the same runtime) instead of `handle.block_on(fut)`. Also flipped perry-ext-ws and perry-ext-http to the new shim. Net binaries now run to completion without crashing — though one observable diff remains in `test_net_min`'s 'connect' event firing (separate, smaller event-pump bug).

- **v0.5.577** — **Closes #482** (refs #421/#420): `class Foo {}` followed by `export { Foo }` in a separate clause was lowered with `is_exported = false` (because the decl wasn't a syntactic `export class Foo {}`). The CLI driver's `exported_classes` lookup (`crates/perry/src/commands/compile.rs:1684`) filters on `class.is_exported`, so the class never reached the importer's `imported_classes` registry — `new Foo()` from another module fell through to the empty-object placeholder, and method calls like `f.add(...)` returned undefined. Hono's `RegExpRouter` / `Trie` / `Node` / `HonoBase` all use this pattern (`var X = class { ... }; export { X };` or `class X {}; export { X };`), so route registration silently no-op'd and `app.fetch()` returned a default Response with no status/body. Fix is one block in `crates/perry-hir/src/lower.rs`'s `ExportNamed` arm: after pushing the export clause, walk `module.classes` for one whose name matches the local and flip its `is_exported = true`. Hand-written class repro (`class Foo { add() {} }; export { Foo };` from a `compilePackages` package) round-trips correctly post-fix; `f.add()` returns the expected value. Hono progresses past route registration but `app.request()` still returns undefined (separate, deeper bug — class-field arrow methods on classes with private fields). Drizzle schema construction also benefits.

- **v0.5.576** — Refs #420 (compile drizzle-orm end-to-end shake-out batch 1 — duplicate-symbol fix on `__perry_wrap_perry_unknown_func`): the v0.5.337 fallback wrapper for unresolvable `Expr::FuncRef` ids was emitted with default (external) linkage, so multi-module programs (drizzle-orm has 5+ modules each emitting it) failed link with `duplicate symbol ___perry_wrap_perry_unknown_func`. Flipped to `internal` linkage matching the surrounding `__perry_wrap_extern_*` wrappers (their comment block explicitly calls out the same pattern). End-to-end smoke verified: `import { pgTable, serial, text, integer } from 'drizzle-orm/pg-core'` compiles and runs through to schema construction. Followups under #420 / #421: hono full dispatch chain still doesn't fire route handlers (separate, deeper bug — class-private-fields + closure-call in `compose()` / `RegExpRouter`); drizzle query execution against a real postgres needs perry-ext-pg integration smoke-test.

- **v0.5.575** — Refs #421 (compile hono end-to-end shake-out batch 2 — `new Response(body, optsVar)` runtime-options extraction): perry's `new Response(body, init)` codegen in `crates/perry-codegen/src/lower_call/builtin.rs` only inspected `init` when it was a literal object expression (`{ status: 404 }`). Hono's `c.text("404 Not Found", 404)` flows through `#newResponse(text, arg, headers)` which builds `{ status, headers }` as a runtime object inside the framework and passes it to `new Response(...)` — perry then dropped the status entirely (defaulted to 200 / no headers) and downstream `res.status` reads were undefined. Fixed by extracting `.status` / `.statusText` / `.headers` at runtime via `js_object_get_field_by_name_f64` whenever `args[1]` isn't an object literal (object-literal short circuit preserved). Bare-construction tests round-trip status / statusText correctly. Hono's full dispatch chain still doesn't fire route handlers (separate bug, tracked under #421 — likely class-private-fields + closure-call in compose() / RegExpRouter) — not blocking this commit since the runtime-options path is a real fix that benefits any framework / library using the same pattern.

- **v0.5.574** — Refs #421 (compile hono end-to-end shake-out batch 1): two pre-existing bugs that blocked hono from compiling. **Bug 1** — HIR's `is_exportable` matcher in `crates/perry-hir/src/lower.rs`'s `ExportNamed` arm omitted primitive literals (`Expr::String`, `Expr::Number`, `Expr::Bool`, `Expr::BigInt`, `Expr::Null`, `Expr::Undefined`), so any JS/TS module with `var X = "literal"; export { X };` (the canonical shape pre-bundled npm packages use for string/number constants — hono's `var METHOD_NAME_ALL = "ALL"` triggered it) failed to flow through `exported_objects`. The importing module's `imported_vars` set then missed the name, codegen's `ExternFuncRef`-as-value path took the closure-pointer wrapper branch instead of the value-getter call branch, and the importer saw `typeof X === "function"` + `X.toString()` → `[object Object]` instead of the actual string. Extended the matcher to include all primitive literal variants. **Bug 2** — `new Set(string)` SIGSEGVed because `Expr::SetNewFromArray` codegen blindly cast the input handle to `*const ArrayHeader` and called `js_set_from_array`, which read garbage when the input was a `*StringHeader`. Hono's `var regExpMetaChars = new Set(".\\+*[^]$()")` (top-level module init) hit it. Added `js_set_from_iterable(value: f64)` runtime function that dispatches by NaN-box tag — STRING_TAG / SHORT_STRING_TAG → iterate UTF-8 codepoints (matches JS spec: `new Set("abc")` → `{"a","b","c"}`); POINTER_TAG → existing array path; undefined / null / number → empty set. Codegen's `Expr::SetNewFromArray` flipped to call the new function with the NaN-boxed value directly. With both fixes, `import { Hono } from 'hono'; const app = new Hono(); app.get(...)` compiles and runs through to route registration. Followups under #421: hono `Response.status` reads as `undefined` (field access on hono's `Response` class returns wrong value), full request/response round-trip not yet end-to-end. Drizzle (#420) shake-out is the parallel followup.

- **v0.5.573** — **Closes #466** (well-known bindings flip default-on): the v0.5.532 introductory env-var gate (`PERRY_USE_WELL_KNOWN=1`) is inverted. The well-known flip — for each imported module that has a `well_known_bindings.toml` entry and a bundled `.a` on disk, drop the corresponding perry-stdlib feature and link the perry-ext-* archive instead — now runs by default. Opt-out via `PERRY_DISABLE_WELL_KNOWN=1` (kept for bisection). End state: `import { createConnection } from 'mysql2'` (or any of the 34 in-tree perry-ext-* wrappers) automatically routes to the standalone crate without the user installing anything; perry-stdlib stops emitting the corresponding symbols at the same time so there's no duplicate-symbol risk. Per-binding fallback: if a bundled `.a` is missing on disk for any individual entry (e.g. partial workspace build), that entry skips the flip and falls back to perry-stdlib's copy — never an all-or-nothing failure. The `framework` module remains in perry-stdlib unconditionally (it backs hyper-based servers that aren't fastify and isn't part of #466's "wrapper around a Rust crate" rubric). Closes #466's "Phase 4 user-visible behavior" goal — the issue's load-bearing promise of `import 'mysql2'` working byte-identically with zero install action is now the default.

- **v0.5.572** — Refs #466 (Phase 5 steps 33+34 — port streams + fastify in parallel via worktree subagents): closes the last two in-tree wrapper ports. Both crates depend only on `perry-ffi` v0.5; both inherit the v0.5.571 GC-root-scanner + closure-call + notify_main_thread pattern. **`perry-ext-streams`** (~1539 LOC, 35 `js_*` exports — full Web Streams API surface: ReadableStream / WritableStream / TransformStream / TextEncoder / TextDecoder / DecompressionStream + chunk-bytes accessor + `js_readable_stream_*` / `js_writable_stream_*` / `js_transform_stream_*` / etc). Pure-Rust port — no tokio dep needed since streams.rs is synchronous on the perry-runtime side (closure invocation + promise resolution + array/object construction). Two punted gaps documented: `blob.stream()` / `response.body` cross-wrapper bridge (perry-stdlib's copy reaches into `crate::fetch::{blob_bytes_clone, response_bytes_clone}` — perry-ffi has no cross-wrapper handle-bytes accessor today, so the variants return an empty single-chunk stream so symbols still link; full bridge needs v0.6.0); BYOB readers + `ReadableStream.from(asyncIterable)` keep the existing throw-from-runtime stubs and route through `js_throw` declared as direct `extern "C"` (perry-ffi has no error/throw helper today). 5 unit tests pass. **`perry-ext-fastify`** (~1971 LOC across 5 files — `lib.rs` + `app.rs` + `context.rs` + `router.rs` + `server.rs`, mirroring perry-stdlib's `fastify/` layout). 36 `js_fastify_*` exports — full hyper-based HTTP framework (FastifyApp register/listen/close + 12 route + hooks + lifecycle exports; FastifyContext request/reply/get/post/put/delete + 19 cookie/header/body/JSON/redirect/status exports; RoutePattern static + param + wildcard matching; server hyper accept loop + main-thread event-loop integration via `try_recv_with_timeout` + 200µs sleep loop bounded at 10ms; `notify_main_thread()` shortens sleep when a request lands). Six punted gaps documented: HTTP/2 (uses `http1::Builder` — needs `auto::Builder` for upgrade), WebSocket upgrade (use perry-ext-ws directly), multipart (raw bytes only), schema validation (parsed but not enforced — call perry-ext-validator inside handlers), plugin isolation (synchronous nested plugins with temporary prefix), streaming bodies (fully buffered, needs `spawn_async`). 10 unit tests pass (4 router + 6 lib). perry-stdlib gate flips: `http-server` umbrella → `["bundled-fastify", "dep:hyper", ...]`, new `bundled-fastify` per-binding gate; `pub mod fastify` → `feature = "bundled-fastify"`. `http-client` umbrella → `[..., "bundled-streams"]` so `--features http-client` callers get streams transitively; `pub mod streams` → `feature = "bundled-streams"`. `stdlib_features::module_to_features("fastify")` → `["bundled-fastify"]`; `"streams"` → `["bundled-streams"]`. `optimized_libs` async-wrapper allowlist gains `bundled-streams` + `bundled-fastify`. `well_known_bindings.toml` gains `[bindings.streams]` + `[bindings.fastify]`. All 6 `perry well_known` tests pass. **Brings the in-tree wrapper port count to 34 — all originally-planned Phase 5 wrappers are now ported.** What stays in perry-stdlib: `framework` module (HTTP request/response plumbing not specific to fastify), readline (#347 async-runtime path), perry/* runtime-coupled modules (perry/ui via per-platform crates, perry/thread, perry/i18n, perry/system, perry/tui, perry/updater), fs / path / os / crypto built-ins, async/Promise machinery, GC. Target end state from #466 reached.

- **v0.5.571** — Refs #466 (Phase 5 steps 30+31+32 — port ws + net + http in parallel): three ports landed together (one manual, two parallel subagents in worktrees) covering the event-driven network surface — Node `http`/`https`, `ws` (WebSocket), and `net`/`tls` (raw TCP). All three crates depend only on `perry-ffi` v0.5 — closure invocation (`JsClosure::call*` for callback dispatch), GC root scanner (`iter_handles_of` + `gc_register_root_scanner` to pin closure pointers stored in handles, issue #35 pattern), and `notify_main_thread()` after every event push so the main loop wakes promptly instead of waiting on the event-loop heartbeat. Each wrapper exposes its own `js_<name>_process_pending` + `js_<name>_has_pending` for codegen's event-loop tick. **`perry-ext-ws`** (~900 LOC, manual port): 16 exports — `js_ws_connect` (Promise) / `js_ws_connect_start` (sync) / `js_ws_send` / `js_ws_close` / `js_ws_send_to_client` / `js_ws_close_client` / `js_ws_is_open` / `js_ws_message_count` / `js_ws_receive` / `js_ws_wait_for_message` / `js_ws_handle_to_i64` / `js_ws_on` / `js_ws_server_new` / `js_ws_server_close` / pump pair. tokio-tungstenite for client + server WebSocket. iOS NSURLSession delegation deliberately skipped — same as perry-stdlib's split. **`perry-ext-net`** (~705 LOC, subagent worktree): 11 exports — `js_net_socket_alloc` / `_connect` / `_method_connect` / `_write` / `_end` / `_destroy` / `_on` / `_upgrade_tls` + `js_tls_*` for direct `tls.connect()` + pump pair. Replaced the agent's `notify_main_thread` workaround with the actual `perry_ffi::notify_main_thread()` call now that the symbol exists on main (added in v0.5.568). **`perry-ext-http`** (~610 LOC, subagent worktree): 13 `js_http_*` exports — `request` / `https_request` / `get` / `https_get` constructors (options-object parsed via `perry_ffi::json_stringify` → `serde_json::Value` so `{ host, port, path, method, headers, timeout }` Just Works without per-field shape gymnastics); `client_request_write` / `_end` for body building (idempotent on `ended=true`); `_on` for `'response'` / `'error'` (ClientRequest) and `'data'` / `'end'` / `'error'` (IncomingMessage); `_set_header` / `_set_timeout`; `_status_code` / `_status_message` / `_response_headers` (last builds a shape-keyed object via `build_object_shape` + `js_object_alloc_with_shape`); pump pair. Both `http` and `https` route through one shared `dispatch_request` that funnels into a lazy per-process `HTTP_CLIENT` (reqwest with rustls-tls + http2, pool_max_idle_per_host=16, default User-Agent — same connection-pool reuse trick perry-ext-fetch uses). spawn_blocking + tokio::Handle::current().block_on bridges async reqwest into perry-ffi's blocking-pool surface. perry-stdlib gate flips: `websocket` umbrella → `["bundled-ws"]`, new `bundled-ws = ["dep:tokio-tungstenite", "dep:futures-util", "async-runtime"]` per-binding gate; `net` umbrella → `["bundled-net"]`, new `bundled-net = ["async-runtime"]`; `tls` now depends on `bundled-net`. `pub mod ws` / `pub mod net` flipped to the new feature names. `stdlib_features::module_to_features("ws")` → `["bundled-ws"]`; `"net"` → `["bundled-net", "tls"]`; `"http"` / `"https"` join the `http-client` umbrella alongside axios/node-fetch. `optimized_libs` async-wrapper allowlist gains `bundled-ws` / `bundled-net` / `http-client`. `well_known_bindings.toml` gains `[bindings.ws]` / `[bindings.net]` / `[bindings.http]` / `[bindings.https]` (last two share the `perry-ext-http` archive). 12 unit tests pass across the three crates (4+3+5); all 6 `perry well_known` tests pass including the load-bearing `every_entry_references_a_workspace_crate` invariant. **Body chunking gap** documented across all three: `reqwest::Response::chunk()` / WebSocket message dispatch are async, but we run inside spawn_blocking, so bodies / messages are delivered as single events rather than streamed — true streaming requires a cooperative `spawn_async` perry-ffi surface (v0.6.0 followup). Brings the in-tree wrapper port count to 32. Remaining Phase 5 wrappers: streams (1341 LOC, 35 exports — should now be straightforward since http-client is the umbrella and streams imports it transitively), fastify+framework (multi-file, ~3500 LOC total).

- **v0.5.568** — Refs #466 (Phase 5 steps 28+29 — port fetch + mongodb in parallel; perry-ffi grows json_stringify + notify_main_thread): two ports landed in one release plus two new perry-ffi surface dimensions to unblock the next batch.

  - **`perry-ext-fetch`** (manual port; subagent A stalled on the watchdog so I did it inline). 41 `js_*` exports across the Web Fetch API surface — fetch core (`get` / `get_with_auth` / `post` / `post_with_auth` / `with_options` / `text`), Response (`status` / `statusText` / `ok` / `text` / `json` / `array_buffer` / `blob` / `get_headers` / `clone` / `body` / `static_json` / `static_redirect` / `count`), Headers (`new` / `set` / `get` / `has` / `delete` / `for_each` — last one uses `JsClosure::call2` with the spec-correct `(value, key)` arg order), streaming (`stream_start` / `stream_poll` / `stream_status` / `stream_close` via tokio::sync::mpsc per stream), Blob (`size` / `type` / `text` / `array_buffer` / `bytes` / `slice` / `stream`), Request (`new` / `get_url` / `get_method` / `get_body`). Single shared `do_fetch(method, url, headers, body, promise)` helper funnels every variant through one spawn_blocking + reqwest path. Lazy per-process `HTTP_CLIENT` reuses connection pool / DNS cache / TLS sessions across calls (~250KB savings per fetch); default `User-Agent` (closes #236). 6 unit tests cover headers round-trip, blob slice, request round-trip, response_static_json. perry-stdlib's `http-client` umbrella stays as `["dep:reqwest", "async-runtime"]` — both `node-fetch` and `axios` route through it via the well-known table (entries `[bindings.node-fetch]` and `[bindings.fetch]` added; `axios` already had its `[bindings.axios]` entry from v0.5.545). The optimized_libs async-wrapper allowlist will need `bundled-fetch` if a per-binding split is ever needed; for now the umbrella covers both.

  - **`perry-ext-mongodb`** (subagent B, worktree mode). 26 `js_mongodb_*` exports — full surface (`client_new` / `client_connect` / `connect` / `client_db` / `db_collection` / `client_close` / `client_list_databases` / `db_list_collections` plus `collection_*` for find_one / find / insert_one / insert_many / update_one / update_many / delete_one / delete_many / count, with paired `*_value` variants that JSON-stringify the JsValue arg via `perry_ffi::json_stringify` then deserialize into `bson::Document`). The `*_value` variants are the canonical user case: codegen's `NA_F64` arg coercion passes the JS object literal as a NaN-boxed `f64` at the FFI boundary; the wrapper json-stringifies it, parses with `serde_json`, converts to `bson::Document`, hands to mongodb. perry-stdlib's `database-mongodb` umbrella retained for backwards-compat but flipped to `["bundled-mongodb"]`; new `bundled-mongodb = ["dep:mongodb", "dep:bson", "dep:futures-util", "async-runtime"]` per-binding gate. `optimized_libs::build_optimized_libs` re-asserts `async-runtime` when stripping `bundled-mongodb`. 4 unit tests cover client URI parse, BSON Document round-trip via `json_stringify`, collection-lookup-on-invalid-db-handle, pre-connect client ref errors. Followups documented: BSON `ObjectId` as a JsValue (today's port returns it stringified), `find` cursor streaming (today batches via `cursor.try_collect()`), change streams (`watch()`), aggregation pipelines.

  - **perry-ffi v0.5.x grows two more surface dimensions** in this release. New `crates/perry-ffi/src/json.rs` exposes `json_stringify(JsValue) -> Option<String>` over the runtime's `extern "C" fn js_json_stringify` — what mongodb's `*_value` variants need to bridge codegen's NA_F64 coercion onto serde_json + bson::Document. New `crates/perry-ffi/src/event_pump.rs` exposes `notify_main_thread()` over the runtime's `js_notify_main_thread` — wakes the main thread from `js_wait_for_event` when a tokio worker has pushed an event onto a wrapper's per-module pending-events queue. Brings the perry-ffi surface to 11 dimensions total (strings, async/Promise, handles, JsValue+objects/arrays, closures, GC root scanner, BigInt, Buffer, JSON, event-pump). Both unblock the next port batch (ws / http / streams / net / fastify) which all need to wake the main thread from worker threads.

  Brings the in-tree wrapper port count to 29.

- **v0.5.567** — Refs #466 (Phase 5 step 27 — port mysql2): new `crates/perry-ext-mysql2/` ports the npm `mysql2` MySQL client. 15 `js_mysql2_*` exports — full surface (`createConnection` async ctor; `connection_end` / `connection_query` / `connection_execute` / `connection_begin_transaction` / `connection_commit` / `connection_rollback`; `createPool` sync (uses spawn_blocking + std::sync::mpsc::channel for the eager pool build since createPool returns a Handle synchronously); `pool_end` / `pool_query` / `pool_execute` / `pool_get_connection` / `pool_connection_release` / `pool_connection_query` / `pool_connection_execute`). Single-file port collapses perry-stdlib's multi-file mysql2/{connection,pool,result,types}.rs into one ~870 LOC `lib.rs`. Result format mirrors perry-stdlib's mysql2-npm-compatible `[rows, fields]` tuple — rows are objects keyed by column name (built with `build_object_shape`), fields are `{ name, type, length }` shape. Non-SELECT queries return `[ResultSetHeader, []]` where ResultSetHeader is `{ affectedRows, insertId, warningStatus }`. URL encoder for password preserved verbatim (handles `# @ : / `). URI parsing in `parse_mysql_uri` for the `{ uri: "mysql://..." }` config form. Pool-connection pattern (`getConnection` + `release`) uses a separate `MysqlPoolConnectionHandle` type — `release` simply calls `take_handle::<MysqlPoolConnectionHandle>(h)` so the underlying `PoolConnection<MySql>` returns to the pool via Drop. perry-stdlib's `database-mysql` umbrella retained for backwards-compat but flipped to `["bundled-mysql2"]`; new `bundled-mysql2 = ["dep:sqlx", "async-runtime"]` per-binding gate is what the well-known flip toggles. perry-stdlib's pg + mysql2 modules now share the cfg `any(bundled-pg, bundled-mysql2)` since both rely on sqlx (the legacy `database-postgres`/`database-mysql` umbrellas now go through their respective `bundled-*` features). `optimized_libs::build_optimized_libs` re-asserts `async-runtime` when stripping `bundled-mysql2`. 4 unit tests cover config defaults / URL password encoding / URI parse / SELECT-vs-INSERT classifier. Brings the in-tree wrapper port count to 27.

- **v0.5.566** — Refs #466 (Phase 5 step 26 — port pg): new `crates/perry-ext-pg/` ports the npm `pg` PostgreSQL client. 10 `js_pg_*` exports — full surface (`client_new` sync ctor + `client_connect` deferred TCP open; `client_query` no-params + `client_query_params` parameterized; `client_end`; combined-API `connect` for back-compat; `pool_new` sync + lazy `connect_lazy`-style + `create_pool` eager factory; `pool_query`; `pool_end`). Single-file port collapses perry-stdlib's multi-file pg/{connection,pool,result,types}.rs into one ~600 LOC `lib.rs` using only perry-ffi v0.5 surface. Row objects keyed by column name via `build_object_shape(&row.columns().iter().map(|c| c.name()).collect::<Vec<_>>())` + `js_object_alloc_with_shape` + `js_object_set_field` (replaces perry-stdlib's `js_object_alloc(0, n)` no-shape pattern). Result wrapper builds `{ rows, fields, rowCount, command }` shape; field-def objects are `{ name, dataTypeID, tableID }`. Param-array extraction handles `Null` / `String` / `Int` / `Number` / `Bool`; BigInt deferred (perry-ffi BigInt surface is in place from v0.5.556 but the JS array iteration shape needs an extra adapter — followup once a wrapper actually demands it). `dispatch`-style helper not used here because each method's `get_handle_mut` + connection-or-error logic is structurally different (pre-connect / connected / pool / pool-pending). perry-stdlib's `database-postgres` umbrella retained for backwards-compat but flipped to `["bundled-pg"]`; new `bundled-pg = ["dep:sqlx", "async-runtime"]` per-binding gate is what the well-known flip toggles. perry-stdlib's pg + mysql2 modules share the sqlx dep; for now both compile when EITHER `bundled-pg` OR `database-mysql` is on (mysql2 keeps its existing umbrella until its own port lands). `optimized_libs::build_optimized_libs` re-asserts `async-runtime` when stripping `bundled-pg` (joins the existing async-wrapper allowlist) so perry-stdlib's perry_ffi_async.rs shim still compiles. 5 unit tests cover PgConfig defaults / to_url with and without database / SELECT-vs-INSERT classifier / client_new returns a non-zero handle. Brings the in-tree wrapper port count to 26.

- **v0.5.565** — Refs #466 (Phase 5 step 25 — port ioredis): new `crates/perry-ext-ioredis/` ports the npm `ioredis` Redis client. 18 `js_ioredis_*` exports — full surface (`new` constructor sync; `connect` / `set` / `setex` / `get` / `del` / `exists` / `incr` / `decr` / `expire` / `ping` / `hget` / `hset` / `hgetall` / `hdel` / `hlen` / `quit` async via `spawn_blocking + JsPromise + tokio::Handle::current().block_on`; `disconnect` sync). Lazy connection: `MultiplexedConnection` cached per handle in a global `HashMap` (matches perry-stdlib's existing copy). Env-var-driven URL construction (`REDIS_HOST` / `REDIS_PORT` / `REDIS_PASSWORD` / `REDIS_TLS`) preserved verbatim. Internal `dispatch<F, Fut, T>(handle, op_label, op)` helper takes ownership of the connection-acquisition + 10s timeout + result-mapping boilerplate, so each command function is two lines: read the args, call dispatch with a closure that runs the redis op. The new `ToJsValue` trait converts Redis return types (`()`, `i64`, `Option<String>`, `String`, `bool`) into `JsValue` for promise resolution — `()` → `"OK"` string per ioredis semantics, `Option<String>` → `JsValue::NULL` on None. `hgetall` returns a dynamically-keyed object built via `build_object_shape(&keys)` + `js_object_alloc_with_shape` + `js_object_set_field` (replaces perry-stdlib's `js_object_alloc(0, n)` no-shape pattern; observable behavior is identical since user code accesses fields through dynamic property lookup). perry-stdlib's `database-redis` umbrella retained for backwards-compat but flipped to `["bundled-ioredis"]`; new `bundled-ioredis = ["dep:redis", "async-runtime"]` per-binding gate is what the well-known flip toggles. `optimized_libs::build_optimized_libs` re-asserts `async-runtime` when stripping `bundled-ioredis` (joins the existing async-wrapper allowlist) so perry-stdlib's `perry_ffi_async.rs` shim still compiles. Brings the in-tree wrapper port count to 25.

- **v0.5.564** — Refs #466 (Phase 5 step 24 — port cron + clear external-fetch blocker): two related landings.

  First, the rogue `.claude/worktrees/agent-*` submodule gitlinks (mode 160000) that perry's index has carried since v0.4.23 without a matching `.gitmodules` were untracked. Cargo's `git = "https://github.com/PerryTS/perry"` fetch from external bindings repos failed with `failed to update submodule .claude/worktrees/agent-a1599b8e — no URL configured for submodule`. With those entries removed and pushed, `PerryTS/tursodb-bindings` and `PerryTS/iroh-bindings` (and any future external wrapper) `cargo build` cleanly against the live `main` branch. `docs/src/native-libraries/abi.md` updated to document the v0.5.x git-URL approach as the supported consumption mechanism + the v0.6.0 plan to invert type ownership and unblock crates.io publication.

  Second, new `crates/perry-ext-cron/` ports the npm `node-cron` scheduler. 14 `js_cron_*` exports — full surface (`validate` / `schedule` / `job_start` / `job_stop` / `job_is_running` / `next_date` / `next_dates` / `describe` for user-facing API; `timer_tick` / `timer_has_pending` for the codegen event-loop hooks; plus the 4 `set_interval` / `clear_interval` / `set_timeout` / `clear_timeout` symbol-surface placeholders matching perry-stdlib's no-op behavior). Architecture parallels perry-stdlib: global `CRON_TIMERS` queue + main-thread `js_cron_timer_tick` walks expired entries and invokes `JsClosure::call0` + GC root scanner pins each callback closure pointer so a malloc-triggered sweep between schedule and tick can't free it. Codegen calls `js_cron_timer_tick` and `js_cron_timer_has_pending` by symbol name from `module_init.rs`, so as long as perry-ext-cron's `.a` exports them, the well-known flip can swap perry-stdlib's copy out without touching codegen. perry-stdlib's `scheduler` umbrella retained for backwards-compat but flipped to `["bundled-cron"]`; new `bundled-cron = ["dep:cron", "dep:tokio-cron-scheduler", "async-runtime"]` per-binding gate is what the well-known flip toggles. `module_to_features("cron"|"node-cron") => &["bundled-cron"]`. 5 unit tests cover validate (5-field + 6-field + garbage), schedule-then-stop round-trip, describe rendering. Brings the in-tree wrapper port count to 24.

- **v0.5.563** — Refs #469 (status audit: unified plan harmonious end-to-end as of v0.5.562): comprehensive cross-phase audit confirming the v0.5.528 schema in `perry-api-manifest::ApiEntry` IS the foundation Phases B / C / E all reuse, with no schema drift between phases. Phase B (HIR `module_has_symbol` from two sites — standalone access in `lower/expr_member.rs`, chained calls in `lower/expr_call.rs` after the v0.5.562 fix), Phase C (`emit_markdown` / `emit_dts` walk `API_MANIFEST` directly), Phase E (`resolve.rs::parse_native_library_manifest` parses the same conceptual shape from `package.json`'s `perry.nativeLibrary` block; `abi_version` flows through the same `ApiEntry::abi_version` field). The `source` enum's `Stdlib → WellKnown → External` migration path works without changing user-visible behavior — the unimplemented-API check still fires, the docs serializer still emits, the linker still finds the symbol regardless of which arm sources the entry. Audit posted as a comment on #469. Closure status: #463 / #464 / #465 closed; #424 / #425 closed (external repos); #466 still open pending Phase 5 cleanup (5 wrapper batches remain: `ws` / DB drivers / `fetch` / `fastify` / misc — plus the `PERRY_USE_WELL_KNOWN=1` opt-in flip and the perry-stdlib defaults shrink).
- **v0.5.562** — Closes #463 (compile-time error for unimplemented Node / Web APIs — chained-call leak fix): the v0.5.528 unimplemented-API gate fired for standalone property accesses (`const x = crypto.subtle;` errored cleanly) but missed chained method calls (`crypto.subtle.encrypt(...)` compiled cleanly and silently returned undefined). Root cause: the `module.Class.staticMethod()` arm in `crates/perry-hir/src/lower/expr_call.rs` pattern-matches the AST shape directly and short-circuits the chain into a single `NativeMethodCall` HIR variant, never recursing through `lower_member` where the gate lived. Without recursion the gate never inspected the inner `crypto.subtle` access, so `Expr::NativeMethodCall { module: "crypto", class_name: Some("subtle"), method: "encrypt" }` flowed through to codegen, which silently failed to find a handler and emitted `undefined`. Fix is a parallel manifest check at the same site: before constructing the `NativeMethodCall`, consult `perry_api_manifest::module_has_symbol(module_name, &class_name)` and bail with the same `(#463)` diagnostic when the manifest knows about the module but doesn't list the class/sub-namespace. Same `PERRY_ALLOW_UNIMPLEMENTED=1` escape hatch as the lower_member gate. The 4 sub-namespaces in the existing exclude list (`fs.promises` / `fs.constants` / `path.posix` / `path.win32`) keep working — they're allowed-listed before the manifest check fires. End-to-end smoke verified: `crypto.subtle.{generateKey, encrypt, foo}` and `await crypto.subtle.encrypt(...)` all error correctly; `crypto.randomBytes(16)` (legitimate API) still compiles. All perry-hir + perry-api-manifest + perry-codegen unit tests pass. Closes the last unchecked #463 acceptance criterion ("`crypto.subtle` reference produces a compile error pointing at the source line" — for the chained-call form).
- **v0.5.561** — Refs #465 (stretch goal — ship the auto-generated `.d.ts` to user editors): `perry types` now writes the auto-generated stdlib `.d.ts` to `.perry/types/stdlib/index.d.ts` alongside the hand-curated `perry/{ui,thread,i18n,system,media,tui}` stubs. The `docs/api/perry.d.ts` artifact is `include_str!`-embedded into perry, so the binary always carries a current snapshot — the v0.5.560 drift gate guarantees the embedded copy matches the manifest. `perry init`'s default tsconfig.json picks it up via `include: ["src", ".perry/types/stdlib/index.d.ts"]`, so user code that imports `dotenv` / `crypto` / `mysql2` / etc. gets editor squiggles in VSCode / WebStorm before the user runs `perry`. Together with v0.5.560's drift gate, this closes the entire #465 acceptance list — including the "Stretch — `.d.ts` emitted alongside, opt-in via package config" item, which v0.5.561 makes opt-out (default-on for `perry init` projects) since the cost of carrying it is just one `include_str!` at compile time.
- **v0.5.560** — Closes #465 (release-workflow drift gate for the auto-generated API docs): closes the last unchecked acceptance criterion of #465 — "Release workflow regenerates docs automatically (no drift from code)". New `api-docs-drift` job in `.github/workflows/test.yml` runs `./scripts/regen_api_docs.sh` on every push + PR, then `git diff --exit-code`s `docs/src/api/reference.md` and `docs/api/perry.d.ts`. If a code change updates `crates/perry-api-manifest/src/entries.rs` without re-committing the artifacts, the job fails with an explicit fix recipe (`./scripts/regen_api_docs.sh && git add ... && git commit ...`) and a stat-summary + first 200 diff lines so reviewers see immediately what drifted. Caught 45 lines of staleness on first run (artifacts were last regenerated at v0.5.529; tursodb queryAll/queryOne, iroh peer-conn surface, and a handful of new perry-stdlib bindings had landed since). Regenerated `docs/src/api/reference.md` (now 397 entries across 45 modules, was 378/43) and `docs/api/perry.d.ts` (auto-stamped with `Perry version: 0.5.560`). The flow now: PR touches the manifest → CI re-runs the regen → drift gate fails until the PR also commits the regenerated artifacts → reviewer sees the doc diff alongside the code diff. With the drift gate in place, the docs stay in sync without depending on the maintainer remembering to run a script.
- **v0.5.559** — Refs #466 (Phase 3 — `perry native` CLI scaffolding): new `perry native init` / `validate` / `list` subcommands drop the barrier to writing a native-bindings wrapper from "hours of archaeology in Bloom's source" to "minutes of init+validate". `perry native init <name>` scaffolds a new wrapper package — package.json (perry.nativeLibrary block with abiVersion + functions[] + per-target build config), src/index.ts (TypeScript surface), Cargo.toml (perry-ffi git dep + user's upstream crate), src/lib.rs (one example #[no_mangle] entry that reads/echoes a string), README, MIT LICENSE, .gitignore, and a multi-target `.github/workflows/release.yml` that prebuilds staticlibs for x86_64 + aarch64 macOS/Linux + Windows on tag and attaches them to the GH release. `perry native validate` parses package.json, runs `cargo build --release` (skippable with `--no-build`), locates the resulting `.a` / `.lib` / `.dylib` / `.so`, walks `nm -gP` output for defined `js_*` symbols, and diffs against the manifest's `functions[].name` array — reports missing-from-staticlib (❌, exits 1) and undeclared-in-manifest (⚠) lists. `perry native list` enumerates the well-known table (text + JSON formats); JSON includes package / crate / lib / tracking issue. Templates ship as `include_str!` blobs from `crates/perry/templates/native-package/` so `perry native` works without any external resource lookups. End-to-end smoke verified: `init` produces a directory that `cargo build --release` accepts (modulo the user adding their upstream Rust crate dep), and `validate` correctly diffs declared-vs-exported on a fixture. Together with the v0.5.557 external-repo move (tursodb + iroh) and the v0.5.532-onwards well-known table, this completes #466's Phase 3 — the developer-experience tooling story external wrapper authors needed. **Open questions still outstanding:** prebuilt artifact distribution (npm postinstall vs vendor in tarball — tracked separately), `perry-ffi` publication to crates.io.
- **v0.5.558** — Refs #466 (Phase 5 step 23 — port nodemailer): new `crates/perry-ext-nodemailer/` ports the npm `nodemailer` SMTP transport package via `lettre`. 3 `js_nodemailer_*` exports — `createTransport` (sync, parses nested SMTP config object via `js_object_get_field` indexed lookups: host=0, port=1, secure=2, auth={user=0, pass=1}=3 — same shape-ordered layout perry-runtime emits at the call site), `sendMail` (async via `spawn_blocking` + `JsPromise` + `tokio::Handle::current().block_on`, builds `lettre::Message` from the mail-options object's `from`/`to`/`subject`/`text`/`html` fields, resolves with an info object built via `build_object_shape(&["messageId","response"])` + `js_object_alloc_with_shape` + `js_object_set_field`), `verify` (async, tests SMTP connection). The async exports take options as `f64` (NaN-boxed JsValue at the FFI boundary) to avoid SysV AMD64 ABI mismatches with `JsValue`-typed args — same trick perry-stdlib uses. perry-stdlib's `email = ["dep:lettre", "async-runtime"]` umbrella retained for backwards-compat but flipped to `["bundled-nodemailer"]`; new `bundled-nodemailer = ["dep:lettre", "async-runtime"]` per-binding gate is what the well-known flip toggles. `optimized_libs::build_optimized_libs` re-asserts `async-runtime` when stripping `bundled-nodemailer` (joins the existing `bundled-bcrypt` / `bundled-argon2` async-wrapper match) so perry-stdlib's `perry_ffi_async.rs` shim still compiles after the flip. 3 unit tests cover SMTP-config defaults, handle round-trip, null-config fallback. Brings the in-tree wrapper port count to 23.
- **v0.5.557** — Refs #466 (move tursodb + iroh to external repos, validating the third-party-wrapper hypothesis end-to-end): the perry-ext-tursodb (#424) and perry-ext-iroh (#425) wrappers are no longer in the perry monorepo. They moved to standalone third-party repositories at https://github.com/PerryTS/tursodb-bindings (`bun add @perryts/tursodb`) and https://github.com/PerryTS/iroh-bindings (`bun add @perryts/iroh`). Both ship a Cargo.toml that depends on `perry-ffi = { git = "https://github.com/PerryTS/perry", branch = "main" }`, the existing src/lib.rs unchanged, a `package.json` with a full `perry.nativeLibrary` block (abiVersion `"0.5"` + `functions[]` declarations + per-target `cargo_features`/`frameworks`/etc.), a README with usage examples, and an MIT LICENSE. perry's compile-time resolver already walks `node_modules/<pkg>/package.json` for `perry.nativeLibrary` blocks (see `crates/perry/src/commands/compile/resolve.rs` + `collect_modules.rs`), so user code that does `import * as tursodb from "tursodb"` after a `bun add @perryts/tursodb` Just Works without any well-known-table magic. perry workspace surgery: removed `crates/perry-ext-tursodb/` and `crates/perry-ext-iroh/` directories; removed both from `[workspace.members]` / `[workspace.default-members]` / `[workspace.dependencies]` in the root Cargo.toml; removed `[bindings.tursodb]` and `[bindings.iroh]` from `well_known_bindings.toml` (with a doc comment explaining why and pointing readers at the external repos); kept `module_to_features("tursodb"|"iroh") => &[]` in `stdlib_features.rs` (no perry-stdlib feature gate to manage either way); kept the API_MANIFEST entries (the API surface description still applies — it's just sourced externally now). `cargo build --release -p perry-stdlib -p perry` clean post-removal; all 6 well-known tests pass (including `every_entry_references_a_workspace_crate` — no entries reference removed crates anymore). Wrapper port count drops to 22 in-tree, plus 2 external. The 3 wrappers that originated as net-new functionality outside perry-stdlib (#424 tursodb, #425 iroh) now live in their natural home; the 22 wrappers that port existing perry-stdlib code stay in the monorepo as the "well-known" set the compiler ships out of the box.
- **v0.5.556** — Refs #466 (Phase 1.6 + Phase 5 step 22 — perry-ffi BigInt+Buffer surface, port ethers): perry-ffi gains its 8th + 9th surface dimensions. New `crates/perry-ffi/src/bigint.rs` re-exports `BigIntHeader` + `BIGINT_LIMBS` from perry-runtime, declares `js_bigint_from_string` extern, and exposes safe `alloc_bigint_from_str(&str) -> *mut BigIntHeader` + `read_bigint_limbs(ptr) -> Option<[u64; 16]>`. New `crates/perry-ffi/src/buffer.rs` re-exports `BufferHeader`, exposes `alloc_buffer(&[u8]) -> *mut BufferHeader` (calls perry-runtime's `buffer_alloc` + sets length + memcpy's the bytes) + `read_buffer_bytes(ptr) -> Option<&[u8]>`. Both modules' types are part of the perry-ffi semver promise — runtime renumbering bumps perry-ffi major. 7 new unit tests cover BigInt round-trip / zero / null + Buffer round-trip / empty / null / non-UTF-8 binary bytes. New `crates/perry-ext-ethers/` ports the npm `ethers` utility surface — 8 `js_ethers_*` + `js_keccak256_native` / `js_keccak256_native_bytes` exports — full surface (getAddress / Wallet.createRandom / parseEther / formatEther / parseUnits / formatUnits / keccak256_native / keccak256_native_bytes). The `Wallet.createRandom` constructor uses perry-ffi's `build_object_shape(&["address","privateKey"]) + js_object_alloc_with_shape + js_object_set_field` — same shape construction pattern commander/sharp/etc use. Hand-rolled keccak256 implementation (same as perry-stdlib's) — no extra crypto crate dep. perry-stdlib's `pub mod ethers` flipped to `bundled-ethers` feature gate (default-on through `crypto`); module_to_features("ethers") now returns `&["bundled-ethers"]` instead of the broader `crypto` umbrella since the keccak256 dep is hand-rolled. 4 unit tests cover keccak256 known vector, EIP-55 reference vector (Vitalik's address), format-with-decimals basic, parse-units round-trip. Brings the wrapper port count to 24 + future BigInt-using wrappers (postgres int8, mongodb decimal128, web3.js, ethereum-cryptography) unblocked.
- **v0.5.555** — Refs #466 (Phase 5 step 21 — port commander): new `crates/perry-ext-commander/` ports the npm `commander` CLI parser. 15 `js_commander_*` exports — full surface: fluent setters (`name` / `description` / `version` / `option` / `requiredOption` / `command` / `action`), `parse` entry that dispatches to `.action(opts => ...)` after binding `--flag value` / `-f value` / `--flag=value` / `--flag` (boolean) / positional args / subcommands, post-parse query accessors (`opts` / `getOption` / `getOptionNumber` / `getOptionBool` / `argsCount` / `getArg`), automatic `--help` / `-h` and `--version` / `-V` (process::exit(0) on hit). Replaces perry-stdlib's `js_object_alloc(0, count)` + `js_object_set_field_by_name` with perry-ffi's shape-aware `build_object_shape(&keys)` + `js_object_alloc_with_shape` + `js_object_set_field` (by index in the shape) — same observable behavior since user code accesses `options.port` through dynamic property lookup either way. GC root scanner registered via `iter_handles_of::<CommanderHandle> + gc_register_root_scanner` so `.action(cb)` closures stay alive across malloc-triggered GC cycles between `.action()` registration and `.parse()` invocation. `closure.call1(opts_value.bits())` invokes the user's action via perry-ffi's closure surface. 5 unit tests cover flag-spec parsing (long+short / boolean / optional-value variants), fluent-setter round-trip, option registration. perry-stdlib's `pub mod commander` flipped to `#[cfg(feature = "bundled-commander")]`; default-on through `full`. Brings the wrapper port count to 23.
- **v0.5.554** — Refs #425 (iroh peer-connection + stream surface): perry-ext-iroh gains the full request-response peer-to-peer surface — `connect` / `acceptOne` / `openBi` / `acceptBi` / `streamWrite` / `streamFinish` / `streamReadToEnd` / `connClose`. Server flow: `bind() -> acceptOne() -> acceptBi() -> streamReadToEnd() + streamWrite() + streamFinish()`. Client flow: `bind() -> connect(serverNodeId) -> openBi() -> streamWrite() + streamFinish() + streamReadToEnd()`. ALPN is hardcoded to `b"perry-iroh/0"` for v0 — `js_iroh_bind` switched from `Endpoint::bind(presets::N0)` to `Endpoint::builder(presets::N0).alpns(vec![PERRY_IROH_ALPN.to_vec()]).bind()` so the same endpoint can serve both client and server roles. New handle types: `IrohConnection` (wraps `iroh::endpoint::Connection<HandshakeCompleted>`), `IrohBiStream` (wraps `(SendStream, RecvStream)` in a pair of `tokio::sync::Mutex`es so the async `write_all` / `read_to_end` / `finish` calls can hold a lock across awaits without deadlocking). `js_iroh_conn_close` follows the take_handle pattern from `js_iroh_close` so closing is idempotent. `EndpointId::from_str` parses the base32-hex node id strings users share. `streamReadToEnd` returns the bytes UTF-8-decoded as a JS string (binary-bytes payload variant deferred — needs the same JsValue::from_object_ptr trick zlib uses). API_MANIFEST gains 8 new method entries so #463's check accepts user code that calls them. Together with v0.5.553's tursodb queryAll/queryOne extension, this completes the meaningful-implementation pass on both #424 and #425. Followups: per-call ALPN strings (we hardcode v0), connection-event callbacks (closure invocation already shipped in v0.5.542; just needs an `on()` surface), broadcast-style fan-out across connections.
- **v0.5.553** — Refs #424 (tursodb queryAll / queryOne — close the row-object gap): perry-ext-tursodb gains `js_turso_query_all` and `js_turso_query_one` so user code can `await db.queryAll("SELECT * FROM users")` and get back `Array<{ id, name, email, … }>` row-objects keyed by column name. Implementation walks `Statement::columns()` for the column-name list, calls `build_object_shape(&[name1, name2, …])` to get the (packed_keys, shape_id) pair perry-ffi v0.5 wants, then for each row builds the object with `js_object_alloc_with_shape` and fills fields by index using `js_object_set_field`. Values map turso `Null` → `JsValue::NULL`, `Integer` (i32-fitting) → `from_int32`, `Real`/large-Integer → `from_number`, `Text` → `from_string_ptr` over a fresh `alloc_string`, `Blob` → lowercase-hex `from_string_ptr` (matches perry-stdlib sqlite.rs convention to avoid pulling the `hex` crate into the link). All shape construction runs inside `spawn_blocking` so the main thread stays responsive while turso fetches — `JsValue` is `repr(transparent) u64` and `Send`, so resolving the promise from the worker thread crosses the boundary cleanly. API_MANIFEST gains `tursodb.queryAll` / `queryOne` so #463's check accepts user code that calls them. Param-binding (`?` placeholders) is the next gap — needs a JS `Array<JsValue>` reader on the perry-ffi side that maps onto turso's `IntoParams`. Closes #424's row-object construction TODO from v0.5.543.
- **v0.5.552** — Refs #466 (Phase 5 step 20 — port rate-limiter-flexible): new `crates/perry-ext-ratelimit/` ports the npm `rate-limiter-flexible` token-bucket rate-limiting package via `governor`. 11 `js_ratelimit_*` exports — 2 sync constructors (`new` / `new_keyed`), 6 async (`consume` / `get` / `delete` / `block` / `penalty` / `reward`) bridged through `spawn_blocking` + `JsPromise`, 3 sync (`check` / `remaining` / `reset`). All async exports return JSON-encoded result strings (same format as perry-stdlib's existing copy: `remainingPoints` / `msBeforeNext` / `consumedPoints` / `isFirstInDuration`). `rate-limit` umbrella feature retained for backwards-compat but flipped to `["bundled-ratelimit"]`; new `bundled-ratelimit = ["dep:governor", "async-runtime"]` per-binding gate is what the well-known flip toggles. New stdlib_features mapping `"rate-limiter-flexible" => &["bundled-ratelimit"]`. 5 unit tests cover handle creation, check passes when under quota, remaining for unknown key, reset clears keyed limiters, invalid-handle returns permissive true. Brings the wrapper port count to 22.
- **v0.5.551** — Refs #466 (Phase 5 step 19 — port sharp): new `crates/perry-ext-sharp/` ports the npm `sharp` image-processing package — image transforms via the `image` crate. 18 `js_sharp_*` exports — full surface (`fromFile` / `fromBuffer` constructors; `resize` / `rotate` / `flip` / `flop` / `grayscale` / `blur` / `sharpen` / `crop` sync transforms; `jpeg` / `png` / `webp` format selectors; `toFile` / `toBuffer` / `metadata` async exports via spawn_blocking + JsPromise; `width` / `height` accessors). The 3 async exports use the same `JsPromise::new() + spawn_blocking { promise.resolve_string(...) | promise.reject_string(...) }` pattern as bcrypt/argon2 — image processing is CPU-bound, not async-Rust, so spawn_blocking is the right bridge. `read_buf` (perry-ffi's `read_bytes` wrapper) handles the binary-bytes path for `fromBuffer` since image payloads aren't UTF-8. perry-stdlib's `image` umbrella feature retained for backwards-compat but flipped to `["bundled-sharp"]`; new `bundled-sharp = ["dep:image"]` per-binding gate is what the well-known flip toggles. 6 unit tests cover width/height, resize-with-aspect-ratio, rotate-90 dimension swap, jpeg quality setting, invalid-handle return paths. Brings the wrapper port count to 21.
- **v0.5.550** — Refs #466 (Phase 5 step 18 — port cheerio): new `crates/perry-ext-cheerio/` ports the npm `cheerio` HTML-parsing package — jQuery-like traversal via the `scraper` crate. 19 `js_cheerio_*` exports — full surface (`load` / `loadFragment` for document creation; `select` / `find` / `children` / `parent` / `first` / `last` / `eq` for traversal; `text` / `html` / `attr` / `length` for accessors; `hasClass` / `is` for predicates; `toArray` / `texts` / `attrs` for array-returns). Uses perry-ffi v0.5 surface only — `register_handle` for CheerioHandle / CheerioSelectionHandle (handle stores html + selector strings, not Html parse trees, since `Html` isn't `Send`); `js_array_alloc` / `js_array_push` / `JsValue::from_string_ptr` for the array-returning variants. perry-stdlib's `html-parser` umbrella feature retained for backwards-compat but flipped to `["bundled-cheerio"]`; new `bundled-cheerio = ["dep:scraper"]` per-binding gate is what the well-known flip toggles. 5 unit tests cover load + select + text, length, attr, hasClass, first traversal. Brings the wrapper port count to 20.
- **v0.5.549** — Refs #466 (Phase 5 step 17 — port moment): new `crates/perry-ext-moment/` ports the npm `moment` package — momentjs-compatible date manipulation via chrono. 28 `js_moment_*` exports — full surface (now/from_timestamp/parse + format/toISOString + valueOf/unix + year/month/date/day/hour/minute/second/millisecond + add/subtract/startOf/endOf/diff + isBefore/isAfter/isSame/isBetween/isValid + clone/fromNow/toDate). MomentHandle carries `is_valid` flag (preserved from perry-stdlib's existing copy) so `moment("garbage").isValid()` returns false instead of throwing. Boolean predicates return NaN-boxed `TAG_TRUE` (0x7FFC_..._4) / `TAG_FALSE` (0x7FFC_..._3) f64s — same ABI as perry-stdlib so codegen-side dispatch keeps working unchanged. perry-stdlib's `pub mod moment` flipped to `#[cfg(feature = "bundled-moment")]`; default-on through `full`. 7 unit tests cover now / round-trip / add-subtract / comparison predicates / clone / parse-valid / parse-garbage. Brings the wrapper port count to 19.
- **v0.5.548** — Refs #466 (Phase 5 step 16 — port dayjs / date-fns): new `crates/perry-ext-dayjs/` ports the npm `dayjs` and `date-fns` date-manipulation packages. Same shape as decimal (sync, handle-based, chrono under the hood) — 24 `js_dayjs_*` exports (now/from_timestamp/parse + format/toISOString + valueOf/unix/year/month/date/day/hour/minute/second/millisecond + add/subtract/startOf/endOf/diff + isBefore/isAfter/isSame/isValid) + 11 `js_datefns_*` exports (format/parseISO + addDays/Months/Years + differenceInDays/Hours/Minutes + isAfter/isBefore + startOfDay/endOfDay). Uses perry-ffi v0.5 surface only: `register_handle` / `get_handle` for DayjsHandle wrapping `chrono::DateTime<Utc>`, `alloc_string` / `read_string` for the format-pattern + ISO-string round-trip, `Handle` ↔ `f64` bit-cast via the same trick perry-stdlib uses (NaN-boxed handle as f64 return). chrono is already an always-on perry-stdlib dep so the `bundled-dayjs` feature gate just toggles whether `pub mod dayjs;` compiles in (default-on through `full`; the well-known flip strips it). Both `dayjs` and `date-fns` route to the same wrapper. 6 unit tests cover now / from_timestamp round-trip / parse-ISO / add-subtract-days / comparison predicates / datefns difference. Brings the wrapper port count to 18.
- **v0.5.547** — Refs #466 (Phase 5 step 15 — port decimal): new `crates/perry-ext-decimal/` ports the npm `decimal.js` / `bignumber.js` arbitrary-precision arithmetic packages. 30 FFI exports — full surface (from_number/from_string + plus/minus/times/div/mod/pow/sqrt + abs/neg/round/floor/ceil + toFixed/toString/toNumber + eq/lt/lte/gt/gte/cmp/isZero/isPositive/isNegative + per-binop number variants). Same dispatch as perry-stdlib's existing copy: `rust_decimal` under the hood, divide-by-zero returns ZERO (perry-stdlib convention), pow/sqrt fall back to f64 round-trip since rust_decimal lacks them. Macros (`binop_handle!` / `binop_number!`) dedupe the basic arithmetic operators. Sync, no closures, no GC scanner — fits perry-ffi v0.5 string + handle surface exactly. perry-stdlib's `rust_decimal` flipped to `optional = true`; new `bundled-decimal = ["dep:rust_decimal"]` feature gates `pub mod decimal;`. Both `decimal.js` and `bignumber.js` npm names route to the same wrapper. 4 unit tests cover arithmetic round-trip, comparison predicates (lt/gt/eq/cmp), divide-by-zero handling, toFixed rounding. Brings the wrapper port count to 17.
- **v0.5.546** — Refs #466 (Phase 1.5 + Phase 5 step 14 — GC root scanner surface, port events): perry-ffi gains its 7th surface dimension. New `iter_handles_of<T, F>` walks every registered handle whose stored type matches `T`; new `gc_register_root_scanner(scanner)` is a thin re-export of perry-runtime's existing `extern "C"` GC scanner registration. Wrappers that store user closures (or any other GC-managed pointer) inside their handles need the scanner pair to keep those values alive across malloc-triggered GC cycles — without it, a GC between `.on(cb)` and `.emit()` sweeps `cb` (issue #35 pattern). New `crates/perry-ext-events/` ports Node's `events` module — full EventEmitter pattern (`new` / `on` / `emit` (no-args + 1-arg) / `removeListener` / `removeAllListeners` / `listenerCount`). Closures are stored as raw i64 pointers (Send + Sync — same trick perry-stdlib uses); the GC scanner reconstructs each pointer as a POINTER_TAG-tagged f64 and forwards to the runtime's `mark` callback. perry-stdlib's `pub mod events` becomes feature-gated (`bundled-events = []`, default-on through `full`). 4 unit tests cover empty / add / remove-listener / remove-all flows. Brings the wrapper port count to 16. cron + net + future wrappers that need GC-scanner integration unlocked by this commit.
- **v0.5.545** — Refs #466 (Phase 5 step 13 — port axios): first HTTP-client wrapper port. New `crates/perry-ext-axios/` ports the npm `axios` HTTP client using reqwest under the hood (same as perry-stdlib's existing copy). 5 method exports (get/post/put/delete/patch — all return Promise<Response>) + 3 response accessors (status / statusText / data). Uses perry-ffi v0.5.x's existing surface — no new helpers needed: `JsPromise` + `spawn_blocking` + `tokio::Handle::current().block_on` for the async bridge, `register_handle` for the AxiosResponseHandle, `JsValue::from_object_ptr` for POINTER_TAG-tagged response handles (issue #340 NaN-box trick preserved from perry-stdlib's original — without it, the awaiter sees a subnormal float that decays to undefined on `r.status` accesses). Refactored the 5 methods around a single `run_request` helper that takes a `FnOnce(reqwest::Client, String) -> reqwest::RequestBuilder` to dedupe the per-method boilerplate. `well_known_bindings.toml` gains `[bindings.axios]`. `module_to_features("axios") => &["http-client"]` (already mapped — no perry-stdlib feature split needed since `http-client` is already a per-binding gate). Brings the wrapper port count to 15.
- **v0.5.544** — **Closes #425** (Iroh P2P native bindings): new `crates/perry-ext-iroh/` ports the [Iroh](https://www.iroh.computer/) Rust framework — direct peer-to-peer QUIC connections with hole punching + relay fallback. Same architecture as #424's tursodb port — uses perry-ffi v0.5.x's full surface (handle registry + spawn_blocking + JsPromise + tokio::Handle::current().block_on bridge), pulls `iroh = "0.98"` directly with no perry-stdlib involvement. MVP API surface — `iroh.bind()` → Promise<handle> using N0 relay preset (sane defaults: discovery via n0 number-DNS + n0 relay servers for hole-punch fallback), `iroh.nodeId(handle)` → Promise<string> (hex-encoded Ed25519 public key, what users share for peer connections), `iroh.close(handle)` → Promise<undefined> (graceful endpoint shutdown). Full peer-connection / stream API (open_bi / read_to_end / event callbacks) deferred to followups — needs longer-lived per-stream handles + ergonomic Promise / AsyncIterable mapping over QUIC stream semantics, not blocked on perry-ffi (closure invocation already shipped in v0.5.542). NATIVE_MODULES gains `iroh`; `well_known_bindings.toml` gains `[bindings.iroh] tracking = "#425"`. API_MANIFEST gains 3 method entries. Brings the wrapper port count to 14 — covers all original Phase 5 plan + both #424/#425 from issue #466's "external bindings ecosystem v1" mandate. Both #424 and #425 confirm the third-party-wrapper hypothesis end-to-end: Rust-crate-backed bindings outside perry-stdlib live alongside the existing wrappers without conflict, on the same well-known infrastructure.
- **v0.5.543** — **Closes #424** (Tursodb native bindings): new `crates/perry-ext-tursodb/` ports the pure-Rust SQLite-compatible Tursodb engine to Perry as the first wrapper that *originated* outside perry-stdlib (i.e., not a port of an existing in-tree implementation — net-new functionality enabled by #466's bindings ecosystem). Pulls `turso = "0.2"` directly, uses perry-ffi v0.5.x's full surface (handle registry + spawn_blocking + JsPromise + JsValue). MVP API surface — `tursodb.open(filename)` → Promise<handle>, `tursodb.exec(sql)` → Promise<rowsAffected>, `tursodb.execBatch(sql)` → Promise<undefined>, `tursodb.lastInsertRowid()` (sync), `tursodb.isAutocommit()` (sync), `tursodb.close()`. Prepare / run / get / all are followups (need JsValue object construction across the spawn_blocking boundary, straightforward incremental work but not blocking). turso's API is async-first, bridged via `tokio::runtime::Handle::current().block_on(async { ... })` inside spawn_blocking closures. tursodb is the first binding accessible ONLY through the well-known path (no perry-stdlib copy exists), so `module_to_features("tursodb") => &[]` (no feature to strip; well-known table picks up by module-name match alone). NATIVE_MODULES gains `tursodb`; `well_known_bindings.toml` gains `[bindings.tursodb] tracking = "#424"`. API_MANIFEST gains 6 method entries so #463's check accepts `tursodb.*` calls. Brings the wrapper port count to 13 and validates the third-party-wrapper hypothesis end-to-end: a Rust-crate-backed binding that's NOT in perry-stdlib lives alongside the existing wrappers without conflict.
- **v0.5.542** — Refs #466 (Phase 1.4 + Phase 5 step 12 — closure invocation surface, port exponential-backoff): perry-ffi gains its 6th surface dimension. New `crates/perry-ffi/src/closure.rs` exposes `JsClosure(*const ClosureHeader)` with `from_raw` / `as_raw` / `is_null` / `call0` / `call1` / `call2` / `call3` / `call4` plus a `RawClosureHeader` re-export for FFI signatures. perry-runtime's `js_closure_call0..4` are already extern "C", so perry-ffi just declares them and wraps with type safety. `JsClosure` is `Send` so wrappers can hand closures to `spawn_blocking` for retry loops or background workers. Higher-arity calls (5..8) deferred until a real wrapper demands them. New `crates/perry-ext-exponential-backoff/` ports the npm `exponential-backoff` package's `backOff(fn)` retry-with-exp-delay loop — first wrapper that exercises the closure surface. Pure-Rust, no extra deps. Carried over the trick from perry-stdlib's existing copy where `is_valid_result(f64)` distinguishes a "real" callback result (any non-NaN OR a NaN-boxed pointer/string/int32/bool) from a "treat as failure, retry" raw IEEE NaN. perry-stdlib's `pub mod exponential_backoff` becomes feature-gated (`bundled-exponential-backoff = []`, default-on through `full`). 2 unit tests cover `is_valid_result`'s tag-aware predicates and the null-closure null-return path. Brings the wrapper port count to 12. Closure surface unblocks ws + fastify + db.transaction(fn) + #425 Iroh's event callbacks.
- **v0.5.541** — Refs #466 (perry-ffi binary-bytes helpers + Phase 5 step 11 — port zlib): perry-ffi gains `read_bytes(JsString) -> Option<&[u8]>` (binary view, no UTF-8 validation) and `alloc_bytes(&[u8]) -> JsString` (allocate without validation), plus `JsPromise::resolve(JsValue)` / `reject(JsValue)` for resolution paths that don't fit the string/bool/number shortcuts. New `crates/perry-ext-zlib/` ports gzipSync/gunzipSync/deflateSync/inflateSync (sync) + gzip/gunzip/deflate/inflate (async via spawn_blocking). Async results NaN-box as `JsValue::from_object_ptr` (POINTER_TAG) rather than STRING_TAG since gzip output isn't valid UTF-8 — matches the trick perry-stdlib's existing zlib uses. End-to-end smoke verified: `zlib.gzipSync` → `zlib.gunzipSync` round-trips a 60-char ASCII input through both default and well-known paths with matching output. The well-known flip strips perry-stdlib's `compression` feature (already a per-binding gate, no Cargo.toml split needed). Pre-existing `perry-runtime::compute_utf16_len` debug-mode UB check on `from_utf8_unchecked` blocks unit tests that exercise the FFI round trip with non-UTF-8 binary data — release-mode TS smoke tests cover the path correctly. Brings the wrapper port count to 11.
- **v0.5.540** — Refs #466 (Phase 1.3 + Phase 5 step 10 — JSValue + object/array helpers, port better-sqlite3): perry-ffi grows its 4th surface dimension (after strings v0.5, async v0.5.1, handles v0.5.x). New `crates/perry-ffi/src/jsvalue.rs` exposes a `#[repr(transparent)]` `JsValue(u64)` with constructors (`UNDEFINED`/`NULL`/`TRUE`/`FALSE` consts plus `from_bool`/`from_number`/`from_int32`/`from_string_ptr`/`from_object_ptr`), type predicates (`is_undefined`/`is_null`/`is_bool`/`is_string`/`is_pointer`/`is_int32`/`is_number`), accessors (`to_bool`/`to_number`/`to_int32`/`as_string_ptr`/`as_pointer<T>`), and `bits()`/`from_bits()` for round-tripping through promise resolutions. NaN-box tag values (TAG_UNDEFINED/NULL/FALSE/TRUE, STRING_TAG, POINTER_TAG, INT32_TAG, masks) are now part of perry-ffi's stable contract — runtime renumbering bumps perry-ffi major. Also exports `extern "C"` declarations for `js_array_alloc`/`js_array_push`/`js_array_get`/`js_array_set` and `js_object_alloc_with_shape`/`js_object_get_field`/`js_object_set_field` (the runtime symbols are already stable extern "C" exports — perry-ffi just declares them with `JsValue` instead of `JSValue`). Re-exports `ArrayHeader` + `ObjectHeader` for FFI signatures. New `build_object_shape(keys: &[&str]) -> (Vec<u8>, u32)` helper produces the (packed_keys, shape_id) pair the runtime's `js_object_alloc_with_shape` expects — `0x4646_0000` ("FF" prefix) namespaces perry-ffi-built shapes from perry-stdlib's `0x5143_0000` ("SQ"). 6 unit tests cover constants, number / int32 round trip, type predicate disjointness, shape-hash determinism, packed-keys format. New `crates/perry-ext-better-sqlite3/` (12 FFI exports — open/exec/prepare/run/get/all/pragma/begin/commit/rollback/close/inTransaction; transaction-with-closure deferred to the closure-invocation expansion) is the acceptance test for the new surface; uses `rusqlite = "0.32"` with `bundled` feature for static SQLite C source link. Caught a pre-existing perry-stdlib bug while porting (`db.pragma('user_version')` returned `""` instead of `"0"` because integer columns can't be `row.get::<String>()` — fix in this port tries String → i64 → f64 in order). 5 unit tests in perry-ext-better-sqlite3 + end-to-end TS smoke that creates a 3-row users table, queries with ORDER BY, prints rows + count. Both default and well-known paths produce identical output. **First database wrapper port — also the canonical reference for #424 (Tursodb): `perryts/tursodb-bindings` will look almost identical with `rusqlite::Connection` swapped for `tursodb::Connection`.**
- **v0.5.539** — Refs #466 (Phase 1.2 + Phase 5 step 9 — handle registry surface, port lru-cache): perry-ffi grows a 3rd surface dimension after strings (v0.5) and async (v0.5.1) — handles. New `crates/perry-ffi/src/handle.rs` exposes a generic `register_handle<T>` / `get_handle<T>` / `with_handle<T>` / `with_handle_mut<T>` / `take_handle<T>` / `drop_handle` / `handle_exists` API plus `pub type Handle = i64` and `INVALID_HANDLE = 0`. Storage is a process-wide `DashMap<i64, Box<dyn Any + Send + Sync>>` with `OnceCell`-lazy init; counter starts at 1 so `0` is reserved as the JS-falsy null sentinel. perry-ffi gets `dashmap = "6"` and `once_cell = "1"` deps (already in the link surface via perry-stdlib's transitive deps — no binary growth). Generics are Rust-side (the registry can't go through extern "C" because each `T` monomorphizes per call site); the `pub static HANDLES` symbol from perry-ffi's rlib gets linked once, so all wrapper crates that link perry-ffi share the same registry. Wrapper crates use private newtypes (`pub struct LruCacheHandle { ... }`) to namespace their entries within the shared map — type-id-based downcast in `with_handle_mut::<MyHandle>` ensures handles never leak across wrappers. perry-stdlib's `crates/perry-stdlib/src/common/handle.rs` keeps its OWN copy of the same registry; the two are separate integer spaces (the well-known flip means only one is used per program, so no collision). New `crates/perry-ext-lru-cache/` (8 FFI exports — new/get/set/has/delete/clear/size/peek) is the acceptance test; uses `lru = "0.16"` directly. perry-stdlib's `lru` dep flipped to `optional = true`; new `bundled-lru-cache = ["dep:lru"]` feature gates `pub mod lru_cache;`. `default = ["full"]` rolls in `bundled-lru-cache`. `stdlib_features::module_to_features("lru-cache") => &["bundled-lru-cache"]`. `well_known_bindings.toml` adds the `[bindings.lru-cache]` entry. End-to-end smoke verified: identical TS output across default and well-known paths (`size: 3`, correct LRU eviction order). 6 unit tests in perry-ext-lru-cache cover basic round trip, eviction-at-max-size, delete+clear, peek-doesn't-bump-recency, missing-key-returns-NaN, invalid-handle-is-no-op. Brings the wrapper port count to 9 (dotenv, nanoid, uuid, slugify, bcrypt, argon2, jsonwebtoken, validator, lru-cache); remaining Phase 5 wrappers (ws, db drivers, fetch/axios, fastify) need additional perry-ffi surface (closure invocation + JSValue object/array helpers) — separate followups.
- **v0.5.538** — Refs #466 (Phase 5 steps 7+8 — port jsonwebtoken + validator): two more sync, string-only wrappers join the well-known path. New `crates/perry-ext-jsonwebtoken/` (5 FFI exports — sign, sign_es256, sign_rs256, verify, decode) uses jsonwebtoken 10.3 with `default-features = false, features = ["rust_crypto", "use_pem"]` — explicit crypto-backend selection is required from jsonwebtoken 10.x onwards (default panics with "Could not automatically determine the process-level CryptoProvider"; docs not loud about this). New `crates/perry-ext-validator/` (15 FFI exports covering isEmail/isURL/isUUID/isAlpha/isAlphanumeric/isNumeric/isInt/isFloat/isHexadecimal/isEmpty/isJSON/isLength/contains/equals/isLowercase/isUppercase). `OnceLock`-cached UUID regex inside isUUID since the regex compiles to ~1KB+ and the original perry-stdlib copy paid that cost on every call. perry-stdlib's `crypto` umbrella forks `bundled-jsonwebtoken = ["dep:jsonwebtoken"]` (umbrella keeps it transitively for backwards compat); `validation` forks `bundled-validator = ["dep:validator", "dep:regex"]` (validation umbrella expands to bundled-validator). `stdlib_features::module_to_features` dispatches per-binding for both. `well_known_bindings.toml` gains both entries. End-to-end smoke verified: identical output across default and well-known paths. JWT-encoded token printing `eyJ` prefix mismatch (default + well-known both show `false`) is a pre-existing `NATIVE_MODULE_TABLE` dispatch issue unrelated to this port — `validator.isEmail`/`isURL` outputs match expected behavior on both paths. 8 unit tests across the two crates (3 jwt + 5 validator).
- **v0.5.537** — Refs #466 (Phase 1.1 + Phase 5 steps 5+6 — async surface for perry-ffi, port bcrypt + argon2): closes the issue's "Open question 5" by extending perry-ffi with `JsPromise`, `spawn_blocking`, `nanbox_string_bits`, and a `Promise` re-export, then ports the two password-hashing wrappers as the contract test. Surface delivery: new `crates/perry-stdlib/src/perry_ffi_async.rs` exposes four `#[no_mangle] extern "C"` shims (`perry_ffi_promise_new`, `perry_ffi_promise_resolve_bits`, `perry_ffi_promise_reject_bits`, `perry_ffi_spawn_blocking`) wrapping `async_bridge`'s public Rust functions; perry-ffi's new `crates/perry-ffi/src/async_runtime.rs` declares those symbols `extern "C"` and exposes safe Rust wrappers — `JsPromise` (Send) with `resolve_string` / `resolve_bool` / `resolve_number` / `resolve_undefined` / `resolve_null` / `reject_string`, plus `spawn_blocking<F>(f)` that boxes the FnOnce into a `*mut c_void`+trampoline pair the shim invokes on tokio's blocking pool. NaN-boxing tag constants (STRING_TAG, POINTER_MASK, TAG_UNDEFINED, TAG_NULL) are duplicated into perry-ffi as `pub` so the perry-ffi semver promise binds to them — runtime renames bump perry-ffi major. New crates: `crates/perry-ext-bcrypt/` (5 FFI exports — hash/compare/genSalt async + hashSync/compareSync sync) and `crates/perry-ext-argon2/` (5 FFI exports — hash/verify/needsRehash + sync variants). perry-stdlib's `crypto` umbrella splits: `bundled-bcrypt = ["dep:bcrypt", "async-runtime"]` and `bundled-argon2 = ["dep:argon2", "async-runtime"]` are now the per-binding gates; `crypto = [..., "bundled-bcrypt", "bundled-argon2"]` keeps `--features crypto` byte-identical for legacy callers. `stdlib_features::module_to_features("bcrypt") => &["bundled-bcrypt"]`, `"argon2" => &["bundled-argon2"]`. The well-known flip in `optimized_libs` now special-cases async wrappers — when `bundled-bcrypt` / `bundled-argon2` is stripped, `async-runtime` is re-asserted so perry-stdlib's perry_ffi_async shims still compile (otherwise the wrapper's `.a` carries unresolved `perry_ffi_*` references). End-to-end smoke verified: clean rebuild → `nm libperry_ext_bcrypt.a` shows `T _js_bcrypt_hash` defined and `U _perry_ffi_promise_new` undefined, `nm libperry_stdlib.a` (built with `--features async-runtime`) shows `T _perry_ffi_promise_new` defined; final link resolves cleanly. Both default and well-known paths produce identical observable behavior. Pre-existing `NATIVE_MODULE_TABLE` dispatch bug (bcrypt args declared `NA_F64, NA_F64` should be `NA_STR, NA_F64`) affects both paths equally — separate followup. Migration from here is mechanical for ws / db drivers / fetch — same recipe as bcrypt.
- **v0.5.536** — Refs #466 (Phase 5 step 4 — fourth port): `slugify` joins the well-known path. New `crates/perry-ext-slugify/` (staticlib + rlib, zero runtime deps beyond perry-ffi) replicates all three FFI exports — `js_slugify`, `js_slugify_with_options`, `js_slugify_strict` — including the inline accent-folding table so the port is bit-for-bit equivalent to the perry-stdlib copy without pulling a transliteration crate. Ratchets up `default = ["full"]` to include a new `bundled-slugify` feature that gates `pub mod slugify;` and `pub use slugify::*;` in `crates/perry-stdlib/src/lib.rs`. `stdlib_features::module_to_features("slugify")` now returns `&["bundled-slugify"]` so the auto-optimize default path enables the feature exactly when imported. `well_known_bindings.toml` gains `[bindings.slugify] crate = perry-ext-slugify`. End-to-end smoke verified: `perry compile <slugify ts>` (default) prints `hello-world` / `cafe-au-lait`; `PERRY_USE_WELL_KNOWN=1 perry compile <ts>` prints `well-known: routing slugify → libperry_ext_slugify.a` and `features=(no optional features)`, identical output. 4 unit tests cover lowercase + dashify, accent folding, strict-mode collapse, and custom-replacement-char (input alphabet allocated through perry-ffi to exercise the FFI path end-to-end). Migration recipe is now stable enough to be a cookie-cutter; bcrypt + argon2 next under #466 Phase 5.
- **v0.5.535** — Refs #466 (Phase 5 step 3 + bug fix): `uuid` joins the well-known path. New `crates/perry-ext-uuid/` (staticlib + rlib) implements all six FFI exports (`js_uuid_v4`, `js_uuid_v1`, `js_uuid_v7`, `js_uuid_validate`, `js_uuid_version`, `js_uuid_nil`) on top of perry-ffi v0.5 — third wrapper port, no new helpers needed in perry-ffi. Bundled crate enters `well_known_bindings.toml` as `[bindings.uuid]`. Bug fix found while porting: an orphan `#[cfg(feature = "ids")]` was lingering from the old umbrella above the new `bundled-uuid` cfg in `crates/perry-stdlib/src/lib.rs`; rustc stacks adjacent attributes onto the next item, so `pub mod uuid;` was silently being gated on BOTH `ids` AND `bundled-uuid`. Auto-optimize sets only `bundled-uuid`, so `pub mod uuid;` was excluded — `nm libperry_stdlib.a | grep _js_uuid_v4` returned nothing, and any program importing uuid link-failed under v0.5.534. The fix removes the orphan attribute. The same bug *might* have hit `bundled-nanoid` in v0.5.534 in some situations but didn't surface because every smoke test compiled with `bundled-nanoid` ran with `--features ids` from `default = ["full"]`. Verified the fix end-to-end: clean rebuild → `nm libperry_stdlib.a | grep _js_uuid_v4` shows the symbol, `perry compile test_uuid.ts` links cleanly. Note: `uuid.validate(id)` and `uuid.version(id)` return wrong values (`0` and `undefined` instead of `true`/`4`) in both default and well-known paths — pre-existing bug in `NATIVE_MODULE_TABLE`'s `args: &[NA_F64]` dispatch (should be `NA_STR` since the runtime fn takes `*const StringHeader`). Not introduced by this commit; tracked separately.
- **v0.5.534** — Refs #466 (Phase 5 step 2 — second wrapper port): `nanoid` joins `dotenv` on the per-binding feature-gated path. New `crates/perry-ext-nanoid/` (staticlib + rlib) implements the same three FFI exports (`js_nanoid`, `js_nanoid_sized`, `js_nanoid_custom`) using only `perry-ffi` — proves the v0.5 surface remains sufficient for a second port. perry-stdlib's `ids` umbrella feature splits into `bundled-uuid` + `bundled-nanoid` so each binding can be flipped independently; the umbrella stays as `ids = ["bundled-uuid", "bundled-nanoid"]` for backwards compat with existing `--features ids` callers (existing `cargo build -p perry-stdlib --features ids` produces a byte-identical archive). `stdlib_features::module_to_features` now dispatches per-binding (`"nanoid" → &["bundled-nanoid"]`, `"uuid" → &["bundled-uuid"]`). `well_known_bindings.toml` gains a `[bindings.nanoid]` entry referencing `perry-ext-nanoid`. `every_entry_references_a_workspace_crate` continues to pass — both bindings now validated. Smoke test: `PERRY_USE_WELL_KNOWN=1 perry compile <ts that imports nanoid>` logs `well-known: routing nanoid → libperry_ext_nanoid.a` and `features=(no optional features)`, produced binary calls perry-ext-nanoid's `js_nanoid` and prints the same 21-char URL-safe id as the perry-stdlib path. 4 new unit tests in perry-ext-nanoid (default length, custom size, zero-falls-back, custom-alphabet through perry-ffi). nanoid 0.4 + rand 0.9 are direct deps of the new crate. Migration recipe is mechanical now: ~20 minutes per remaining single-file wrapper (`uuid`, `slugify`, `bcrypt`, `argon2`, …).
- **v0.5.533** — Refs #466 (Phase 4 step 2): wired the actual resolution flip — `import 'dotenv'` now routes to `perry-ext-dotenv`'s bundled `.a` instead of the perry-stdlib copy, with no duplicate-symbol risk. New `bundled-dotenv` perry-stdlib feature (default-on, included in `default = ["full"]`) gates `pub mod dotenv;` and `pub use dotenv::*;`. `stdlib_features::module_to_features("dotenv")` now maps to `&["bundled-dotenv"]` so the auto-optimize default path enables the feature exactly when the user imports dotenv — preserving byte-identical behavior. New `OptimizedLibs::well_known_libs: Vec<PathBuf>` carries bundled archives from `optimized_libs::build_optimized_libs` to `link::build_and_run_link`, where they're concatenated to the link line right after `stdlib_lib`. The flip itself runs in `build_optimized_libs` after `compute_required_features`: walk `ctx.native_module_imports`, look each up in `well_known_bindings.toml`, locate the bundled `target/release/lib<lib>.a` via `bundled_staticlib_path`, then strip the corresponding perry-stdlib feature from the rebuild and queue the `.a`. End-to-end smoke verified: `PERRY_USE_WELL_KNOWN=1 perry compile test.ts` prints `well-known: routing dotenv → /…/libperry_ext_dotenv.a (#466)`, the auto-optimize log shows `features=(no optional features)` (no `bundled-dotenv`), the produced binary reads `process.env` exactly the same way as the default path. The flip is gated behind `PERRY_USE_WELL_KNOWN=1` for this introductory cycle so default behavior stays byte-identical until the route is proven across CI; the env-var gate is removed in a follow-up commit (or per-binding once the well-known table grows beyond dotenv).
- **v0.5.532** — Refs #466 (Phase 4 step 1): added the well-known native bindings registry that future-Phase-5 wrappers will resolve through. `crates/perry/well_known_bindings.toml` is a `[bindings.<npm-name>]` table with `crate` / `lib` / `tracking` keys; new `crates/perry/src/commands/compile/well_known.rs` parses and caches it via `OnceLock`, exposing `lookup_well_known(pkg) -> Option<&WellKnownBinding>`, `iter_well_known()` (for the eventual `perry native list` subcommand under #466 Phase 3), and `bundled_staticlib_path(workspace_root, binding)` (which the linkage flip in step 2 will consume). Today's table has one entry — `dotenv` → `perry-ext-dotenv` — so the contract gets exercised end-to-end without flipping any user-visible resolution. 6 unit tests cover: parser correctness, shipped-toml parsability (panics in OnceLock would surface here), `dotenv_is_registered`, `node:` prefix stripping, unknown-package miss path, and the load-bearing `every_entry_references_a_workspace_crate` check (#466 Phase 4 acceptance: "errors at install time, not user-import time, if a bundled crate is missing"). Step 2 — the actual resolution flip + perry-stdlib `dotenv` feature gate to avoid duplicate `_js_dotenv_*` symbols — is split into a separate commit; this one is pure machinery and zero observable behavior change. Today's `import 'dotenv'` continues to bind to perry-stdlib's copy unchanged.
- **v0.5.531** — Refs #466 (Phase 2 of the native-package ecosystem): froze the `perry.nativeLibrary` manifest spec at v1 and wired `abiVersion` enforcement into the resolve path. `docs/src/native-libraries/manifest-v1.md` (linked from SUMMARY.md) is the authoritative spec — every field, every `params`/`returns` ABI type, every per-target key, the resolution order, the deprecation timetable. Companion JSON schema at `docs/api/manifest.schema.json` for editor validation. `NativeLibraryManifest` gains an `abi_version: Option<String>` field populated from `pkg.perry.nativeLibrary.abiVersion`; `validate_abi_version` (semver crate) checks the declared range covers the bundled `perry-ffi` version (currently equal to the workspace version since perry-ffi ships in lockstep with perry through the v0.5.x cycle). Bare-major declarations (`"0.5"`) auto-promote to `^0.5` so wrappers don't have to learn caret syntax to express the common case. Behavior on this branch: missing field → stderr warning, compilation continues (transitional rule; the v0.6.0 plan flips it to an error); valid range that excludes the bundled version → `anyhow::Error` propagated up through `collect_modules` so the user sees the message at compile time, not segfaults at runtime; unparseable string → error pointing at the offending package. Wired in both arms of `collect_modules.rs` (NativeCompiled and Interpreted paths) so packages with `perry.nativeLibrary` get validated regardless of how their TS surface is consumed. Four unit tests cover the matrix: missing → ok, matching caret → ok, far-future major → err, garbage string → err. `semver = "1.0"` added to `crates/perry/Cargo.toml` (was already a transitive dep via `perry-updater`).
- **v0.5.530** — Refs #466 (Phase D of #469): extracted the stable ABI surface for native-bindings packages into a new `perry-ffi` crate, and ported the smallest stdlib wrapper (`dotenv`) onto it as the acceptance test. `perry-ffi` v0.5 ships an intentionally tiny surface — `JsString` opaque handle, `alloc_string(&str) -> JsString`, `read_string(JsString) -> Option<&str>`, `JsString::{from_raw, as_raw, is_null}`, plus a re-export of `StringHeader` for `extern "C"` signatures. That's enough to port the four single-file stdlib wrappers (`dotenv`, `nanoid`, `uuid`, `slugify`); larger surfaces (arrays / objects / closures / async-runtime sharing) wait for the wrappers that actually need them so we don't commit to APIs we'll regret. New `crates/perry-ext-dotenv/` is a staticlib + rlib that depends only on `perry-ffi` and `serde_json` — zero references to `perry-runtime` internals. Functionally identical to `crates/perry-stdlib/src/dotenv.rs` (which stays in place — additive port, not a replacement); the well-known bindings table flip from #466 Phase 4 is what eventually swaps `import 'dotenv'` resolution from the stdlib copy to the new crate. `nm libperry_ext_dotenv.a | grep js_dotenv` shows the same three exports the stdlib version has (`js_dotenv_config`, `js_dotenv_config_path`, `js_dotenv_parse`). Tests round-trip a string through perry-ffi → js_dotenv_parse → JSON output, proving the contract end-to-end without touching the runtime crate. `docs/src/native-libraries/abi.md` documents the v0.5 surface, the semver contract (perry-ffi major bumps independently of perry-runtime), and what's deliberately *not* yet covered. Mdbook SUMMARY.md links the new doc under "npm Packages → Writing Native Bindings".
- **v0.5.529** — Closes #465 (Phase C of #469): markdown + `.d.ts` serializers for the API manifest. New `crates/perry-api-manifest/src/emit.rs` exposes `emit_markdown(version)` (one combined reference page; modules in alpha order; classes / properties / methods grouped within each module; stubs flagged ⚠) and `emit_dts(version)` (one `declare module "<name>" { ... }` block per supported module; loose `any`-typed signatures since the manifest doesn't carry argument types yet — followup under #466 Phase 2). The `--print-api-manifest` global flag now takes an optional value (`--print-api-manifest=markdown` / `=dts`); bare flag stays JSON for compat with v0.5.528 callers. `default` is the one TypeScript reserved keyword that hits in practice (npm modules with a callable default export — `dotenv`, `slugify`, `sharp`, `better-sqlite3`, `commander`, `dayjs`, `moment`, `cheerio`, `lru-cache`, `lodash`); the .d.ts emitter renders those as `export default function (...args: any[]): any;` rather than the syntactically-invalid `export function default(...)`. Tests assert every module from `API_MANIFEST` appears in both formats and that `crypto.randomUUID` shows up under `declare module "crypto"`. Generated artifacts committed at `docs/src/api/reference.md` (676 lines, 378 entries across 43 modules) and `docs/api/perry.d.ts` (476 lines), with `scripts/regen_api_docs.sh` as the one-shot regenerator that release CI will eventually invoke (the workflow integration itself is a follow-up — committing the artifacts means the diff stays visible in PRs that touch the dispatch table or the manifest). SUMMARY.md now links the auto-generated reference under "Standard Library".
- **v0.5.528** — Closes #463 (Phase A + B of the unified plan in #469): new `perry-api-manifest` crate exposes `API_MANIFEST: &[ApiEntry]` — a structured (module, name, kind, source, stub) listing of every supported stdlib symbol, populated mechanically from `NATIVE_MODULE_TABLE` (317 methods) plus hand-curated entries for modules whose dispatch goes through custom `Expr::*` HIR variants (`crypto`, `os`, `path`, `process`) and a small set of class registrations (`Buffer`, `EventEmitter`, `URL`, etc., 374 entries total). HIR lowering of `Expr::PropertyGet { object: NativeModuleRef(M), property: P }` consults `module_has_symbol(M, P)` and emits an `R005 UnimplementedApi` diagnostic with span info when the symbol isn't registered — `crypto.subtle.encrypt(...)` now errors at the offending line instead of silently returning undefined. Strictness is gated on `module_has_any_entries(M)` so modules whose surface hasn't been enumerated yet (incremental coverage) keep working — adding entries promotes the module to strict mode automatically. Stubs (`stub: true` in the manifest) are NOT treated as unimplemented; #464's runtime first-call warning surfaces those instead. Two escape hatches: env var `PERRY_ALLOW_UNIMPLEMENTED=1` skips the check (useful when the manifest has a real gap a followup will fix), and the `--print-api-manifest` global flag emits the manifest as JSON to stdout — drives #465's docs/.d.ts generators and lets editor tooling discover the supported surface without reading Rust source. `perry-codegen`'s `tests/manifest_consistency.rs` walks `NATIVE_MODULE_TABLE` and asserts every row has a counterpart in `API_MANIFEST` so drift can't ship. `perry-hir::ir::NATIVE_MODULES` and `requires_stdlib` migrated to thin re-exports / delegates over `perry_api_manifest::{NATIVE_MODULES, is_runtime_only_module}`. Parity sweep: 209 pass / 2 fail / 1 compile_fail (test_issue_446 is a pre-existing #462-era regression unrelated to this PR — verified by disabling the new check and re-running). Same manifest is the foundation for #465 (docs/.d.ts emit) and #466 Phase 2 (external `perry.nativeLibrary` spec freeze).
- **v0.5.527** — Closes #470: `Buffer.{read,write}{U,}Int{BE,LE}(offset, byteLength)` variable-byteLength forms were no-ops (`readUIntBE` returned `undefined`, `writeUIntBE` wrote nothing) — `dispatch_buffer_method` only handled the fixed-width `readUInt8/16/32` / `writeUInt8/16/32` cases and fell through to `TAG_UNDEFINED` for the parametric forms. Added `js_buffer_{read,write}_{uint,int}_{be,le}(buf, [value,] offset, byte_length)` to `crates/perry-runtime/src/buffer.rs` (1..=6 byte_length, sign-extends through `(n*8)`-bit two's complement for the signed reads, returns `undefined` / no-ops on out-of-range byteLength to match the rest of the buffer dispatch's tolerant-on-bad-args convention) and wired the 8 method-name arms into `dispatch_buffer_method` in `crates/perry-runtime/src/object.rs`. The codegen-side `try_emit_buffer_read_intrinsic` fast path is opt-in by exact method name (`classify_buffer_numeric_read` doesn't list these) so the variable-byteLength calls naturally fall through to the runtime dispatch — no codegen changes needed. Unblocks BSON `ObjectId` generation in pure-TS Mongo drivers (the 3-byte counter no longer serialises as zero, so `insertMany(N)` actually inserts N).
- **v0.5.526** — Closes #462: `PropertyGet` codegen now splits the existing `pget.recv_bad` block into a TAG_UNDEFINED/TAG_NULL nullish check + `js_throw_type_error_property_access` runtime helper that prints a node-shaped `TypeError: Cannot read properties of {undefined|null} (reading '<prop>')` and aborts. Non-nullish invalid receivers (numbers, bools, raw f64) keep the silent-undefined fall-through since Perry has no primitive auto-boxing yet. Optional chaining and `??` already short-circuit before reaching PropertyGet, so they're unaffected.
- **v0.5.525** — Refs #461: emit a fallback `perry_fn_<modprefix>__<exported>` undefined-stub on the export side for every `Export::Named` that no other emission path has already claimed — closes the `Order` / `Union` / `Emit` link errors that #460 carved out as follow-up. Two distinct symptom classes shared the same root cause: (1) exported classes (`export class Union<M>` in SchemaAST.ts, `export class Emit<R>` in subexecutor.ts) — the class declaration emits `perry_class_keys_*` + `perry_method_*` + a constructor symbol but no `perry_fn_<mod>__<ClassName>` value-getter, so consumer-side namespace property accesses (`AST.Union.make(...)`) link-failed because lower_call.rs's `ExternFuncRef` arm always resolves to `perry_fn_<src>__<name>`; (2) exported interfaces / type aliases (`export interface Order<in A>` in Order.ts) — type-only at runtime, but type annotations like `order.Order<RuntimeFiber<unknown,unknown>>` in `export const Order: order.Order<…> = internal.Order` (Fiber.ts:308) leak into the value-position symbol resolver and emit a load of `perry_fn_<Order_ts>__Order` that never had a definition. Fix is one block in `crates/perry-codegen/src/codegen.rs` after the #460 forwarding-wrapper pass: walk `hir.exports` for `Export::Named { exported }`, compute `perry_fn_<modprefix>__<sanitize(exported)>`, skip if `LlModule::has_function` (new helper) reports the symbol is already defined by an earlier path (function body / value-getter / #460 forwarding wrapper), otherwise emit a `() -> double` stub returning NaN-boxed undefined. Matches the consumer-side no-op wrapper at codegen.rs:1955 (which already returns undefined for imported classes referenced as values), so cross-module class/type-only references behave symmetrically — link cleanly, return undefined at runtime. After fix, `bun add effect && perry compile test_minimal.ts` drops `Order` / `Union` / `Emit` from the undefined-symbol list (down to 1: bare `_pull` from `internal/stream.ts`'s nested-arrow capture-mangling — separate bug, #461 stays open). Parity sweep: 210 pass / 2 known fails (was 209 / 2 — one test that was previously compile-failing now compiles cleanly through the new stub path).
- **v0.5.524** — Closes #464: stub registry + first-call runtime diagnostic for the harmonyos `perry_ui_*` / `perry_system_*` / `perry_updater_*` no-op stubs (and the runtime-only `js_ws_*` / `js_readline_*` stubs). `perry-runtime/build.rs` walks the four `perry-dispatch` tables + the hand-listed `direct_call_stubs` and emits both the stub function bodies (when `ohos-napi` is on) and a `STUB_MANIFEST: &[StubEntry]` constant (always — `perry check` runs on a host build). Each generated stub funnels through `crate::stub_diag::perry_stub_warn(symbol, reason, issue)` which prints `[perry] warning: \`<sym>\` is a no-op stub on this platform — <reason> (tracking: <issue>)` once per symbol per process; `PERRY_STUB_DIAG=off|silent|0|false` silences and `=verbose|all|every` prints every call. The same manifest powers a new static scan in `perry check --target harmonyos` (also `harmonyos-simulator`) that walks named imports from `perry/ui` / `perry/system` / `perry/updater` against `STUB_MANIFEST.ts_name` and emits an `R004 NoOpStub` diagnostic at the import span — surfaces the same warnings before the binary runs. Hot-loop drains (`js_ws_process_pending`, `js_stdlib_process_pending`) deliberately skip the warn helper since they tick every event-loop pass. `target_stubs_out_symbols` gates the static scan so a native-host `perry check` (where the platform UI crate owns those symbols) doesn't false-positive.
- **v0.5.523** — Closes #460: `export { local as keyword }` re-exports of TypeScript contextual keywords (`void` / `async` / `await` / `try` / `delete` / `continue`) had no `_perry_fn_<mod>__<keyword>` definition in the renamed module's object file — Effect's `node_modules/effect/src/{Effect,Deferred,Either,FiberRef,Fiber,Option,ScheduleDecision,Stream,internal/core,…}.ts` link-failed on 14 such symbols. Two parallel root causes were both leaving the symbol at the export site undefined: (1) function-decl renames — `function _void(){} ; export { _void as void }` — populated `module.exported_functions: [("void", func_id)]` correctly, but `crates/perry-codegen/src/codegen.rs::scoped_fn_name` only mangled `f.name` (`_void`), so the body was emitted as `perry_fn_<mod>___void` and nothing claimed the `__void` symbol; (2) const-binding renames — `const _await = core.deferredAwait ; export { _await as await }` — never even reached `exported_functions` (the `lookup_func` gate fails for non-fn-decls), and the `is_exportable` filter in `crates/perry-hir/src/lower.rs` excluded `Expr::PropertyGet` / `Expr::LocalGet` / `Expr::ExternFuncRef` / `Expr::FuncRef` so the local was also missing from `exported_objects` — the local id was never registered in `module_globals`, the value-getter loop in `codegen.rs` skipped emitting any symbol, and the public name was undefined. Fix: in codegen, after the user-function body loop, walk `hir.exported_functions` and emit a forwarding wrapper from `perry_fn_<mod>__<exported>` to `perry_fn_<mod>__<f.name>` whenever the two names differ (skip when equal — `export function foo(){}` self-export); also widen the `is_exportable` matcher in `lower.rs` to cover the four function-reference initializer shapes and additionally pin the local name into `exported_objects` so the global-emission gate fires; in codegen's value-getter loop, walk `hir.exports` for `Export::Named { local, exported }` rename pairs and emit a duplicate getter under the exported name. The forwarding wrapper for function-decl renames also has to suppress the value-getter (added an `is_function_alias` predicate against `exported_functions`) — without it the two paths collide and clang errors `invalid redefinition of function 'perry_fn_<mod>__async'`. After fix, all 14 keyword-name undefined-symbol errors disappear from `bun add effect && perry compile test_minimal.ts`; the remaining 3 (`Order`, `Union`, `Emit`) are class-as-function symbols carved out by #460 as a separate follow-up. Parity sweep stays at 209 pass / 2 known fails.
- **v0.5.522** — Closes #467: sanitize scoped package.json names (`@scope/pkg` → `scope-pkg`) before they flow into the linker's `-o` arg or default bundle ID — ld64 was treating the leading `@` as a response-file directive.
- **v0.5.521** — Closes #457: skip inlining of generator functions in `is_inlinable` — the inliner was erasing `yield` semantics and collapsing `gen()` to its terminating `return` value.
- **v0.5.520** — Closes #456: unroll pass aliased LocalIds + FuncIds across cloned iterations, producing duplicate `@perry_global_*` definitions and collapsing per-iteration closures into one body.
- **v0.5.519** — Dependabot security cleanup: `jsonwebtoken` 9.3 → 10.3, `lru` 0.12 → 0.16, `validator` 0.18 → 0.20, drop `atty` for `std::io::IsTerminal`.
- **v0.5.518** — Closes #454: bump `redis` 0.25 → 1.2, `sqlx` 0.8.0 → 0.8.6, `rusqlite` 0.31 → 0.32.1 — clears never-type-fallback future-incompat warnings.
- **v0.5.517** — Closes #443 (followup to v0.5.498): widen `is_nanboxed_string` to also accept `SHORT_STRING_TAG` (0x7FF9) in windows/android/gtk4/macos state bindings.
- **v0.5.516** — Closes #434: SSO (0x7FF9) vs heap-string (0x7FFF) representation mismatch broke `Set.has` / `Map.get` / `obj[key]` for short JSON.parse'd keys.
- **v0.5.515** — Closes #407: `console.log(...arr)` dropped all but the first arg — added `console.*` fast path to the `Expr::CallSpread` codegen arm.
- **v0.5.514** — Closes #393: `compute_max_func_id` missed `NativeMethodCall` args, so async-to-generator's `next_func_id` collided with route-handler closure ids.
- **v0.5.513** — Closes #399 (round 2): add `notificationSchedule_{interval,calendar,location}` to `direct_call_stubs` so harmonyos `.so` link-resolves.
- **v0.5.512** — Closes #396/#397/#398: macOS bottle bundles tvOS/visionOS/watchOS cross-libs (device + sim) alongside iOS.
- **v0.5.511** — release-CI gate fix: add `test_edge_closures` to compile-smoke `SKIP_TESTS` (separate skip mechanism from parity's `known_failures.json`).
- **v0.5.510** — release-CI gate fixes: `cargo fmt --all` for lint drift; triage #456 + #457 into `known_failures.json` to unblock v0.5.509 publish.
- **v0.5.509** — Closes #447 + un-skip 6 async tests; v0.5.508 ABI fix transitively closed #447's iter-object register-class mismatch.
- **v0.5.508** — Closes #448 + #451: ABI mismatch in `js_object_set_field` (DOUBLE arg vs runtime `JSValue` u64) put closure values in xmm0 instead of GP register.
- **v0.5.507** — Closes #450: `defineProperty` accessor `this` was descriptor literal not target obj; codegen flags `captures_this`, runtime clone-rebinds.
- **v0.5.505** — Closes #449: fold `new.target.<prop>` to a literal at HIR lowering, bypassing the broken `MetaProp(NewTarget)` Object-literal path.
- **v0.5.503** — Closes #446: bind class-method PropertyGet via `js_class_method_bind`; flow type-only-import class metadata into `imported_classes`.
- **v0.5.502** — Closes #444: fold `import.meta.{url,main,dirname,filename}` to literal at lowering.
- **v0.5.501** — Closes #422: `new net.Socket()` + `sock.connect(port, host)` + `net.connect(...)` — pure-TS TCP clients work end-to-end.
- **v0.5.500** — Closes #429: GTK4 `appSetTimer` callbacks scheduled mid-event-loop now install immediately.
- **v0.5.495** — `perry-transform` static-trip-count for-loop full-unroll pass — 22% improvement on `image_convolution`'s 5×5 blur kernel.

Older entries → CHANGELOG.md.
