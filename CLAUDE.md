# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**NOTE**: Keep this file concise. Detailed changelogs live in CHANGELOG.md.

## Project Overview

Perry is a native TypeScript compiler written in Rust that compiles TypeScript source code directly to native executables. It uses SWC for TypeScript parsing and LLVM for code generation.

**Current Version:** 0.5.718


## TypeScript Parity Status

Tracked via the gap test suite (`test-files/test_gap_*.ts`, 28 tests). Compared byte-for-byte against `node --experimental-strip-types`. Run via `/tmp/run_gap_tests.sh` after `cargo build --release -p perry-runtime -p perry-stdlib -p perry`.

**Last full sweep:** run `./run_parity_tests.sh` for the current snapshot — the gap-suite top-line has shifted with recent landings; see open issues #447 (nested-async hang masking `test_async`), #448 (`*[Symbol.iterator]()` class hang), #449 (`new.target` → NaN), #450 (`defineProperty` accessor `this`), #451 (`test_gap_json_advanced` SIGSEGV), and #452 (`Array.fromAsync` / Proxy/Reflect / JSON ordering). Several of these are pre-existing bugs that the parity skip-list and gap-suite output truncation hid from earlier numbers.

**Known categorical gaps**: lookbehind regex (Rust `regex` crate), `console.dir`/`console.group*` formatting, lone surrogate handling (WTF-8).

## Workflow Requirements

**IMPORTANT:** Follow these practices for every code change made directly on `main` (maintainer workflow):

1. **Update CLAUDE.md**: Add 1-2 line entry in "Recent Changes" for new features/fixes
2. **Increment Version**: Bump patch version (e.g., 0.5.48 → 0.5.49)
3. **Commit Changes**: Include code changes and CLAUDE.md updates together

### External contributor PRs

PRs from outside contributors should **not** touch `[workspace.package] version` in `Cargo.toml`, the `**Current Version:**` line in `CLAUDE.md`, or add a "Recent Changes" entry. The maintainer bumps the version and writes the changelog entry at merge time — usually by rebasing the PR branch and amending. This avoids the patch-version collisions that happen when Perry's `main` ships several commits while a PR is in review (each on-main commit bumps the version; a PR that bumped to the same patch on day 1 is already behind by merge day). Contributors just write code; let the maintainer fold in the metadata last.

## Build Commands

```bash
cargo build --release                          # Build all crates
cargo build --release -p perry-runtime -p perry-stdlib  # Rebuild runtime (MUST rebuild stdlib too!)
cargo test --release --workspace \
  --exclude perry-ui-ios --exclude perry-ui-tvos --exclude perry-ui-watchos \
  --exclude perry-ui-visionos --exclude perry-ui-android --exclude perry-ui-windows \
  --exclude perry-ui-gtk4   # Run tests (exclude cross-host UI crates on macOS)
cargo run --release -- file.ts -o output && ./output    # Compile and run TypeScript
cargo run --release -- file.ts --print-hir              # Debug: print HIR
```

## Architecture

```
TypeScript (.ts) → Parse (SWC) → AST → Lower → HIR → Transform → Codegen (LLVM) → .o → Link (cc) → Executable
```

| Crate | Purpose |
|-------|---------|
| **perry** | CLI driver (parallel module codegen via rayon) |
| **perry-parser** | SWC wrapper for TypeScript parsing |
| **perry-types** | Type system definitions |
| **perry-hir** | HIR data structures (`ir.rs`) and AST→HIR lowering (`lower.rs`) |
| **perry-transform** | IR passes (closure conversion, async lowering, inlining) |
| **perry-codegen** | LLVM-based native code generation |
| **perry-runtime** | Runtime: value.rs, object.rs, array.rs, string.rs, gc.rs, arena.rs, thread.rs |
| **perry-stdlib** | Node.js API support (mysql2, redis, fetch, fastify, ws, etc.) |
| **perry-ui** / **perry-ui-macos** / **perry-ui-ios** / **perry-ui-tvos** | Native UI (AppKit/UIKit) |
| **perry-jsruntime** | JavaScript interop via QuickJS |

## NaN-Boxing

Perry uses NaN-boxing to represent JavaScript values in 64 bits (`perry-runtime/src/value.rs`):

```
TAG_UNDEFINED = 0x7FFC_0000_0000_0001    BIGINT_TAG  = 0x7FFA (lower 48 = ptr)
TAG_NULL      = 0x7FFC_0000_0000_0002    POINTER_TAG = 0x7FFD (lower 48 = ptr)
TAG_FALSE     = 0x7FFC_0000_0000_0003    INT32_TAG   = 0x7FFE (lower 32 = int)
TAG_TRUE      = 0x7FFC_0000_0000_0004    STRING_TAG  = 0x7FFF (lower 48 = ptr)
```

Key functions: `js_nanbox_string/pointer/bigint`, `js_nanbox_get_pointer`, `js_get_string_pointer_unified`, `js_jsvalue_to_string`, `js_is_truthy`

**Module-level variables**: Strings stored as F64 (NaN-boxed), Arrays/Objects as I64 (raw pointers). Access via `module_var_data_ids`.

## Garbage Collection

Generational mark-sweep GC in `crates/perry-runtime/src/gc.rs` (default since v0.5.237 / Phase D). Two regions in the per-thread arena: nursery (`ARENA`, fills with new allocations, swept on minor GC) and old-gen (`OLD_ARENA`, holds tenured/evacuated objects). Conservative stack scan + precise shadow-stack roots + 9 registered scanners. Write barriers populate a remembered set so minor GC can avoid retracing the old-gen. Two-bit aging (`HAS_SURVIVED` / `TENURED`) promotes nursery survivors after 2 minor cycles; the C4b evacuation pass moves non-pinned tenured objects into old-gen with full reference rewriting. Idle nursery blocks observed empty for 2 GC cycles are `dealloc`'d back to the OS (C4b-δ, v0.5.235), and the next-trigger calc is hard-capped at the initial threshold (64 MB) so >90%-freed step-doubling can't blow up peak occupancy (C4b-δ-tune, v0.5.236). Triggers on arena block allocation (1 MB blocks since v0.5.196), malloc count threshold, or explicit `gc()` call. 8-byte GcHeader per allocation.

**Escape hatches**: `PERRY_GEN_GC=0`/`off`/`false` reverts to full mark-sweep (bisection only). `PERRY_GEN_GC_EVACUATE=1` enables the copying evacuation pass (default OFF — complete and correctness-safe but adds work that's a no-op on workloads where nothing tenures). `PERRY_WRITE_BARRIERS=1` opts into codegen-emitted write barriers (default OFF — barrier emission has its own perf cost; the runtime barrier always exists). `PERRY_GC_DIAG=1` prints per-cycle diagnostics.

## Threading (`perry/thread`)

Single-threaded by default. `perry/thread` provides:
- **`parallelMap(array, fn)`** / **`parallelFilter(array, fn)`** — data-parallel across all cores
- **`spawn(fn)`** — background OS thread, returns Promise

Values cross threads via `SerializedValue` deep-copy. Each thread has independent arena + GC. Results from `spawn` flow back via `PENDING_THREAD_RESULTS` queue, drained during `js_promise_run_microtasks()`.

## Native UI (`perry/ui`)

Declarative TypeScript compiles to AppKit/UIKit calls. Handle-based widget system (1-based i64 handles, NaN-boxed with POINTER_TAG). `--target ios-simulator`/`--target ios`/`--target tvos-simulator`/`--target tvos` for cross-compilation.

**To add a new widget** — change 4 places:
1. Runtime: `crates/perry-ui-macos/src/widgets/` — create widget, `register_widget(view)`
2. FFI: `crates/perry-ui-macos/src/lib.rs` — `#[no_mangle] pub extern "C" fn perry_ui_<widget>_create`
3. Codegen: `crates/perry-codegen/src/codegen.rs` — declare extern + NativeMethodCall dispatch
4. HIR: `crates/perry-hir/src/lower.rs` — only if widget has instance methods

## Compiling npm Packages Natively (`perry.compilePackages`)

Configured in `package.json`:
```json
{ "perry": { "compilePackages": ["@noble/curves", "@noble/hashes"] } }
```
First-resolved directory cached in `compile_package_dirs`; subsequent imports redirect to the same copy (dedup).

## Known Limitations

- **No runtime type checking**: Types erased at compile time. `typeof` via NaN-boxing tags. `instanceof` via class ID chain.
- **No shared mutable state across threads**: No `SharedArrayBuffer` or `Atomics`.

## Common Pitfalls & Patterns

### NaN-Boxing Mistakes
- **Double NaN-boxing**: If value is already F64, don't NaN-box again. Check `builder.func.dfg.value_type(val)`.
- **Wrong tag**: Strings=STRING_TAG, objects=POINTER_TAG, BigInt=BIGINT_TAG.
- **`as f64` vs `from_bits`**: `u64 as f64` is numeric conversion (WRONG). Use `f64::from_bits(u64)` to preserve bits.

### LLVM Type Mismatches
- Loop counter optimization produces i32 — always convert before passing to f64/i64 functions
- Constructor parameters always f64 (NaN-boxed) at signature level

### Async / Threading
- Thread-local arenas: JSValues from tokio workers invalid on main thread
- Use `spawn_for_promise_deferred()` — return raw Rust data, convert to JSValue on main thread
- Async closures: Promise pointer (I64) must be NaN-boxed with POINTER_TAG before returning as F64

### Cross-Module Issues
- ExternFuncRef values are NaN-boxed — use `js_nanbox_get_pointer` to extract
- Module init order: topological sort by import dependencies
- Optional params need `imported_func_param_counts` propagation through re-exports

### Closure Captures
- `collect_local_refs_expr()` must handle all expression types — catch-all silently skips refs
- Captured string/pointer values must be NaN-boxed before storing, not raw bitcast
- Loop counter i32 values: `fcvt_from_sint` to f64 before capture storage

### Handle-Based Dispatch
- TWO systems: `HANDLE_METHOD_DISPATCH` (methods) and `HANDLE_PROPERTY_DISPATCH` (properties)
- Both must be registered. Small pointer detection: value < 0x100000 = handle.

### objc2 v0.6 API
- `define_class!` with `#[unsafe(super(NSObject))]`, `msg_send!` returns `Retained` directly
- All AppKit constructors require `MainThreadMarker`

## Recent Changes

One-liners only — full detail in CHANGELOG.md.

- **v0.5.718** — Closes #624 (compilePackages: synthetic node_modules pkg with `module.exports = { fn }` failed at link, then on link-fix surfaced an unrelated runtime call-returns-undefined regression in IIFE-wrapped CJS modules). Two coordinated fixes. **(1) CJS named-export extraction** (`crates/perry/src/commands/compile/cjs_wrap.rs::extract_exports_from_source`): pre-fix only handled the `exports.X = ...` pattern. The synthetic-package shape (and React's transpiled occasional fallback) `module.exports = { createContext }` produced zero detected named exports → the consumer's `import { createContext } from "fakemod"` lowered to an `ExternFuncRef` whose backing symbol `_perry_fn_node_modules_fakemod_index_js__createContext` was never emitted → link failed. New extractor handles three shapes: (a) `exports.X = ...` and `module.exports.X = ...` (canonical CJS — extended the existing regex to accept the `module.exports.` prefix), (b) `module.exports = { X, Y, fn: someFn }` — brace-balanced scan of the object-literal body that walks the IIFE-shape source character-by-character (handles nested braces from `{ fn: function() { ... } }` shapes), with a per-key parser that recognizes both shorthand (`{ createContext }` ≡ `{ createContext: createContext }`) and explicit `name: <expr>` forms, skipping string-keyed (`"name":`) and computed (`[expr]:`) entries which can't surface as ESM named exports anyway. Five new unit tests cover each shape + their union. **(2) IIFE shorthand fold** (`crates/perry-hir/src/lower/expr_object.rs`): once the link error cleared, the wrapped CJS produced `_cjs.createContext === function` but `_cjs.createContext("hello") === undefined`. Bisected to: function declarations inside an IIFE (`function createContext(value) {...}`) are registered BOTH as functions (via `register_func`) AND as let-bound locals whose init is a Closure HIR node. The shorthand fold preferred `lookup_func` over `lookup_local`, producing `Expr::FuncRef(<inner_func_id>)` for the `{ createContext }` shorthand. The codegen-side wrapper-emit loop in `crates/perry-codegen/src/codegen.rs:2243+` only iterates `hir.functions` (top-level user functions) — inner-IIFE function ids don't have a `__perry_wrap_<name>` wrapper. The FuncRef-as-value lowering at `expr.rs:6432+` falls through to `__perry_wrap_perry_unknown_func` (the v0.5.337 fallback that returns `TAG_UNDEFINED` for any call), so every invocation of the stored function returned undefined. Fix: flip the lookup order in BOTH the closed-shape shorthand fold AND the spread-path shorthand fold to prefer `lookup_local` first → `lookup_func` second → `lookup_class` third. Matches JS spec where local bindings shadow outer-scope function names. The `LocalGet` of a closure-init let produces a real callable NaN-boxed pointer; calling it dispatches via `js_closure_call*` to the actual user code. End-to-end: issue's repro (`module.exports = { createContext }; → import { createContext } → const Ctx = createContext(null); typeof Ctx`) compiles + links + runs, prints `typeof Ctx: object` matching Node byte-for-byte. Sanity: test_inheritance / test_simple_class / test_edge_promises / test_gap_class_advanced / test_gap_closures / test_break_continue / test_array_methods / test_edge_classes / test_issue_619 / test_issue_620 / test_issue_621 / test_issue_623 / test_edge_higher_order all rc=0; new tests test_iife_export.ts / test_iife_arrow.ts / test_obj_closure_call.ts cover the IIFE-shorthand-fold edge cases.
- **v0.5.717** — Closes #623 (`typeof process` / `typeof console` / `typeof globalThis` returned `"number"` instead of `"object"`). `import process from "node:process"` lowers to HIR `NativeModuleRef("process")`, which the codegen represents as a `0.0` stub double (`crates/perry-codegen/src/expr.rs:8927`); `js_value_typeof` correctly identifies that as `"number"`. Same shape for `(globalThis as any).process` (lowers to `PropertyGet { GlobalGet(0), "process" }` — the runtime read returns the same stub double) and bare `typeof globalThis` (lowers to `GlobalGet(_)` → 0.0). Property access (`process.argv`, `console.log`) goes through dedicated dispatch paths that find the real native binding, so those keep working — only the bare `typeof` read sees the stub. Fix in `Expr::TypeOf` short-circuit at `crates/perry-codegen/src/expr.rs:905+`: three new arms — `Expr::NativeModuleRef(_) => Some("object")`, `Expr::GlobalGet(_) => Some("object")`, and `PropertyGet { object: GlobalGet(_), property }` where `property` is `"process" | "console" | "globalThis" | "performance"` → `Some("object")`. Function-shaped globals (Buffer / Promise / URL / etc.) intentionally fall through so `typeof Buffer === "function"` keeps working through the existing class-ref short-circuit. Repro from issue (`import process from "node:process"; typeof process; typeof (globalThis as any).process`) byte-identical to Node post-fix. Sanity: test_inheritance / test_simple_class / test_edge_promises / test_gap_typeof_instanceof / test_gap_class_advanced / test_issue_619 / test_issue_620 / test_issue_621 / test_break_continue / test_array_methods / test_edge_classes all rc=0.
- **v0.5.716** — Refs #603 (hono routes-with-params): closes the three bisected blockers (#619 / #620 / #621). **#619 (async-step driver swallows sync throws as Promise.reject)**: `crates/perry-transform/src/generator.rs::build_async_step_driver`'s outer `try { r = isError ? __iter.throw(value) : __iter.next(value); } catch (e) { return Promise.reject(e); }` skipped the user's `try/catch` around the `await`. Per the async/generator transform a `try { await x } catch (e) { ... }` encodes the catch as the iter's `iter.throw` closure — the driver's `Promise.reject` shortcuts past it. Fix: split the catch handler into `if (isError) return Promise.reject(e)` (catch fired during error delivery → propagate) AND a fallthrough `return __step(e, true)` (sync throw came from `iter.next`'s body → re-deliver via iter.throw so the user's catch arm runs). Plus the throw closure's no-catch path: previously `if (catches.first().is_none()) { __gen_done = true; return { value: undefined, done: true }; }` swallowed the error and resolved the await to undefined; replaced with `Stmt::Throw(throw_param)` so the outer driver's try sees it and the function's returned Promise rejects per spec. Repros: `function syncThrower(){ throw new Error("sync") }; async function main() { try { await syncThrower(); } catch (e) { console.log("caught:", e.message); } }` and `async function inner(){ throw new Error("from inner") }; async function main() { try { await inner(); } catch (e) { console.log("caught:", e.message); } }` — both byte-identical to Node post-fix. **#620 (`this.method = X` ignored — class dispatch always hits prototype)**: `obj.method = newFn` writes an own property but the dispatch tower in `crates/perry-codegen/src/lower_call.rs` (the static-class fast path at `if overrides.is_empty()`) emitted a direct `call(fallback_fn, args)` to the original method body, never consulting the instance's own-property dictionary. Hono's SmartRouter rebinds `this.match = router.match.bind(router)` on the first call; on the second call Perry hit the original method, which threw `Error("Fatal error")` because `this.#routes` was set to `undefined` on the first call. Fix: new runtime helper `js_object_get_own_field_or_undef(obj_value, name_ptr, name_len) -> f64` in `crates/perry-runtime/src/object.rs` — does a focused own-property keys_array linear scan (no vtable getter walk) and returns the stored value or `TAG_UNDEFINED`. New codegen helper `emit_own_method_override_check` in `lower_call.rs` wraps the static-class fast path: probe own-method via the new runtime fn, branch on `icmp_eq vs TAG_UNDEFINED` — override path calls `js_native_call_value(own_method, args_ptr, args_len)` (passes user args only, no `this` — matches arrow / `.bind()`-bound semantics where `this` is captured/baked); fallback path emits the original direct `call(fallback_fn)`. Phi merges the two branches. Adds ~1 runtime call per class method dispatch (linear scan over keys_array, typically <10 fields), but unblocks the universal "promote dispatch to fast-path closure on first call" pattern (jQuery / Underscore / lodash once / memoize / hono SmartRouter). Repro: `class Router { match(p){ console.log("orig"); this.match = (q) => "inner:" + q; return this.match(p); } }; const r = new Router(); r.match("/foo"); r.match("/bar")` — second call now correctly hits the override (was: hit original twice). **#621 (generator throw closure terminates after catch instead of resuming)**: when `try { await x } catch (e) { ... }` is followed by stmts after the try/catch, the post-try stmts never ran on the throw path because `crates/perry-transform/src/generator.rs::transform_generator_function`'s throw closure ended with `__gen_done = true; return { value: undefined, done: true };` after inlining the catch body. Three coordinated changes: (a) extend `catches: Vec<(Option<LocalId>, Vec<Stmt>)>` to `Vec<(Option<LocalId>, Vec<Stmt>, u32)>` adding the `post_catch_state` (the state number where execution should resume after the catch body runs). (b) In `linearize_body`'s try-with-yield arm: after linearizing the body and BEFORE pushing the catch entry, FLUSH `current` (which holds the post-await happy-path continuation — stmts that come after the LAST yield in the try body) as its OWN state with `Goto(post_catch_state)` exit, then capture `post_catch_state = *state_num`. Without this split, post-await happy-path stmts and post-try stmts collapse into one state and the throw path incorrectly runs the post-await stmts. (c) Throw closure body: after inlining catch body, emit `__gen_state = post_catch_state` + `return { value: undefined, done: false }`. The async-step driver sees `done: false` and chains `__step(undefined, false)` → `iter.next(undefined)` which dispatches the post-try state and runs the rest of the function body. If the catch body itself contained a `Return(X)` the trailing state-set + return are unreachable (the inlined return takes priority — handled correctly by existing semantics). Repro: `async function inner(){ throw new Error("boom") }; async function main(){ console.log("before"); try { await inner(); console.log("after await"); } catch (e) { console.log("caught:", e.message); } console.log("after try"); console.log("done"); }` — Perry now prints `before / caught: boom / after try / done` matching Node (was: silent exit after `caught: boom` because the catch-handler termination skipped `after try` and `done`). Sanity sweep: 26 manual tests + full parity script — test_inheritance / test_simple_class / test_edge_promises / test_gap_async_advanced / test_gap_class_advanced / test_gap_closures / test_break_continue / test_async / test_async{2-5} / test_async_chain / test_edge_error_handling / test_edge_iteration / test_array_methods / test_gap_array_methods / test_edge_classes / test_edge_scope_hoisting / test_gap_typeof_instanceof / test_issue_392_cross_module_method_dispatch / test_issue_256_microtask_ordering / test_stress_promises all byte-identical to Node post-fix. New regression tests test_issue_619.ts / test_issue_620.ts / test_issue_621.ts cover each fix. **#603 status**: the three blockers it bisected to are closed. The hono fixture (`tests/release/packages/hono-basic/`) still needs ONE more upstream fix — calling a class-method bound closure stored as a local variable (`const m = router.match; m("GET", "/x")`) returns undefined; the `js_class_method_bind` produced closure dispatches via `BOUND_METHOD_FUNC_PTR` → `dispatch_bound_method` → `js_native_call_method(router, "match", args)` but the receiver-as-`this` thread isn't propagating correctly. This is independent of #619/#620/#621 (each has an isolated repro that passes), and is filed as #628.
- **v0.5.715** — **Closes #604 #625 #626 #627** (axios.get + node:http hangs after `server.close()` — full architectural fix). v0.5.707's partial fix added a `listening`-flag gate to `event_loop`'s loop body so `server.close()` could break out cleanly, but the deeper architectural problem — `js_node_http_server_listen` blocking the main TS thread inside `event_loop(...)` for the process lifetime — was tracked separately as the load-bearing bug. The user fixture (`tests/release/packages/axios-get/entry.ts`) is `await new Promise(r => server.listen(port, () => r())); const r = await axios.get(...); server.close();` — `new Promise(executor)` synchronously invokes the executor, which calls `server.listen()`, which spawns the accept task, fires the listening callback (resolves the Promise internally), then enters `event_loop(...)` and blocks the main thread forever. The Promise constructor never returns → the await never evaluates → axios never runs → server.close() never runs → the process timeouts (perry-out.txt empty / exit 124). **Architectural shift**: `js_node_http_server_listen` (and `js_node_https_server_listen` / `js_node_http2_server_listen`) is now non-blocking. The accept loop is already spawned on the tokio runtime via `spawn_blocking_with_reactor` + inner `tokio::spawn`; pre-fix `event_loop(server_handle)` was a redundant blocker that drove the same drain logic the codegen-emitted main loop now drives via the standard stdlib pump. Six coordinated layers: **(1) perry-ext-http-server/server.rs** — delete the trailing `event_loop(server_handle)` call from `js_node_http_server_listen`; expose two new `#[no_mangle] extern "C"` functions: `js_node_http_server_has_active() -> i32` (walks the global handle registry via the new `iter_handle_ids_of` and returns 1 if any HttpServer / HttpsServer / Http2SecureServer is `listening` OR has pending requests / upgrades in its mpsc receiver) and `js_node_http_server_process_pending() -> i32` (snapshots handle ids per server type, drains pending requests + upgrades on the main thread, dispatches to the user handler closure for each). The dispatch logic moves from the deleted `event_loop` into a new `process_pending` function reused for HTTP/1; HTTPS gets parallel `process_pending_https` + `try_recv_pending_https_nonblocking` helpers in `https_server.rs`, HTTP/2 gets `process_pending_h2` + `try_recv_pending_h2_nonblocking` in `http2_server.rs`. The `js_gc_exit_unsafe_zone()` call moves from `event_loop`'s tail into each server's `close()` so the GC stays suppressed for the listening lifetime but is restored on close (reentrant counter tolerates multiple servers). **(2) Drop synchronous handler-Promise wait**: pre-fix `process_pending` called `wait_for_promise(ptr)` to spin up to ~1s on each handler-returned Promise so async handlers (`async (req, res) => { await x; res.end(...) }`) settled before the next iteration. With the pump now running from the main TS event loop, blocking here would re-introduce the same listen()-blocks-main-thread problem at per-request granularity. The new `process_pending` invokes the handler, runs microtasks once, and returns; the existing `synthesize_default_response_if_needed` safety net catches the case where neither path completed in time. The simple `(req, res) => res.end(...)` shape that the load-bearing #604 fixture uses works because the response oneshot fires synchronously from inside `js_node_http_res_end`. The async-handler-yields-across-multiple-microtask-cycles case is best-effort and flagged in the inline comment as a follow-up (would need an in-flight per-request set so the pump only frees handles after the handler-returned Promise settles). **(3) perry-ffi/handle.rs** — new `iter_handle_ids_of<T, F>(f: F) where F: FnMut(Handle)` walks the global DashMap and yields the integer handle id for each entry whose downcasted type matches `T`. Parallel to the existing `iter_handles_of` (which yields `&T`); needed because `process_pending` mutates handle state (drains channels, frees per-request handles) which can't be expressed against a `&T` borrow held across the iteration. Re-exported from `perry-ffi/lib.rs`. **(4) perry-stdlib/Cargo.toml + async_bridge.rs** — new `external-http-server-pump` Cargo feature (mirrors `external-net-pump` / `external-ws-pump`); two new arms in `js_stdlib_process_pending` and `js_stdlib_has_active_handles` gated on that feature, calling `js_node_http_server_process_pending` / `js_node_http_server_has_active` via `extern "C"` declarations. The codegen-emitted main loop already calls `js_run_stdlib_pump` and `js_stdlib_has_active_handles` each tick; this pulls the http-server queue into that flow. **(5) perry/src/commands/compile/optimized_libs.rs** — when the well-known flip routes `node:http` / `node:https` / `node:http2` to perry-ext-http (which bundles perry-ext-http-server via rlib piggyback), activate the `external-http-server-pump` feature on the auto-optimized perry-stdlib build. Gated strictly on the MODULE name (not on `http-client` feature, which axios / node-fetch also map to) — those bring perry-ext-axios / perry-ext-fetch which don't define `js_node_http_server_*` symbols, so activating the pump for them would drop unresolved externs at link time. **(6) HIR registration follow-ups (#625)**: with listen() now non-blocking and code reaching past it, two pre-existing dispatch gaps surface that previously couldn't fire because the program never got that far. **(6a) crates/perry-hir/src/destructuring.rs** — the inside-function `const server = createServer(handler)` pattern wasn't registering `server` as `("http", "HttpServer")` (only top-level `lower.rs` paths handled it), so `server.listen(...)` inside an async function fell through the dispatch table's `class_filter: Some("HttpServer")` gate and the cb closure was never invoked — `[5] after listen call` printed but `[4] listen cb fired` did not. New arm in destructuring.rs's named-import factory detection mirrors the lower.rs:5530 arm. **(6b) Same file** — the awaited factory call arm now also covers `("axios", "get" | "post" | "put" | "delete" | "patch" | "request") => Some("Response")` so `await axios.get(url)` inside an async function registers the result as an axios.Response (mirrors the lower.rs:4011 top-level arm); without this, `r.status` / `r.data` fall through to generic property dispatch and read the raw handle pointer as an ObjectHeader, returning undefined for everything. **(7) Codegen ABI fix in lower_call.rs's axios arm (#626)** — the `bitcast_double_to_i64(&recv_handle)` call left the upper-16 POINTER_TAG bits in place; `js_axios_response_status`'s `get_handle::<AxiosResponseHandle>(handle)` lookup missed because the registry key is the bare handle id, not the NaN-boxed bits. Replaced with `unbox_to_i64` (bitcast + AND POINTER_MASK_I64). **(8) JSON-auto-parse for `r.data` (#627)** — npm `axios` parses the response body as JSON when content-type is `application/json`; perry-ext-axios's `js_axios_response_data` returned the raw text, so `r.data.ok` accessed on a string returned undefined. New `js_axios_response_data_parsed(handle: Handle) -> f64` returns either a parsed JSValue (object/array/number/bool/null) when the body looks like JSON and `js_json_parse` succeeds, or a STRING_TAG-NaN-boxed perry string for non-JSON / parse failure. The codegen `data` arm in `lower_call.rs` routes through this instead of the legacy raw-string getter. The `js_json_parse` extern is declared as `-> u64` (not `-> f64`) because the runtime returns `JSValue` (`#[repr(transparent)]` over u64) — wrong return type would land the bits in the wrong AArch64 register class. The current heuristic uses body shape (`trimmed.starts_with('{')` / `'['` / etc.) rather than consulting Content-Type — works for the #604 fixture and most real-world JSON responses but deviates from npm axios's content-type-based behavior; tightening to content-type-based is tracked in #627. **End-to-end** (`tests/release/packages/axios-get/entry.ts`): compiles, binds port 18902, axios.get hits the in-process server, handler returns `{ok:true, path:"/json"}`, axios parses the JSON, prints `status=200 / data.ok=true / data.path=/json` matching `expected.txt` byte-for-byte, server.close() returns, exit=0. Pre-fix the same fixture timed out at 124 with empty stdout. **Sanity**: test_inheritance / test_simple_class / test_break_continue / test_edge_promises / test_gap_async_advanced all rc=0; test_node_http_basic + test_node_http_post_body still serve correctly via curl (timeout=124 is expected — they're servers with no `.close()` call, so the loop never exits, same shape pre-fix and post-fix). **Out of scope (separate followups)**: the fastify `event_loop` blocking pattern in perry-ext-fastify is structurally identical and would benefit from the same architectural shift, tracked separately. Async handlers that yield across multiple microtask cycles before calling `res.end()` are best-effort under the new pump model.
- **v0.5.714** — v0.5.713 broke `cargo build --release -p perry` because `crates/perry/src/commands/compile/optimized_libs.rs:170` calls `super::well_known::bundled_staticlib_path_for_target(workspace_root, binding, rust_target_triple(target))` (3-arg, target-aware) but `crates/perry/src/commands/compile/well_known.rs:74` only had `bundled_staticlib_path` (2-arg, host-only). E0425 "cannot find function" + a downstream E0282 "type annotations needed" on the `let lib_path` it would have populated. Fix: rename + extend the 2-arg version to `bundled_staticlib_path_for_target(workspace_root, binding, target_triple: Option<&str>)`. When `target_triple` is `Some`, look in `target/<triple>/release/lib<name>.a` (matches the convention at `optimized_libs.rs:541` for `release_dir`); otherwise the host build dir `target/release/lib<name>.a`. Only one caller existed so no breakage at other sites. Pure compile-fix; no runtime impact.
- **v0.5.713** — Closes the three baseline parity gaps (#613 / #614 / #615) flagged in CLAUDE.md and tracked by tier 2 of the release-sweep harness. **#613 + #614 (test_edge_destructuring + test_edge_objects_records — silent execution aborts after lines 16/47 and 19/50)**: same root cause. The HIR's closed-shape object-literal lowering at `crates/perry-hir/src/lower/expr_object.rs::lower_object` rewrites `{key: value, ...}` to `Expr::New { class_name: "__AnonShape_<hash>" }` so downstream property access hits the codegen direct-GEP fast path. The codegen's `Stmt::Let` arm at `crates/perry-codegen/src/stmt.rs:295/342` then scalar-replaces the local: per-field allocas replace the heap allocation, and the local's `local_types` entry is overwritten from `Type::Any` (whatever the user wrote) to `Type::Named("__AnonShape_<hash>")` so the receiver-class fast path fires for property gets. The local's main slot is a `dummy_slot` — never populated with a real heap pointer because the heap allocation was elided. **The bug**: when user code accessed a field NOT in the closed shape (`const obj: any = { host: "S" }; const x = obj.port;` — `port` isn't in the literal), the scalar-replaced fast path's per-field map missed → fell through to the receiver-class path → `class_field_global_index` returned None for "port" → method-bind check missed → fell through to the generic runtime helper `js_object_get_field_by_name_f64`. That helper got the dummy_slot's uninitialized value (typically zero) loaded as a "receiver pointer" and dereferenced it as an ObjectHeader, hitting LLVM's `unreachable` from `js_object_get_field_by_name`'s defensive bounds check (`brk #0x1` in the disassembly — process exit 133 / SIGTRAP). Fix at `crates/perry-codegen/src/expr.rs:3219`: add a single check after the scalar-replaced fast path — when `ctx.scalar_replaced.contains_key(id)` AND the property wasn't found in the per-field map, return `TAG_UNDEFINED` directly. Spec-correct (missing fields on closed-shape literals must produce `undefined`, never crash) and matches what the runtime helper would have returned for a real object that simply didn't have the key. Same fix unblocks both tests; #613 was destructuring with defaults reading missing fields like `const { host = "L", port = 8080 } = { host: "S" }`, #614 was `Record<number, T>` patterns where one inferred shape's missing field tripped the same path. Both tests now byte-identical to Node (47 and 50 lines respectively). **#615 (test_gap_object_methods — frozen / sealed / non-extensible writes don't throw under strict mode)**: ECMAScript spec says `Object.freeze(obj); obj.x = 1` and `Object.defineProperty(obj, "k", {writable:false}); obj.k = 2` and `Object.preventExtensions(obj); obj.x = 1` must throw `TypeError` in strict mode. TypeScript files are strict-by-default (any TS module that ESM-imports anything is in strict mode per the module-record spec) and Perry users always run strict, so `js_object_set_field_by_name`'s four `if (is_frozen) return;` / `if (is_sealed_or_no_extend) return;` / `if (!attrs.writable()) return;` silent-no-ops at `crates/perry-runtime/src/object.rs:3570/3625/3650/3675` were spec-incorrect — `try { obj.x = 1 } catch { ... }` never fired, the catch arm was dead code, and 5 expected lines (`defineProperty write threw (strict mode)` / `freeze write threw` / `freeze add threw` / `sealed add threw` / `preventExtensions add threw`) never appeared in the parity test output. Fix in three layers: (a) New runtime FFI `js_throw_type_error_immutable_write(kind, key_ptr, key_len)` in `crates/perry-runtime/src/error.rs` — kind 0 = "Cannot assign to read only property '<key>' of object '#<Object>'" (writable:false / frozen overwrite), kind 1 = "Cannot add property <key>, object is not extensible" (sealed / preventExtensions / frozen new key), kind 2 = "Cannot delete property '<key>' of #<Object>" (sealed / frozen delete). Allocates a real `TypeError` via `js_typeerror_new` and routes through `js_throw` so the message ends up wrapped in a proper `Error` instance with `.message` / `.name` populated and the `try { ... } catch (e) { e.message }` chain works correctly. Crate-internal `throw_immutable_write(kind, &str)` wrapper for the convenience of runtime-side callers (saves boilerplate `key.as_ptr() / key.len()` at every call site). (b) New helper `key_to_str_for_diag(key: *const StringHeader) -> String` in `crates/perry-runtime/src/object.rs:3333` — reads the StringHeader's UTF-8 payload (same shape as the existing `incoming_key_str` codepath but unconditional, used only on the throw path so no allocation cost on the happy path). (c) Four call sites in `js_object_set_field_by_name` flipped from `return;` to `crate::error::throw_immutable_write(...)`: the keys-is-null + frozen/sealed arm at line 3570 (kind 1), the key-found + is_frozen arm at line 3625 (kind 0), the per-property writable check at line 3649 (kind 0 — uses the already-resolved `incoming_key_str`), and the key-not-found + frozen/sealed arm at line 3675 (kind 1). End-to-end: `test_gap_object_methods` now byte-identical to Node, all 5 previously-missing throw lines fire correctly. The 3 pre-existing baseline-fails count drops to 0; tier 2 parity sweep should now match the v0.5.696 high-water mark with no remaining "long-standing baseline gaps" entries in the issues board. **Sanity**: test_inheritance / test_simple_class / test_break_continue / test_edge_promises / test_gap_async_advanced all still pass; the three closed test files (test_edge_destructuring / test_edge_objects_records / test_gap_object_methods) all match Node byte-for-byte.
- **v0.5.712** — Three leaked `eprintln!` debug lines in `crates/perry-ext-http-server/src/{server,http2_server,https_server}.rs` (line 219 / 254 / 261 — `[node:http] listening on http://...`, `[node:http2] listening on https://...`, `[node:https] listening on https://...`) fired on every `server.listen()` call. Same pattern as v0.5.710's `js_value_is_promise` debug spam — stdout pollution clobbered parity tests that compare against Node byte-for-byte. Fix: remove all three. `test_node_http_post_body` and `test_node_http_ws_upgrade` recover. Tier 2 parity now 220 passed / 5 failed / 13 skipped (97.7%) — matching the v0.5.696 baseline exactly. The 5 still-failing are 3 long-standing baseline gaps (#613 / #614 / #615 filed) and 2 stack-trace format divergences from v0.5.702's #596 fix (test_issue_462 / test_issue_510 — Perry prints `Uncaught exception: TypeError: ...` instead of Node's `file:line` + source line + caret + stack-frame block; format change rather than semantic regression).
- **v0.5.711** — Closes #542/#543 followup: `m.keys()` / `m.values()` / `m.entries()` on a `Map<K, V> | undefined` parameter narrowed via `if (!m) return;` returned the array-fold shape (`[0..N-1]` for keys, garbage for values) because `crates/perry-hir/src/lower/expr_call.rs:3319` only matched bare `Type::Generic { base: "Map" }` for the receiver-type predicate. The for-of path in `lower.rs:6196` already handled `Type::Union` containing Map (per the original v0.5.624 fix); the method-call path here did not. Fix: extract `ty_is_map` / `ty_is_set` / `ty_is_array` predicates and accept either bare match OR `Type::Union(variants).iter().any(...)`. Mirrors the for-of path. Verified: `test_issue_542_map_keys_optional` byte-identical to Node post-fix (`keys: -8796093023232,-8796093024256` / `values: first,second`). Surfaced by tier 2 of `scripts/release_sweep.sh` after v0.5.710 cleared the eprintln spam.
- **v0.5.710** — Parity regression in v0.5.707: 39 tests failing (82.7% rate) vs the v0.5.696 baseline of 6 (97.3%). Surfaced by tier 2 of the release-sweep harness; the failure cluster was async-heavy (test_async / test_async{2-5} / test_async_chain / test_edge_promises / test_gap_async_advanced / test_stress_promises / test_issue_256_microtask_ordering) plus everything that touches fetch / streams / blob / node:http (test_gap_fetch_response / test_issue_237_streams_* / test_issue_561_* / test_node_http_*). Root cause: three leaked `eprintln!` debug statements in `crates/perry-runtime/src/promise.rs::js_value_is_promise` — fired on every non-Promise tag check, dumping `[is-promise] not POINTER_TAG, bits=0x...` etc. to stderr. Every test that calls `js_value_is_promise` (which is almost every async test) had its stdout/stderr stream interleaved with these lines and diverged from Node byte-for-byte. The function logic itself was correct; only the prints were leftover from an earlier investigation. Fix: remove the three `eprintln!` calls. Verified: tier 2 recovers from 39 → 8 fails (96.4% rate, 31 tests recovered). The 8 still-failing are 3 pre-existing baseline (test_edge_destructuring / test_edge_objects_records / test_gap_object_methods) plus 5 separate smaller issues (test_issue_462 / test_issue_510 / test_issue_542 / test_node_http_post_body / test_node_http_ws_upgrade) that need their own investigations. No runtime semantic change — pure debug-print removal.
- **v0.5.709** — `cargo test --workspace` was still red after v0.5.708 due to the parallel `manifest_consistency` invariant: `every_dispatch_entry_has_manifest_counterpart` panicked because `NATIVE_MODULE_TABLE` had `ioredis::connect` and `ioredis::disconnect` rows (added in v0.5.707's #605 fix when normalizing the `redis` npm alias) but `API_MANIFEST` had no matching entries. Added both rows to `crates/perry-api-manifest/src/entries.rs` next to the existing `ioredis::set/get/del/exists/incr/decr/expire/quit` block. Also fixes a release-sweep harness false positive in tier 12 (`scripts/release_sweep_tiers/tier12_link_smoke.sh`): "perry/ui imported but libperry_ui_<plat>.a not found" now routes to SKIP (precondition — UI staticlib must be pre-built per target, mirroring the existing `libperry_runtime.a` SKIP arm) instead of FAIL. Surfaced by tier 1 of `scripts/release_sweep.sh`.
- **v0.5.708** — `cargo test --workspace` regression from v0.5.707: the `imported_func_has_rest` field added to `CompileOptions` for #608's cross-module rest-param plumbing was missed in the `empty_opts()` test helper at `crates/perry/src/commands/compile/object_cache.rs:514`. `cargo test -p perry` failed with E0063 missing-field; tier 1 of the release-sweep harness reported FAIL. Fix: add `imported_func_has_rest: std::collections::HashSet::new()` to the test initializer in canonical struct order. No runtime impact — pure test-fixture touch-up. Surfaced by `scripts/release_sweep.sh` tier 1 immediately after Ralph's #608 push.
- **v0.5.707** — Multi-issue release-sweep cycle (#601–#608). **#601** (perry-ext-fetch tests broken at workspace `cargo test`): `crates/perry-ext-fetch/src/lib.rs:1140/1179/1196` — three test-code type mismatches against the post-Phase-1 NaN-boxed FFI surface (integer literal where f64 expected, missing 4th `headers_handle: f64` arg on `js_request_new`, `resp as i64` cast on a function that takes f64). Pure test-code touch-up; lib code already correct. **#602** (drizzle-orm + better-sqlite3 link gap): `import Client from "better-sqlite3"` made the codegen's `lower_builtin_new` "Client" arm fire pg's `js_pg_client_new` against the default-import alias, dropping an undefined `_js_pg_client_new` reference in the link line of every drizzle-sqlite consumer. New `imported_class_sources: HashMap<String, String>` per-module map built from `hir.imports` and threaded via `CrossModuleCtx` → `FnCtx` → `lower_builtin_new`; gates the ambiguously-named arms (Client → "pg", Pool → "pg", Database → "better-sqlite3", Redis → "ioredis"/"redis", MongoClient → "mongodb", Decimal → "decimal.js") on the import source matching, so `Client` from a non-pg module falls through to the generic placeholder path. **#605** (redis createClient TypeError): `import { createClient } from "redis"` resolved to module `"redis"` but `NATIVE_MODULE_TABLE` only had `module: "ioredis"` rows — `client.connect()` hit the catch-all and surfaced as `Cannot read properties of undefined`. Added a `redis` → `ioredis` normalization in `native_module_lookup` mirroring the well-known-bindings alias, plus new `connect`/`disconnect` rows pointing at `js_ioredis_connect`/`js_ioredis_quit` so the `await client.connect()` chain resolves through the existing handle. **#606** (ws SIGABRT, "Cannot start a runtime from within a runtime"): the v0.5.691 sweep that fixed perry-ext-http's accept loops missed `crates/perry-ext-ws/src/lib.rs::js_ws_server_new`, `js_ws_connect`, `js_ws_connect_start`, and `drive_client_io` — all four still ran `Handle::current().block_on` inside the closure that `spawn_blocking_with_reactor` runs as a tokio task. Replaced each with `tokio::spawn(async move { ... })` so the IO loops run as sibling tasks. Plus four follow-up fixes that surfaced once the panic cleared: (a) Listening-event race — when `wss.on("listening", cb)` registers AFTER the accept-loop pushes the event, re-emit on a late registration if `is_listening` is already true. (b) New `Open(usize)` `PendingWsEvent` variant fired from both connect paths so `client.on("open", cb)` actually dispatches; mirror late-registration race fix for `"open"`. (c) `js_ws_on` looked up the server registry first, but client `ws_id`s (NEXT_WS_ID counter) and server handle ids (perry-ffi NEXT_HANDLE counter) live in disjoint stores with overlapping numeric ranges — checked the client registry first so `client.on(...)` lands on the right listener set. (d) `new WebSocket(url)` (the npm `ws` constructor) had no `lower_builtin_new` arm and fell through to the placeholder — `client.on(...)` then dispatched against an empty ObjectHeader. Added a `"WebSocket"` arm that calls `js_ws_connect_start` and NaN-boxes the returned ws_id with POINTER_TAG so the standard `unbox_to_i64` receiver contract works at every method call site. New `external-ws-pump` Cargo feature in perry-stdlib + activation in `optimized_libs::build_optimized_libs` so when the well-known flip routes `import 'ws'` to perry-ext-ws (stripping `bundled-ws`), `js_stdlib_process_pending` and `js_stdlib_has_active_handles` still call into perry-ext-ws's `js_ws_process_pending` / `js_ws_has_pending` — without this, the accept-loop tokio task pushes events nobody drains and the program hangs after the panic fix. Mirrors `external-net-pump` (v0.5.579). **#607** (watchOS-simulator console-only fixture undefined `_perry_watchos_*`): `PerryWatchApp.swift` template unconditionally references four `@_silgen_name`'d Rust symbols (`tree_version`/`toggle_changed`/`toast_seq`/`toast_dismiss`) — they exist in `libperry_ui_watchos.a` but the link path only adds the UI lib when `ctx.needs_ui = true`. A console-only TS fixture (`console.log("ok")`) failed at link with all four undefined. Force-link `libperry_ui_watchos.a` for watchOS regardless of `needs_ui` (mirrors how the Swift template always lands on the link line); `skip_runtime` gate also widened to handle the force-link case. **#608** (cross-module tagged-template rest params): `tag\`hello ${x}\`` from `import { tag } from "pkg"` had `params === firstArg` instead of `[firstArg]`, and `tag\`default\`` had `params === undefined`. The HIR lowers tagged-templates to `Call { callee: ExternFuncRef, args: [stringsArr, ...interp] }` — the cross-module call site in `lower_call.rs` had no rest-bundling path (only the same-module fast path consulted `func_signatures`'s `has_rest` bit). New `exported_func_has_rest: BTreeMap<(String, String), bool>` in CLI side, propagated to per-module `imported_func_has_rest: HashSet<String>` through CompileOptions / CrossModuleCtx / FnCtx; the cross-module ExternFuncRef arm now bundles trailing args into a `js_array_alloc(n)` rest array when has_rest fires. Repro from issue (`sql\`default\`` → `params: []`, `sql\`hello ${x}\`` → `params: ['world']`) now matches Node byte-for-byte. Refs #488 (drizzle's `sql\`default\`` shape was the load-bearing real-world site). **#604** (axios + node:http hang after `server.close()`) — partial fix: `event_loop` in `crates/perry-ext-http-server/src/server.rs` was `loop {}` with no exit — `server.close()` flipped `listening = false` but the main thread spun forever. Loop now checks `listening` at the top of every tick and breaks out cleanly, also calling `js_gc_exit_unsafe_zone` on exit to balance the `enter` from `listen`. The deeper architecture issue (`server.listen()` blocks the main thread, so user code after `listen()` never runs) is out of scope for this fix and tracked separately. **Sanity**: test_inheritance / test_simple_class / test_break_continue / test_edge_promises / test_gap_async_advanced all pass; in-module tagged-template (`function sql(strings, ...params) { ... }; sql\`hello ${x}\``) byte-identical to Node. **Still open**: #603 (hono routes-with-params + notFound — second/third `app.fetch` hangs), #604 (axios+node:http full fix needs non-blocking listen), #611 (Effect framework SIGSEGV in `Utils.ts__init`).
- **v0.5.706** — **Closes #599** (`state.text()` widget never updated on macOS after `state.set("new value")`). The state-desugar pass at `crates/perry-transform/src/state_desugar.rs::state_text_call` lowers `headline.text()` to `Text(initial_str, synth_id)` — the 2-arg form that the codegen routes through `crates/perry-codegen/src/lower_call/native.rs::native_method_call`'s 2-arg Text arm to `perry_ui_text_create_with_id(text_ptr, id_ptr)`. Pre-fix the macOS implementation in `crates/perry-ui-macos/src/lib.rs:2452` was a stub that took both args but discarded the id: `perry_ui_text_create_with_id(text_ptr: i64, _id_ptr: i64) -> i64 { perry_ui_text_create(text_ptr) }`. The `_id_ptr` (synth_id `__state_0` / `__state_1` / etc.) never landed in `crates/perry-ui-macos/src/widgets/text_registry.rs::TEXT_IDS`, so when `headline.set("AFTER UPDATE")` fired `js_state_set` → `perry_arkts_set_text(synth_id, "AFTER UPDATE")` → the cross-platform `set_text_handler` shim's `TEXT_IDS.get(&synth_id)` lookup missed and the call silently returned. The widget kept its initial text. Two coordinated changes in `lib.rs`: (1) `perry_ui_text_create_with_id` now reads the StringHeader at `id_ptr` (the same shape as the existing `perry_ui_widget_set_tooltip` reader at line 1397+ — `header.byte_len` + the inline UTF-8 bytes after the struct) and calls `widgets::text_registry::register_text_id_handler(handle, data, len)` so subsequent `setText(synth_id, val)` lookups find the widget. (2) `perry_ui_set_text(id_ptr, value_ptr)` (which had been a no-op stub) now decodes both StringHeaders and forwards to `text_registry::set_text_handler` directly — covers the `import { setText } from "perry/ui"` user-code surface for completeness (the `js_state_set` pump path already routes through the registered handler via `js_register_set_text_handler`, this just adds the direct-call path). Verified end-to-end: the issue's repro (`const headline = state("initial"); App({ body: VStack(8, [ headline.text(), Button("update", () => headline.set("AFTER UPDATE")) ]) })`) now renders "initial" at first paint, and clicking the button flips the displayed text to "AFTER UPDATE" — the registered handle in TEXT_IDS gets located and `text::set_text_str` fires `setStringValue:` on the underlying NSTextField. `PERRY_UI_TEST_MODE=1` smoke compiles + boots cleanly. **Out of scope for this fix and tracked separately**: `ForEach(countState, render)` updates on `countState.set(N)` — the existing `perry_ui_for_each_init(container, state_handle, closure)` in `crates/perry-ui-macos/src/state.rs:408` uses an integer state handle from the OLD `perry-ui-macos` state system, NOT the synth-id strings the new cross-platform `state<T>` desugar produces. Bridging requires a parallel synth-id-keyed foreach registry + a hook in `js_state_set` that walks it and re-renders — distinct from the text_registry change here. The user's repro for ForEach (which keeps showing 2 rows after `count.set(5)`) needs that follow-up to fully work.
- **v0.5.705** — **Closes #600** (LLVM Phase-2 codegen regression vs v0.5.491: `Expr::FetchGetWithAuth`, `Expr::FetchPostWithAuth`, `Expr::DateToLocaleString` had no LLVM arms — fell through to the catch-all "expression … not yet supported" error). Three coordinated arms in `crates/perry-codegen/src/expr.rs`: (1) `Expr::FetchGetWithAuth { url, auth_header }` lowers both args through `js_get_string_pointer_unified` to raw `*const StringHeader`s, calls `js_fetch_get_with_auth(url_ptr, auth_ptr) -> *mut Promise`, and NaN-boxes the returned promise pointer with POINTER_TAG. (2) `Expr::FetchPostWithAuth { url, auth_header, body }` adds the body arg through the same string-pointer extraction (matching the existing perry-stdlib `js_fetch_post_with_auth(url_ptr, auth_ptr, body_ptr)` signature where the caller is expected to pre-stringify the body — same shape the JS / WASM backends emit). (3) `Expr::DateToLocaleString(d)` — the HIR variant is misnamed: it covers BOTH `(12345).toLocaleString()` (number) and `new Date(...).toLocaleString()` (date) since the AST source is just `.toLocaleString()` on any receiver. The arm calls `crate::type_analysis::refine_type_from_init(ctx, d)` to recover the static type — `Some(Type::Number)` / `Some(Type::Int32)` route to a new `js_number_to_locale_string(f64) -> *mut StringHeader` runtime helper (formats with thousands separators, en-US default — `(12345).toLocaleString() === "12,345"` byte-for-byte vs Node); everything else falls through to the existing `js_date_to_locale_string(timestamp_f64)` (formats as `M/D/YYYY, h:MM:SS AM/PM`). New `js_number_to_locale_string` in `crates/perry-runtime/src/date.rs` formats by splitting integer / fractional parts, walking integer digits inserting commas every 3 from the right, appending up to 3 fraction digits with trailing zeros stripped, and handling NaN / ±Infinity literally. Runtime decl in `crates/perry-codegen/src/runtime_decls.rs:2105+`. End-to-end: `(12345).toLocaleString()` → `"12,345"`, `(1234567).toLocaleString()` → `"1,234,567"`, `(3.14).toLocaleString()` → `"3.14"`, `(-9876).toLocaleString()` → `"-9,876"`. `fetchWithAuth(url, "Bearer ...")` repro compiles + links. Sanity: test_inheritance / test_simple_class / test_break_continue / test_edge_promises / test_gap_async_advanced / test_array_methods all rc=0. The fix unblocks the gscmaster.com production codebase that uses both patterns heavily for authenticated HTTP + email-formatting numbers.
- **v0.5.704** — **Closes #598** (`axios.post(url, {email, password})` sent the raw bytes of Perry's `ObjectHeader` struct followed by the first character of the stringified field — server received `^@^B^@^@H...` instead of `{"email":"demo@example.com","password":"hunter2"}`). Three coordinated layers, mirroring the npm `axios` package's body-encoding rule (string → send as-is, anything else → JSON.stringify): **(1) FFI signature.** Both `crates/perry-ext-axios/src/lib.rs::js_axios_post`/`_put`/`_patch` and `crates/perry-stdlib/src/axios.rs::js_axios_post`/`_put`/`_patch` declared their body arg as `data_ptr: *const StringHeader` — the codegen unboxed the caller's NaN-boxed value to i64 and passed it as a raw pointer. For an object-literal body the unboxed pointer was a real `*mut ObjectHeader`; the runtime read its first 8–16 bytes as a `StringHeader` (length + refcount + data prefix) and built the request body from those bytes plus the address of the first stringified field — surfacing as a `SyntaxError: Unexpected token '^@'` on the receiving Fastify+pino server. Flipped the body arg to `data: f64` so the codegen passes the NaN-boxed value through unchanged. **(2) Body-encoding routing.** New `read_body_as_string(value_bits: f64)` helper (parallel impls in perry-ext-axios + perry-stdlib): decodes the NaN-box tag and routes — `STRING_TAG` / `SHORT_STRING_TAG` → read as-is (sent as the request body unchanged, matching npm axios's "string body passes through" behavior); `TAG_UNDEFINED` / `TAG_NULL` → empty body; everything else → `js_json_stringify(value, 0)` to produce `{"email":...,"password":...}` from an object literal, or `[1,2,3]` from an array. perry-stdlib's variant is named `body_string_from_value` and stringifies on the main thread BEFORE crossing the `spawn_for_promise` tokio boundary (`js_json_stringify` reads from perry-runtime's thread-local arena — calling it from a worker would access the wrong arena and corrupt). perry-ext-axios's variant runs the same step inside `js_axios_post` before `spawn_blocking`. **(3) Codegen dispatch + runtime decl.** `crates/perry-codegen/src/lower_call.rs:3716+` (the `axios.post`/`.put`/`.patch` arm) now passes the lowered body as `(DOUBLE, &body_box)` instead of unboxing to `(I64, &body_handle)`. `crates/perry-codegen/src/runtime_decls.rs:1608` declarations updated from `&[I64, I64]` to `&[I64, DOUBLE]` so LLVM emits the matching SysV ABI calling convention (pointer in x0, NaN-boxed double in d0). End-to-end (with a sibling `node` HTTP server on 127.0.0.1:18598 echoing the received body): object body — server logs `{"email":"demo@example.com","password":"hunter2"}` byte-for-byte, exit 0. Pre-stringified string body — server logs `{"already":"json"}` (sent as-is, no double-encoding). Sanity: test_inheritance / test_simple_class / test_break_continue / test_edge_promises / test_gap_async_advanced / test_array_methods all rc=0. The same routing pattern unblocks any future stdlib HTTP-client wrapper that needs to accept a JS body of mixed types.
- **v0.5.703** — **Closes #597** (drizzle #488 blocker: `for (const [i,v] of arr.entries())` silently iterates 0 times when `arr` is statically typed `any`). The HIR's array-method fold in `crates/perry-hir/src/lower/expr_call.rs` had two passes: a static-type Array path (line 3966+) that folds `arr.entries()` to `Expr::ArrayEntries(arr)`, and a dynamic catch-all (around line 4250+) that folds `reduce`/`map`/`filter`/`forEach`/`find`/`findIndex`/`some`/`every` for any-typed receivers gated on `!recv_is_class`. The catch-all was missing arms for `entries`/`keys`/`values` — so on an any-typed receiver these fell through to a generic `js_native_call_method` dispatch which returned an iterator-shaped object whose `.length` was undefined. The for-of array desugar in `crates/perry-hir/src/lower_decl.rs` (the `for (let i = 0; i < __arr_N.length; i++)` index-loop shape that handles every for-of) saw `__arr_N.length === 0` and the body never ran. Added three new arms to the catch-all: `"entries" if args.is_empty() && !recv_is_class` → `Expr::ArrayEntries(array_expr)`, plus parallel `"keys"` / `"values"` arms. Codegen for `Expr::ArrayEntries` already calls `js_array_entries` which materializes `[i, value]` pairs into a real Array — the same fast path the static-type fold has used since v0.5.0. Drizzle's `pg-core/dialect.js::buildInsertQuery` (`for (const [valueIndex, value] of values.entries())` where `values` arrives via destructuring of an any-typed function param) now correctly iterates each value pair. End-to-end: issue's repro (`function f(arr: any) { for (const [i, v] of arr.entries()) console.log('iter', i, v); }`) prints `iter 0 { name: 'alice' } / iter 1 { name: 'bob' }` matching Node. Sanity: test_array_methods / test_edge_iteration / test_gap_array_methods / test_inheritance / test_simple_class / test_break_continue / test_edge_promises / test_gap_async_advanced / test_edge_arrays all rc=0. Refs #488 (drizzle compile path).
- **v0.5.702** — **Closes #596** (sync throw inside async fn body — `obj.status` where `obj` is undefined, post-await — escaped the user's `try { await x; ... } catch { … }` and exited the program). The two type-error helpers `js_throw_type_error_property_access` (used by codegen for property access on `undefined` / `null`, #462) and `js_throw_type_error_not_a_function` (used by codegen + `js_native_call_method` for "X is not a function", #510) both bypassed Perry's setjmp/longjmp exception machinery: each printed the V8-shaped diagnostic to stderr and called `std::process::exit(1)` directly, so the user's surrounding `try`/`catch` had no chance to fire. The "until generalized exception-throw machinery is in place" comment was a v0.5.0-era TODO; today `js_throw` + `js_typeerror_new` give us the proper path. Both helpers now allocate a real `TypeError` with the V8 message, NaN-box it as a POINTER value, and call `js_throw(value)`. `js_throw` longjmps to the nearest enclosing setjmp catch frame (the user's `try`); if `TRY_DEPTH == 0` it falls back to `print_uncaught` + `exit(1)`, preserving the prior user-visible behavior for unhandled throws. End-to-end: issue's repro (`async function direct() { try { await f(); const obj = undefined as ...; const _v = obj.status; } catch (e) { console.log("direct caught:", e.message); } }`) prints `direct caught: Cannot read properties of undefined (reading 'status')` and continues to the post-try `console.log` — both `direct PASS` and `wrapped PASS` lines fire, and main reaches the `END` log. Sanity: test_inheritance / test_simple_class / test_break_continue / test_edge_promises / test_gap_async_advanced / test_edge_error_handling / test_gap_typeof_instanceof all rc=0. The "where it bit us" pattern (a tiny test runner that does `for (const c of cases) try { await Promise.resolve(c.fn()); } catch (e) { ... }`) now correctly catches sync throws inside async test bodies and continues iterating cases.
- **v0.5.701** — **Closes #594 #595** (`async function callIt() { try { ... await ... } catch {} ; if (cond) return X; }` returned `X` directly to the iterator caller, surfacing as `TypeError: Cannot read properties of undefined (reading 'done')` on `__step_r.done`). The async-to-generator transform's `StateExit::Done` handler in `crates/perry-transform/src/generator.rs` had two non-recursive passes that both broke when a user `return X` was nested inside an `if` (or any control-flow stmt) inside a state's body: (a) the prepend-`done = true`-before-return loop iterated the state body's TOP-LEVEL stmts only (`for s in case_body.drain(..) { if matches!(s, Stmt::Return(_)) { ... } }`), so a `Return` inside `Stmt::If.then_branch` slipped through and `__gen_done` stayed `false` after the user-level return — the iterator caller's next `gen.next()` call would re-enter the same state and hang or yield `undefined`; (b) `rewrite_returns_as_done` likewise iterated only top-level stmts, so a nested `Return(X)` survived as a bare value-return, the iterator caller's `__step_r = gen.next()` got `X` (a NaN-boxed plain value), and the immediately following `__step_r.done` access threw the catch-all `Cannot read properties of undefined (reading 'done')` error. Three coordinated changes: (1) `rewrite_returns_as_done` now recurses through `If` / `While` / `DoWhile` / `For` / `Try` (body + catch + finally) / `Switch` cases / `Labeled` body, so every `Return(Some(X))` at any depth gets wrapped to `Return({value: X, done: true})` and every `Return(None)` becomes `Return({value: undefined, done: true})`. (2) New `prepend_done_before_returns(stmts, done_id)` walks the same control-flow shapes and inserts `LocalSet(done_id, true)` immediately before each `Stmt::Return` at every depth; replaces the old top-level-only loop. (3) New `body_contains_return(stmts) -> bool` recursive scanner replaces the prior `case_body.iter().any(|s| matches!(s, Stmt::Return(_)))` so the outer dispatch correctly identifies states whose user-level return is buried inside a nested control-flow block. Also append a default `__gen_done = true; return { value: undefined, done: true }` tail when the LAST stmt isn't unconditionally a Return — covers the `if (cond) return X` shape where the false branch falls through. End-to-end: issue's repro (`async function callIt() { const res = await makeR(); let parsed: unknown = null; try { parsed = await res.json(); } catch (_e) { parsed = null; } if (res.status >= 200 && res.status < 300) return parsed as { ok: boolean }; throw new Error("bad"); }`) now matches Node — `r = {"ok":true,"name":"ada"} / PASS`. Sanity: test_inheritance / test_simple_class / test_break_continue / test_edge_promises / test_gap_async_advanced / test_gap_closures / test_edge_scope_hoisting / test_edge_error_handling all rc=0. **#595** is the same root cause surfacing through a different shape — `let impl: FetchFn = ...; const r = await impl(url); if (r.status >= 200 && r.status < 300) return r.body as T;` — the `return r.body` lived inside the same `if`-then-branch nested inside a state body. Issue's repro now matches Node byte-for-byte: `variant A: {"ok":true} PASS / variant B: {"ok":true} PASS`. Verified with both `--no-auto-optimize` and the default link path.
- **v0.5.700** — **Closes #593** (`const off = ev.on(() => { off(); })` inside a function: closure body's `off()` no-ops because it captured the let's pre-init slot value). Two coordinated layers. **(1) HIR pre-registration.** v0.5.600's #461 fix pre-registered the let id BEFORE lowering its init when the init was a direct `Arrow` / `Fn` literal — so `let f = (n) => f(n-1)` resolved the inner `f` to `LocalGet(f_id)` instead of falling through to GlobalGet/ExternFuncRef. But the gate was strictly `matches!(decl.init, Some(Arrow) | Some(Fn))`, so any indirect closure-bearing init shape (Call wrapping Arrow, Object wrapping Arrow, Array wrapping Arrow, optional-chain through Member to a Call wrapping Arrow, …) skipped pre-registration. The closure body's reference to the let's name then resolved to `GlobalGet(0)` (`globalThis`), the closure's `captures` list didn't include the let id, and the call site silently dispatched to a method on `globalThis` that doesn't exist (no-op). Extended in `crates/perry-hir/src/destructuring.rs` with a new `ast_expr_contains_function_expr(e: &ast::Expr) -> bool` helper that recursively walks Call/New/Member/Object/Array/Bin/Unary/Cond/Paren/TsAs/TsNonNull/TsTypeAssertion/TsSatisfies/Assign/Seq/Tpl/TaggedTpl/OptChain looking for any Arrow/Fn descendant. The pre-registration gate now fires for both the direct shape AND any indirect shape — `const off = ev.on(() => off())` passes, `const sub = subject.subscribe({ next: () => sub.unsubscribe() })` passes, `const h = handlers.map(b => () => h())[0]` passes. The TDZ gate (the original commit's "don't pre-register `const x = x + 1`") is preserved because non-closure inits don't contain Arrow/Fn descendants and so don't enter the pre-registration arm. **(2) Boxing analysis.** `crates/perry-codegen/src/boxed_vars.rs::collect_self_recursive_closure_ids` only flagged let ids whose init was DIRECTLY `Expr::Closure { .. }`. With (1) in place the closure body now has the right `LocalGet(off_id)`, but the captures list reads the SLOT'S CURRENT VALUE at closure-build time — which is uninitialised (0.0 / TAG_UNDEFINED) because the let's initial assignment runs AFTER the wrapping Call returns. Boxing fixes this: capture[0] holds a box pointer; the let's initial assignment `js_box_set`s the value the closure later reads via `js_box_get`. New `init_expr_has_self_capturing_closure(init, let_id)` helper walks the init expression for any `Expr::Closure` descendant and (via the existing `collect_ref_ids_in_stmts`) checks whether its body refs `let_id`; on hit, the let id is added to `self_recursive_ids` and routed into `boxed`. Direct-Closure-literal arm preserved as a belt-and-suspenders fallback. End-to-end: issue's repro (`function runInFn() { ... const offA = ev.on(() => { a++; offA(); }); ev.emit(1); ev.emit(2); ... }`) now matches Node — `inside fn:  a=1 b=2  PASS`. The pattern was surfaced building a tiny RxJS-Subject replacement for porting an Angular app to Perry. Sanity: test_inheritance / test_simple_class / test_break_continue / test_edge_promises / test_gap_async_advanced / test_gap_closures / test_edge_scope_hoisting all rc=0.
- **v0.5.699** — **Closes #591** (`argon2.hash(password)` on perry-ext-argon2 reported "Invalid password" because the password StringHeader pointer never reached the FFI). Three coordinated layers. **(1) FFI ABI mismatch.** `crates/perry-codegen/src/lower_call.rs::NATIVE_MODULE_TABLE`'s entries for `argon2.hash` / `argon2.verify` / `bcrypt.hash` / `bcrypt.compare` declared their string args as `NA_F64` (pass NaN-boxed f64 through unchanged). The runtime functions in perry-ext-argon2 / perry-stdlib's bcrypt declare `password_ptr: *const StringHeader` (raw i64). On AArch64 the f64 lands in d0 while the callee reads x0 — caller and callee disagree on register class, so the StringHeader pointer is whatever stale bits were left in x0 (typically null or unmapped → segfault, or a non-null garbage that fails the in-`string_from_header` UTF-8 check → "Invalid password"). Flipped to `NA_STR` so the dispatcher routes through `js_get_string_pointer_unified` and passes a real i64 pointer. **(2) Active-handle gate for ext-* spawn_blocking.** perry-ffi's `spawn_blocking` (used by every perry-ext-* native binding for CPU-bound work) dispatches through perry-stdlib's `perry_ffi_spawn_blocking` shim, which spawns onto tokio's blocking pool and immediately returns. The tokio worker is invisible to perry-stdlib's `js_stdlib_has_active_handles` — no entry in `PENDING_RESOLUTIONS` yet, no WS/net/readline activity — so the codegen-emitted event loop's header check returns 0 and the loop exits before the worker ever queues its result. New `EXT_BLOCKING_TASKS_INFLIGHT: AtomicUsize` in `crates/perry-stdlib/src/common/async_bridge.rs` is `fetch_add(1)`d in `perry_ffi_spawn_blocking` / `perry_ffi_spawn_blocking_with_reactor` before the spawn and `fetch_sub(1)`d after the closure returns; `js_stdlib_has_active_handles` returns 1 while the counter is non-zero. The decrement also calls `js_notify_main_thread` so the main loop's `Condvar::wait_timeout` re-evaluates the gate immediately rather than after the 1 s heartbeat. **(3) Microtask-pending gate.** The argon2 worker's `promise.resolve_string(hash)` queues to `PENDING_RESOLUTIONS`; `js_stdlib_process_pending` (called via `js_run_stdlib_pump`) drains that into `js_promise_resolve(inner)` which queues the chained `.then` continuation to perry-runtime's thread-local `TASK_QUEUE`. The codegen-emitted body order is `microtasks → timers → stdlib_pump → wait_for_event`, so the resolution fires INSIDE stdlib_pump but the continuation sits in TASK_QUEUE until the NEXT body's microtask drain — yet the header check between iterations sees `inflight=0` AND `PENDING_RESOLUTIONS=0` AND no timers and exits before the next body runs. New `js_microtasks_pending() -> i32` in `crates/perry-runtime/src/promise.rs` exposes `TASK_QUEUE.borrow().is_empty()` to the codegen; `crates/perry-codegen/src/codegen.rs::generate_module` ORs the result into the existing `has_timers || has_callbacks || has_intervals || has_stdlib` header gate. End-to-end: issue's repro (`async function main() { const hash = await argon2.hash("correct-horse-battery-staple"); console.log("hash:", hash); }`) compiles, runs the hash on a tokio worker, and prints a 97-char `$argon2id$v=19$...$...` matching Node byte-for-byte. The same 3-layer fix unblocks `bcrypt.hash` / `bcrypt.compare` and any future perry-ext-* wrapper using `JsPromise::resolve_string` from a blocking-pool worker. Out of scope and tracked separately: `argon2.hash(password, options)` (a separate FFI `js_argon2_hash_options` is still a stub returning 0 in `crates/perry-runtime/src/closure.rs:2123`), and `argon2.hashSync` / `argon2.verifySync` (the dispatch table has no entry for `Sync` variants — they fall through to undefined). Refs #536 (sets the stage for native-binary deploy of @perryts/mysql servers).
- **v0.5.698** — **Closes #592** (`String.prototype.toLocaleLowerCase` / `toLocaleUpperCase` were silently routed through the unknown-method catch-all in `crates/perry-codegen/src/lower_string_method.rs:675-709`, which emits `js_throw_type_error_not_a_function("string", "<prop>")` + `unreachable`. Effect 3.21.2's `effect/src/Cron.ts:846` (`function aliasOrValue(field, aliases?) { return aliases?.[field.toLocaleLowerCase()] ?? Number(field) }`) was the load-bearing site — the throw fired INSIDE an in-flight optional-chain bracket-access lowering, leaving the `iget.sym/iget.str/iget.num/iget.merge` dispatch chain dangling with stale references to receiver temps that were supposed to be defined by the now-collapsed normal path. clang's verifier rejected with `error: use of undefined value '%r32'` and the entire `Cron.ts` `.o` file failed to emit; the link cascaded into 3 missing symbols (`_node_modules_effect_src_Cron_ts__init` + `_perry_fn_..._isCron` + `_perry_fn_..._parse`) and Effect's DoD repro `Effect.runSync(Effect.succeed(42))` blocked at link. Fix: add `toLocaleLowerCase` / `toLocaleUpperCase` to the unary-string-returning-methods arm in `lower_string_method.rs:156` and route both through the existing `js_string_to_lower_case` / `js_string_to_upper_case` runtime fns. The optional `locales` arg permitted by ECMAScript spec is evaluated for side effects but ignored — Perry has no `Intl.*` (a documented categorical gap in CLAUDE.md) and ASCII / non-Turkic strings produce identical output. Parallel update in `crates/perry-codegen/src/lower_call.rs:993-997` adds both names to the `is_string_only_method` list so any-typed-receiver calls (`(s: any).toLocaleLowerCase()`) also dispatch correctly. End-to-end: issue's standalone 2-line repro (`aliasOrValue("hello") → NaN; aliasOrValue("hello", { hello: 5 }) → 5; aliasOrValue("42") → 42`) now matches Node byte-for-byte. Effect end-to-end (#321 DoD repro) gets past the codegen + link gate; runtime then segfaults during Effect's module init — that's a separate blocker filed downstream of this fix. Refs #321 (Effect framework end-to-end compat tracking).
- **v0.5.697** — **Closes #590** (`Object.assign(target, src)` was orphaning Symbol-keyed properties on `target` and breaking `result === target` identity / `target instanceof Class`). Pre-fix the HIR lowering in `crates/perry-hir/src/lower/expr_call.rs::lower_call` folded EVERY `Object.assign(target, ...sources)` shape — including the load-bearing drizzle pattern `Object.assign(rawTable, builtColumns)` followed by `table[Symbol.for("Columns")] = ...` followed by `Object.assign(table, { enableRLS })` — into `Expr::ObjectSpread { parts }` with the target as the first spread part. Codegen for `ObjectSpread` allocates a FRESH `ObjectHeader`, copies own string-keyed fields from each part, and returns the new pointer; this drops three things at once: (a) the `class_id` (the new header is allocated with class_id=0), so `instanceof PgTable` flips to false; (b) the `SYMBOL_PROPERTIES` side-table entries (the table is keyed by raw object pointer — the new header is a different pointer with no entries), so any Symbol-keyed write before the second Object.assign reads back as `undefined`; (c) object identity (`Object.assign` per spec returns target; the fresh-allocation path returns a different pointer, so `result === target` is false). The drizzle-orm `pg-core/table.js:42-53` chain `const table = Object.assign(rawTable, builtColumns); table[Table.Symbol.Columns] = builtColumns; return Object.assign(table, { enableRLS })` saw both Symbol writes (`Table.Symbol.Columns`, `Table.Symbol.ExtraConfigColumns`) silently dropped by the trailing Object.assign, and `dialect.buildInsertQuery({ table })` then SIGSEGV'd in `js_object_entries(undefined)` reading `0x11` as a pointer. Three coordinated layers. **(1) Runtime helper** (`crates/perry-runtime/src/object.rs::js_object_assign_one(target_f64, source_f64) -> f64`): mutates target with each of source's own enumerable string-keyed AND Symbol-keyed properties, returns target unchanged. String-keyed copy mirrors `js_object_copy_own_fields` (walk `src.keys_array`, dispatch through `js_object_set_field_by_name` so setters / dispatch arms still fire). Symbol-keyed copy snapshots `SYMBOL_PROPERTIES.get(src_raw)` via the new `pub(crate)` accessor `crate::symbol::clone_symbol_entries_for_obj_ptr` then iterates the cloned `Vec<(sym_ptr, value_bits)>` outside the lock — the inner `js_object_set_symbol_property` re-acquires the same Mutex, so holding the lock across the iteration would deadlock. Defensive: undefined / null target / source silently no-op (per-rest-of-runtime style; spec says throw TypeError on null target but stays consistent with permissive Object dispatch). **(2) HIR variant** — new `Expr::ObjectAssign { target: Box<Expr>, sources: Vec<Expr> }` in `crates/perry-hir/src/ir.rs:881-888` distinct from `ObjectSpread` precisely because the spec semantics differ (mutate-and-return vs allocate-and-return). Walker arms in `crates/perry-hir/src/walker.rs:533+1647` walk target + each source; matches in `analysis.rs::replace_this_in_expr` and other HIR consumers fall through their `_ => {}` catch-alls (verified via `cargo build --release --workspace`). **(3) HIR lowering** (`crates/perry-hir/src/lower/expr_call.rs:778-820`) — `Object.assign(target, ...sources)` now produces `Expr::ObjectAssign { target, sources }`. Two fast-path preservations: `Object.assign()` (zero args) coerces to `Expr::Object(vec![])`; `Object.assign({}, ...sources)` (empty object literal target) keeps the OLD `ObjectSpread` path because there's no pre-existing identity / class_id / Symbol side-table state to preserve and the downstream Object literal fold is more efficient. **(4) Codegen** (`crates/perry-codegen/src/expr.rs::Expr::ObjectAssign`) — chains `js_object_assign_one(target, src1)` → `js_object_assign_one(_, src2)` → ... so target is evaluated exactly once; the helper's return value is target itself, threaded through each source. Zero-source case (`Object.assign(target)`) returns target directly without any helper call. Runtime decl in `crates/perry-codegen/src/runtime_decls.rs:818-821`: `js_object_assign_one(DOUBLE, DOUBLE) -> DOUBLE`. End-to-end: issue's repro (`new TBase(); t[TBase.Symbol.Columns] = {...}; Object.assign(t, { extraField: 1 }); t[TBase.Symbol.Columns]`) matches bun byte-for-byte; the load-bearing drizzle shape (`const table = Object.assign(rawTable, built); table[Sym] = built; const result = Object.assign(table, { enableRLS: true })`) now satisfies `result === table: true` AND `result[Sym]` returns the columns map (was `undefined`). The class_id side-bug (`Object.assign(t, { extra: 1 }) instanceof PgTable` was false) is fixed in the same pass — target's `class_id` is preserved because we mutate target rather than allocating a fresh header. Edge cases verified: multiple sources with later-wins (`Object.assign(c, {x:1}, {x:2}, {x:3})` → `c.x=3`), null/undefined sources skipped (`Object.assign(d, null, undefined, { y: 2 })` → preserves d), single-arg form (`Object.assign(f) === f`), Symbol-keyed source props copied to target. Refs #488 (drizzle blocker), #590.
- **v0.5.696** — **Closes #589 (runtime path)**: builds on v0.5.694's link-gap fix to make the issue's full acceptance criterion pass — `compilePackages: ["hono"]` + `import { createServer } from "node:http"` + `app.fetch(new Request(...))` + `fetchRes.headers.forEach((v,k) => res.setHeader(k,v))` + `res.end(await fetchRes.text())` now compiles, runs, and `curl localhost:3000` returns hono's JSON response. Three layers, all in code paths perry-stdlib's `fetch.rs` already covers but perry-ext-fetch (the staticlib that v0.5.694 pulled in alongside perry-ext-http when `node:http` strips `http-client`) had drifted from. **(1) ABI signature alignment** — `crates/perry-ext-fetch/src/lib.rs` had five FFI exports declared as `i64` handle when the codegen's runtime_decls.rs:1072-1082 emits DOUBLE: `js_fetch_response_status` / `_status_text` / `_ok` / `_text` / `_json`. The call site put the handle in d0 (floating-point register); the `i64` Rust signature read x0 (general register) and got whatever leftover bits were there — `await fetchRes.text()` rejected with "Invalid response handle" because `id as usize` was garbage from a stale register. All five flipped to `f64` and route through a new `handle_id(value: f64) -> usize` helper that mirrors perry-stdlib's (accepts both NaN-boxed POINTER_TAG handles top-16 ≥ 0x7FF8 and the legacy raw-float form, so callers see the same id regardless of which staticlib produced the handle). **(2) Constructor arity** — `js_response_new` and `js_request_new` were 3-arg (body, status, status_text) while perry-stdlib's are 4-arg with a trailing `headers_handle: f64`. Codegen always emits the 4-arg call (per `runtime_decls.rs:1045 / :1064` and the `lower_call/builtin.rs:381-389` Response path that builds a Headers handle via `build_headers_from_object`); the 4th arg silently dropped, so `new Response(body, { status, headers: { ... } })` produced a Response whose stored headers map was empty. Added the `headers_handle: f64` param to both, and in `js_response_new` resolve it via `handle_id` → `HEADERS_HANDLES.get` → clone the headers map onto the FetchResponse. `js_request_new` accepts the arg as `_headers_handle` for ABI shape (RequestData has no headers field yet — separate followup, doesn't block the acceptance criterion). **(3) HIR: `r.headers.forEach(cb)` was getting array-method-folded** — `crates/perry-hir/src/lower/expr_call.rs::lower_call` walks `Expr::Member(inner)` receivers (line 4096) to decide whether to fold `forEach` / `map` / `filter` etc. into the array fast path. The existing arm only handled `this.<field>.<method>(...)` patterns; for non-`this` member receivers like `r.headers.forEach(cb)` it returned `false` (don't bail), which meant the array-method fold fired and lowered the call to `Expr::ArrayForEach { array: NativeMethodCall { module: "fetch", method: "headers", ... }, callback }`. Codegen then dispatched `js_array_forEach` against a Headers handle (an integer registry id, not an ArrayHeader pointer) — the iteration silently no-op'd (no body fired, no headers propagated to res). Added a parallel arm: when `inner.obj` is an Ident registered as a Response/Request/Headers native instance AND `inner.prop == "headers"`, set `recv_is_class = true` so the array-method catch-all bails. The HIR then leaves the call as a NativeMethodCall and codegen's chained-Web-Fetch dispatch in `crates/perry-codegen/src/lower_call.rs:1313` (the `chain_mod == "fetch" && chain_method == "headers"` arm) routes `.forEach` / `.get` / `.has` / `.keys` / `.values` / `.entries` through the actual Headers FFI. **Bonus** — added `js_headers_keys` / `js_headers_values` / `js_headers_entries` to perry-ext-fetch (perry-stdlib added them in v0.5.665 #576 but ext-fetch didn't track), plus made `js_headers_for_each` sort entries by header name (WHATWG Fetch spec; matches perry-stdlib's `snapshot_sorted` helper). End-to-end: issue's repro byte-for-byte against Node — `curl localhost:3000` returns `HTTP/1.1 200 OK / content-length: 11 / {"ok":true}`. Sanity: `test_node_http_basic.ts` (v0.5.691 fixture, doesn't touch Web Fetch) still routes through perry-ext-http only and serves `/health` → `{"ok":true,"path":"/health"}` byte-identical; `hono_alone.ts` (compilePackages hono, no node:http — exercises perry-stdlib's fetch.rs path) still prints `status: 200 / body: {"ok":true}`. **Out of scope (separate followups)**: hono's `c.json()` chain doesn't propagate `content-type: application/json` to the final response in this repro (curl receives the body and status but no content-type) — the headers Response built via `c.json()`'s nested compose-context-context hop hits a different code path; the issue's acceptance line says "returns hono's JSON response" which the body+status alone satisfy, but the content-type is a clean followup. Refs #421 (compile-e2e), #486 (middleware), #487 (@hono/node-server pattern).
- **v0.5.695** — **Closes #588** (closure body `() => new <ImportedClass>(...)` inside a compilePackages module: link-fail or silent class-id-collapse — `_factory` reads back as `1.26e-321` regardless of what's assigned). Reproduced minimally on real drizzle-orm 0.45.2's `pg-proxy/driver.js`: `function drizzle(callback, config = {}, _factory = () => 42)` called with 1 arg → `_factory` = `f64::from_bits(small_int)` ≈ 1.26e-321 — the default-param desugar `if (_factory === undefined) _factory = ...` never fires because the value isn't TAG_UNDEFINED, it's stale stack/register state. Bisected to: `repro-pkg2/index.js` containing JUST `function drizzle(...) {...}; export { drizzle };` triggers the bug; same code with `export function drizzle(...) {...}` (export-decl form) does not. Root cause in `crates/perry-hir/src/lower.rs::lower_module_decl`: the `ExportNamed` arm at line 4319 (`export { name }` form) handles classes (line 4421-4426 since #482) and closures-assigned-to-const (line 4434-4495) but has NO equivalent for function declarations — the function decl's `is_exported` stays false and it never lands in `module.exported_functions`. The CLI driver in `crates/perry/src/commands/compile.rs` then misses it on BOTH paths: `exported_func_param_counts` at line 1907 filters on `func.is_exported`, and the fallback at line 1921 iterates only `exported_functions`. Importer's `imported_func_param_counts` ends up without `drizzle` → `lower_call.rs:767` falls back to `args.len()` for both target_arity AND the `pending_declares` LLVM signature → declares `f64(f64)` for a function actually defined as `f64(f64,f64,f64)`. ABI mismatch on AArch64: source body reads d1/d2 with whatever stale floats the caller had in those registers (often a class-id raw bit pattern from earlier inline ops, hence the consistent `1.26e-321` shape). Without the param-count plumbing, the desugar and arg-padding logic both go wrong; the bug surfaces as either a link-fail (when the imported class's constructor symbol isn't declared on the consumer side) or as the silent value-collapse (when it is). Fix: parallel of the class arm — when `export { drizzle }` lands and `ctx.lookup_func(&local)` finds a function named `local`, flip `func.is_exported = true` AND push `(exported_name, func_id)` to `module.exported_functions` (deduped against existing entries). Both compile.rs param-count paths now find the function; importers see `imported_func_param_counts.get("drizzle") == Some(3)`; the lower_call.rs declare emits `f64(f64,f64,f64)` and pads missing trailing args with TAG_UNDEFINED; the body's default-param desugar fires and `_factory` gets the closure. End-to-end: minimal repro (`function drizzle(cb, config={}, _factory=()=>42); export { drizzle };` called as `drizzle(cb)`) prints `_factory typeof= function`, `dialect= 42` matching Node. Real drizzle-orm 0.45.2 (`drizzle-orm/pg-proxy/driver.js` with the exact `_dialect = () => new PgDialect({ casing: config.casing })` shape) now correctly invokes the closure and returns a PgDialect instance — `db.dialect.casing` is set, the downstream `new PgRemoteSession` / `new PgRemoteDatabase` chain proceeds. Refs #488 (drizzle compile path); the issue's separately-mentioned link-fail variant (undefined `_node_modules_repro_pkg2_dialect_js__PgDialect_constructor`) was the same root cause surfacing through the closure body — same fix unblocks both. Sanity: test_inheritance / test_simple_class / test_edge_promises / test_edge_scope_hoisting / test_gap_closures / test_gap_class_advanced / test_issue_392_cross_module_method_dispatch all byte-identical to Node; test_fastify_integration / test_node_http_basic still compile + run unchanged.
- **v0.5.694** — **Closes #589** (link gap: `import { Hono } from "hono"` + `import { createServer } from "node:http"` failed at link with `Undefined symbols: _js_headers_new / _js_response_new / _js_request_new`). The well-known flip in `crates/perry/src/commands/compile/optimized_libs.rs::build_optimized_libs` strips perry-stdlib's `http-client` feature when `node:http` (or `https` / `http2`) is imported and routes the import to perry-ext-http. perry-ext-http's surface is `js_http_*` / `js_node_http_*` (HTTP client + server via hyper) — it does NOT export the Web Fetch ctors (`js_headers_new` / `js_response_new` / `js_request_new` / `js_fetch_with_options` / etc.); those live in perry-ext-fetch. With the `http-client` feature stripped from perry-stdlib AND no `'fetch'` / `'node-fetch'` in `native_module_imports` (the user only imported `node:http`), the link line referenced symbols neither staticlib defined. hono's compiled output references those symbols (it does `new Headers()` / `new Response(body, opts)` internally — and the Web Fetch detection in `crates/perry-hir/src/destructuring.rs:1466-1546` flips `ctx.uses_fetch = true` whenever `let r = new Headers()` / `let r = new Request()` / `let r = new Response()` lands in HIR, so the auto-optimize already KNOWS the Web Fetch surface is needed). Fix: when `ctx.uses_fetch == true` AND neither `'fetch'` nor `'node-fetch'` is in the user's `native_module_imports`, synthetically add `"fetch"` to the iteration set the well-known flip walks — perry-ext-fetch then gets pulled into the link line via the existing tokio-using-bindings rebuild path. The `'fetch'` binding strips no perry-stdlib feature (`module_to_features("fetch")` falls through `_ => &[]` per `crates/perry/src/commands/stdlib_features.rs:42`), so it's a pure-add — perry-ext-fetch's symbols ride alongside perry-ext-http's without duplicate-symbol risk (perry-ext-http exports `js_http_*` / `js_node_http_*`; perry-ext-fetch exports `js_headers_*` / `js_response_*` / `js_request_*` / `js_fetch_*` — disjoint sets, verified via `nm` on each `.a`). Repro from the issue (`compilePackages: ["hono"]` + `new Hono(); app.get(...) ; createServer((req, res) => { const fetchReq = new Request(...); const fetchRes = await app.fetch(fetchReq); ... })`) now compiles + links cleanly with `well-known: routing fetch → rebuilding perry-ext-fetch with shared tokio (#507) (#466)` + `well-known: routing http → rebuilding perry-ext-http with shared tokio (#507) (#466)` in the auto-optimize log; binary boots, server binds port, curl reaches the handler. Sanity smoke: existing `test_node_http_basic.ts` (v0.5.691 fixture, no Web Fetch ctors) still routes through perry-ext-http only — `ctx.uses_fetch` stays false, perry-ext-fetch isn't pulled in (1.6 MB binary unchanged); the synthetic add only fires when Web Fetch ctors are detected. **Out of scope (separate runtime issues, NOT link gap)**: the curl-returns-hono-JSON acceptance criterion needs more — perry-ext-fetch's `js_response_new` has a 3-arg signature (`body`, `status`, `status_text`) while perry-stdlib's was extended to 4 (adding `headers_handle`) and codegen emits the 4-arg call shape, so the headers-handle arg silently drops on the floor. `fetchRes.headers.forEach(...)` and `await fetchRes.text()` then hit "Invalid response handle" at runtime. That's a perry-ext-fetch / perry-stdlib divergence + dispatch-table mismatch that needs its own fix; the LINK gap that #589 names is closed.
- **v0.5.693** — **Refs #551** — documentation only. Lists `@bradenmacdonald/s3-lite-client` as a known-working npm package alongside `hono` in `docs/src/packages/porting.md`, and adds a new "AWS S3 / S3-Compatible Object Storage" section to `docs/src/stdlib/http.md` (between WebSocket and Next Steps). Section covers `compilePackages` config, the canonical Put/Get/Head/Delete/List/presign API surface inline, the S3-compatible-endpoint table (AWS / R2 / MinIO / B2 / Spaces / Supabase / LocalStack), and a one-paragraph note on why `@aws-sdk/client-s3` v3 currently can't be compiled (`@smithy/*` middleware uses `Proxy` + dynamic property assignment outside Perry's TypeScript subset). The byte-identical-to-bun verification chain (`crypto.subtle` HMAC-SHA-256, `TextEncoder` packed bytes, typed-array iteration, `URLSearchParams.toString()`, `Headers.keys()` iteration, `extends Error`, namespace imports, class-method object-destructure params, the `str.split().map().sort()` chain, and the `js_string_addref` extern declaration) is the cumulative result of issues #561 / #562 / #567 / #568 / #569 / #572 / #573 / #574 / #575 / #576 / #578 / #579 / #580 / #584 / #587 — 15 general-purpose stdlib fixes, all closed before this docs commit. The example code block stays inline (markdown literal) rather than going through `docs/examples/stdlib/http/snippets.ts` because the doc-tests harness doesn't have `@bradenmacdonald/s3-lite-client` in `node_modules` at compile time; inlining keeps the docs self-contained without requiring a CI-side npm install. No code changes; no manifest / runtime / codegen impact.
- **v0.5.692** — **Closes #585 #586** (drizzle-orm #488 query-side blockers). **#585 (cross-module `instanceof` returns true for unrelated imported classes)** — pre-fix `users instanceof Subquery` returned true unconditionally when both `users.class_id` (Object.assign'd `new PgTable(...)`) and the requested cross-module class id resolved to 0; the runtime's direct-match arm at `crates/perry-runtime/src/object.rs::js_instanceof:4047-4050` matched `0 == 0` as TRUE. Two coordinated changes: (a) early-return false in `js_instanceof` when `class_id == 0` (the codegen sentinel for unresolved class names — see `Expr::InstanceOf` fallback at `crates/perry-codegen/src/expr.rs:5526-5539` that lands here when `ctx.class_ids.get(ty)` misses), so the "unknown class" sentinel can never spuriously match; (b) reserved id `0xFFFF0050` for `Object` in both codegen `Expr::InstanceOf` and a new runtime arm in `js_instanceof` that returns true for any pointer-tagged value (plus Date timestamps + raw-bitcast Buffer/Set/Map/typed-array forms). Without (b), the (a) gate would regress `{} instanceof Object` (which pre-#585 worked accidentally via `0 == 0`) — `test_gap_typeof_instanceof` caught it. Drizzle's `is(value, kind)` brand check now correctly returns false for `users instanceof Subquery` / `PgViewBase` / `SQL`; the from()-builder no longer takes the Subquery branch. Note: the underlying `Object.assign` also-loses-class_id problem (mentioned as fix surface 3 in the issue) is independent and pre-existing — `users instanceof PgTable` still returns false. Drizzle uses the `entityKind` chain walk after the instanceof short-circuit fails, so this fix unblocks the path. **#586 (`await thenable` returns the thenable instead of calling `.then(resolve, reject)`)** — drizzle's `QueryPromise` (`drizzle-orm/query-promise.js`) defers `.execute()` until `.then` is called by the awaiter; pre-fix `await db.select().from(users)` returned the builder object unchanged because Perry's await machinery didn't recognize the thenable protocol. The async-to-generator transform rewrites every `await x` into `Promise.resolve(x).then(continueOnFulfilled, continueOnRejected)`, so the assimilation has to happen in `js_promise_resolved` (NOT just in the codegen-side `Expr::Await` lowering — the latter only fires for un-transformed paths). Three layers: **(1) Runtime probe** (`crates/perry-runtime/src/promise.rs::js_assimilate_thenable`) — new public FFI: takes a NaN-boxed value, returns either the same value (real Promise / non-thenable) or a fresh wrapper Promise that follows the thenable. Walks the `CLASS_VTABLE_REGISTRY` parent chain via the new `crate::object::lookup_class_method_in_chain(class_id, "then")` helper (also exposed in this commit). When `then` is found: allocates a wrapper Promise via `js_promise_new`, builds resolve / reject closures bound to the wrapper using `promise_resolve_fn` / `promise_reject_fn` (the same handlers used by `js_promise_new_with_executor`), invokes `value.then(resolve, reject)` via `std::mem::transmute` matching `call_vtable_method`'s 0/1/2+-param dispatch (NaN-boxes `this` with POINTER_TAG so the method body sees a real instance pointer; passes resolve/reject as raw-bitcast f64 closure pointers matching the existing executor convention). Defensive early returns for non-pointer values, side-table-tracked types (Buffer/Set/Map/Symbol/RegExp/Date), small handles (< 0x100000), and non-`GC_TYPE_OBJECT` heap types. **(2) Promise.resolve hookup** (`crates/perry-runtime/src/promise.rs::js_promise_resolved`) — adds a thenable arm between the existing real-Promise unwrap (issue #77) and the plain-resolve fallback: routes through `js_assimilate_thenable`, and if the result is a Promise (i.e. assimilation actually fired) chains its eventual state into the outer resolved promise via `js_promise_resolve_with_promise` — the same pattern the real-Promise arm uses. **(3) Codegen belt-and-suspenders** (`crates/perry-codegen/src/expr.rs::Expr::Await`) — pipes the operand through `js_assimilate_thenable` BEFORE the existing `js_value_is_promise` branch so non-transformed await paths (e.g. `was_unrolled = true` async functions, simple await patterns) also benefit; the function is a passthrough for real Promises and non-thenable values, so it's a no-op when not needed. End-to-end: issue's repros match Node byte-for-byte — `await new Thenable()` prints `[then] called` then `result = 42`; `users instanceof Subquery: false`. Sanity tests (test_inheritance, test_simple_class, test_edge_promises, test_gap_async_advanced) all pass; parity 220 pass / 3 fail / 13 skip (+1 vs baseline 219, gained `test_gap_typeof_instanceof`). The 5 `test_node_http*` / `test_node_https*` failures are pre-existing port-conflict / TLS issues from v0.5.691 (#577 http server), unrelated to this commit.
- **v0.5.691** — **Closes #577** (Node-compat HTTP server modules — `node:http` + `node:https` + `node:http2` + WebSocket upgrade integration). All four phases of the issue ship end-to-end, verified byte-for-byte with curl + websocat: idiomatic `import { createServer } from "node:http"; const s = createServer((req, res) => { req.method; req.url; res.statusCode = 200; res.setHeader(...); res.end(...) }); s.listen(port);` (Phase 1 + body collection via `req.on('data', cb)/req.on('end', cb)`); `import { createServer } from "node:https"` with `{key, cert}` PEM strings over rustls (Phase 2); `import { createSecureServer } from "node:http2"` with ALPN auto-negotiation between h2 and http/1.1 on the same port (Phase 3); `httpServer.on('upgrade', (req, wsId, head) => { wsId.on('message', cb); wsId.send(msg); wsId.close() })` (Phase 4). New crate **`perry-ext-http-server`** layered on hyper 1.4 + tokio-rustls 0.26 + rustls 0.23 (ring provider) + tokio-tungstenite 0.28; statically linked into `libperry_ext_http.a` via an rlib piggyback so the existing `bindings.http` / `bindings.https` resolutions in `crates/perry/well_known_bindings.toml` (plus a new `bindings.http2`) cover both client and server in one staticlib. **Modules**: `server.rs` (HTTP/1.1 handler-push event loop mirroring perry-ext-fastify's mpsc → main-thread → oneshot dance — handler closure stored as raw `i64` + GC root scanner pinning), `request.rs` (`IncomingMessage` Readable-stream surface; method/url/headers/rawHeaders/httpVersion/complete/aborted/destroyed/socket{remoteAddress,remotePort}/on(data|end|close|error)/read/pause/resume/destroy + synchronous `'data'` + `'end'` emit on listener registration so the canonical Express body-collection pattern resolves through the microtask queue), `response.rs` (`ServerResponse` Writable surface — statusCode get/set, statusMessage, setHeader/getHeader/removeHeader/hasHeader/getHeaders/getHeaderNames, headersSent, writableEnded, writableFinished, writeHead/write/end/flushHeaders, finish/close events; auto Content-Length when `transfer-encoding` unset), `tls.rs` (rustls `ServerConfig` builder accepting PKCS#8 / RSA / EC keys + multi-cert PEM chains; `ensure_crypto_provider_installed()` Once-guard installing `rustls::crypto::ring::default_provider()` so the multi-rustls-version dep graph doesn't panic at first `ServerConfig::builder()`), `https_server.rs` (`https.createServer({ key, cert, alpnProtocols? }, handler)` wrapping each TCP stream in `tokio_rustls::TlsAcceptor`; `parse_https_opts` decodes the opts object via `json_stringify`; ALPN defaults to `http/1.1` only — opt-in to h2 via `alpnProtocols: ["h2", "http/1.1"]`), `http2_server.rs` (`http2.createSecureServer({ key, cert }, handler)` driving `hyper-util::server::conn::auto::Builder` with `TokioExecutor` so HTTP/2 + HTTP/1.1 share the same port; Phase 1's IncomingMessage/ServerResponse model reused as Http2ServerRequest/Http2ServerResponse since each `:path` request becomes a single buffered IncomingMessage), `upgrade.rs` (Phase 4 `is_websocket_upgrade` detector + `fire_upgrade_listeners` dispatch — derives Sec-WebSocket-Accept via `tungstenite::handshake::derive_accept_key`, returns 101 Switching Protocols immediately, spawns awaiter that resolves `hyper::upgrade::on(req)` and hands the upgraded `Upgraded` stream to `tokio_tungstenite::WebSocketStream::from_raw_socket(_, Role::Server, None)` before passing to perry-ext-ws's new `register_external_ws_stream<S>(WebSocketStream<S>) -> i64` helper for unified ws_id namespace with standalone `WebSocketServer({port})` connections). **GC root scanner** walks `HttpServer.handler` + `HttpServer.listeners` + `IncomingMessage.listeners` + `ServerResponse.listeners` so a malloc-triggered sweep between listener registration and dispatch can't free user closures (issue #35 pattern). **Codegen integration**: `crates/perry-codegen/src/runtime_decls.rs::declare_stdlib_ffi` declares all 51 new `js_node_http_*` / `js_node_https_*` / `js_node_http2_*` exports through LLVM (`module.declare_function(name, ret_ty, &[arg_ty…])`); `crates/perry-codegen/src/lower_call.rs::NATIVE_MODULE_TABLE` adds 60+ rows for `(module, has_receiver, method, class_filter)` → runtime fn dispatch covering HttpServer / IncomingMessage / ServerResponse / HttpsServer / Http2SecureServer / ws Client classes — class_filter disambiguates same-named methods (`on` / `addListener` / `close` exist on multiple); `crates/perry-hir/src/lower/expr_member.rs` PropertyGet on a registered IncomingMessage / ServerResponse native instance rewrites to `Expr::NativeMethodCall` with `class_name` preserved + method renamed to `__get_<name>` for the property-getter set (method/url/httpVersion/complete/aborted/destroyed/statusCode/headersSent/writableEnded/writableFinished); `crates/perry-hir/src/lower/expr_assign.rs` parallel rewrite: `res.statusCode = 200` → `Expr::NativeMethodCall { method: "__set_statusCode", args: [200] }`; `crates/perry-hir/src/lower.rs` factory class registration in both export-decl and lower_stmt::Decl::Var paths matches `("http", "createServer") | ("https", "createServer") | ("http2", "createSecureServer")` and registers the binding as `("http", "HttpServer")` / `("https", "HttpsServer")` / `("http2", "Http2SecureServer")`; `crates/perry-hir/src/lower_patterns.rs::pre_scan_node_http_create_server_params` registers the `(req, res)` arrow params as IncomingMessage / ServerResponse native instances BEFORE the body lowers; `crates/perry-hir/src/lower_patterns.rs::pre_scan_node_http_upgrade_params` registers the `wsId` arrow param as `("ws", "Client")` for the upgrade event handler so `wsId.send(...)` / `wsId.on(...)` / `wsId.close()` dispatch through dedicated Client-class entries to the `js_ws_send_client_i64` / `js_ws_on_client_i64` / `js_ws_close_client_i64` shims (`unbox_to_i64` extracts the integer ws_id from the POINTER_TAG-NaN-boxed value the upgrade dispatcher passes in). **API manifest** (`crates/perry-api-manifest/src/entries.rs`) — adds `"http2"` to `NATIVE_MODULES` + 50+ method/class entries for the unimplemented-API gate (#463); the four `manifest_consistency` CI tests in `perry-codegen` (every dispatch entry has manifest counterpart, every well-known binding has manifest entry, etc.) all pass. **`stdlib_features::module_to_features`** flips http2 into the `http-client` umbrella so the auto-optimized stdlib build enables `async-runtime` and `perry_ffi_spawn_blocking_with_reactor` resolves at link time. **Architectural fixes worth flagging**: (a) every accept loop replaced `Handle::current().block_on(async { ... })` with `tokio::spawn(async move { ... })` since perry-stdlib's `spawn_blocking_with_reactor` already runs the closure inside a tokio worker task — block_on there panics with "Cannot start a runtime from within a runtime"; (b) `http2_server.rs` specifically hits a runtime context regression for its dep set (auto::Builder + rustls + tokio-tungstenite all linked) where `Handle::try_current() = Err` inside `spawn_blocking_with_reactor` — workaround uses plain `spawn_blocking + Builder::new_current_thread().enable_all().build() + block_on` (perry-ext-fastify's pattern); (c) `crates/perry-ext-ws/src/lib.rs::drive_server_client_io` switched from `Handle::current().block_on` to `tokio::spawn` so the upgrade-from-http path (which lands the closure already inside a tokio runtime task) doesn't panic; (d) `crates/perry-ext-ws/src/lib.rs::js_ws_on_client_i64` drains queued `c.messages` on listener registration to fix the race between IO loop reading the first frame and TS-side `wsId.on('message', cb)` running. **Smoke fixtures** (`test-files/test_node_http_basic.ts` / `test_node_http_post_body.ts` / `test_node_https_basic.ts` / `test_node_http2_basic.ts` / `test_node_http_ws_upgrade.ts`) all compile + run, all five round-trip byte-for-byte against curl / websocat. **Docs** (`docs/src/stdlib/http.md` + `docs/examples/stdlib/http/snippets.ts`) — five new ANCHOR'd snippets cover createServer / createServer over TLS / createSecureServer / upgrade-with-ws — all compile through the doc-tests harness. The four `manifest_consistency` tests pass; sanity tests (test_inheritance, test_simple_class, test_break_continue, test_fastify_integration) compile + run unchanged. Closes #487 as a side effect (the `@hono/node-server` style of program — createServer + handler + listen + request/response surface — works on the HTTP/1.1 path).
- **v0.5.684** — **Closes #583** (deep links: Universal Links / App Links / URL schemes). New `appOnOpenUrl(cb)` + `appGetLaunchUrl()` on `perry/system` plus a build-time `package.json` `perry.deepLinks` config (`{ schemes: [...], universalLinks: { ios: [...], android: [...] } }`) that emits the platform manifest entries. Six coordinated layers. **(1) Stdlib API surface** — `crates/perry-dispatch/src/lib.rs` adds `appOnOpenUrl` (Closure → Void) + `appGetLaunchUrl` (() → Str) rows to `PERRY_SYSTEM_TABLE`; `crates/perry-api-manifest/src/entries.rs` parallel manifest entries; `crates/perry/src/commands/compile/collect_modules.rs` adds both names to `triggers_ui` so importing them flips `needs_ui = true` and the platform UI staticlib gets linked; `types/perry/system/index.d.ts` exports both with `source: "cold-start" | "foreground"` typing. **(2) iOS runtime** (`crates/perry-ui-ios/src/deeplinks.rs` + `app.rs` wiring) — single-handler registry with cold-start replay (a URL captured before the JS handler registers is held in `PENDING_COLD_START` and fired synchronously when `appOnOpenUrl` arrives — registering at module-load is enough; no separate `appGetLaunchUrl` read is required for the cold-start flow). PerryAppDelegate gets `application:openURL:options:` (custom-scheme legacy) + `application:continueUserActivity:restorationHandler:` (Universal Link delivery while running) methods; PerrySceneDelegate gets `scene:openURLContexts:` + `scene:continueUserActivity:` selectors registered dynamically via `class_addMethod`; `scene:willConnectToSession:options:` drains the connection options' `URLContexts` (custom-scheme cold start) AND `userActivities` (Universal Link cold start) into the same dispatch path. ObjC bridge helpers (`nsurl_to_string`, `dispatch_scene_open_url_contexts`, `dispatch_scene_connection_options`, `dispatch_continue_user_activity`, `dispatch_app_open_url`) extract NSURL `absoluteString` values and feed `dispatch_cold_start` / `dispatch_foreground`. **(3) macOS runtime** (`crates/perry-ui-macos/src/deeplinks.rs` + `app.rs` wiring) — same handler-registry shape; AppDelegate gets `application:openURLs:` (modern AppKit URL surface, fires for both schemes and Universal Links) + the legacy `kAEGetURL` Apple Event handler (some launchers — older Spotlight, AppleScript `open location` — deliver schemes through this path instead of the AppKit method). The Apple Event surface is wired in `app_run` after `setDelegate`, calling `NSAppleEventManager.sharedAppleEventManager` and `setEventHandler:andSelector:forEventClass:andEventID:` with class+ID = `'GURL'`. `applicationDidFinishLaunching:` flips an `APP_LAUNCHED` flag so URL deliveries before launch get `source = "cold-start"` and after get `source = "foreground"`. **(4) Android runtime** (`crates/perry-ui-android/src/deeplinks.rs` + `callback.rs` JNI entrypoint + Kotlin bridge changes) — Rust side calls `PerryBridge.appOnOpenUrl(callbackKey)` / `PerryBridge.appGetLaunchUrl()` via JNI; the Kotlin side owns the launch URL (extracted from `intent.data?.toString()` in `PerryActivity.onCreate`) and the foreground delivery surface (from a new `onNewIntent` override that calls `setIntent(intent)` first, then `PerryBridge.onDeepLinkForeground(url)`). New `nativeInvokeDeepLinkCallback(key, url, source)` JNI entrypoint in `crates/perry-ui-android/src/callback.rs` decodes both Java strings, NaN-boxes them, and dispatches to the registered Perry closure via `invoke2`. Single-handler model matches iOS / macOS — `appOnOpenUrl` replaces the previous handler key; cold-start URL is held in `pendingColdStartUrl` until the first `appOnOpenUrl` registers, at which point it's drained synchronously through `nativeInvokeDeepLinkCallback`. **(5) Stubs** for tvOS / visionOS / watchOS / GTK4 / Windows (`crates/perry-ui-{tvos,visionos,watchos,gtk4,windows}/src/deeplinks_stub.rs`) — register the handler so user code that calls `appOnOpenUrl` doesn't crash, but never invoke it; `appGetLaunchUrl` returns `""`. Apps targeting those platforms should branch on `getDeviceIdiom()` if deep linking is load-bearing. **(6) Build-time config** — new `inject_ios_deeplinks` helper in `crates/perry/src/commands/compile.rs` walks up to find `package.json`, reads `perry.deepLinks`, and (a) appends a `CFBundleURLTypes` array to the generated Info.plist (one `<dict>` per scheme with `CFBundleURLName` = `<bundle-id>.<scheme>` and `CFBundleURLSchemes` = `[<scheme>]`); (b) writes a sidecar `app.entitlements` file at the bundle root with the `com.apple.developer.associated-domains` entitlement (`applinks:<host>` per host under `universalLinks.ios`). The signing pipeline picks the entitlements up via `codesign --entitlements app.entitlements`. Parallel `inject_android_deeplinks` in `crates/perry/src/commands/run.rs` runs after the Android template is materialized in `build_and_run_android`: rewrites `AndroidManifest.xml` to add (a) `android:launchMode="singleTop"` to the PerryActivity tag (so foreground URL deliveries hit `onNewIntent` instead of relaunching the Activity), (b) one `<intent-filter android:autoVerify="true">` per `universalLinks.android` host (`android:scheme="https"` + `android:host="<host>"` + BROWSABLE category for App Links auto-verification), and (c) one `<intent-filter>` per `schemes[*]` (`android:scheme="<scheme>"` + BROWSABLE category for custom-scheme delivery). End-to-end: `examples/issue_583_demo.ts` compiles + links + runs on macOS host (0.9 MB binary, prints `[startup] launchUrl=""` + `[startup] handler installed; tap a deep link to test...` and exits cleanly). On a real iPhone tapping `perryapp://chat/123` from Mail, on a real Android tapping the same from Gmail, and on a Mac with the AASA file hosted at `https://example.com/.well-known/apple-app-site-association` clicking a Universal Link — all three route through the same `appOnOpenUrl` callback with the right `source` label. **Two server-side files YOU still own** (called out in the issue and the .d.ts doc block): `https://yourdomain.com/.well-known/apple-app-site-association` (Apple App Site Association — JSON, signed via team ID + bundle ID) and `https://yourdomain.com/.well-known/assetlinks.json` (Android Asset Links — JSON, lists the SHA-256 fingerprint of your APK signing certificate). Without those files on the host, iOS / Android refuse to associate the domain with the app and the link falls through to the browser; with them, `appOnOpenUrl` fires. **Out of scope** and tracked as follow-ups: Windows toast-activation deep-link surface (Windows 11 has its own URI activation pipeline distinct from the AppKit / UIKit / Activity model and warrants its own design pass — current behavior is a clean stub); deep-link source `"push"` (URL embedded in a notification payload — overlaps with `notificationOnTap` and would need a payload-shape contract that's not yet specified); `URL` object variant of the callback signature (the issue's "Proposed" shape — current API hands you `url: string` so user code can `new URL(url)` itself, simpler at the FFI boundary and avoids a synthetic URL ctor call from native); a `--target macos` Info.plist mutation (macOS apps don't go through the same `compile.rs` Info.plist path — the macOS bundle today reads `[macos.info_plist]` from `perry.toml`, so until the deepLinks config is also threaded through there, macOS users add their `CFBundleURLTypes` manually).
- **v0.5.690** — **Closes #584** (`new TextEncoder().encode(s)` Uint8Array had wrong backing bytes — every byte read returned the IEEE-754 representation of the byte value, not the byte itself). Repro: `new TextEncoder().encode("/test/hello"); bytes[0..3] = 0 0 0 0` (Node: `47 116 101 115`). The bytes `[0,0,0,0,0,128,71,64]` at the start of the for-of dump are the LE f64 of `47.0` — `js_text_encoder_encode_llvm` was allocating an `ArrayHeader` with one f64-per-byte and registering it in `BUFFER_REGISTRY`; after #578's typed-array iteration landed, the for-of and indexed paths read those f64 elements as packed u8 (the spec-correct shape for a real Uint8Array), yielding garbage. Pre-#578 the bug was hidden because the indexed path also did f64-stride reads, which matched the f64-per-byte storage by accident. Three coordinated layers: **(1) Runtime** (`crates/perry-runtime/src/text.rs`) — `js_text_encoder_encode_llvm` now allocates a `BufferHeader` via `buffer_alloc(len)`, copies the UTF-8 bytes via `ptr::copy_nonoverlapping` to `buffer_data_mut(buf)`, and calls `mark_as_uint8array(buf)` so `instanceof Uint8Array` still holds. The shape is now identical to `new Uint8Array([47, 116, ...])`. The corresponding decoder side (`js_text_decoder_decode_llvm`) drops the dual-path `is_text_encoder_result` side-table check; both TextEncoder output and `new Uint8Array([...])` share the same packed-u8 BufferHeader layout so a single read path covers both. The thread-local `TEXT_ENCODER_RESULTS` registry + `remember_text_encoder_result` / `is_text_encoder_result` helpers go away entirely. **(2) HIR type inference** (`crates/perry-hir/src/lower_types.rs::infer_call_return_type`) — adds `("TextEncoder", "encode") → Type::Named("Uint8Array")` and `("TextDecoder", "decode") → Type::String` arms in the `Type::Named(class_name)` block. Pre-fix the receiver typed as `Type::Named("TextEncoder")` fell through `lookup_class_method_return_type` (not a user class) all the way to `Type::Any`, so `const bytes = new TextEncoder().encode(s)` had `bytes: Any` in `ctx.locals`. With `bytes: Uint8Array`, the `expr_member.rs:608` arm fires and lowers `bytes[i]` to `Expr::Uint8ArrayGet`, which the codegen emits as a u8-load at `getelementptr inbounds i8, ptr %base, i32 %idx` — one byte per index, correct semantics. Without the type refinement my runtime change alone left `bytes[0..3] = 3.13e+180 ...` because the index path still went through `js_dyn_index_get`'s f64-stride reader (8 packed bytes treated as one f64). **(3) Codegen type refinement** (`crates/perry-codegen/src/type_analysis.rs::refine_type_from_init`) — flips the `Expr::TextEncoderEncode(_)` arm from `HirType::Array(Box::new(HirType::Number))` to `HirType::Named("Uint8Array")` so codegen-side type refinement also produces the right shape (the HIR-side `infer_call_return_type` only fires for AST-level Call expressions; once the call has been lowered to the dedicated `Expr::TextEncoderEncode` HIR variant, the codegen-side refinement takes over). **(4) WebCrypto follow-up** (`crates/perry-stdlib/src/webcrypto.rs::bytes_from_jsvalue`) — drops the `is_text_encoder_result(raw) → unpack-as-f64-elements` branch added for #561; the standard `is_registered_buffer(raw) → packed-u8 BufferHeader` arm now handles TextEncoder output directly. SHA-256("abc") still returns `ba7816bf...` (NIST vector); the SigV4 chain test still produces `c4afb1cc5771...` matching Node byte-for-byte. End-to-end: issue's repro (`new TextEncoder().encode("/test/hello")` → `bytes[0..3] = 47 116 101 115`, for-of yields `[47, 116, 101, 115, 116, 47, 104, 101, 108, 108, 111]`) matches Node byte-for-byte. Multi-byte UTF-8 (`encode("héllo")` → `[104, 195, 169, ...]`), empty string (`encode("")` → 0-length), and `decoder.decode(encoder.encode(s)) === s` round-trip all pass. Pre-existing `test_gap_encoding_timers.ts` (`encoder.encode("hello")` → `[104, 101, 108, 108, 111]`) still passes — the test's assertions match the now-correct byte values directly. Refs #551 (s3-lite-client `awsUriEncode` SigV4 path canonicalization, the load-bearing user impact).
- **v0.5.677** — **Closes #582** (network reachability — online/offline + change events). Already shipped via the v0.5.676 omnibus + the follow-up commit `627cc9f9` that wired the missing dispatch / manifest / collect_modules / .d.ts surfaces and the GTK4 / visionOS / watchOS / Windows stubs.
- **v0.5.676** — **Refs #577** (HIR/codegen integration: idiomatic `import { createServer } from "node:http"` + `server.listen(...)` + handler closure with `req.method()` / `res.end(...)` now compiles + runs end-to-end). v0.5.669 landed the Rust crate `perry-ext-http-server` (HTTP/1.1 server + HTTPS + HTTP/2 + WS upgrade) but only exposed the FFI surface; user TS code still needed raw `declare function js_node_*` extern calls. This commit wires the dispatch path so the canonical Node-compat shape works directly. Six coordinated changes: **(1) NATIVE_MODULE_TABLE** (`crates/perry-codegen/src/lower_call.rs`) — 50+ rows mapping `(module, has_receiver, method, class_filter)` to runtime fns. Class filters disambiguate same-named methods across HttpServer / IncomingMessage / ServerResponse (`on` / `addListener` / `close` exist on multiple classes). Method-call aliases for property-style accessors (`req.method()` / `req.url()` / `res.setStatus(N)`) cover the common cases until full HIR-level PropertyGet→`__get_<name>` rewrite lands; the `__get_*` / `__set_*` synthetic-method shapes are also registered for the future rewrite. **(2) runtime_decls.rs** — 51 `module.declare_function(...)` calls for every `js_node_http_*` / `js_node_https_*` / `js_node_http2_*` exported FFI so LLVM IR can emit calls without "implicit declaration" errors and the linker resolves against `libperry_ext_http.a`. **(3) HIR namespace lowering** (`crates/perry-hir/src/lower.rs`) — adds `("http", "createServer")` / `("https", "createServer")` / `("http2", "createSecureServer")` arms to BOTH the export-side factory class table (already-exported `export const server = http.createServer(...)` ... resolves through `register_native_instance` to HttpServer) AND a new arm in the non-exported `lower_stmt::Decl::Var` factory detection that runs against the LOWERED `Stmt::Let { init: Some(Expr::NativeMethodCall { module, method, object: None, ... }) }` shape — matches both the `import { createServer } from "node:http"` (named import → `Expr::NativeMethodCall { module: "http", method: "createServer", object: None }`) and the `import * as http from "node:http"; http.createServer(...)` (namespace member → same lowered shape) call sites. Registers the binding in both `native_instances` (function-scoped) and `module_native_instances` (survives scope exits). **(4) Handler-param pre-scan** (`crates/perry-hir/src/lower_patterns.rs::pre_scan_node_http_create_server_params`) — issue #577 mirror of `pre_scan_fastify_handler_params`. When `http.createServer((req, res) => ...)` is detected, registers `req` as `("http", "IncomingMessage")` and `res` as `("http", "ServerResponse")` BEFORE the arrow body lowers, so `req.method()` / `res.end(...)` inside the handler dispatch through NATIVE_MODULE_TABLE with the correct class filter. **(5) Manifest entries** (`crates/perry-api-manifest/src/entries.rs`) — adds `"http2"` to `NATIVE_MODULES` (the resolution allowlist) and 47 method/class entries to `API_MANIFEST` so the unimplemented-API gate (#463) sees them; the four `manifest_consistency` CI tests in `perry-codegen` (every dispatch entry has manifest counterpart, every well-known binding has manifest entry, etc.) all pass. **(6) Architectural fix in the Rust accept loop**: `spawn_blocking_with_reactor` actually spawns its closure on a tokio worker (perry-stdlib's shim wraps it in `runtime().spawn(async { invoke(...) })`), NOT on a blocking-pool thread — calling `Handle::current().block_on(fut)` from inside that closure panics with "Cannot start a runtime from within a runtime." Replaced the `Handle::current().block_on(async { ... })` wrapper in `server.rs` / `https_server.rs` / `http2_server.rs` with a direct `tokio::spawn(async move { ... })` so the accept loop runs as a sibling task on the existing runtime. **End-to-end smoke** (`test-files/test_node_http_basic.ts`): idiomatic `import { createServer } from "node:http"; const server = createServer((req, res) => { ... }); server.listen(18877);` compiles + binds + answers `curl /health` (200 JSON) / `curl POST /echo-method` (200 text/plain "POST") / `curl /missing` (404 text/plain "not found") byte-for-byte. **Followup work tracked in #577**: HIR-level PropertyGet→`__get_<name>` rewrite so user code can write `req.method` / `res.statusCode = 200` instead of the method-call aliases; `@hono/node-server` / Express / Koa / Polka end-to-end smoke tests (the Phase 4 WS upgrade path is also wired in this commit but not yet exercised by a TS-level test). Sanity tests test_inheritance, test_simple_class, test_break_continue, test_fastify_integration all compile + run unchanged. The four `manifest_consistency` tests pass.
- **v0.5.675** — **Closes #536** (`@perryts/mysql` hangs after `pool.end()` because `try { return await ... } finally { Y }` skipped Y entirely). The headline `js_box_get: invalid box pointer 0x0 / TypeError: (number).next is not a function` from the v0.5.503-era issue body had silently been fixed by intervening box-pointer / closure-capture landings (notably v0.5.658 + v0.5.663), but the deeper bug — `try { return X } finally { Y }` losing `Y` — was masking a connection leak in `@perryts/mysql`'s `Pool.query` (`pool.ts:51`: `try { return await conn.query(...); } finally { this.release(conn); }` — `this.release(conn)` never ran, the conn never reached `this.idle`, `pool.end()` saw zero idle entries and the underlying tokio socket task in `perry-ext-net` stayed parked, holding the event loop open forever). Two layers in the codegen pipeline both lose the finally body around an abrupt completion: `lower_try` in `crates/perry-codegen/src/stmt.rs` only emitted `br finally_label` when the try-body block wasn't already terminated (a `return` / `break` / `continue` terminates first → the branch is never emitted, finally is unreachable); the async-to-generator transform in `crates/perry-transform/src/generator.rs:1820-1885` linearizes the body and pushes the finally clause AFTER it as plain stmts — but the body's last state ends at the abrupt completion and the appended finally is dead. **Fix** (single HIR-level pass that handles both layers): new `crates/perry-transform/src/finally_inline.rs` runs as `inline_finally_into_returns` BEFORE `transform_async_to_generator` in `crates/perry/src/commands/compile/collect_modules.rs:649`. Walks every Function / class method / constructor / getter / setter / closure body. For each `Stmt::Try { body, catch, finally: Some(f) }`, every nested abrupt completion (`Stmt::Return(Some(X))` / `Stmt::Return(None)` / `Stmt::Break` / `Stmt::Continue` / `Stmt::LabeledBreak(L)` / `Stmt::LabeledContinue(L)`) inside body or catch is rewritten to inline a clone of `f` (and every outer enclosing finally that the abrupt completion would skip past, innermost→outermost matching ECMAScript abrupt-completion ordering) immediately before the abrupt-completion stmt. The `Return(Some(X))` arm hoists `X` into a fresh `let __finally_ret_<id> = X` BEFORE inlining the finally — non-negotiable for `return await foo()` (issue's load-bearing shape) because without the hoist, `f` would run BEFORE the await suspends and the async transform's state-machine boundary would resolve the await with an already-spent finally. `break`/`continue` only inline finallies for trys INSIDE the loop being escaped — the pass tracks `loop_depth_at_push` per `EnclosingFinally` and a label→loop-depth map for `LabeledBreak L` / `LabeledContinue L`, so an outer try wrapping the loop keeps running through the existing setjmp/longjmp throw path and only inner trys-inside-loop get inlined. Closures inside expressions are walked via `perry_hir::walker::walk_expr_children_mut` so a closure body with its own try-with-finally-and-abrupt-completion gets the same treatment scoped to its own enclosing-finally stack (closures are fresh function frames; outer enclosing finallies don't apply across the closure boundary). The original `Stmt::Try { finally: Some(f) }` is preserved unchanged so non-abrupt-completion paths still run finally and the throw path keeps using `lower_try`'s setjmp/longjmp lowering of the catch + finally. **String-aliasing follow-up**: the `let __finally_ret_<id> = X` hoist exposed a latent in-place-mutation aliasing bug — when `X` is `LocalGet` of a string-typed local, the refcount stayed at 1 because Perry never called `js_string_addref` on `let y = x`, then a subsequent `x = x + suffix` took `js_string_append`'s in-place fast path and mutated the buffer that `__finally_ret_<id>` still pointed at. `test_edge_error_handling`'s `finallyReturn` (`try { return result } finally { result += "-finally" }`) returned `start-try-finally` instead of `start-try`. Fix: `Stmt::Let` in `crates/perry-codegen/src/stmt.rs` now emits `js_string_addref` on the loaded value when init is `Expr::LocalGet(src_id)` and `local_types[src_id] == String` — marks the underlying StringHeader as shared (refcount=0) so the next append allocates fresh. Declared the FFI in `crates/perry-codegen/src/runtime_decls.rs`. End-to-end: original issue repro (`new Pool({url:...}); await pool.query("SELECT 1 AS n"); console.log(rows); await pool.end()`) compiles, runs the query, prints `{ rows: [{ n: 1 }], ... }`, exits 0 (was: hung). Realistic flow (sequential + transaction + concurrent Promise.all + pool.end) all clean. Sync `try { return inner() } finally { ... }` ordering matches Node byte-for-byte (pre-fix sync also ran finally BEFORE the return-expression evaluation). Break/continue cases verified: nested labeled break, continue outer skipping iterations, plain break only escaping inner loop, all match Node. **Limitations**: if the inlined finally itself throws, the throw routes to the same try's catch (if any) instead of propagating directly per spec — rare; matters only when finally throws AND there's a same-level catch. Parity 219 pass / 3 fail / 13 skip (98.6%, same 3 pre-existing fails — test_edge_destructuring, test_edge_objects_records, test_gap_object_methods). Refs #551 (s3-lite-client SigV4 cleanup-then-return positions also unblock).
- **v0.5.674** — **Refs #552** (mobile stdlib gaps for production apps). Geolocation + photo-library picker had landed in v0.5.631-632; the remaining gap was image compression on Android — `crates/perry-ui-android/src/stdlib_stubs.rs:1407-1461` had every `js_sharp_*` returning `0`, so `sharp(path).resize(w,h).jpeg(q).toFile(out)` silently no-op'd at runtime under `--target android`. (iOS and macOS already work because `libperry_stdlib.a` cross-compiles cleanly to those targets and link.rs's standard runtime+stdlib path covers them.) Three coordinated changes: (a) `crates/perry-ui-android/Cargo.toml` adds `perry-ext-sharp` as a path dep — clean staticlib that depends only on `perry-ffi` + the `image` crate (no OpenSSL, cross-compiles cleanly to `aarch64-linux-android`). (b) `crates/perry-ui-android/src/lib.rs` adds `extern crate perry_ext_sharp;` so the rlib forces `libperry_ui_android.a` to bundle the `js_sharp_*` symbols (without it the rlib dep would be optimized out and the stubs would mask sharp at link time). (c) `crates/perry-ui-android/src/stdlib_stubs.rs` deletes the 14 conflicting `js_sharp_*` no-op stubs (11 superseded by ext-sharp's real impls; the 3 unimplemented FFIs `negate`/`quality`/`to_format` stay in `perry-runtime/src/closure.rs` where they always lived). Stale "Perry-stdlib can't be cross-compiled for Android (OpenSSL dependency)" header comment also rewritten — `cargo build --release --target aarch64-linux-android -p perry-stdlib` succeeds today, but the architecture choice (smaller staticlib via stub overrides) is still intentional. End-to-end: `llvm-nm libperry_ui_android.a | grep "T js_sharp_"` shows all 18 ext-sharp symbols + the 3 runtime-stub symbols, single definition each (no duplicate-symbol warnings). `types/perry/system/index.d.ts` geolocation block now spells out the required Info.plist (`NSLocationWhenInUseUsageDescription`) and AndroidManifest.xml (`ACCESS_FINE_LOCATION` / `ACCESS_COARSE_LOCATION`) entries verbatim — the OS denies the permission silently when those are missing. New `examples/issue_552_demo.ts` exercises the full 4-step acceptance flow (location permission → getCurrent → pick 2 photos → quality-search compress under 500 KB → fetch upload); compiles + links on host (5.3 MB binary). Out of scope for this commit and noted in the issue: physical-device run on iPhone+Android (no hardware in CI), and the unrelated `js_sharp_quality` / `js_sharp_to_format` runtime FFIs which neither perry-stdlib nor perry-ext-sharp implement (Perry users today configure quality via the `.jpeg(quality)` arg instead — wired correctly through both impls). Closes 3 of 5 acceptance items; Refs #552.
- **v0.5.668** — **Closes #578 #579** (s3-lite-client SigV4 byte-handling blockers from #551). **#578 (`for…of` / `Array.from` / spread on typed arrays yield raw NaN-box bits)** — four layers: (a) `crates/perry-runtime/src/typedarray.rs` adds `typed_array_to_array(*const TypedArrayHeader) -> *mut ArrayHeader` that materializes each element through the per-kind `load_at` (so `Uint8Array([10,20,30,40])` becomes `[10.0, 20.0, 30.0, 40.0]` not four byte-pattern reinterpretations). (b) `crates/perry-runtime/src/buffer.rs` adds the parallel `buffer_to_array` for the legacy Uint8Array-as-BufferHeader path (the load-bearing one — `new Uint8Array([…])` currently allocates BufferHeader, not TypedArrayHeader). (c) `js_array_clone` (Array.from) and `js_array_concat` (spread `[...ta]` and `arr.concat(ta)`) in `crates/perry-runtime/src/array.rs` get parallel typed-array + buffer detection arms — mirroring the Set / Map arms — that delegate to those materializers. (d) `crates/perry-hir/src/lower.rs` + `crates/perry-hir/src/lower_decl.rs` for-of lowering recognizes typed-array iterable types (Uint8Array / Int8Array / Uint8ClampedArray / Uint16Array / Int16Array / Uint32Array / Int32Array / Float32Array / Float64Array) and wraps the iterable expression in `Expr::ArrayFrom` so the synthesized `for(let i=0; i<__arr.length; ++i) __item = __arr[i]` index-based loop runs against a real Array (the inline IndexGet fast path at `crates/perry-codegen/src/expr.rs:2231` reads f64s straight off `arr+8` and would otherwise reinterpret the byte-packed storage). Issue's repro `for (const b of new Uint8Array([10,20,30,40])) collected.push(b)` and `Array.from(uint8arr)` and `[...uint8arr]` all now produce `[10, 20, 30, 40]` matching bun byte-for-byte. Edge cases verified: every typed-array kind (Uint16/Int32/Float32/Float64), spread inside an array literal with surrounding elements (`[0, ...new Uint8Array([1,2,3]), 99]`), spread in function call (`Math.max(...new Uint8Array([1,2,3]))`), `Array.from(ta, mapper)`, `new Set(uint8array)`, and function-body for-of all match. **#579 (`new Uint8Array(arrayBuffer)` does not alias)** — pre-fix `new ArrayBuffer(n)` had no constructor handler and fell through to the empty-ObjectHeader placeholder, then `new Uint8Array(ab)` ran `js_uint8array_new` against an unrecognized pointer and silently produced a 1-byte buffer (no aliasing). Three layers: (a) new `js_array_buffer_new(size: i32) -> *mut BufferHeader` in `crates/perry-runtime/src/buffer.rs` allocates a real zero-filled BufferHeader and registers it in a new `ARRAY_BUFFER_REGISTRY` thread-local so the alias decision survives across subsequent `mark_as_uint8array` calls. (b) `crates/perry-codegen/src/runtime_decls.rs` declares the new FFI; `crates/perry-codegen/src/lower_call/builtin.rs` adds the `"ArrayBuffer"` arm in `lower_builtin_new` that fptosi's the size arg to i32 and routes through `js_array_buffer_new`. (c) `js_uint8array_new`'s POINTER_TAG arm is split into two paths: `is_array_buffer(raw)` → ALIAS the storage (return the same pointer, mark as Uint8Array — every subsequent `new Uint8Array(ab)` view of the same ArrayBuffer shares the same BufferHeader so writes through one view are visible through every other), else → COPY (ECMAScript spec: `new Uint8Array(otherUint8Array)` creates a fresh storage region — preserves pre-existing #227 memcpy behavior). The dedicated `ARRAY_BUFFER_REGISTRY` (rather than reusing `is_uint8array_buffer`) is non-negotiable for the multi-view case — without it, the first view's `mark_as_uint8array` would make the second `js_uint8array_new` call mistake the source for a Uint8Array and incorrectly fall into the COPY branch. End-to-end: issue's repro (`v1[i] = …; new Uint8Array(ab) → reads what v1 wrote`) matches bun, multi-view (`a, b, c` all aliasing `ab` with disjoint writes) matches bun, AWS SigV4 use case (`new Uint8Array(await crypto.subtle.sign("HMAC", key, data))` → hex-encode → matches bun byte-for-byte). Out of scope for this fix and tracked separately: partial views (`new Uint8Array(ab, byteOffset, length)`), `new DataView(ab)` shared storage, and aliasing for `Uint16Array`/`Float32Array`/etc. on top of an ArrayBuffer (Perry's TypedArrayHeader currently has its own data region, not a view into a shared buffer).
- **v0.5.665** — **Closes #575 #576** (s3-lite-client SigV4 blockers from #551). **#575 (`URLSearchParams.toString()` returns `"[object Object]"`)** — two coordinated fixes. (a) Constructor: object-literal init (`new URLSearchParams({a:"1",b:"2"})`) routed every input through `js_url_search_params_new(*mut StringHeader)` and codegen pre-converted via `js_get_string_pointer_unified`, which on a NaN-boxed object pointer reinterpreted the pointer bits as a `*mut StringHeader` and read garbage (typed-local repro printed `"%00="`). New `js_url_search_params_new_any(init: f64) -> *mut ObjectHeader` in `crates/perry-runtime/src/url.rs` decodes at runtime: `undefined`/`null` → empty, STRING/SHORT_STRING tag → `parse_query_string`, POINTER tag → enumerate as record (`read_record_entries` walks the `keys_array` and stringifies each field via `stringify_field_value`), with a try-as-URLSearchParams shape probe (`try_read_as_search_params`) that detects the `_entries` keys-array and copies entries — covers `new URLSearchParams(orig)` copy-construct. Codegen `Expr::UrlSearchParamsNew` in `crates/perry-codegen/src/expr.rs:10167` now passes the lowered init f64 directly instead of stringifying it. (b) Chained `new URLSearchParams(init).<method>(args)` without an intermediate let binding: every shape (including string init) printed `"[object Object]"` because the typed-local arm in `crates/perry-hir/src/lower/expr_call.rs:3373` only fires for `LocalGet`-shaped receivers; the chain fell through to a generic `Object.prototype.toString`. Extracted `build_url_search_params_method_call(recv, method, args)` helper and added an AST chain detect — when `member.obj` is `ast::Expr::New { callee: URLSearchParams }`, lower the receiver via `lower_expr` and dispatch through the helper, mirroring the typed-local arm. Both call sites now share identical method dispatch. Bonus: new `Expr::UrlSearchParamsEntries(Box<Expr>)` HIR variant + `js_url_search_params_entries_arr` runtime FFI lets `for (const [k, v] of params)` iterate via the standard array path (the for-of detection in `crates/perry-hir/src/lower.rs` recognizes a URLSearchParams local or `new URLSearchParams(...)` and wraps the receiver in the new variant). End-to-end repros from the issue (`""` / `"a=1&b=2"` / `"a=1"` / iterate / has / delete / append / copy-construct / `+` for space) all match bun byte-for-byte. **#576 (`for…of headers.keys()` returns empty)** — `module=="Headers"` dispatch in `crates/perry-codegen/src/lower_call.rs:3742` only handled `set/get/has/delete/append/forEach`; `keys/values/entries` returned `Ok(None)` and the call fell through to a generic `js_native_call_method` fallback that returned an empty array on a Headers handle (handle bits don't look like an object/array → silent `[]`). Added three new runtime FFIs in `crates/perry-stdlib/src/fetch.rs` (`js_headers_keys`/`_values`/`_entries`) returning NaN-boxed POINTER_TAG f64 to ArrayHeader of strings (entries returns nested `[k, v]` pair arrays) plus a shared `snapshot_sorted` helper that sorts entries by key (WHATWG Fetch spec: iteration order is sorted lexicographically by header name, regardless of insertion order — bun follows the spec; pre-fix Perry's `forEach` was insertion-order). `forEach` now also routes through `snapshot_sorted` so all five surfaces (forEach / keys / values / entries / direct iteration) agree byte-for-byte. New for-of detect in `crates/perry-hir/src/lower.rs` (mirroring the new URLSearchParams arm): when `right` is an Ident with `lookup_native_instance(...) == Some((_, "Headers"))`, wrap the lowered receiver in `Expr::NativeMethodCall { module: "Headers", method: "entries", ... }` so `for (const [k, v] of h)` direct iteration works too. End-to-end: issue's repro (`for…of` / `forEach` / `Array.from(h.keys())` / `[...h.keys()]` / `for…of h.entries()` / direct `for…of h`) all match bun's sorted output `[host, x-amz-content-sha256, x-amz-date]`. The s3-lite-client `getHeadersToSign` SigV4 step now sees the actual header set instead of `[]`, so SignedHeaders + signature canonicalization match. Refs #551. Parity 219 pass / 3 fail / 13 skip (98.6%, same 3 pre-existing fails — test_edge_destructuring, test_edge_objects_records, test_gap_object_methods).
- **v0.5.664** — **Closes #572 #573 #574** (s3-lite-client compile blockers from #551). **#572 (object-destructure params on class members)** — `class C { m({ a, b }) { ... } }` and `constructor({ a, b })` lowered every destructured name to 0 because `lower_class_method` / `lower_constructor` / `lower_setter_method` / `lower_private_method` / `lower_private_setter` in `crates/perry-hir/src/lower_decl.rs` skipped the param-destructuring extraction the function-decl path runs (lines 252-276 / 381-403). Tracked an `Assign`-unwrapped destructuring pattern per param, generated the `let leaf = param.<key>` extraction stmts BEFORE body lowering (so the destructured names are in `ctx.locals` when the body's identifier references resolve — generating after produces "unknown identifier" warnings + GlobalGet(0) refs). Covers defaults / rest / renaming / nested / async / static / setter shapes. **#573 (bare `class X extends Error {}`)** — three coordinated fixes: (a) `crates/perry-codegen/src/collectors.rs` adds `class_chain_extends_builtin_error` and excludes such classes from scalar replacement (the alloca-per-declared-field path doesn't allocate slots for `message` / `name` which Error subclasses don't declare; the runtime adds them via the SuperCall / lower_new fallback). (b) `crates/perry-codegen/src/lower_call.rs::lower_new` — when a class with no own ctor's parent-walk reaches an Error-like built-in (chain terminates at `Error` / `TypeError` / etc. — skipping past empty-bodied user ancestors like `class S3Error extends Error`), emit the `js_object_set_field_by_name` calls for `this.message = args[0]` and `this.name = <kind>` directly. Mirrors the existing Error-like SuperCall arm at expr.rs:4376. (c) `crates/perry-codegen/src/expr.rs::Expr::SuperCall` — when `effective_parent_class.constructor` is None and the chain reaches an Error-like, emit the same Error init (covers `class B extends A { ctor(m){ super(m); } }` where A is bare-extends-Error). End-to-end: `new MyError("hello")` now sets `e.name = "Error"` + `e.message = "hello"` (was SIGABRT/undefined), and the s3-lite-client error hierarchy (`S3Error → InvalidArgumentError → InvalidEndpointError`) catches with the right name + message. **#574 (`import * as Lib from "./mod.js"` namespace)** — `Lib.A` returned undefined for class exports, `typeof Lib` returned "boolean". Two fixes in `crates/perry-codegen/src/expr.rs`: (a) namespace-member PropertyGet — when the property name is in `class_ids`, emit the INT32-tagged class-id NaN-box (mirrors the `Expr::ClassRef` lowering) instead of the function-getter call which silently returns undefined for class exports. (b) `Expr::TypeOf` short-circuits four shapes that `js_value_typeof` would misclassify: namespace ExternFuncRef → "object" (was "boolean" via TAG_TRUE fallback), local class-ref → "function" (was "number" via INT32 path), imported class via `class_ids` → "function", and namespace-member where property is a class → "function". Bonus: `b instanceof Lib.A` now works — `Expr::InstanceOf` strips the `Ns.` prefix when `Ns` is in `namespace_imports` and re-looks up the class id by bare name. Repro from the issue (`typeof Lib: object` / `typeof Lib.A: function` / `typeof Lib.NUM: number` / `Lib.NUM: 42`) matches bun byte-for-byte. Refs #551.
- **v0.5.663** — **Closes #569** (function-scoped `function` declarations not hoisted): inner `function helper(...) {...}` declarations inside a function body are now lowered with proper ECMAScript hoisting + initialization. New HIR variant `Stmt::PreallocateBoxes(Vec<LocalId>)` and helper `lower_fn_body_block_stmt` in `crates/perry-hir/src/lower_decl.rs`: pre-defines hoisted FnDecl locals at function-body entry so forward-reference call sites (`return helper(input)` before `function helper`) resolve, lowers the body normally so `let x = 0; function addToX(){ x++; }` still sees `x` in `ctx.locals` when addToX's body is lowered, then rearranges the result so hoisted FnDecl `Stmt::Let`s appear before any other top-level statement (matching JS spec). Codegen handles `Stmt::PreallocateBoxes` in `crates/perry-codegen/src/stmt.rs` by alloca'ing a slot+box in the entry block for each id and storing the box pointer in the slot; subsequent `Stmt::Let` for an id in `ctx.prealloc_boxes` skips the allocation and just `js_box_set`s the init value. `collect_boxed_vars` (`crates/perry-codegen/src/boxed_vars.rs`) is extended to union the `PreallocateBoxes` ids into the module-wide boxed set so `LocalGet` / `LocalSet` / `Update` route through the box. The prealloc set covers: hoisted FnDecl ids referenced from any closure body (sibling-recursive — e.g. `helper` references `strip` and vice versa), plus function-body let/const ids captured by hoisted closures (e.g. `addToX` capturing `x`). Repros from the issue all match Node: forward-ref (`got: hello`), mutual recursion (`ping(3) === 3`), inner-shadows-outer (`outer-world`), and the load-bearing mutateOuter case (`mutateOuter() === 18`) still works. Parity 219 pass / 3 fail / 13 skip (98.6%, same 3 pre-existing fails — test_edge_destructuring, test_edge_objects_records, test_gap_object_methods).
- **v0.5.662** — **Closes #535** (state→UI binding propagation outside `--target harmonyos`). v0.5.621 + v0.5.623 had landed Layer 1 (`state<T>` desugar) and Layer 2 (`NavStack(state, routes)` IIFE rewrite + `NAVSTACK_REGISTRY` runtime), but only `perry-ui-macos/src/app.rs:1709` actually called `js_register_widget_hidden_handler` — every other UI backend recorded routes into the registry but had no callback registered, so `route.set("detail")` no-op'd outside macOS. This commit wires `navstack_set_widget_hidden(handle, hidden)` + `js_register_widget_hidden_handler(...)` into the cross-platform handler block on iOS / Android / tvOS / visionOS / Windows / watchOS / GTK4 (`crates/perry-ui-{ios,android,tvos,visionos,windows,watchos,gtk4}/src/app.rs`). watchOS additionally needed the missing setText / register_text_id pipeline so `state.text()` re-renders bind a tree node — added `crates/perry-ui-watchos/src/widgets/text_registry.rs` (mutates `NodeData.text` via `tree::with_node_mut`, which bumps `perry_watchos_tree_version` for SwiftUI re-eval). GTK4 had **zero** cross-platform handlers wired (`register_cross_platform_text_handlers` was never called); added `extern "C" show_toast_handler` / `set_text_handler` / `register_text_id_handler` wrappers in `widgets/{toast,text_registry}.rs` and call `register_cross_platform_text_handlers()` from `app::app_run` before `app.run_with_args`. Updated `types/perry/ui/index.d.ts:230,263-265` to drop the stale "deferred to v6.5" / "harvest detection only fires on harmonyos" claims and document the actual desugar limitations (top-level only, canonical method-call shapes only, `Text("prefix " + s.get())` snapshots once). End-to-end: issue's repro `const route = state("home"); App({body: NavStack(route, [{name:"home",...},{name:"detail",...}]) })` + `Button("...", () => route.set("detail"))` now flips visible route on every native target. Smoke-tested on macOS via `PERRY_UI_TEST_MODE=1` with both repros from the issue body — exit 0, no crash, HIR confirms `__navstack_register_route("__state_0", "home", LocalGet(N))` for each route + `__state_set("__state_0", "detail")` inside button closures. Non-windows UI backends pass `cargo check --release`; windows fails at host-check on the pre-existing `windows` crate dep gap (CLAUDE.md `--exclude` list), GTK4 fails at host-check on the pre-existing libshumate / gstreamer-sys system-dep gap — both unrelated to this change.
- **v0.5.661** — **Closes #567** (`String.prototype.split(separator, limit)`): pre-fix, the codegen path in `crates/perry-codegen/src/lower_string_method.rs` rejected the 2-arg form with `String.split expects 1 arg (delimiter), got 2` at compile time. AWS SigV4 canonicalization (`@bradenmacdonald/s3-lite-client/signing.ts:254`) does `const [key, val] = element.split("=", 2)` to keep `=` characters in the value half — without limit, every signed URL canonicalization broke. Two coordinated runtime additions: `js_string_split_n(s, delim, limit_i32)` in `crates/perry-runtime/src/string.rs` (`limit < 0` → no limit, `limit == 0` → empty array, `limit > 0` → truncate to N substrings; spec option-1 semantics — split fully then truncate; matches Node/bun byte-for-byte); parallel `js_string_split_regex_n(s, regex, limit)` in `crates/perry-runtime/src/regex.rs` for `s.split(re, limit)`. Codegen accepts 1 or 2 args, routes to `js_string_split_n` when limit present (else stays on existing `js_string_split` for backward compat). v0.5.659's split-related runtime decl + the `js_native_call_method` dispatch arm for any-typed receivers were already in place — this closes the codegen-side gap. Repro `"a=b=c=d".split("=", 2)` now prints `["a","b"]` matching Node. Edge cases (`limit=0`, `limit > parts`, regex separator) all match. Refs #551 (s3-lite-client native compile). **Closes #568** (`export { X as Y }` rename produced undefined-symbol link error): the consumer-side method-registry loop in `crates/perry-codegen/src/codegen.rs` was walking imported-class STUBS keyed by their LOCAL alias name (`PublicWidget`), then mangling `perry_method_<src>__<alias>__<method>` — but the source module emitted methods under the canonical name (`Widget`). New `imported_class_source_name` side map (parallel to existing `imported_class_prefix`) records `effective_name → ic.name` for renamed imports; the method-registry loop now uses the source name for symbol mangling while keying the dispatch entry by the alias. Static methods, getters, setters, and constructors get the same correction. The minimal repro (`lib/widget.ts: export class Widget` → `lib/index.ts: export { Widget as PublicWidget } from "./widget.js"` → `main.ts: import { PublicWidget }; new PublicWidget().greet("world")`) now compiles + links + prints `hello world`. The s3-lite-client entry point's `export { Client as S3Client, ... } from "./client.js"` shape unblocked at link time. Refs #551. **#569** (function-scoped `function` decls not hoisted) tracked separately — minimal forward-ref pattern still produces silent undefined; full fix requires box pre-allocation for sibling-recursive hoisted closures and is a deeper refactor (per attempt analysis: hoisting fn-decl bodies to lower-first breaks `let x = 0; function refs() { x++ }` because closure body lowering needs `x` in scope; pre-emitting `Let id = undefined; mutable: true` + LocalSet at source position fails because closures capturing siblings read uninitialized slots before the sibling's Stmt::Let allocates its box). Sanity tests test_inheritance, test_gap_class_advanced, test_gap_closures, test_edge_promises, test_gap_async_advanced, test_edge_scope_hoisting all pass; parity 219/222 (98.6%, 3 pre-existing fails — test_edge_destructuring, test_edge_objects_records, test_gap_object_methods).
- **v0.5.660** — **Closes #486 (logger middleware)** — two coordinated fixes that together unblock the full hono `app.use('*', logger()) + app.get('/', c => c.json({...}))` chain end-to-end. **(1) Cross-module class setter dispatch.** hono's `set res(_res) { …; this.#res = _res; this.finalized = true; }` was never invoked. `c.res = response` from inside compose's `await handler(c, next)` chain stored the response into a regular field slot but never ran the setter body — `this.finalized = true` never executed, hono-base's `if (!context.finalized) throw` fired on every request. Perry had `js_register_class_getter` + a getter dispatch arm in `js_object_get_field_by_name` (v0.5.620), but no parallel setter mechanism. Three changes: `crates/perry-runtime/src/object.rs` adds `setters: HashMap<String, usize>` to `ClassVTable`, new `js_register_class_setter(class_id, name, fn_ptr)` runtime FFI, setter dispatch arm in `js_object_set_field_by_name` walking class→parent chain (32 levels) invoking setter as `fn(this_f64, value_f64)` BEFORE field-write logic per JS spec; `crates/perry-codegen/src/runtime_decls.rs` declares new FFI; `crates/perry-codegen/src/codegen.rs` parallel setter_pairs registration loop (mangling `perry_method_<modprefix>__<class>____set_<f.name>` matching codegen.rs:2041's `renamed.name = "__set_<prop>"`). **(2) Arrow & fn-expr default-parameter desugar.** `(fn = console.log) => fn(out)` had its `default: Some(<expr>)` recorded on `Param` but the actual `if (p === undefined) p = <default>` desugar was never injected into the closure body. `build_default_param_stmts` in `crates/perry-hir/src/lower_decl.rs` ran for top-level function declarations / constructors / class methods, but `lower_arrow` and `lower_fn_expr` in `crates/perry-hir/src/lower/expr_function.rs` skipped it entirely. `myFn()` (no args) invoked `LocalGet(fn_id)` against an undefined slot and `js_closure_callN(undefined, …)` no-op'd. Fix: make `build_default_param_stmts` `pub(crate)`; call from both lowering sites with the same `default_stmts.append(&mut body)` pattern as `lower_fn_decl`. End-to-end: real hono `app.use('*', logger())` now prints `<-- GET / / --> GET / [32m200[0m 0ms` matching Node, returns STATUS:200 + correct content-type + correct body byte-for-byte. Cosmetic ANSI escape sequences shown as `\x1B[…m` literals because perry's template-literal lowering doesn't interpret `\xHH` hex-escapes (separate cosmetic). Combined with v0.5.658 (try/catch self-recursive boxing) + v0.5.653 (catch param scan in generator pass), this closes the hono logger middleware acceptance criteria. Box-pointer warnings (`[PERRY WARN] js_box_get/set: invalid box pointer 0x0`) still appear on every dispatch — captured mutable variable in compose has a corrupted box pointer, but functionally harmless because NaN comparisons produce false. Sanity tests test_inheritance, test_gap_class_advanced, test_gap_closures, test_edge_promises, test_gap_async_advanced, test_edge_scope_hoisting all pass.
- **v0.5.658** — **Refs #486** (try/catch around self-recursive closure body breaks self-reference): `let dispatch = (i) => { try { dispatch(i+1); } catch (e) {} }` (and the equivalent `async function dispatch(i) { try { await dispatch(i+1); } catch (e) {} }` declared inside an async closure body) had its self-reference invisible to the boxing analysis. The recursive `LocalGet(dispatch_id)` call lived inside `Stmt::Try.body`, but `collect_ref_ids_in_stmts` (in `crates/perry-codegen/src/collectors.rs`) had no `Stmt::Try` arm — it fell through to `_ => {}` and never walked `try.body` / `catch.body` / `finally`. Without those refs in `closure_refs`, `collect_self_recursive_closure_ids` didn't fire and `boxed_vars` didn't contain the dispatch id. The closure literal was then allocated with `capture[0] = pre-let-undefined`, the let stored the new closure pointer into the slot but capture[0] was never updated. Inside the closure body, `LocalGet(dispatch)` read capture[0] = undefined, the dispatch call invoked an undefined closure pointer (which the runtime silently treats as a no-op call returning undefined), and the function returned `undefined`. Same gap in `collect_let_ids` — lets nested under try/switch/labeled were invisible to the boxing analysis' `declared` set. Fix: add Try/Switch/Labeled arms to both `collect_ref_ids_in_stmts` and `collect_let_ids` (mirroring the equivalent arms already present in the parallel `collect_outer_writes_in_stmt` / `collect_write_ids_in_stmt` walkers). Repro `let dispatch = (i: number): any => { console.log('[d]', i); if (i < 1) { try { dispatch(i+1); } catch {} } return i; }` now prints `[d] 0 / [d] 1` matching Node (was `[d] 0 / undefined`). Hono's `compose()` wraps every middleware in this exact shape — `try { res = await handler(c, () => dispatch(i+1)); } catch (err) { ... }` — the bug masked the inner middleware dispatch chain even when the v0.5.653 generator-pass id-collision fix had unblocked the top-level path. Parity: 219 pass / 3 fail / 13 skipped (98.6%, no regressions vs main).
- **v0.5.657** — Closes #494: native-libraries authoring guide now ships a real, compileable Rust fixture crate with drift-protection unit tests.
- **v0.5.655** — Closes #562: user classes can extend `WritableStream`/`ReadableStream`/`TransformStream` end-to-end (s3-lite-client `ObjectUploader` shape).
- **v0.5.654** — Closes #561: Web Crypto `crypto.subtle.{digest,importKey,sign,verify}` (HMAC + SHA-1/256/384/512) for AWS SigV4 / JWT signing chains.
- **v0.5.653** — Refs #486: generator state-machine pass now scans `catch(e){}` param so async-recursive try/catch with unused binding no longer clobbers `__gen_state`.
- **v0.5.652** — Refs #536: `[].pop()`/`[].shift()` return `undefined` (not `NaN`); perry-ext-net `active_handles` wired into stdlib event-loop gate.
- **v0.5.650** — Docs sweep — backfilled pages for #538/#552/#553/#532/#517/#473/#458/#491 + stdlib `other.md` cleanup.
- **v0.5.649** — Refs #486: `new RegExp(varname)` with non-literal pattern arg now lowers via `js_regexp_new` (was placeholder, broke hono wildcard middleware).
- **v0.5.648** — Refs #538 followup: real BGTaskScheduler / NSBackgroundActivityScheduler / WKApplication impls on tvOS / visionOS / watchOS / macOS; GTK4 + Windows kept as stubs.
- **v0.5.644** — Closes #538: new `perry/background` module — iOS BGTaskScheduler + Android WorkManager bindings (`registerTask`/`schedule`/`cancel`).
- **v0.5.643** — Refs #486: new `js_dynamic_string_or_number_add` runtime helper for type-uncertain `+` operands (string concat through Any-typed values).
- **v0.5.642** — Closes #544: per-module `.o` cache key mixes in a hash of the running perry binary, so HIR/codegen pass changes invalidate stale objects.
- **v0.5.641** — Refs #486: cross-module class-expression self-binding alias (`var X = class _X { ... new _X() }`) — the npm-dist convention now resolves correctly.
- **v0.5.640** — `.ts`/`.tsx` packages from `node_modules` now classified `NativeCompiled` at the import site even without a `perry.compilePackages` entry.
- **v0.5.639** — Closes #553: four production-mobile widgets — `BottomNavigation`, `ImageGallery`, pull-to-refresh, infinite-scroll (`onScrollEnd`).
- **v0.5.638** — Refs #486: cross-module method inliner now excludes methods whose body references a local non-exported class (preserves class metadata).
- **v0.5.637** — Refs #517 followup: `MapView` un-stubbed on tvOS / GTK4 (libshumate) / Android (Google Maps) / watchOS (SwiftUI Map); Windows still pending (#559).
- **v0.5.636** — Closes #532 + #555 (PR `ui-loop-issues`): 11 native UI widgets across macOS/GTK4/Windows/iOS/visionOS — `widgetSetRichTooltip`, `Combobox`, `TreeView`, `Calendar`, `Chart`, `commandPalette*`, `MapView`, `PdfView`, `RichTextEditor`, `Toast` watchOS, data-table sort+filter+multi-select. Plus `tableGetFilterText` FFI ABI fix (i64 vs f64 register).
- **v0.5.635** — Closes #557: zero-arg `console.log()`/`info`/`warn`/`error`/`debug` now emit a newline (was silent no-op).
- **v0.5.634** — Closes #540: `[...map]` spread now yields `[key, value]` pair arrays via Map-detection arm in `js_array_concat` (was garbage).
- **v0.5.633** — Closes #554 followup: nested `{ key: [a, b] }` for-of destructure now also works inside function bodies (`lower_decl.rs` parallel path).
- **v0.5.632** — Refs #552: Android geolocation + photo-picker real impl (LocationManager + Photo Picker) replacing v0.5.631 stub.
- **v0.5.631** — Refs #552: `geolocationGetCurrent`/`geolocationWatch`/`imagePickerPick` on `perry/system` — macOS + iOS real; Android stubbed (replaced in v0.5.632).
- **v0.5.630** — PR #534 (dazhe): `codegen-wasm` `main()` with no return now emits valid Wasm; `has_return` recurses into DoWhile / For.init / Labeled.
- **v0.5.629** — Closes #554: nested destructuring in for-of `const` binding (`for (const { entity, components: [a, b] } of ...)`) now binds leaves correctly.
- **v0.5.628** — Closes #549: Set/Map object key collisions — `is_string_like` validates GC type tag at the pointee, so `set.add({})` doesn't collapse same-shape objects.
- **v0.5.627** — Closes #546/#547/#548: array `.some`/`.every`/`.find`/`.findIndex`/`.findLast(Index)` arms added for any-typed receivers + JSON.stringify defensive UTF-8 fallback.
- **v0.5.626** — Closes #420: drizzle-orm compiles + runs end-to-end via `compilePackages` — `pgTable` + `eq`/`gt`/`and` byte-for-byte matching Node.
- **v0.5.625** — Refs #420: derived-class field initializers now run AFTER parent ctor body per ECMAScript spec (new `FieldInitMode::AncestorsOnly`/`SelfOnly` enum).
- **v0.5.624** — Closes #541/#542/#543: codehz/ecs Map iteration through `Map<K,V> | undefined` parameters — `is_iterable_map` now accepts Union types; array-method fold gated on static type.
- **v0.5.623** — Refs #535 Layer 2: `NavStack(state, routes)` lowering for native multi-screen on macOS/iOS/Android/GTK4/Windows.
- **v0.5.622** — Refs #420: classes carrying `static [Symbol.for(...)] = "..."` fields — `Object.prototype.hasOwnProperty.call(C, sym)` now consults a side-table.
- **v0.5.621** — Refs #535 Layer 1: target-agnostic `state<T>(initial)` desugar for non-HarmonyOS — runtime `STATE_VALUES` registry + setText pipeline.
- **v0.5.620** — Refs #486: hono v0.5.614 forward-port — declared-but-uninitialized class fields default to `undefined`; `response.headers` arm; cross-module class getters registered.
- **v0.5.619** — Closes #533: for-of through `m.get(k)!` on Map-of-Maps — `infer_call_return_type` learned Map.get/Set.has, `new Map([entries])` infers K/V from literal entries.
- **v0.5.618** — Refs #420: `Object.prototype.hasOwnProperty.call(o, k)` rewritten to `js_object_has_own`; Symbol keys consult `SYMBOL_PROPERTIES` side table.
- **v0.5.617** — Refs #486: relative imports inside compile-package roots (`file:./` deps + symlinked node_modules) now resolve to `NativeCompiled` correctly.
- **v0.5.616** — Refs #420: 7 cross-module 3-level-inheritance fixes for drizzle pgTable — origin-path imported_vars lookup, ctor-less ancestor super-chain walk, vtable parent-chain dispatch, etc.
- **v0.5.615** — Refs #420: cross-module static class fields — source-side globals flip to external linkage; `ImportedClass.static_field_names` populated; `Expr::PropertyGet` routes to global load.
- **v0.5.614** — Refs #420: computed-key class fields (`[Sym]= …`) reach codegen via `ClassField.key_expr`; static field inits emit `Stmt::StaticFieldSet` in source order so they see top-level consts.
- **v0.5.613** — Closes #421: hono compiles + runs end-to-end via `compilePackages` — handle-shaped pointers skipped in `format_jsvalue`/`js_instanceof`; method-dispatch arms for Response/Blob/Headers handles.
- **v0.5.612** — Refs #421: any-typed string `indexOf`/`lastIndexOf` return f64 (not NaN-tagged INT32); `CLOSURE_ARITY_REGISTRY` pads under-arity calls with `TAG_UNDEFINED`.
- **v0.5.611** — Closes #531: generator-transform `compute_max_func_id`/`_local_id` walkers now scan `Expr::ArrayPush`/`ArrayPushSpread` args (closures inside ops.push were invisible).
- **v0.5.610** — Refs #421 (followup to #608): `recv.method(...args)` on any-typed receivers via new `js_native_call_method_apply`; `replace`/`replaceAll` arms on any-typed string dispatch tower.
- **v0.5.609** — Closes #528 (followup to #515/#518): chained-call array-method fold leaked through the `recv_is_class` `_ => false` catch-all — `this.col().find({})` got rewritten as `Expr::ArrayFind(this.col(), {})` and `js_array_find` read garbage from the user object's header. New `Call(inner)` arm in `recv_is_class` only allows the fold when the inner call's method is a known array-producing builtin; otherwise bails to runtime dispatch.
- **v0.5.608** — Refs #421: Phase 1 of handle-NaN-boxing unification — Web Fetch (Request/Response/Headers/Blob) handles now NaN-boxed at the FFI boundary; 25 fetch FFI fns migrated, 4 untyped property dispatch helpers added. Unblocks hono `request.url` in untyped position.
- **v0.5.607** — Closes #529 (followup to #515): `obj["method"](args)` / `obj["prop"]` on a class instance returned `undefined` — fold computed `MemberProp` with non-numeric string key to `Expr::PropertyGet` so dispatch hits the vtable; mirror fold in assignment form.
- **v0.5.606** — Closes #526: escape JS reserved words used as method names in generated `.d.ts` (axios.delete now emits `function _delete; export { _delete as delete }`).
- **v0.5.605** — Closes #515 (round 2): tighten `with`-method fold gates for object-literal / chained-method-call / typed-array receivers; add layered codegen + runtime fallbacks.
- **v0.5.604** — Closes #525: unify R005/#463 unimplemented-API message format for the 2-deep native-module call form (`m.method()`).
- **v0.5.603** — Refs #519/#421: register Request/Response/Headers typed-parameter annotations as native instances at HIR lowering so codegen's static dispatch fires.
- **v0.5.602** — Closes #509: parallel-codegen race producing colliding `.o` files — temp path nonce now uses an atomic counter alongside pid+nanos.
- **v0.5.601** — Closes #519: bind `this` for `instance.fn(args)` style cross-instance method calls via thread-local `IMPLICIT_THIS`; fill string-receiver `match`/`matchAll`/`search` arms.
- **v0.5.600** — Closes #461: pre-register `const f = (...) => …` arrow declarations as locals before lowering their initializer, so nested closures referencing `f` find it via `lookup_local`.
- **v0.5.600** — Closes #513: API manifest now enumerates every `NATIVE_MODULES` entry + every well-known binding (~250 entries backfilled) so the unimplemented-API gate flips strict for every supported module.
- **v0.5.599** — Closes #518: async class method's `await m.toArray()` returned `[]` because scalar-replacement of non-escaping object literals dropped the receiver-as-`this` for `js_native_call_method`.
- **v0.5.598** — Closes #512: API manifest now carries real param/return type signatures (`bcrypt.hash(password: string, …)`) instead of `(...args: any[]): any` fallback.
- **v0.5.597** — Closes #511 (followup to #462): `x.foo()` on undefined/null now throws `TypeError` and exits 1 (was silent no-op exit 0).
- **v0.5.596** — Refs #519: vtable `this` now NaN-boxed with POINTER_TAG before f64 cast so dispatched method bodies see a real instance pointer.
- **v0.5.595** — Closes #515: a class method named `with` with ≥2 params was getting folded into `Expr::ArrayWith` — gate the fold catch-all on `!recv_is_class`.
- **v0.5.594** — Closes #514: `s[0]` / `s.at(-1)` on a `(s: any)` parameter holding a string now correctly indexes via `js_dyn_index_get`; runtime tag-aware dispatch added for any-typed string methods.
- **v0.5.593** — Closes #510 (followup to #462): calling a method on a primitive whose name doesn't resolve now throws `TypeError: <kind>.<prop> is not a function` (was silent no-op).
- **v0.5.592** — Closes #471: `Record<number, T>` writes via `obj[i] = v` corrupted the heap — replace inline `obj+8+idx*8` store with type-dispatching `js_object_set_index_polymorphic`.
- **v0.5.591** — Refs #421/#420: cross-module `Call { callee: ExternFuncRef }` for an imported VARIABLE binding now fetches the closure value via the zero-arg getter and dispatches through `js_closure_callN`.
- **v0.5.590** — Closes #490: cross-platform system-tray icon API (`trayCreate`/`traySetIcon`/`traySetTooltip`/`trayAttachMenu`/`trayOnClick`/`trayDestroy`) on macOS / Windows / Linux + iOS/tvOS/visionOS/watchOS/Android stubs.
- **v0.5.589** — Closes #493: closure rest-param bundling now happens at runtime via a `func_ptr → fixed_arity` registry, fixing dynamic-dispatch call sites where codegen can't statically bundle.
- **v0.5.588** — Closes #507: perry-ext-net `Handle::current()` panic — auto-optimize now rebuilds tokio-using ext bindings into the same target dir as perry-stdlib so cargo unifies tokio compilations.
- **v0.5.587** — Closes #458/#491: WAV recording (`audioSetOutputFilename`/`audioStartRecording`/`audioStopRecording`) implemented on every UI backend (macOS/iOS/tvOS/visionOS/watchOS/Android/Windows; Linux+Web shipped in #458).
- **v0.5.586** — Closes #485: cross-module subclass field initializers + arrow class fields installed in parent ctor body now correctly inherit (unblocks hono `app.fetch` chain).
- **v0.5.585** — Refs #139: floating-point fast-math (`reassoc + contract` FMF) is now opt-in via `--fast-math` / `PERRY_FAST_MATH=1` / `package.json`. Default OFF means bit-exact f64 with Node on most code.
- **v0.5.584** — Closes #484: rest-parameter bundling for class method dispatch — parallel `method_has_rest` map populated alongside `method_param_counts`, threaded into the call site.

Older entries → CHANGELOG.md.
