# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**NOTE**: Keep this file concise. Detailed changelogs live in CHANGELOG.md.

## Project Overview

Perry is a native TypeScript compiler written in Rust that compiles TypeScript source code directly to native executables. It uses SWC for TypeScript parsing and LLVM for code generation.

**Current Version:** 0.5.655


## TypeScript Parity Status

Tracked via the gap test suite (`test-files/test_gap_*.ts`, 28 tests). Compared byte-for-byte against `node --experimental-strip-types`. Run via `/tmp/run_gap_tests.sh` after `cargo build --release -p perry-runtime -p perry-stdlib -p perry`.

**Last full sweep:** run `./run_parity_tests.sh` for the current snapshot — the gap-suite top-line has shifted with recent landings; see open issues #447 (nested-async hang masking `test_async`), #448 (`*[Symbol.iterator]()` class hang), #449 (`new.target` → NaN), #450 (`defineProperty` accessor `this`), #451 (`test_gap_json_advanced` SIGSEGV), and #452 (`Array.fromAsync` / Proxy/Reflect / JSON ordering). Several of these are pre-existing bugs that the parity skip-list and gap-suite output truncation hid from earlier numbers.

**Known categorical gaps**: lookbehind regex (Rust `regex` crate), `console.dir`/`console.group*` formatting, lone surrogate handling (WTF-8).

## Workflow Requirements

**IMPORTANT:** Follow these practices for every code change made directly on `main` (maintainer workflow):

1. **Update CLAUDE.md**: Add 1-2 line entry in "Recent Changes" for new features/fixes
2. **Increment Version**: Bump patch version (e.g., 0.5.48 → 0.5.49)
3. **Commit Changes**: Include code changes and CLAUDE.md updates together

### External contributor PRs

PRs from outside contributors should **not** touch `[workspace.package] version` in `Cargo.toml`, the `**Current Version:**` line in `CLAUDE.md`, or add a "Recent Changes" entry. The maintainer bumps the version and writes the changelog entry at merge time — usually by rebasing the PR branch and amending. This avoids the patch-version collisions that happen when Perry's `main` ships several commits while a PR is in review (each on-main commit bumps the version; a PR that bumped to the same patch on day 1 is already behind by merge day). Contributors just write code; let the maintainer fold in the metadata last.

## Build Commands

```bash
cargo build --release                          # Build all crates
cargo build --release -p perry-runtime -p perry-stdlib  # Rebuild runtime (MUST rebuild stdlib too!)
cargo test --release --workspace \
  --exclude perry-ui-ios --exclude perry-ui-tvos --exclude perry-ui-watchos \
  --exclude perry-ui-visionos --exclude perry-ui-android --exclude perry-ui-windows \
  --exclude perry-ui-gtk4   # Run tests (exclude cross-host UI crates on macOS)
cargo run --release -- file.ts -o output && ./output    # Compile and run TypeScript
cargo run --release -- file.ts --print-hir              # Debug: print HIR
```

## Architecture

```
TypeScript (.ts) → Parse (SWC) → AST → Lower → HIR → Transform → Codegen (LLVM) → .o → Link (cc) → Executable
```

| Crate | Purpose |
|-------|---------|
| **perry** | CLI driver (parallel module codegen via rayon) |
| **perry-parser** | SWC wrapper for TypeScript parsing |
| **perry-types** | Type system definitions |
| **perry-hir** | HIR data structures (`ir.rs`) and AST→HIR lowering (`lower.rs`) |
| **perry-transform** | IR passes (closure conversion, async lowering, inlining) |
| **perry-codegen** | LLVM-based native code generation |
| **perry-runtime** | Runtime: value.rs, object.rs, array.rs, string.rs, gc.rs, arena.rs, thread.rs |
| **perry-stdlib** | Node.js API support (mysql2, redis, fetch, fastify, ws, etc.) |
| **perry-ui** / **perry-ui-macos** / **perry-ui-ios** / **perry-ui-tvos** | Native UI (AppKit/UIKit) |
| **perry-jsruntime** | JavaScript interop via QuickJS |

## NaN-Boxing

Perry uses NaN-boxing to represent JavaScript values in 64 bits (`perry-runtime/src/value.rs`):

```
TAG_UNDEFINED = 0x7FFC_0000_0000_0001    BIGINT_TAG  = 0x7FFA (lower 48 = ptr)
TAG_NULL      = 0x7FFC_0000_0000_0002    POINTER_TAG = 0x7FFD (lower 48 = ptr)
TAG_FALSE     = 0x7FFC_0000_0000_0003    INT32_TAG   = 0x7FFE (lower 32 = int)
TAG_TRUE      = 0x7FFC_0000_0000_0004    STRING_TAG  = 0x7FFF (lower 48 = ptr)
```

Key functions: `js_nanbox_string/pointer/bigint`, `js_nanbox_get_pointer`, `js_get_string_pointer_unified`, `js_jsvalue_to_string`, `js_is_truthy`

**Module-level variables**: Strings stored as F64 (NaN-boxed), Arrays/Objects as I64 (raw pointers). Access via `module_var_data_ids`.

## Garbage Collection

Generational mark-sweep GC in `crates/perry-runtime/src/gc.rs` (default since v0.5.237 / Phase D). Two regions in the per-thread arena: nursery (`ARENA`, fills with new allocations, swept on minor GC) and old-gen (`OLD_ARENA`, holds tenured/evacuated objects). Conservative stack scan + precise shadow-stack roots + 9 registered scanners. Write barriers populate a remembered set so minor GC can avoid retracing the old-gen. Two-bit aging (`HAS_SURVIVED` / `TENURED`) promotes nursery survivors after 2 minor cycles; the C4b evacuation pass moves non-pinned tenured objects into old-gen with full reference rewriting. Idle nursery blocks observed empty for 2 GC cycles are `dealloc`'d back to the OS (C4b-δ, v0.5.235), and the next-trigger calc is hard-capped at the initial threshold (64 MB) so >90%-freed step-doubling can't blow up peak occupancy (C4b-δ-tune, v0.5.236). Triggers on arena block allocation (1 MB blocks since v0.5.196), malloc count threshold, or explicit `gc()` call. 8-byte GcHeader per allocation.

**Escape hatches**: `PERRY_GEN_GC=0`/`off`/`false` reverts to full mark-sweep (bisection only). `PERRY_GEN_GC_EVACUATE=1` enables the copying evacuation pass (default OFF — complete and correctness-safe but adds work that's a no-op on workloads where nothing tenures). `PERRY_WRITE_BARRIERS=1` opts into codegen-emitted write barriers (default OFF — barrier emission has its own perf cost; the runtime barrier always exists). `PERRY_GC_DIAG=1` prints per-cycle diagnostics.

## Threading (`perry/thread`)

Single-threaded by default. `perry/thread` provides:
- **`parallelMap(array, fn)`** / **`parallelFilter(array, fn)`** — data-parallel across all cores
- **`spawn(fn)`** — background OS thread, returns Promise

Values cross threads via `SerializedValue` deep-copy. Each thread has independent arena + GC. Results from `spawn` flow back via `PENDING_THREAD_RESULTS` queue, drained during `js_promise_run_microtasks()`.

## Native UI (`perry/ui`)

Declarative TypeScript compiles to AppKit/UIKit calls. Handle-based widget system (1-based i64 handles, NaN-boxed with POINTER_TAG). `--target ios-simulator`/`--target ios`/`--target tvos-simulator`/`--target tvos` for cross-compilation.

**To add a new widget** — change 4 places:
1. Runtime: `crates/perry-ui-macos/src/widgets/` — create widget, `register_widget(view)`
2. FFI: `crates/perry-ui-macos/src/lib.rs` — `#[no_mangle] pub extern "C" fn perry_ui_<widget>_create`
3. Codegen: `crates/perry-codegen/src/codegen.rs` — declare extern + NativeMethodCall dispatch
4. HIR: `crates/perry-hir/src/lower.rs` — only if widget has instance methods

## Compiling npm Packages Natively (`perry.compilePackages`)

Configured in `package.json`:
```json
{ "perry": { "compilePackages": ["@noble/curves", "@noble/hashes"] } }
```
First-resolved directory cached in `compile_package_dirs`; subsequent imports redirect to the same copy (dedup).

## Known Limitations

- **No runtime type checking**: Types erased at compile time. `typeof` via NaN-boxing tags. `instanceof` via class ID chain.
- **No shared mutable state across threads**: No `SharedArrayBuffer` or `Atomics`.

## Common Pitfalls & Patterns

### NaN-Boxing Mistakes
- **Double NaN-boxing**: If value is already F64, don't NaN-box again. Check `builder.func.dfg.value_type(val)`.
- **Wrong tag**: Strings=STRING_TAG, objects=POINTER_TAG, BigInt=BIGINT_TAG.
- **`as f64` vs `from_bits`**: `u64 as f64` is numeric conversion (WRONG). Use `f64::from_bits(u64)` to preserve bits.

### LLVM Type Mismatches
- Loop counter optimization produces i32 — always convert before passing to f64/i64 functions
- Constructor parameters always f64 (NaN-boxed) at signature level

### Async / Threading
- Thread-local arenas: JSValues from tokio workers invalid on main thread
- Use `spawn_for_promise_deferred()` — return raw Rust data, convert to JSValue on main thread
- Async closures: Promise pointer (I64) must be NaN-boxed with POINTER_TAG before returning as F64

### Cross-Module Issues
- ExternFuncRef values are NaN-boxed — use `js_nanbox_get_pointer` to extract
- Module init order: topological sort by import dependencies
- Optional params need `imported_func_param_counts` propagation through re-exports

### Closure Captures
- `collect_local_refs_expr()` must handle all expression types — catch-all silently skips refs
- Captured string/pointer values must be NaN-boxed before storing, not raw bitcast
- Loop counter i32 values: `fcvt_from_sint` to f64 before capture storage

### Handle-Based Dispatch
- TWO systems: `HANDLE_METHOD_DISPATCH` (methods) and `HANDLE_PROPERTY_DISPATCH` (properties)
- Both must be registered. Small pointer detection: value < 0x100000 = handle.

### objc2 v0.6 API
- `define_class!` with `#[unsafe(super(NSObject))]`, `msg_send!` returns `Retained` directly
- All AppKit constructors require `MainThreadMarker`

## Recent Changes

One-liners only — full detail in CHANGELOG.md.

- **v0.5.655** — **Closes #562** (user classes can extend `WritableStream` / `ReadableStream` / `TransformStream`): canonical Web-Stream-subclassing pattern (`class ObjectUploader extends WritableStream<Uint8Array> { constructor(...) { super({ write, close, abort }); this.getResult = () => result; } }` from `@bradenmacdonald/s3-lite-client`) now compiles + runs end-to-end. Pre-fix, `super({...})` to a stream base class fell through to the SuperCall arm's "parent not in ctx.classes, not error_like" branch which lowered the args for side effects then returned undefined — the user's `write` / `close` / `abort` (or `start` / `pull` / `cancel` / `transform` / `flush`) callbacks were dropped, and a subclass instance passed to `pipeTo` / `pipeThrough` was a NaN-boxed object pointer that the existing stream FFIs (`js_writable_stream_*` / `js_readable_stream_*` / `js_transform_stream_*`) interpreted as a registry id, found nothing, and silently no-op'd. **Three coordinated fixes**: (1) `crates/perry-hir/src/lower_decl.rs` — add `ReadableStream` / `WritableStream` / `TransformStream` to both `native_parent` matches alongside the existing `EventEmitter` / `AsyncLocalStorage` / `WebSocketServer` arms, AND populate `extends_name` (parent name) alongside `native_extends` so the codegen SuperCall arm reaches the parent-name switch instead of the no-extends fallthrough. New `class_native_extends` registry on `LoweringContext` so `let x = new SubclassOfStream(...)` in `destructuring.rs` can route the local through the parent stream module's dispatch table. New `current_class_super_ident` field for the `super({...})` pre-scan in `expr_call.rs` to register `start`/`pull`/`transform`/`flush` callback's controller param as a `readable_stream` native instance — same shape the `new TransformStream({...})` pre-scan in `expr_new.rs` does. (2) `crates/perry-codegen/src/expr.rs::Expr::SuperCall` — when `parent_name` is one of the three stream classes (and not in `ctx.classes`), invoke a new `lower_stream_super_init(ctx, kind, super_args)` helper that walks `args[0]` via the existing `extract_options_fields` (handles both `Expr::Object` and `__AnonShape_*` forms), lowers each canonical callback (`start`/`pull`/`cancel` for readable, `write`/`close`/`abort` for writable, `transform`/`flush` for transform; missing → `TAG_UNDEFINED`), reads `highWaterMark` from `args[1]` if present, then dispatches to a runtime `js_X_stream_subclass_init(this, ...callbacks, hwm)` shim. Per JS spec, leaf-class field initializers run AFTER super() returns — the helper applies `FieldInitMode::SelfOnly` after the runtime call so subclass fields like `seenLengths = []` are initialized before any inherited callback fires. (3) `crates/perry-stdlib/src/streams.rs` — three new `*_subclass_init` shims reuse the existing `alloc_readable` / `alloc_writable` / `js_transform_stream_new` paths so the registry entries / GC roots / `TRANSFORM_PAIRS` row are identical to a plain `new ReadableStream(...)`; the resulting registry-id is then stashed on the subclass instance via `js_object_set_field_by_name(this, "__perry_stream_handle__", handle)`. Plus a fourth piece: `js_stream_unwrap_handle(value: f64) -> f64` — checks for a NaN-boxed object pointer (top16=0x7FFD), reads `__perry_stream_handle__`, returns its f64 numeric value; falls through to the input unchanged for already-numeric handles. Wrapped around every stream-FFI receiver and stream-handle argument in `crates/perry-codegen/src/lower_call.rs` (`module == "readable_stream"` recv + `pipeTo` dest + `pipeThrough` transform; `module == "writable_stream"` recv; `module == "transform_stream"` recv) so subclass instances and bare numeric handles are interchangeable at the FFI boundary. Plus two gates in `expr_member.rs` + `expr_call.rs` (matching helper `is_stream_api_member` / `is_stream_api_method` keyed on the same dispatch tables in `lower_call.rs`) so subclass-declared user fields (`uploader.uploadedSize`) and methods (`uploader.getResult()`) fall through to regular object property / class-method dispatch instead of hitting the streams `NativeMethodCall` arm and returning the receiver-less zero-sentinel. Stream-API methods/properties themselves still route through the streams arms so inherited `pipeTo` / `getWriter` / `cancel` etc. work on subclass instances. End-to-end smoke (`test-files/test_issue_562_stream_subclass.ts`) exercises all three subclass shapes (Readable / Writable / Transform); a scaled-up s3-lite-client style test (TransformChunkSizes feeding ObjectUploader, 3 chunks totaling 224 bytes) reports the right `uploadedSize` and `completed` via the user's `getResult()` method. The chained `readable.pipeThrough(t).pipeTo(w)` form requires the inner `pipeTo` to dispatch through `module == "readable_stream"` based on `pipeThrough`'s return type — a separate (pre-existing) limitation tracked outside this issue; the split form (`const piped = readable.pipeThrough(t); await piped.pipeTo(w)`) where the intermediate variable picks up the existing `destructuring.rs:1709` ReadableStream-from-pipeThrough recognition works fully. Sanity tests `test_inheritance`, `test_gap_class_advanced`, `test_gap_closures`, `test_edge_promises`, `test_gap_async_advanced`, `test_gap_fetch_response` continue to match Node byte-for-byte; gap suite holds at 26/28 with the same 2 pre-existing fails (`test_gap_console_methods`, `test_gap_object_methods`).
- **v0.5.654** — **Closes #561** (Web Crypto: `crypto.subtle.{digest,importKey,sign,verify}` for AWS SigV4 / JWT / web-push signing chains): pre-fix, every pure-JS S3 / signed-URL / JWT library (s3-lite-client, aws4fetch, jose, oidc-client-ts, web-push) hit the issue-#463 strict-API gate at `crypto.subtle` and refused to compile — `compilePackages` couldn't pull any of them in. New `crates/perry-stdlib/src/webcrypto.rs` ships the symmetric WebCrypto subset: `digest("SHA-1"|"SHA-256"|"SHA-384"|"SHA-512", data)` → `Promise<Uint8Array>`, `importKey("raw", keyBytes, {name:"HMAC", hash:{name:"SHA-256"}}, extractable, usages)` → `Promise<CryptoKey>`, `sign("HMAC", key, data)` → `Promise<Uint8Array>`, `verify("HMAC", key, sig, data)` → `Promise<boolean>`. `CryptoKey` is a Buffer marked Uint8Array with an entry in a process-global `CRYPTO_KEY_REGISTRY: Mutex<HashMap<usize, (algo, hash)>>` recording the `(KeyAlgo::Hmac, HashAlgo)` pair so `sign` / `verify` route to the right primitive. The async wrapping is decorative — SHA / HMAC are CPU-bound and resolve synchronously inside the returned Promise (issue's implementation note explicitly says this). `verify` uses constant-time byte comparison so it doesn't leak the position of the first mismatching byte through timing. `bytes_from_jsvalue` accepts strings (StringHeader / SHORT_STRING_TAG inline SSO), Buffer / Uint8Array (BufferHeader registered in BUFFER_REGISTRY), TypedArrayHeader (Uint8Array via the typed-array path), AND TextEncoder-allocated ArrayHeaders — the last one is the subtle case: `enc.encode("abc")` returns an ArrayHeader registered in BUFFER_REGISTRY but with bytes stored as f64 elements at offset 8 (so `instanceof Uint8Array` works while the decoder can recover UTF-8); without the side-table check (`text::is_text_encoder_result`), reading `enc.encode("abc")` as packed u8 yielded the first 3 bytes of each f64's IEEE-754 LE representation (all-zero for small ints) instead of the source bytes — first iteration of the impl produced `709e80c8...` for SHA-256("abc") instead of the spec value `ba7816bf...`. Made `text::is_text_encoder_result` `pub` to expose the registry to webcrypto.rs. New HIR variants `WebCryptoDigest / WebCryptoImportKey / WebCryptoSign / WebCryptoVerify` in `crates/perry-hir/src/ir.rs`; HIR lowering arm in `crates/perry-hir/src/lower/expr_call.rs` recognizes `crypto.subtle.<method>(args)` (3-deep Member chain `Member(Member(Ident("crypto"), "subtle"), "digest")`) BEFORE the generic `mod.X.Y()` arm, dispatches to the right HIR variant by method+arity, and emits a clear "out of scope" error for unsupported subtle methods (encrypt/decrypt/generateKey/wrapKey/unwrapKey/deriveKey — explicitly listed as out of scope in the issue). Codegen at `crates/perry-codegen/src/expr.rs` lowers each variant to the corresponding `js_webcrypto_*` extern that returns `*mut Promise` and NaN-boxes with POINTER_TAG. New runtime decls in `runtime_decls.rs`. New manifest entry `property("crypto", "subtle")` in `crates/perry-api-manifest/src/entries.rs`. Detection arm in `collect_modules.rs::compute_required_features` flips on the `crypto` Cargo feature so auto-optimize rebuilds perry-stdlib with the SHA / HMAC deps available — without it, link fails with `_js_webcrypto_digest` undefined. End-to-end smoke `test-files/test_issue_561_webcrypto.ts` exercises NIST SHA-256("abc") + AWS SigV4 first-step kDate + RFC 4231 HMAC-SHA-256 Test Case 2 + verify round-trip + tampered-sig rejection — all 6 lines match `node --experimental-strip-types` byte-for-byte. Acceptance smoke `test-files/test_issue_561_sigv4_chain.ts` runs the full `kSecret → kDate → kRegion → kService → kSigning` chain shape from `@bradenmacdonald/s3-lite-client::signing.ts:317-330` (the issue's named acceptance criterion); final hex matches Node byte-for-byte. Updated `crates/perry-hir/tests/unimplemented_api_check.rs` — replaced the pre-#561 "crypto.subtle is rejected" test with three new tests: `crypto_subtle_namespace_compiles`, `crypto_subtle_digest_compiles`, `crypto_subtle_encrypt_is_rejected` (covers the out-of-scope path). All 10 tests in the strict-API check suite pass; the 5 webcrypto unit tests (parse_hash_alg, AWS SigV4 vector, SHA-256 empty + abc vectors, constant_time_eq) pass. Out of scope per the issue: asymmetric (`RSA-PSS`, `ECDSA`, `RSA-OAEP`), `generateKey`, `wrapKey` / `unwrapKey`, `deriveKey`, `encrypt` / `decrypt`.
- **v0.5.653** — **Refs #486** (try/catch with unused catch binding around recursive async): `async function dispatch(i) { ... try { await dispatch(1); } catch (e) {} ... }` returned `undefined` and never even entered state 0. The async-to-generator pre-pass correctly recorded the catch param `e` (id=K) in its `compute_max_local_id` walk — its `Stmt::Try` arm scans `c.param`. The downstream generator state-machine pass (`crates/perry-transform/src/generator.rs`) ALSO has its own `compute_max_local_id` (it re-walks because the async pre-pass may have allocated new ids), but its `scan_stmt_for_max_local`'s `Stmt::Try` arm only walked `body` / `catch.body` / `finally` — it never read `catch.param`. When the catch body was empty (`catch (e) {}`), no `LocalGet`/`LocalSet` carried the catch param's id, so the body-only scan missed it entirely; the generator transform then allocated `__gen_state` over that same id. Every `LocalSet(__gen_state, N)` clobbered `e`, every read of `e` returned the state counter, and the state machine never advanced — the function appeared to return `undefined` immediately because `__async_step(undefined, false)` saw `done=true` on the very first `next()` call (state was already past every arm). Fix: scan `c.param` in `scan_stmt_for_max_local`'s `Stmt::Try` arm, mirroring the equivalent scan in `crates/perry-transform/src/async_to_generator.rs::scan_stmt`. Repro `try { await dispatch(1); } catch (e) {}` now prints `[d] 0 / [d] 1 / result: 0` matching Node. Hono's `compose()` wraps every middleware in this exact shape (`try { res = await handler(c, () => dispatch(i+1)); } catch (err) { ... }` — the `err` binding is unused on the success path), so this fix unblocks a class of hono failures distinct from the v0.5.649 RegExp / v0.5.643 string+string / v0.5.641 alias work.
- **v0.5.652** — **Refs #536** (`@perryts/mysql` connection-pool acquire returned the wrong branch on empty pool + event loop exited before `await sock.on('connect')` resolved when sockets were routed through perry-ext-net): three coordinated fixes. (1) `crates/perry-runtime/src/array.rs::js_array_pop_f64` and `js_array_shift_f64` returned `f64::NAN` (bare NaN bits) for empty arrays — but per ECMAScript §23.1.3.21 / §23.1.3.27, `Array.prototype.pop` / `shift` on an empty array return `undefined`, NOT NaN. Bare NaN compares `!== undefined` (different bit patterns), so connection-pool drivers like `@perryts/mysql` doing `const entry = this.idle.shift(); if (entry !== undefined) { ... } else { /* allocate new */ }` took the truthy branch on an empty pool, attempted to use a NaN-typed entry as a connection, and crashed. Fix returns `TAG_UNDEFINED_F64` (NaN-boxed undefined: `0x7FFC_0000_0000_0001`) which compares correctly against `undefined`. (2) `crates/perry-ext-net/src/lib.rs` — new `js_ext_net_has_active_handles()` exposing perry-ext-net's `pending_events` + `sockets` registries through the same FFI shape as `js_net_has_active_handles` from perry-stdlib's bundled-net. "Live" intentionally counts sockets still establishing TCP — counting only fully-open ones caused early exit before async `connect` ever completed (the open-flag flips inside the spawned task, AFTER `await TcpStream::connect`). (3) `crates/perry-stdlib/src/common/async_bridge.rs::js_stdlib_has_active_handles` — split the existing `feature = "net"` arm into two: `bundled-net` (perry-stdlib's own net impl is compiled in) keeps the direct `crate::net::js_net_has_active_handles()` call; new `external-net-pump` arm (the well-known flip routes `import 'net'` to perry-ext-net) declares `extern fn js_ext_net_has_active_handles()` and consults it. Pre-fix only the bundled gate fired, so programs using TS-source drivers like `@perryts/mysql` (which routes through perry-ext-net) saw `await new Promise(r => sock.on('connect', r))` exit early because perry-stdlib's empty `NET_SOCKETS` map reported no active handles. Plus a docs change: `docs/src/native-libraries/authoring-guide.md` swaps inline ts code blocks for `{{#include ../../examples/_fixtures/native-libraries/...}}` references so the snippets compile-test against real fixture files; matching `crates/perry-doc-tests/src/main.rs` skip-list update adds `_fixtures` (illustrative snippets that may legitimately import packages absent from this tree, drift-protected via PR diff rather than compile-testing).
- **v0.5.650** — **docs sweep** — audited last 200 commits against `docs/src/`. Net new pages: `docs/src/system/background.md` (issue #538 — `registerTask` / `schedule` / `cancel` with the per-platform support matrix); `docs/src/system/geolocation.md` (issue #552 — geolocation + photo-library image picker, including the `Promise` wrapper recipe and the per-platform `Info.plist` / manifest requirements). Existing pages extended: `docs/src/ui/widgets.md` got sections for the issue-#553 mobile widgets (`BottomNavigation`, `ImageGallery`, pull-to-refresh, infinite-scroll) and the issue-#532 / #517 native widgets (`Combobox`, `TreeView`, `Calendar`, `Chart`, `commandPalette*`, `MapView`, `PdfView`, `RichTextEditor`, `widgetSetRichTooltip`); `docs/src/ui/table.md` documents the issue-#473 sort + filter + multi-select extensions; `docs/src/system/audio.md` documents the issue-#458/#491 WAV recording trio (`audioSetOutputFilename` / `audioStartRecording` / `audioStopRecording`); `docs/src/system/overview.md` adds the new APIs to its top-level table. Stale text fixed: `docs/src/stdlib/other.md` removed the "not wired yet — track at #200" warnings on sharp / cheerio / zlib / cron — all four were wired by the v0.5.541 / v0.5.550 / v0.5.551 / v0.5.564 #466 ports. Same page now also covers the npm packages added during the #466 sweep that had no doc presence: events, exponential-backoff, dayjs/date-fns, moment, decimal.js/bignumber.js, rate-limiter-flexible, ethers, plus the external-repo bindings `@perryts/tursodb` (#424) + `@perryts/iroh` (#425) + the pure-TS drivers `@perryts/postgres` / `@perryts/mysql` / `@perryts/mongodb`. `docs/src/stdlib/overview.md` calls out that wired packages live in standalone `perry-ext-*` crates (not `perry-stdlib`) since the v0.5.532 well-known native bindings registry, and points readers at the External-native-bindings list. `docs/src/SUMMARY.md` adds the two new System pages. `mdbook build` clean.
- **v0.5.649** — **Refs #486** (`new RegExp(varname)` with non-literal pattern argument): hono's `buildWildcardRegExp` does `new RegExp(path === "*" ? "" : ...)`. Pre-fix, the `Expr::New { class_name: "RegExp", args: [LocalGet(...)] }` HIR shape had no handler in `lower_builtin_new` — fell through to the placeholder `js_object_alloc(0,0)` path, producing a "fake regex" object with no actual pattern. Subsequent `.test(s)` returned false for every input. Concretely: hono's `RegExpRouter.add` for `app.use('*', mw)` does `re.test(p) && middleware[m][p].push([handler, paramCount])` to populate the wildcard middleware into existing routes — `re.test(p)` was false for every `p`, so the middleware was registered but never propagated into route entries. `matchResult[0]` for `app.get('/')` then had only the GET handler, length=1, and the single-handler fast path in `hono-base.js#dispatch` invoked the handler directly, bypassing the middleware chain entirely. Compile-time RegExp LITERALS (`/foo/g`) were already handled by `Expr::RegExp` at `expr.rs:4964` — only the runtime `new RegExp(arg)` form was broken. Fix adds a `"RegExp"` arm to `lower_builtin_new` in `crates/perry-codegen/src/lower_call/builtin.rs` that lowers args 0/1, unboxes to i64 string handles, calls `js_regexp_new(pattern_handle, flags_handle)` and NaN-boxes the result. End-to-end: hono logger middleware now actually runs (`[MW]` and `[MW-after]` print), `matchResult[0].length` correctly reports 2 (middleware + handler), `compose()` is invoked instead of the single-handler fast path. The deeper "next() doesn't dispatch the inner handler" issue (try/catch + recursive async hoisted-function-in-arrow-body) is a separate blocker tracked as the next iteration. Hono canonical (`c.text`) + JSON-response (`c.json`) both still pass; sanity tests test_inheritance, test_edge_promises, test_gap_class_advanced, test_gap_async_advanced, test_gap_closures all match Node.
- **v0.5.648** — **Refs #538 (followup)** — replaced the v0.5.644 no-op stubs on tvOS / visionOS / watchOS / macOS with real OS-managed implementations; documented why GTK4 (Linux) and Windows remain stubs. **tvOS** (`crates/perry-ui-tvos/src/background.rs`): full `BGTaskScheduler` clone of the iOS impl. `BGAppRefreshTaskRequest` + `BGProcessingTaskRequest` shapes are identical on tvOS 13+; same `registerForTaskWithIdentifier:usingQueue:launchHandler:` + `submitTaskRequest:error:` semantics. Flush hook added to `PerryAppDelegate.application:didFinishLaunchingWithOptions:` in `crates/perry-ui-tvos/src/app.rs`. New `-framework BackgroundTasks` link entry in the tvOS arm of `crates/perry/src/commands/compile/link.rs`. Practical caveat in the .d.ts: tvOS apps only run while the box is on, so "background" here means "during screen saver / different app focused", not "while box sleeping". **visionOS** (`crates/perry-ui-visionos/src/background.rs`): same `BGTaskScheduler` impl, but `flush_pending_registrations()` is called from `app_run()` instead — visionOS apps are SwiftUI-hosted and Perry doesn't own the `UIApplicationDelegate`, so flushing happens before the SwiftUI host takes over UIApplicationMain. New `-framework BackgroundTasks` link entry in the visionOS arm. **watchOS** (`crates/perry-ui-watchos/src/background.rs`): different API surface — `WKApplication.scheduleBackgroundRefresh(withPreferredDate:userInfo:scheduledCompletion:)` (watchOS 7+). Only the `appRefresh` kind has a watchOS equivalent; `processing` is accepted but treated identically. `requiresNetwork` / `requiresCharging` are advisory (OS decides). No native cancel API — `cancel(id)` drops the handler from the registry so a fired refresh becomes a no-op. Identifier is round-tripped via `userInfo["perry_id"]`. New SwiftUI `WKApplicationDelegate` adaptor (`PerryWatchAppDelegate`) added to `crates/perry-ui-watchos/swift/PerryWatchApp.swift` — implements `handle(_ backgroundTasks:)`, reads `userInfo["perry_id"]` from each `WKApplicationRefreshBackgroundTask`, calls `perry_watchos_dispatch_background_task(cstr)` which routes to the registered Rust handler, then signals `setTaskCompletedWithSnapshot(false)`. WatchKit framework imported. **macOS** (`crates/perry-ui-macos/src/background.rs`): real `NSBackgroundActivityScheduler` (Foundation, 10.10+). Different model from iOS — schedule a one-shot block with `repeats=false`, `interval = max(0, earliestStartMs - now)/1000`, `tolerance = interval/4` (or `/2` for processing kind), `qualityOfService = .background` (or `.utility` for processing), `requiresNetworkConnectivity = req_net`. `requiresCharging` doesn't have a direct equivalent on `NSBackgroundActivityScheduler` — set to advisory via QoS hints. Schedulers retained in a per-process registry keyed by id so `cancel(id)` can call `invalidate()`. Block invokes the user handler then calls the OS-supplied `NSBackgroundActivityCompletionHandler` with `NSBackgroundActivityResultFinished` (=1). **GTK4 (Linux)** + **Windows**: stubs kept, but the comments now explain why honestly: Linux desktop has no Perry-callable equivalent (systemd `--user` timer units are deploy-time XML config, cron is admin-managed, neither is a runtime-callable JS API); Win32 has no equivalent without admin elevation (Task Scheduler API requires Schedule Tasks rights and prompts UAC) or being a packaged WinUI 3 / UWP app (MSIX background tasks need declarative `<BackgroundTasks>` extensions in appxmanifest). For periodic refresh while a desktop app IS running, the .d.ts now points users at `setInterval()`. New text in `types/perry/background/index.d.ts` lays out platform-by-platform what's real, what's advisory, and what's a no-op so users can branch via `getDeviceIdiom()` if needed. **Workspace-wide check** passes; cross-target check passes for iOS / tvOS / visionOS / watchOS / macOS — only pre-existing warnings (the `js_string_from_bytes` ABI mismatch present on main). Smoke test `test-files/test_issue_538_background_tasks.ts` still compiles + runs cleanly on macOS host.
- **v0.5.644** — **Closes #538** (native bindings for iOS BGTaskScheduler + Android WorkManager): new `perry/background` module exporting `registerTask(identifier, handler)` / `schedule(identifier, kind, earliestStartMs, requiresNetwork, requiresCharging)` / `cancel(identifier)`. iOS impl in `crates/perry-ui-ios/src/background.rs` keeps a per-process `HANDLERS: HashMap<String, f64>` populated from `registerTask` calls during module init; `PerryAppDelegate.application:didFinishLaunchingWithOptions:` now calls `background::flush_pending_registrations()` which iterates each identifier and invokes `BGTaskScheduler.shared.register(forTaskWithIdentifier:usingQueue:launchHandler:)` with an `RcBlock` that looks up the stored closure and dispatches via `js_closure_call0` (microtasks pumped before + after; result inspected for Promise but completion semantics defer to a follow-up). `schedule` builds either `BGAppRefreshTaskRequest` (kind=`"appRefresh"`) or `BGProcessingTaskRequest` (kind=`"processing"` — adds `setRequiresNetworkConnectivity` / `setRequiresExternalPower`), populates `setEarliestBeginDate` from `earliestStartMs` (epoch-ms; 0 = no delay), submits via `submitTaskRequest:error:`. `cancel` calls `cancelTaskRequestWithIdentifier:`. Identifiers MUST appear in Info.plist `BGTaskSchedulerPermittedIdentifiers` per Apple's documented contract — calling `submit` with an unregistered id throws an internal NSException; callers see no observable signal but the OS rejects the wake-up. New `-framework BackgroundTasks` linker entry on iOS in `crates/perry/src/commands/compile/link.rs`. Android impl in `crates/perry-ui-android/src/background.rs` delegates via JNI to three new `PerryBridge.kt` static helpers: `backgroundRegisterTask(id, key)` records the (identifier → callback key) mapping in a `MutableMap<String, Long>`; `backgroundSchedule(id, kind, earliestStartMs, requiresNetwork, requiresCharging)` reflectively builds an `androidx.work.OneTimeWorkRequest` with `Constraints.Builder` (`setRequiredNetworkType(CONNECTED)` when `requiresNetwork`, `setRequiresCharging(true)` when `requiresCharging`), stashes the identifier in `Data.Builder.putString("identifier", id)`, computes `setInitialDelay` from `earliestStartMs - System.currentTimeMillis()`, and calls `WorkManager.getInstance(activity).enqueueUniqueWork(id, REPLACE, request)`; `backgroundCancel` calls `cancelUniqueWork(id)`. New `PerryBackgroundWorker.kt` extends `androidx.work.Worker` — `doWork` reads the identifier out of `inputData`, calls `PerryBridge.backgroundLookupCallbackKey(id)`, posts a `Handler(Looper.getMainLooper())` runnable that invokes `PerryBridge.nativeInvokeCallback0(callbackKey)` (Perry's runtime is UI-thread-pinned), waits up to 10 s on a `CountDownLatch`, returns `Result.success()`. Reflection-based call into `androidx.work` keeps the bridge's compile-time deps unchanged and lets the runtime no-op cleanly when the WorkManager dep isn't on the classpath (logged at warn level, not thrown). New `androidx.work:work-runtime-ktx:2.9.0` Gradle dep in both `crates/perry-ui-android/template/app/build.gradle.kts` and the parallel `android-build/app/build.gradle.kts` per the v0.5.632 sync convention. Both Android Kotlin templates updated. Wiring: `PERRY_BACKGROUND_TABLE` in `crates/perry-dispatch/src/lib.rs` exposes the three method rows (Str/Closure for `registerTask`, Str/Str/F64/F64/F64 for `schedule`, Str for `cancel` — all `ReturnKind::Void`); `crates/perry-codegen/src/lower_call.rs` adds a `perry_background_table_lookup` arm next to perry/system + perry/updater (same `lower_perry_ui_table_call` machinery); `crates/perry-api-manifest/src/entries.rs` adds `"perry/background"` to NATIVE_MODULES + RUNTIME_ONLY_MODULES + three `method(...)` entries so the issue-#513 strict-API gate flips strict; `crates/perry/src/commands/compile/collect_modules.rs` flips `needs_ui` when `perry/background` is imported (the runtime FFIs live in `libperry_ui_*.a` alongside the OS-framework integration); `crates/perry/src/commands/check.rs` adds `perry/background` to the strict-API pre-filter. No-op stubs added to perry-ui-macos (inline in lib.rs — desktop apps have no equivalent OS pipeline) + the existing `issue_552_stub.rs` files in perry-ui-tvos/watchos/visionos/gtk4/windows. New TS surface at `types/perry/background/{index.d.ts, package.json}`. End-to-end smoke compile-checks pass for perry-codegen + perry-dispatch + perry-api-manifest + perry-ui-macos (host) + perry-ui-ios (cross-target). Workaround in the issue (clients skip background refresh, server-side cron + push-foreground polling) becomes optional — production-mobile apps can now opt into the OS-managed wake-up path on iOS + Android.
- **v0.5.643** — **Refs #486** (string + string concat through type-uncertain operands): `k + c.buildRegExpStr()` inside hono's `Node.buildRegExpStr` for-of body — both operands lower as plain f64s with inferred type Any, the static-string-concat fast path at expr.rs:1118 doesn't fire (`is_definitely_string_expr` returns false because neither has a static narrowing), and the previous fallback path called `js_number_coerce` on each side then `fadd`'d the results — turning `"c" + ""` into `Number("c") + Number("") = NaN + 0 = NaN`. Every recursive level then propagated NaN into the parent's `prefix + tail` concat, until `Trie.buildRegExp`'s `regexp.replace(...)` threw `(number).replace is not a function` because regexp had become NaN. **New runtime helper** `js_dynamic_string_or_number_add` in `crates/perry-runtime/src/value.rs` checks NaN-box tags at runtime: STRING_TAG / SHORT_STRING_TAG → string concat (coerces non-string side via `js_jsvalue_to_string` + `js_string_concat`), BIGINT_TAG → `js_bigint_add`, otherwise numeric `fadd` (with `js_number_coerce` on non-numeric operands the same way the static path did). Codegen in `crates/perry-codegen/src/expr.rs::Expr::Binary` dispatches there for `op==Add` when neither operand is statically narrowed (`!is_numeric_expr && !is_bigint_expr` on both sides) — the static numeric/bigint paths stay on `fadd` to keep the inline codegen for hot arithmetic loops. End-to-end: hono logger middleware acceptance now compiles, `app.use('*', logger())` + `app.get('/', c => c.json({...}))` returns the exact JSON response that matches Node byte-for-byte (response body + headers); the logger middleware's stderr lines (`<-- GET /` / `--> GET / 200 1ms`) are still missing — that's a separate `app.use` middleware-not-running issue (the route table evaluates the handler directly without composing through middleware), tracked as the next iteration. Closer reduction (`Object.keys(this.#children).map(k => k + this.#children[k].build())` recursive on a trie of `_Node` instances) returns the expected `(?:ac|b)` matching Node, was returning `(?:NaN|NaN)` pre-fix. No regressions on standard `+` cases (`"a" + "b"`, `1 + 2`, `"x" + 1`, `1 + "y"`, `true + 1`, `null + 1`, `undefined + 1` all match Node). Sanity tests (test_inheritance, test_edge_promises, test_gap_class_advanced, test_gap_async_advanced, test_gap_closures) all pass.
- **v0.5.642** — **Closes #544** (per-module `.o` cache returned stale objects when only HIR-pass code changed): pre-fix `compute_object_cache_key` mixed in `CARGO_PKG_VERSION` but nothing else identifying the running perry binary, so editing `crates/perry-transform/src/state_desugar.rs` (or any HIR/codegen pass) and rebuilding without bumping the version left the cache key identical — `.perry-cache/objects/<target>/<key>.o` served the previous pass's output and the new pass appeared to do nothing at runtime. Cost the user ~45 min during #535 Layer 1 development debugging a phantom bug; only `rm -rf <project>/.perry-cache` exposed the actual issue. Fix in `crates/perry/src/commands/compile/object_cache.rs` adds `perry_build_id()` — a `OnceLock<u64>` that hashes the running perry executable's bytes via `current_exe() + fs::read + djb2_hash` on first call and reuses the value across all rayon workers in the same process. Mixed into the cache key alongside the existing `v=` (version) field. Any `cargo build -p perry-codegen` / `-p perry-transform` / `-p perry-hir` (or any dep that links into perry) produces a new perry binary, hence a new build id, hence a new cache key — the pass change can't be invisible. Failure modes degrade silently to the pre-fix behavior: if `current_exe()` or the read fails, returns 0 and the version-only path takes over. Cost: one ~16 MB djb2 pass per perry invocation (~tens of ms, amortized across all module compilations and only on first cache-key derivation). Cache scope: still machine-local (`.perry-cache/` in `.gitignore`, host CPU-specific via `-march=native` in clang). New regression test `key_includes_perry_build_id` confirms the helper returns non-zero against the test binary and is stable across calls within a process.
- **v0.5.641** — **Refs #486** (cross-module class-expression self-binding alias resolution): `var X = class _X { ... new _X() ... }` is the npm-dist convention for class declarations (hono's `var Node = class _Node {...}`, plus countless others — Babel emits this shape for every `class X { ... }` it transpiles to ES5). The inner self-binding name `_X` is scoped to the class body and references the same class as the outer `X`. Pre-fix, perry's HIR lowering registered the class only under the outer name `X`; references to `_X` from inside the class body lowered as `Expr::ExternFuncRef("_X")` because `lookup_class("_X")` returned None, then `new _X()` lowered as `Expr::NewDynamic { callee: ExternFuncRef("_X") }` and codegen's `try_static_class_name` couldn't resolve it to a class — falling through to the `js_object_alloc(0, 0)` placeholder path that stamps `class_id=0` on the new instance. Subsequent method dispatch on those instances hit the `class_id != 0` gate in `js_native_call_method` and fell through to NULL_OBJECT_BYTES. **Five coordinated fixes**: (1) `crates/perry-hir/src/ir.rs` — new `aliases: Vec<String>` field on `Class`. (2) `crates/perry-hir/src/lower.rs::lower_stmt`'s `var X = class _X` arm now PRE-REGISTERS the inner name as a class alias before lowering the class body, so `Ident("_X")` inside method bodies resolves through the existing `lookup_class` → `Expr::ClassRef` path. (3) `crates/perry-codegen/src/expr.rs::try_static_class_name` recognizes `Expr::ExternFuncRef { name }` whose `name` is in `class_ids` (covers any case where the ExternFuncRef route still fires). (4) `crates/perry-codegen/src/codegen.rs::compile_module` populates `class_table`, `class_ids`, `method_names`, and `class_keys_globals_map` with every alias entry; the method/getter symbol-emission loops at codegen.rs:4253 / :4336 skip alias keys (`if *class_name != class.name { continue; }`) so the alias keys don't double-emit `perry_method_<modprefix>__<class>__<method>` symbols (the alias-prefixed name `perry_method_<modprefix>___X__method` would have an extra leading underscore from `sanitize("_X")` that doesn't match the canonical body emission, breaking the link). (5) `crates/perry-transform/src/inline.rs::collect_nonexported_class_names` adds inner-alias names to the rejection set even when the OUTER name is exported — preventing the inliner from copying a method body that references the inner alias into a destination module that only has the outer name in its `imported_classes`. End-to-end: `var X = class _X { spawn() { return new _X(); } method() { return 'hello'; } }; new X().spawn().method()` now returns `'hello'` matching Node (was returning `[object Object]` pre-fix). The hono Node case (`var Node = class _Node { #children = Object.create(null); insert(tokens, index) { ...; this.#children[token] = new _Node(); ... } }`) compiles into a working trie that returns proper string concatenations. Logger middleware acceptance still has a separate blocker — `(number).replace is not a function` deep in hono's RegExpRouter.match — that this release doesn't reach (the construction path is fixed but a different dispatch in the buildRegExp chain produces NaN; tracked as the next iteration). Sanity tests `test_inheritance`, `test_gap_class_advanced`, `test_gap_closures`, `test_edge_promises`, `test_gap_async_advanced` all match Node byte-for-byte.
- **v0.5.640** — `.ts` / `.tsx` packages from `node_modules` are now classified `NativeCompiled` at the import site even when not listed in `perry.compilePackages`. Repro: `import { component, relation } from "@codehz/ecs"` (a TS-source npm package without `perry.nativeLibrary` / `perry.nativeModule`) printed `JS module: …` at collect time, emitted real native `.o` files for every `@codehz/ecs/src/*.ts`, and then failed to link with `Undefined symbols: _component, _relation, _pipeline`. Two-sided inconsistency: `resolve_import` returned `ModuleKind::Interpreted` for the bare-specifier fallback (`crates/perry/src/commands/compile/resolve.rs:918`), so the consumer's import record said "this is a JS-runtime module"; but `collect_modules`'s `should_use_js_runtime` check (line 77) gated on `is_js_file(canonical)` — false for `.ts` — so the recursive walk took the native HIR-lowering path. The `.o` files existed with native exports, the consumer's codegen didn't declare them as cross-module externs, `ld` failed. Fix routes resolved `.ts` / `.tsx` entries (not `.d.ts` — `is_ts_file` already excludes those) through `ModuleKind::NativeCompiled` in both bare-specifier (line 918) and `file:`-dep fallback (line 967) sites. `.js` / `.mjs` / `.cjs` keep going to `Interpreted` since those still need V8 / QuickJS. Side benefit: the cosmetic "JS module: foo -> foo/index.ts" log line is gone for TS-only packages — the user-reported confusion ("we should have the typescript file there") was a real symptom of the broken classification, not just a label mistake. End-to-end: the `@codehz/ecs` + `@codehz/pipeline` repro from the issue now compiles + links cleanly with no `compilePackages` entry needed.

- **v0.5.639** — Closes #553: four production-mobile-app widgets shipped across the perry/ui surface so an Angular-Material-style screen (issue's sharity-app reference) can be built natively. **BottomNavigation** — 5-tab bottom bar with `{icon, label, badge?}` items, `selectedIndex` state, `onSelect` callback. Real impls on macOS (custom NSStackView+NSButton strip with SF Symbol icons + Adwaita-style tinted-blue selected state + red badge layer), iOS (UITabBar via `setItems:animated:` + UITabBarItem.badgeValue), Android (custom horizontal LinearLayout with vertical (ImageView+TextView) tabs + drawable lookup via `Resources.getIdentifier(..., "drawable", pkg)` + optional badge TextView), GTK4 (GtkBox+GtkButton tab strip with GtkImage+GtkLabel children + Adwaita CSS classes for selected/badge styling). Stub on Windows (Win32 has no native primitive — same convention as the existing tabbar.rs Win32 stub; would need WinUI 3 transition or 300+ lines of owner-drawn child windows). **ImageGallery** — swipeable carousel with `{url, alt}` images + `initialIndex?` + `onIndexChange?`. Real on macOS (horizontal-paging NSScrollView containing NSStackView of NSImageViews; remote URLs loaded via `+[NSData dataWithContentsOfURL:]` on a background dispatch_global_queue, applied on the main thread via `dispatch_async_f`), iOS (paging UIScrollView with UIScrollViewDelegate.scrollViewDidEndDecelerating to fire `onIndexChange` after swipe; pinch-zoom is a follow-up), Android (HorizontalScrollView containing horizontal LinearLayout of ImageViews with `BitmapFactory.decodeFile` for local paths; `smoothScrollTo` for programmatic page jumps), GTK4 (GtkScrolledWindow + horizontal GtkBox of fixed-size GtkPicture pages + GtkAdjustment::value-changed for index tracking). Stub on Windows (same Win32-no-native rationale). **Pull-to-refresh on LazyVStack** — `lazyvstackSetRefreshControl(view, onPull)` + `lazyvstackEndRefreshing(view)` mirroring the existing #390 ScrollView API. iOS-only real for now (LazyVStack itself is still a stub on iOS pending UITableView wiring — separate issue); macOS / Android / GTK4 / Windows are documented no-ops since none of those platforms have a native pull-to-refresh idiom or AndroidX dep imported. **Infinite-scroll callback (onScrollEnd)** — `scrollviewSetScrollEndCallback(view, fn, thresholdPx)` + `lazyvstackSetScrollEndCallback(view, fn, thresholdItems)` with backpressure: fires once per threshold-cross, re-arms only when the user scrolls back up past the threshold. Real ScrollView impl on every platform: macOS uses NSScrollView clip-view bounds notifications via `NSViewBoundsDidChangeNotification`; iOS uses UIScrollViewDelegate.scrollViewDidScroll; Android wires through PerryBridge.setOnScrollEndCallback (new helper, calls `View.OnScrollChangeListener` with the same backpressure logic and a `scrollEndArmed` per-view map); GTK4 uses GtkAdjustment::value-changed; Windows hooks the existing scrollview's WM_VSCROLL / WM_MOUSEWHEEL handlers via a new `check_scroll_end(handle)` after each offset update. LazyVStack scroll-end is real on macOS (parallel observer on the inner NSTableView's clip view, threshold measured in rows × row_height); other platforms gate on the same iOS-LazyVStack-is-a-stub blocker as pull-to-refresh and ship as no-ops with the FFI in place. New: `crates/perry-dispatch/src/lib.rs` adds 11 MethodRow entries flipping the issue #513 strict-API gate; `crates/perry-api-manifest/src/entries.rs` adds 12 manifest entries; `types/perry/ui/index.d.ts` adds the 11 export declarations + the documentation block describing each platform's behavior. New widget files: `crates/perry-ui-{macos,ios,android,gtk4}/src/widgets/{bottom_nav.rs,image_gallery.rs}` (8 new files); scroll-end logic added inline to existing `scrollview.rs` + `lazyvstack.rs` per platform. PerryBridge.kt + the parallel `android-build/` template both gain the `setOnScrollEndCallback` static helper. Smoke test `test-files/test_issue_553_mobile_widgets.ts` exercises every new symbol so the cross-platform link surface is validated by the test suite. Gap suite holds at 26/28 (same 2 pre-existing fails — `test_gap_console_methods` timing diff, `test_gap_object_methods` strict-mode property descriptor gap).

- **v0.5.638** — **Refs #486** (cross-module method inlining loses class metadata for non-exported classes referenced inside the body): `lib3` exports `class C` with `populate() { this.children['a'] = new Inner(); }`, where `Inner` is a sibling class declared in the same module BUT not exported. The HIR-level inliner in `crates/perry-transform/src/inline.rs` harvested `populate` as a cross-module-inlinable method (the `is_cross_module_safe` filter only rejects FuncRef / ExternFuncRef / GlobalGet / Closure — NOT `Expr::New { class_name }` against an unexported local class), then copied the body verbatim into the consumer module's HIR. At codegen, the consumer's `ctx.classes` doesn't contain `Inner` (it's not in `imported_classes`), so `lower_new("Inner")` fell into the no-class-metadata fallback at `lower_call.rs:2440` which emits `js_object_alloc(0, 0)` — allocating a heap object with `class_id=0`. Subsequent `inst.method()` dispatch via the dynamic-dispatch tower at `lower_call.rs:1494` reads the receiver's class_id, finds 0, doesn't match any tower entry, and falls through to `js_native_call_method`'s vtable lookup which is gated on `class_id != 0` and returns NULL_OBJECT_BYTES. Net symptom: `root.populate()` ran, the inner instance was attached to `root.children['a']`, but `root.call()` then printed `[object Object]` instead of `'hello-inner'` (the value of `inner.method()`). Counter-control: moving `new Inner()` from `populate()` into `C`'s constructor body made the test pass — because constructor bodies aren't subject to the cross-module method inliner; they're emitted as standalone `<src>__C_constructor` symbols and called via direct `bl`. Fix adds `collect_nonexported_class_names(module)` and `body_references_class_in_set(stmts, set)` helpers to `crates/perry-transform/src/inline.rs`, and consults them in both `gather_cross_module_methods` and `gather_cross_module_methods_with_extern_imports` — methods whose body references any local non-exported class via `Expr::New`/`ClassRef`/`StaticFieldGet`/`StaticFieldSet`/`ClassStaticSymbolSet`/`StaticMethodCall` are excluded from the candidates map. Such methods stay as real cross-module method calls (`bl perry_method_<src>__C__populate`), where the source module's codegen — which DOES have the class metadata — emits the correct inline-alloc with the right class_id. `__AnonShape_*` content-addressed shapes are deliberately KEPT inlinable because they're already propagated via `extra_anon_classes` so the destination module synthesizes the same definition. End-to-end: minimal repro from /tmp/issue-486-followup (`new Inner()` inside `populate()`) now prints `result: hello-inner` matching Node byte-for-byte (was `result: [object Object]` pre-fix). Sanity tests `test_inheritance`, `test_gap_class_advanced`, `test_gap_closures`, `test_edge_promises` all match Node. Hono canonical `app.get('/', c => c.text('hello'))` still passes; logger middleware acceptance has a separate blocker chain past this release (the `(number).replace is not a function` failure inside `RegExpRouter.match → Trie.buildRegExp → Node.buildRegExpStr` is a different code path — Node IS exported from hono, so the inliner-side fix doesn't reach it).
- **v0.5.637** — **Refs #517 (followup)** — un-stubs the MapView widget on **tvOS / GTK4 / Android / watchOS** so the v0.5.636 API surface (`MapView(w, h)` + `mapViewSetRegion(lat, lon, latSpan, lonSpan)` + `mapViewAddPin(lat, lon, title)` + `mapViewClearPins()` + `mapViewSetMapType(0|1|2)`) lights up natively on every platform except Windows. Each platform replaces the silent-zero-handle stubs in `crates/perry-ui-<plat>/src/lib.rs` with real impls under `widgets/map_view.rs`. **tvOS** is a source-equivalent UIKit copy of the iOS impl — `MKMapView` works identically on Apple TV (Siri Remote drives focus-engine pan/zoom inside the view); also adds `-framework MapKit` to the tvOS arm of `crates/perry/src/commands/compile/link.rs`. **GTK4** wires up `libshumate` (GNOME's GTK4-native vector-tile widget) — the `set_region` impl converts MapKit's lat-span convention to libshumate's tile-zoom via `zoom = log2(360 / max(latSpan, lonSpan))` so the same TS code is portable; OSM-mapnik is the default tile source (no API key); markers go into a lazily-allocated `MarkerLayer` overlay; `clear_pins` drops the layer; `set_map_type(non-0)` logs an info line and stays at standard since the bundled `MapSourceRegistry` only ships one source. Cargo dep is `libshumate = "0.6"` (matches gtk4-rs 0.9 in the workspace — 0.5/0.7 conflict on the gtk-4 `links =` value). New pkg-config / hardcoded-fallback link block in `link.rs` for `shumate-1.0`, mirroring the existing GTK4 / GStreamer blocks. **Android** wires Google Maps SDK MapView via JNI calls into 4 new `PerryBridge.kt` static helpers (`mapViewCreate` / `mapViewSetRegion` / `mapViewAddPin` / `mapViewClearPins` / `mapViewSetMapType`) plus a `forwardMapsLifecycle(event)` hook. The Kotlin side queues early calls in a per-MapView pending-ops list and replays them once `getMapAsync` fires; `set_map_type` translates 0/1/2 → `MAP_TYPE_NORMAL` / `_SATELLITE` / `_HYBRID` (Google's enum is 1/2/3/4 with `_TERRAIN=3`, so the mapping is explicit). Activity lifecycle (onResume / onPause / onLowMemory / onDestroy) forwarded from `PerryActivity.kt` to keep MapView's stateful tile loading correct across rotation / background. Gradle dep `com.google.android.gms:play-services-maps:18.2.0` added to `template/app/build.gradle.kts`; `<meta-data android:name="com.google.android.geo.API_KEY" .../>` placeholder added to AndroidManifest.xml — user replaces `__YOUR_GOOGLE_MAPS_API_KEY__` with their own key from Google Cloud Console. **watchOS** extends the existing data-driven SwiftUI tree model: new `NodeKind::MapView = 16` + `map_lat / _lon / _lat_span / _lon_span / _type / _pins: Vec<(f64, f64, CString)>` fields on `NodeData`; 9 new FFI getters in `tree.rs` (`perry_watchos_node_map_lat` / `_lon` / `_lat_span` / `_lon_span` / `_type` / `_pin_count` / `_pin_lat(idx)` / `_pin_lon(idx)` / `_pin_title(idx)`); `swift/PerryWatchApp.swift` adds `import MapKit`, the matching `@_silgen_name` declarations, a `PerryMapPin: Identifiable` model, and a `mapView` SwiftUI subview rendering `Map(coordinateRegion:annotationItems:)` with `MapMarker` pins (deprecated-but-watchOS-7-compatible API for hardware reach — newer `Map { Marker(...) }` shape requires watchOS 10). `set_map_type` is recorded in the tree but ignored by the renderer since SwiftUI's watchOS Map doesn't expose `mapStyle` on watchOS 7-9. **Windows** is the one holdout — WinUI MapControl lives in the UWP / WinUI 3 world and hosting it inside Perry's Win32 HWND chain requires XAML Islands + Windows App SDK integration, a substantial body of work tracked as a separate follow-up (#559). Build verified on macOS host: workspace compiles clean (libshumate 0.6 resolves cleanly against gtk4-rs 0.9, no `gtk-4` links collision); `perry-ui-watchos` `cargo check` clean; minimal `MapView(w, h)` + `mapViewSetRegion` + `mapViewAddPin` + `mapViewClearPins` + `mapViewSetMapType(1)` smoke test compiles and the launched binary renders a real MKMapView on macOS. Cross-compile verification (NDK / GTK4-on-Linux / WatchKit) deferred to maintainer's host environments at merge time. Note: the parallel `android-build/` template at the repo root needs the same Kotlin / Manifest / Gradle changes — kept in sync at merge time per the v0.5.632 convention.
- **v0.5.636** — Closes #532 + #555: 11 native UI widgets across macOS/GTK4/Windows/iOS/visionOS via the `ui-loop-issues` PR — `widgetSetRichTooltip` (#479), `Combobox` (#475), `TreeView` (#480), `Calendar` (#481), `Chart` (#474), `commandPalette*` (#477), `MapView` (#517), `PdfView` (#516), `RichTextEditor` (#478), `Toast` watchOS gap-fill (#476), data-table sort + filter + multi-select extensions on the existing `Table` (#473). MacOS implementations are real (NSDatePicker / NSComboBox / NSOutlineView / NSPanel / NSSearchField / MKMapView / PDFView / NSTextView + CoreGraphics); GTK4 / Windows / iOS / visionOS un-stubbed for the widgets that fit each platform's UI conventions; tvOS / Android / watchOS deferred per platform fit. New linker entries for MapKit + PDFKit on macOS / iOS / visionOS in `crates/perry/src/commands/compile/link.rs`. Plus #555: `perry_ui_table_get_filter_text` had `-> f64` while `perry-dispatch`'s `tableGetFilterText` row declares `ReturnKind::Str` (expects `i64` raw NaN-boxed pointer in x0, not f64 in d0) — the round trip `tableSetFilterText("br")` → `tableGetFilterText()` → string concat → `textSetString(...)` panicked inside `NSString -initWithBytes:length:encoding:` because codegen read register x0 (stack garbage) instead of d0. Aligned `widgets/table.rs::get_filter_text` with the canonical `textfield::get_string_value` pattern: returns `*const u8` to a pinned StringHeader; FFI wrapper casts to `i64`. Pre-existing #556 (`Table` renderCell with string-concat closure body produces NSException at layout — `view_for_column` from commit `d7bcd0e4`, 2026-02-28, three months before this PR) tracked as a standalone follow-up since it's not introduced by this PR.

- **v0.5.635** — **Closes #557** (zero-arg `console.log()` emitted nothing — should emit a newline matching Node/bun): the codegen catch-all in `crates/perry-codegen/src/lower_call.rs` for the entire `console.*` surface returned `undefined` immediately when `args.is_empty()` for any non-special method (`groupEnd` and `clear` had explicit arms; `count`/`countReset`/`time*` fell through to a labeled-default handler), so `log`/`info`/`warn`/`error`/`debug`/etc. all silently no-op'd at zero arity. Cosmetically visible: `examples/collision-detection.ts` and several other examples use empty `console.log()` calls as section separators that disappeared under perry. Fix routes `log`/`info`/`debug` → `js_console_log_spread(0)`, `warn` → `js_console_warn_spread(0)`, `error` → `js_console_error_spread(0)`. All three runtime fns already short-circuit on null and print a single `\n` to the appropriate stream (stdout for log/info/debug, stderr for warn/error per Node convention). Unknown zero-arg console methods (`dir`, `assert`, etc.) keep printing nothing — no change vs main. New regression test `test-files/test_issue_557_console_log_empty.ts` covers the issue's repro plus zero-arg `info`/`debug` and a multi-arg sanity check. Gap suite holds at 26/28 (same 2 pre-existing fails — `test_gap_console_methods` timing diff, `test_gap_object_methods` strict-mode property descriptor gap).
- **v0.5.634** — **Closes #540** (`[...map]` spread produced garbage when source was a Map): `[...map]` lowered through `Expr::ArraySpread` codegen at `crates/perry-codegen/src/expr.rs:1781` which calls `js_array_concat(dest, src)`. The runtime had a Set-detection arm (`is_registered_set` → `js_set_to_array`) but no parallel Map arm, so `js_array_concat`'s loop walked the `MapHeader` as if it were an `ArrayHeader` — reading the `size` field (offset 0) as `length` and pulling values out of `(map_ptr + sizeof(ArrayHeader))` which on a `MapHeader` lands inside the entries-array pointer. Result was a single bogus f64 (`1.086462978432e-311` for the issue's repro — bit-reinterpretation of an entries-array pointer) instead of the `[key, value]` pair Array. `Array.from(map)` worked because it routes through `js_array_clone` which already had matching Map detection (added at the same time as the Set arm). Fix mirrors the Set arm in `js_array_concat`: when `is_registered_map(src)` is true, materialize `js_map_entries(src)` (already produces an Array of `[k, v]` pair Arrays — the exact shape `[...map]` should produce) and recursively concat that. End-to-end: issue's repro now matches Node byte-for-byte. New regression test `test-files/test_issue_540_spread_map.ts` covers single-entry undefined-value, multi-entry mixed-type, spread inside larger literal, `[...set]` regression, and empty Map. Gap suite holds at 26/28 (same 2 pre-existing fails).
- **v0.5.633** — **Closes #554 (followup)** — the v0.5.629 fix landed only on the top-level for-of lowering path in `crates/perry-hir/src/lower.rs`. The exact same `{ key: [a, b] }` for-of binding inside a function body (e.g. codehz/ecs `examples/serialization.ts::printWorldState`) still produced `pos = 0` because function bodies route through a parallel `ast::Stmt::ForOf` arm in `crates/perry-hir/src/lower_decl.rs` that had the same pair of structural gaps the top-level path had pre-fix: the pre-pass at line ~4012 walked `ObjectPatProp::KeyValue` props and only pre-defined leaf locals when the value was `Pat::Ident` (nested patterns silently fell through and `a`/`b` never entered `ctx.locals`); the binding-emit phase at line ~4184 had the matching gap (non-Ident KeyValue values dropped off the bottom of the match block, producing no `Stmt::Let` for the leaves). Fix promotes the v0.5.629 helpers (`collect_for_of_pattern_leaves` / `emit_for_of_pattern_binding`) from private to `pub(crate)` in `lower.rs` and wires them into the matching sites in `lower_decl.rs`'s function-body for-of arm — the same recursion shape that fixed the top-level path. Pre-pass now recurses into nested `Pat::Array`/`Pat::Object`/`Pat::Assign`/`Pat::Rest` values inside KeyValue; binding-emit routes non-Ident KeyValue values through `emit_for_of_pattern_binding` mirroring `destructuring::lower_pattern_binding_into` but reusing the pre-allocated leaf ids. Also picked up the `PropName::Str` arm that the top-level path got along the way (string-literal keys on object patterns now work in function bodies too). End-to-end: minimal repro `function printResults(items: typeof results) { for (const { entity, components: [a, b] } of items) … }` now matches Node byte-for-byte (was `a=0 b=0` pre-fix). Existing `test-files/test_issue_554_nested_destructure_for_of.ts` extended with three function-body cases (canonical, triple-nested, single-element `[pos]` mirroring the issue's followup repro). Gap suite holds at 26/28 (same 2 pre-existing fails — `test_gap_console_methods` timing diff, `test_gap_object_methods` strict-mode property descriptor gap). Note: nested patterns inside an array pattern (`for (const [a, [b, c]] of arr)` at function scope) still have the same structural gap at the `Pat::Array` arms here in `lower_decl.rs` (lines ~3992 and ~4073) — symmetric with the surviving gap noted in v0.5.629 and left for a separate change to keep blast radius small.

- **v0.5.632** — **Refs #552** (Android side of the mobile stdlib): replaces the v0.5.631 Android stub with a real LocationManager + Photo Picker pipeline. Five new public symbols on Android (`perry_system_geolocation_get_current` / `_watch` / `_stop_watch` / `_request_permission` / `image_picker_pick`) backed by `crates/perry-ui-android/src/{geolocation.rs,image_picker.rs}` which delegate via JNI to new `PerryBridge.kt` static methods (`requestGeolocationGetCurrent(successKey, errorKey)`, `requestGeolocationWatch(callbackKey): Long`, `stopGeolocationWatch(id)`, `requestGeolocationPermission(callbackKey)`, `requestImagePickerPick(maxCount, allowMultiple, callbackKey)`). Two new JNI invoke shapes added to `crates/perry-ui-android/src/callback.rs`: `invoke4(key, a, b, c, d)` for the 4-double geolocation success callback (lat/lng/accuracy/timestamp_ms) plus matching `Java_com_perry_app_PerryBridge_nativeInvokeCallback4` entry, and `invoke_with_string_array(key, paths)` plus `Java_..._nativeInvokeCallbackWithStringArray` for the image-picker callback (each path is allocated as a NaN-boxed Perry string and pushed into a `js_array_alloc` array; the array pointer is then NaN-boxed and passed as the sole arg). Geolocation flow: Kotlin checks `ACCESS_FINE_LOCATION` / `ACCESS_COARSE_LOCATION`, falls back to ACCESS_COARSE if FINE not granted, prefers `getLastKnownLocation` for low latency, falls back to `requestSingleUpdate` on the network provider (or GPS if network is disabled). Watch listeners stored in `watchListeners: MutableMap<Long, LocationListener>` keyed by an auto-incrementing watch id; Rust side blocks briefly on the UI thread via a `CountDownLatch` to obtain the synchronous return value. Image picker flow: API 33+ uses `MediaStore.ACTION_PICK_IMAGES` (Photo Picker — privacy-preserving, no permission required); older devices fall through to `ACTION_GET_CONTENT` with `EXTRA_ALLOW_MULTIPLE`. Selected URIs are copied via `contentResolver.openInputStream` into the app's cacheDir as `perry_pick_<ms>_<idx>.<ext>` (extension inferred from MIME type — jpg/png/gif/webp/heic/heif/bmp), absolute paths returned to JS so the user can `fs.readFileSync` or upload directly. Activity-result wiring: pre-existing FILE dialog had a documented but unwired `onFileDialogResult` — added `onActivityResult` override to `PerryActivity.kt` that routes both request 42 (file dialog) AND request 46 (#552 image picker) back to PerryBridge. Permission flow: new `GEOLOCATION_PERMISSION_REQUEST = 45` case added to `onRequestPermissionsResult`, so a `requestGeolocationPermission` that needs to prompt and a `requestGeolocationGetCurrent` that's stalled waiting on a permission both fulfill correctly through `onGeolocationPermissionResult(granted)`. AndroidManifest.xml gains `READ_MEDIA_IMAGES` (consumed only by the API 33+ ACTION_GET_CONTENT fallback path; the Photo Picker itself needs no permission). Pre-existing `request_location` 2-arg legacy API stays untouched. Both `crates/perry-ui-android/template/` and the parallel `android-build/` checked-in template are updated to keep the existing build flow's source of truth in sync. `cargo check --release -p perry-ui-android` passes; full NDK cross-build needs the toolchain on the developer's host (unchanged from main).

- **v0.5.631** — **Refs #552** (mobile stdlib gaps for production apps — geolocation + photo-library image picker + image compression): adds `geolocationGetCurrent` / `geolocationWatch` / `geolocationStopWatch` / `geolocationRequestPermission` / `imagePickerPick` to `perry/system`; geolocation success callback signature is `(lat, lng, accuracy, timestamp_ms)` with a separate `onError(message)` callback. Five `MethodRow` entries in `crates/perry-dispatch/src/lib.rs::PERRY_SYSTEM_TABLE` so the LLVM backend's table-driven dispatch resolves the names without per-platform codegen wiring; matching entries in `crates/perry-api-manifest/src/entries.rs` so the issue-#513 strict-API gate flips for them. Full implementation on **macOS** (CoreLocation via dynamic ObjC `PerryGeolocationDelegate` registration in `crates/perry-ui-macos/src/geolocation.rs`; `NSOpenPanel` filtered to image UTIs in `image_picker.rs`) and **iOS** (CLLocationManager via parallel `crates/perry-ui-ios/src/geolocation.rs`). Stubs that invoke `onError("unsupported-platform")` / empty-array on Android (replaced in v0.5.632 with the real impl), tvOS, watchOS, visionOS, GTK4, Windows via shared `issue_552_stub.rs` per crate. Image compression (third bullet of issue) verified end-to-end: `crates/perry-ext-sharp/src/lib.rs` already exposes `js_sharp_resize` + `js_sharp_jpeg(quality)` + `js_sharp_to_buffer` — covers `compressJpeg(buf, {quality, maxWidth, maxHeight})` via the npm `sharp` package through `perry.compilePackages` (issue #193 wiring confirmed; no new code needed). The non-native targets (Web/wasm) reach the catch-all browser warning since the geolocation FFIs aren't aliased in `crates/perry-codegen-js/src/emit.rs`'s `emit_system_method_call` — an explicit `navigator.geolocation` shim there would be a small follow-up. End-to-end smoke: `import { geolocationGetCurrent } from "perry/system"; geolocationGetCurrent((lat, lng, acc, ts) => console.log(lat, lng), e => console.error(e))` compiles cleanly on every platform; on macOS it delivers a real CoreLocation reading after the user accepts the OS permission prompt. Plus a `collect_modules.rs` change (`needs_ui = true` flips when `perry/system` imports any of the audio-recording / geolocation / image-picker symbols, namespace imports, or the catch-all) so the libperry_ui_*.a archive actually links — pre-fix the symbol set lived in the platform UI crates but `needs_ui` only flipped on `perry/ui` / `perry/media`, leaving programs that import only `perry/system` with `Undefined symbols` at link time.
- **v0.5.630** — **PR #534 (dazhe)** — `codegen-wasm`: a user function named `main` whose body has no `return` produced an invalid Wasm module — `func_section` declared `main` as `() -> i64` whenever the function name is `main` (lines 1338 and 1557 of `crates/perry-codegen-wasm/src/emit.rs`), but `compile_function` (line 2076) only emitted the trailing `TAG_UNDEFINED` constant when `has_return(body)` was true. Fallthrough left the operand stack empty at the final `End`, so the declared `i64` result type didn't match — `WebAssembly.compile` / `instantiate` rejected the module with a stack-depth mismatch. Fix: align the body emission predicate with the type-declaration predicate (`hir_func.body.iter().any(has_return) || hir_func.name == "main"`). Plus extend `has_return` to recurse into `Stmt::DoWhile`, `Stmt::For.init` (an `Option<Box<Stmt>>`), and `Stmt::Labeled` — previously the bare `For { body, .. }` pattern only walked `body`, and `DoWhile` / `Labeled` had no arm at all, so any `return` reachable only through those paths was invisible to the signature decision and led to the symmetric pre-fix bug (signature said no result, body left a value on the stack). The fix is symmetrical with the `func_section` predicate already present at the type-declaration sites.
- **v0.5.629** — **Closes #554** (nested destructuring in for-of `const` binding produced 0 instead of array elements): `for (const { entity, components: [a, b] } of results)` bound `a` and `b` to literal 0 because `crates/perry-hir/src/lower.rs`'s for-of binding handler had two parallel gaps in its object-pattern path. (1) Pre-pass at line ~5952 walked `ObjectPatProp::KeyValue` props and only pre-defined leaf locals when the value was `Pat::Ident`; nested patterns (`components: [a, b]`) silently fell through and `a`/`b` never entered `ctx.locals`, so the body lowering reported `unknown identifier 'a' — assuming global` and emitted a literal 0 read. (2) Binding-emit phase at line ~6147 had the matching gap — non-Ident KeyValue values fell off the bottom of the match block, producing no `Stmt::Let` for the leaves. The pure-array form `for (const [a, b] of arr)` and the two-step workaround `const { entity, components } = ...; const [a, b] = components;` both worked because the latter routes through `crates/perry-hir/src/destructuring.rs::lower_pattern_binding_into` which already handles arbitrary nested patterns recursively. Fix adds two helpers in `lower.rs` next to `lower_stmt`: `collect_for_of_pattern_leaves` walks an arbitrary destructure pattern and pre-defines every leaf identifier (handling `Array` / `Object` / `Assign` / `Rest` recursively, plus shorthand `Assign` props inside `Object`); `emit_for_of_pattern_binding` mirrors `destructuring::lower_pattern_binding_into` but reuses the pre-allocated leaf ids passed in `var_ids` (in source order) instead of calling `define_local` itself, so the loop body — already lowered against those ids — sees the correct bindings. The KeyValue arms in both pre-pass and binding-phase now route non-Ident values through these helpers. Array-pattern temp's `ty` is `Type::Array(Box::new(Type::Any))` (not `Type::Any`) to match `destructuring.rs`'s convention; the wrong type pulled OOB reads through the typed-element fast path which returned 0 and broke `[first, second = 99]` default-value handling. End-to-end: issue's repro `for (const { entity, components: [a, b] } of results)` now prints `a={"x":50,"y":30} b={"x":1,"y":2}` matching bun. New regression test `test-files/test_issue_554_nested_destructure_for_of.ts` covers the canonical case plus triple-nesting (`components: [{ x: ax }, { x: bx }]`), nested-with-default, and nested-with-rest. Gap suite holds at 26/28 (same 2 pre-existing fails — `test_gap_console_methods` timing diff, `test_gap_object_methods` strict-mode property descriptor gap). Note: nested patterns *inside* an array pattern (`for (const [a, [b, c]] of ...)`) still have the same structural gap at the `Pat::Array` arms (lines ~5923 and ~6014); not in the issue-#554 repro and left for a separate change to keep blast radius small.
- **v0.5.628** — **Closes #549** (codehz/ecs followup — Set/Map object key collisions): `set.add({}); set.add({})` returned size 1, `map.set(a, "a"); map.set(b, "b")` returned `"b"` for both keys, and same-shape objects (`{x:1}` / `{x:1}`) collided. `is_string_like` in both `crates/perry-runtime/src/set.rs` and `.../src/map.rs` returned `true` for any POINTER_TAG value because `extract_string_ptr_from_value` accepted the tag without validating the GC type at the pointee. The follow-on `jsvalue_eq` content-comparison branch then reinterpreted each `ObjectHeader` as a `StringHeader`: `class_id` (offset 4) became `byte_len`, the data pointer landed at offset 20 (size_of::<StringHeader>) which falls inside `keys_array`'s upper 4 bytes plus whatever was past the 24-byte `ObjectHeader`, and two empty `{}` literals (same class_id = 0, no fields) compared as equal because zero-length string slices match. Same root cause for the same-shape `{x:1}` collision (both have identical class_id from the AnonShape cache plus a single field). Fix: validate the GC header in `is_string_like` so only `GC_TYPE_STRING` pointees enter the content-comparison path. STRING_TAG (0x7FFF) values are accepted unconditionally (the tag is itself the type proof); POINTER_TAG (0x7FFD) and raw pointers are now subjected to a `(ptr - GC_HEADER_SIZE)->obj_type == GC_TYPE_STRING` check before content comparison. Non-string POINTER_TAG values (objects, closures, maps, etc.) fall through to the bit-identity fast path that JS Set/Map `SameValueZero` semantics call for on object keys. End-to-end: codehz's repro (`set.add(a); set.add(b)`) now reports size 2 matching bun; `map.get(a)` / `map.get(b)` return `"a"` / `"b"`; same-shape-after-mutation paths produce 4 entries; `Map.get(JSON.parse('"hello"'))` (issue #434 cross-representation string equality) still works; SameValueZero `0` / `-0` collapse still works; mixed object+string keys still dispatch to the right buckets. Map/Set test suite (`test_edge_map_set`, `test_gap_map_set_extended`, `test_edge_closure_module_map`) all pass; gap suite holds at 26/28 (same 2 pre-existing fails — `test_gap_console_methods` timing diff, `test_gap_object_methods` strict-mode property descriptor gap).
- **v0.5.627** — **Closes #546, #547, #548** (codehz/ecs followup): three issues from the codehz/ecs library all rooted in array-method dispatch on Any-typed receivers with one defensive JSON.stringify cleanup. **#546** (`world.hook` on_set never fires while on_remove works): `triggerMultiComponentHooks` in `src/world/hooks.ts` does `requiredComponents.some(c => anyComponentMatches(addedComponents, c))` where `requiredComponents` is destructured out of an `entry: LifecycleHookEntry` parameter — the destructured local lost its `EntityId<any>[]` type and ended up as `Type::Any`. The HIR fast-path lowering at `crates/perry-hir/src/lower/expr_call.rs:2766` (ident-receiver) bails on `is_unknown_recv && is_class_overlapping_method` for `some`/`every`/`find`/`findIndex`/etc. (so user-class methods with the same name still dispatch correctly), and the runtime fallback `js_native_call_method` had arms for `map`/`filter`/`forEach`/`slice`/`with` but **not** `some`/`every`/`find`/`findIndex`/`findLast`/`findLastIndex`. The `arr.some(cb)` call then fell through to the object field-scan and returned the array itself, producing `typeof = object` instead of a boolean — every `shouldTriggerSet` evaluation got a truthy non-boolean for `anyRequiredAdded`, but the chain `enteredMatchingSet || (...)` short-circuited on the typeof-object value. on_remove worked because that branch's logic happens to fire on a different code path that didn't depend on the `some` result. Two coordinated fixes: (a) `crates/perry-runtime/src/object.rs::js_native_call_method` adds the missing array-method arms for any-typed receivers (mirrors the existing #267/#493/#291/#515 arms — gates on `obj_type == GC_TYPE_ARRAY || GC_TYPE_LAZY_ARRAY`, dispatches directly to `js_array_some` / `js_array_every` / `js_array_find` / `js_array_findIndex` / `js_array_find_last` / `js_array_find_last_index`; `findIndex` / `findLastIndex` return `i32` so cast through `as f64` for the f64 ABI). (b) `crates/perry-hir/src/lower/expr_call.rs` adds `"some"` and `"every"` arms to the member-receiver match block at line ~3988 — siblings of the existing `"map"` / `"filter"` / `"find"` / `"findIndex"` arms that `e.arr.some(...)` previously fell past, lowering directly to `Expr::ArraySome` / `Expr::ArrayEvery`. End-to-end: codehz/ecs `examples/state-machine.ts` now emits all `[SET]` lines (28 lines under bun, 0 under perry pre-fix); `world.hook([Health], { on_set, on_remove })` fires both callbacks; minimal repro `const { arr } = e; arr.some(p)` returns boolean true matching Node. **#547** (wildcard relation queries on `dontFragment` relations): repro from issue passes on v0.5.624 already — apparently fixed by the v0.5.624 Map-iteration-through-optional-Map fix (#541/#542/#543). `examples/inventory-system-relations.ts` and `examples/parent-child-hierarchy.ts` now match `bun run` (modulo Map iteration ordering). Closing as already-fixed against the v0.5.622 repro. **#548** (JSON.stringify panic at `crates/perry-runtime/src/json.rs:1489`: "byte index N is not a char boundary; it is inside '\0'"): the exact repro from the issue passes on v0.5.624 in my environment, but the panic site IS reachable in principle — `str_from_header` uses `from_utf8_unchecked` on bytes from a StringHeader pointer, and the GC-type fallback heuristic in `stringify_value`'s `_ =>` arm can misclassify an unrecognised pointer as a string when the array-shape sniff fails. If the underlying bytes aren't valid UTF-8, the resulting `&str` triggers UB at the first `&s[start..i]` slice operation that lands mid-multibyte. Defensive fix in `write_escaped_string`: switch the slow-path slice operations from `&s[start..i]` (UTF-8-boundary-checked) to `buf.as_mut_vec().extend_from_slice(&bytes[start..i])` (raw bytes). The fast path stays unchanged (`buf.push_str(s)` on valid UTF-8 inputs). Output is byte-identical for valid UTF-8 input; non-UTF-8 input writes through verbatim instead of aborting the process. Replaces a SIGABRT with at-worst an ill-formed UTF-8 String — every consumer in the codebase treats stringify output as a byte stream. Parity verified: existing gap tests (`test_gap_array_methods`, `test_gap_class_advanced`, `test_gap_closures`) all match Node byte-for-byte; codehz/ecs `examples/serialization.ts` JSON-roundtrip step no longer panics (separate Position-data-not-propagating bug remains, out of scope).
- **v0.5.626** — **Closes #420** (drizzle-orm compiles + runs end-to-end via `compilePackages`): two layered fixes get `pgTable("users", { id: serial("id").primaryKey(), name: text("name") })` + `eq(users.name, "alice")` + `and(eq(...), gt(...))` byte-for-byte matching Node. (1) `crates/perry-codegen/src/expr.rs::Expr::ExternFuncRef`: when an imported binding name resolves in `ctx.class_ids` (populated from `opts.imported_classes` for cross-module classes too), emit the same INT32-tagged class-id NaN-box that local `Expr::ClassRef` produces. Pre-fix, `import { Table } from "drizzle-orm"` lowered to a closure-pointer NaN-box (POINTER_TAG), so `Object.prototype.hasOwnProperty.call(Table, entityKind)` consulted the regular-object Symbol-property path which returned false on every drizzle ancestor — `is(value, type)` then fell through to the "doesn't look like an entity" throw at every brand-check. With the INT32-tagged route, the receiver is recognised as a class ref and `js_object_has_own`'s class-static-symbol branch (v0.5.622) consults `CLASS_STATIC_SYMBOLS` correctly. Side effect: `typeof ImportedClass` reports "number" instead of "function" — same documented tradeoff as v0.5.622's local `ClassRef`. (2) `crates/perry-hir/src/lower.rs` `is_exportable` matcher (the gate that decides whether a `const X = ...; export { X }` shape lands in the module's `exported_objects` and from there the importer's `imported_vars` set): pre-fix the matcher had arms for `Closure`, `Object`, `Array`, `Call`, `New`, `LocalGet`, `FuncRef`, `ExternFuncRef`, `PropertyGet`, primitive literals — but **not** `Expr::SymbolFor`. drizzle's `entity.js` declares `const entityKind = Symbol.for("drizzle:entityKind")` followed by `export { entityKind }` — the init is `Expr::SymbolFor(...)` which fell off the matcher and the const never reached `exported_objects`. Importing modules then saw `entityKind` outside `imported_vars` and routed every read through the closure-pointer fallback (returning the closure global address as if it were the symbol's bits). Static-symbol registration in `class_register_static_symbol` saw a non-Symbol pointer for the key, the magic-check failed, `sym_key` came back 0, and the `(class_id, sym_key)` insert was rejected — so even though both halves of the v0.5.622 wiring existed, nothing ever made it into the side table. Adding `Expr::SymbolFor(_)` to the `is_exportable` arm threads the const through correctly. End-to-end smoke: drizzle's `test_query.ts` (the issue-#420 minimal) now prints `condition built: object\nok` matching Node byte-for-byte. The full pgTable + eq/gt/and chain works without any drizzle-side throws. Parity: 211 pass / 3 known fails / 13 skipped (98.5%, +1 vs main; the 3 fails are pre-existing).

- **v0.5.625** — Refs #420 (drizzle followup — class field initializers must run AFTER parent ctor body, not before): pre-fix, perry applied ALL field initializers (root → leaf) at the TOP of constructor lowering, BEFORE any constructor body ran. drizzle's `class PgText extends PgColumn extends Column { enumValues = this.config.enumValues }` then read `this.config` (set in Column's ctor body via `this.config = config`) before Column's body ever ran — `this.config` was undefined, the field init threw `TypeError: Cannot read properties of undefined (reading 'enumValues')`, and `pgTable("users", { name: text("name") })` blew up at `colBuilder.build(rawTable)` time. Per ECMAScript spec, derived-class field initializers run AT THE TOP of [[Construct]] but logically AFTER super() returns — i.e. after the parent body has run. Fix interleaves field-init application with body inlining across three lowering paths: (1) `crates/perry-codegen/src/lower_call.rs` — new `FieldInitMode` enum (`All` / `AncestorsOnly` / `SelfOnly`) on the existing `apply_field_initializers_recursive`. `lower_new` now applies `AncestorsOnly` up front, then inlines the chosen body (own-ctor or walked-parent), then applies `SelfOnly` for the leaf class after the parent-body chain has run. Base classes (no extends) keep the simpler "All up front" path. (2) `crates/perry-codegen/src/expr.rs::Expr::SuperCall` — after inlining the parent body inside an own-ctor body, applies the current class's `SelfOnly` field inits so the JS-spec "fields run immediately after super()" semantics is preserved. (3) `crates/perry-codegen/src/codegen.rs` standalone-ctor emit (used for cross-module class metadata) — uses `AncestorsOnly` for derived classes plus a `SelfOnly` apply after the explicit parent-ctor symbol call (no-own-ctor case). (4) `crates/perry-codegen/src/stmt.rs` scalar-replacement path — independent bug discovered along the way: `Stmt::Let { init: Expr::New { .. } }` for non-escaping construction took its own scalar-replace fast path that **never inlined the parent body when the leaf had no own ctor** — `class.constructor.clone()` returns `None` for default-ctor classes, so the parent body was simply skipped. Updated to mirror `lower_new`'s logic: walk parent chain, inline first ancestor body with forwarded args, then apply leaf `SelfOnly`. End-to-end smoke: minimal 2-deep + 3-deep repros (`class Leaf extends Mid extends Base { enumValues = this.config.enumValues }`) match Node byte-for-byte; drizzle's `test_query.ts` no longer throws the enumValues TypeError (progress past the brand-check + column-builder chain exposes a downstream SIGSEGV in the query-builder phase tracked separately). Sanity tests `test_inheritance` and `test_gap_class_advanced` still pass byte-for-byte. Parity unchanged: 210 pass / 3 known fails / 13 skipped (98.5%).

- **v0.5.624** — **Closes #541, #542, #543** (codehz/ecs library — three-issue stack reduces to two coordinated HIR fixes once the surface symptoms are unwound). #541 (link error: `@codehz/ecs` package name passed to `ld` as a response file) was already addressed in v0.5.522 — `read_app_metadata` in `crates/perry/src/commands/run.rs:1497` strips `@` and `/` via `sanitize_app_name`. Closing as already-fixed against the v0.5.503 repro. #542 (entity ID corruption from `world.spawn().build()`) and #543 (`query.forEach` callback never fires) collapse to the same root cause once the false leads are followed: codehz/ecs encodes relations as `-(componentId * 2^42 + targetId)` — negative f64s outside i32 range — and stores them as keys in `Map<EntityId, any>` parameters typed `Map<K, V> | undefined`. Two HIR-lowering bugs corrupted iteration of those Maps. **(a)** `crates/perry-hir/src/lower/expr_call.rs:2991` (the array-method fast path's `entries`/`keys`/`values` arms) unconditionally folded the call to `Expr::ArrayKeys`/`ArrayValues`/`ArrayEntries` when the receiver type was unknown. `keys`/`values`/`entries` are shared between Array, Map, and Set — for an `Any`-typed receiver (e.g. an interface method return) the optimistic fold ran `js_array_keys` against a real `MapHeader`, which read `size` as `length` and produced `[0..N-1]` (a 1-entry Map produced `[0]`, masquerading as a valid array of one zero key). Replace the unconditional fall-through with a Union-aware Map/Set/Array detector — only emit `ArrayKeys`/`ArrayValues`/`ArrayEntries` when the receiver is statically known to be `Type::Array(_)` or `Type::Tuple(_)`; otherwise fall through to the runtime `js_native_call_method` dispatcher whose `is_registered_map` check picks the right path. Apply the same Union check to the `forEach` arm (line 2740). **(b)** `crates/perry-hir/src/lower_decl.rs:3815` and `crates/perry-hir/src/lower.rs:5709` (the for-of fast path's `is_iterable_map` / `is_iterable_set` checks) only matched the bare `Type::Generic { base: "Map", .. }` shape. A `Map<K, V> | undefined` parameter shows up as `Type::Union([Generic{Map}, Void])`, even after the `if (!m) return;` narrowing guard — perry doesn't propagate the narrow through the union type. The for-of fell through to array iteration, treating the Map as Array-of-`__arr.length`-many elements indexed via `IndexGet(__arr, i)` — produced zero iterations or yielded swapped/garbage `[k, v]` pairs depending on which f64 bytes were aliased. Extend `is_iterable_map`/`is_iterable_set` to also accept `Type::Union(variants)` where any variant is `Generic{Map}`/`Generic{Set}`. The companion `arr_expr` wrapping match in both files is rewritten to dispatch on the new flags directly (so the Union shape gets `MapEntries`/`SetValues` wrapping when the fast path doesn't apply). The K/V type-args extraction in `lower.rs` walks the Union to find the Map variant. End-to-end: codehz/ecs at commit 159e292 — `examples/inventory-system-relations.ts`, `examples/parent-child-hierarchy.ts`, `examples/spatial-grid.ts`, `examples/tag-filtering.ts`, `examples/state-machine.ts`, `examples/collision-detection.ts`, `examples/advanced-scheduling.ts`, `examples/simple.ts` — now all match `bun run` byte-for-byte (modulo trailing-blank-line whitespace and one Map-iteration ordering difference that's both spec-allowed). `examples/serialization.ts` still has a separate JSON/TypedArray encoding mismatch beyond this release's scope. New regression test `test-files/test_issue_542_map_keys_optional.ts` exercises both paths against Node. Parity: 207 pass / 3 known fails / 13 skipped (98.5%, no regressions vs main; the 3 environmental compile failures for `test_issue_248_phase2_*` / `test_issue_255_jsruntime_reentrancy` are unrelated `libperry_jsruntime.a not found` build-dep issues that also fail on main).
- **v0.5.623** — Refs #535 (Layer 2 — `NavStack(state, routes)` lowering for native multi-screen): builds on v0.5.621's Layer 1 desugar to make the documented multi-screen pattern (`types/perry/ui/index.d.ts:370-374`) actually work on macOS/iOS/Android/GTK4/Windows. Pre-fix the 2-arg form silently fell through to the 0-arg `perry_ui_navstack_create()` and the state/routes args were dropped — the app showed an empty NavStack and `route.set("detail")` had no effect. Three coordinated changes: (1) `crates/perry-transform/src/state_desugar.rs` adds a `try_rewrite_navstack` arm that detects `NavStack(LocalGet(state), Array([{name, body}, ...]))` for any state-bound local and rewrites it to an IIFE (closure-as-`Call` with empty args, same shape as `try_desugar_reactive_text` at `lower.rs:7367`) whose body allocates the host via 0-arg `NavStack()`, then for each route binds the body widget to a fresh local, calls `widgetAddChild(host, body)`, and emits a synthetic `__navstack_register_route("__state_X", "name", body)` call. Routes with non-string `name` literals or non-canonical shapes silently bail to the 0-arg fallback. Fresh `LocalId`/`FuncId` allocation via new `compute_max_local_id` / `compute_max_func_id` walkers (mirrors `async_to_generator.rs:204`'s shape but exhaustively scans class methods + try/catch/switch arms so allocations don't collide with `ctx.fresh_local()` ids). (2) `crates/perry-codegen/src/lower_call/native.rs` adds the `__navstack_register_route` lowering arm: lowers the body to a widget handle, unboxes to i64, calls the runtime FFI, returns the body unchanged so the caller can keep chaining. (3) `crates/perry-runtime/src/ui_text_registry.rs` adds the `NAVSTACK_REGISTRY: Mutex<HashMap<String, Vec<NavRoute>>>` per-state-id route table, the `js_navstack_register_route(synth_id, name, handle)` FFI (records + sets initial visibility by hiding the widget if its name doesn't match the current state value), the `SetWidgetHiddenHandler` registration FFI (`js_register_widget_hidden_handler`) called by perry-ui-macos at app startup with a thin wrapper around `widgets::set_hidden`, and a `navstack_dispatch_state_change` hook called from `js_state_set` after every state write that walks the registered routes for the changed synth id and toggles each route's visibility through the platform handler. End-to-end: `NavStack(route, [{name: "home", body: ...}, {name: "detail", body: ...}])` lowers to the construction IIFE, builds both screens at App-build time, hides the inactive one at first paint, and `route.set("detail")` flips visibility through the registered handler chain. Visual verification of the running app requires a GUI session (compile-time HIR rewrite + runtime registration both verified). Skipped routes / dynamic state / nested NavStacks: each route's body is evaluated eagerly at App-build time so closures referenced from buttons stay alive; routes with computed (non-literal-string) names bail to the 0-arg fallback for now. The arkts harvest's existing visibility-binding pass stays the source of truth on harmonyos (gated off in `collect_modules.rs`).
- **v0.5.622** — Refs #420 (drizzle entityKind hasOwnProperty): pre-fix, `Object.prototype.hasOwnProperty.call(SomeClass, Symbol.for("entityKind"))` always returned `false` on classes carrying `static [Symbol.for(...)] = "..."` fields, because perry's `lower_decl.rs` `lower_class_prop` skipped computed-key static fields entirely (only the instance variant was lowered). drizzle's `is(value, type)` brand-check (used by `eq()`/`gt()`/`pgTable()` internally) hit this path on every Column / Table / Selection ancestor, returning false at the first ancestor and falling through to the "doesn't look like an entity" path which threw further downstream. Six coordinated edits: (1) `lower_decl.rs` no longer skips computed-key static fields — they flow through `lower_class_prop` like instance fields, populating `class.static_fields` with `key_expr: Some(...)`; (2) new HIR variant `Expr::ClassStaticSymbolSet { class_name, key, value }` in `ir.rs` + walker arms in `walker.rs`; (3) `lower.rs` emits `ClassStaticSymbolSet` from both class-decl arms (export and non-export, lines 4123 / 5174) when a static field has a `key_expr`; (4) `codegen.rs` `static_field_globals` skips computed-key static fields (no global slot — they live in a side table) and `init_static_fields` dispatches them to a new `js_class_register_static_symbol(class_id, sym, val)` extern; `Expr::ClassRef` returns the class id NaN-boxed with `INT32_TAG` so it's distinguishable at runtime; (5) `runtime_decls.rs` declares the new extern; (6) `symbol.rs` adds `static CLASS_STATIC_SYMBOLS: Mutex<HashMap<(class_id, sym_key), value_bits>>` populated by `js_class_register_static_symbol` at module init, plus `class_static_symbol_lookup(class_id, sym_f64)` consumer. `object.rs::js_object_has_own`'s Symbol-key branch checks if the receiver is an INT32-tagged class ref — if so, look up `(class_id, sym_key)` in `CLASS_STATIC_SYMBOLS` and return TAG_TRUE / TAG_FALSE accordingly; otherwise fall through to the existing `js_object_has_own_symbol` instance-Symbol-property path. End-to-end smoke: `class C { static [Symbol.for("k")] = "hello" }; Object.prototype.hasOwnProperty.call(C, Symbol.for("k"))` returned `false` pre-fix, returns `true` post-fix matching Node. drizzle's `test_query.ts` no longer throws "doesn't look like an entity" at brand-check time — progress past `is()` exposes a downstream `enumValues` undefined read (separate followup, not in this fix). The static-field VALUE on the perry side is the actual runtime value (works for the brand-check return), but `(C as any)[sym]` to read it back through normal property access still returns undefined since the Symbol-keyed get path on class refs isn't wired — left for a follow-up since drizzle's pattern only needs the `hasOwnProperty.call` side.

- **v0.5.621** — Refs #535 (Layer 1 — `perry/ui` `state<T>` desugar for non-HarmonyOS targets): pre-fix, `state<T>(initial)` had no codegen lowering on macOS / iOS / Android / GTK4 / Windows and the strict-API gate (#513) hard-errored with `'state' is not a known function`. Only `--target harmonyos` worked, because `perry-codegen-arkts::emit_index_ets` runs its own harvest that rewrites these shapes into `setText` calls before LLVM ever sees them; the d.ts at `types/perry/ui/index.d.ts:263-265` documented this as "deferred to v6.5", which blocked any multi-screen native app per #535. New `crates/perry-transform/src/state_desugar.rs` ports the arkts pass's `collect_state_bindings` + `rewrite_state_calls_in_stmts` (lib.rs:1233 / :3725) into a target-agnostic HIR rewrite that runs in `collect_modules` for every target except harmonyos: walks `module.init` for `let x = state(initial)` declarations, assigns each a synth id (`__state_<N>`), replaces the let initializer with `Expr::Undefined` so any not-yet-rewritten escape stays compilable, prepends a synthetic `__state_init("synth_id", initial)` call to seed the runtime registry, then rewrites every `x.set(v)` / `x.get()` / `x.value` / `x.text()` access (recursively into closure bodies and nested expressions — children-first traversal so `state.set(state.get() + 1)` evaluates the inner read once and pre-rewrite). Three new perry/ui method arms in `crates/perry-codegen/src/lower_call/native.rs` lower the synthetic methods (`__state_init` / `__state_set` → `js_state_init` / `js_state_set`; `__state_get` → `js_state_get`). Three new FFIs in `crates/perry-runtime/src/ui_text_registry.rs` back them with a `STATE_VALUES: Mutex<HashMap<String, f64>>` registry; `__state_set` writes through to the existing `perry_arkts_set_text` so any `Text("initial", "<synth_id>")` widget bound under the same id (via `perry_ui_text_create_with_id` → `text_registry.rs::register_text_id`) re-renders. End-to-end: issue #535's repro `state.text()` form lowers to `Text("loading", "__state_0")` and `state.set("home")` triggers the bound widget update through the existing setText pipeline. Limitations: only matches `state(...)` declared at the top level of `module.init` (function-body declarations TBD); only matches the canonical method-call shapes (state escaping through a function arg or array misses the rewrite, but the `let x = undefined` shim keeps the program compilable); `.text()` snapshots the literal initial-value string at compile time (computed initials get an empty initial, corrected at runtime by the first `.set()`). Layer 2 (visibility binding + `NavStack(state, routes)` for native multi-screen) is the next iteration.
- **v0.5.620** — Refs #486 (forward-port of the unmerged v0.5.614 hono branch onto main): three layered fixes for `app.get('/', c => c.json({...}))` end-to-end via `app.fetch(req)`, now combined with the v0.5.617 cross-module-class-metadata fix so the dependency tree from the original branch lines up against current main. (1) Class fields declared without an initializer (`#x;` / `x: any;`) used to leave the field slot uninitialized; arrow-class-field bodies that load `this.x` through the captured-`this` slot read raw zero bytes and `0 ?? fallback` took the wrong branch (0 is falsy but not nullish). Hono's `Context.#newResponse` does `this.#preparedHeaders ?? new Headers()`, evaluated to `0`, and `responseHeaders.set(k, v)` threw `(number).set is not a function`. `apply_field_initializers_recursive` in `crates/perry-codegen/src/lower_call.rs` now writes `Expr::Undefined` for declared-but-uninitialized fields (both string- and computed-key forms — merged with v0.5.614's `key_expr` handling). (2) `response.headers` returned `undefined` because `dispatch_response_property` in `crates/perry-stdlib/src/fetch.rs` had no `"headers"` arm. Added a lazy-allocated cached Headers handle on `FetchResponse` (snapshot copy of the response's HashMap; spec-perfect "live header view" that mutates back through FetchResponse storage is a wider refactor). (3) Cross-module class getters — e.g. hono's `get req()` declared in `hono/dist/context.js` and read from `c.req.url` in main.ts — were never registered in `CLASS_VTABLE_REGISTRY`. Codegen now emits `js_register_class_getter(class_id, name, fn_ptr)` for every defined getter alongside the existing method-registration loop, and `js_object_get_field_by_name` consults `vtable.getters` (walking the class→parent chain via `get_parent_class_id`) before falling through to undefined. With v0.5.617's relative-import-inside-compile-package fix already on main, these three changes now have the cross-module class metadata they need to actually fire for hono. Verified: `app.get('/', c => c.json({message: 'hello', ok: true}))` returns status 200 + `application/json` content-type + correct body. Logger middleware acceptance still has a separate blocker chain past this release.
- **v0.5.619** — Closes #533: `for (const [k, v] of c)` silently produced zero iterations when `c = m.get(k)!` and `m` was a Map-of-Maps. Two coordinated type-inference gaps in `crates/perry-hir/src/lower_types.rs`: (1) `infer_call_return_type` had no `Map.get` / `Set.has` / etc. arms — receiver-typed-as-`Map<K,V>` `.get(...)` returned `Type::Any`. (2) `new Map([entries])` without explicit `<K, V>` returned `Type::Generic { base: "Map", type_args: vec![] }` even when the literal entries arg made K and V trivially recoverable, so even with the Map.get arm there'd be nothing to forward. Together: `b.get(0)!` lowered with `Type::Any`, the for-of arm at `lower_decl.rs:3815` (`is_iterable_map`) didn't match, the loop fell through to the array `__arr.length` path which read 0 from a Map and skipped the body. Fix: add `infer_map_entries_type` / `infer_set_elements_type` walkers that scan the first non-elided element of the literal entries arg (Map's first 2-tuple → K,V; Set's first elem → T) and feed Generic.type_args; add Map/WeakMap/Set/WeakSet method-return arms to `infer_call_return_type` matching the existing `Array<T>.pop() → T` (not `T|undefined`) precedent so downstream type-driven dispatch sees the right element type. End-to-end: original repro now prints `0 1` matching Node. The annotation-only escape hatches users were previously forced to write (`const c: Map<...> = b.get(0)!`) are no longer required. The console-log formatter still shows `Map {}` instead of expanding entries — that's a separate `format_jsvalue` Map-pretty-print bug, not the for-of bug. Parity: 210 pass / 3 known fails / 13 skipped (98.5%, no regressions vs main).
- **v0.5.618** — Refs #420 (drizzle-orm followup — `Object.prototype.hasOwnProperty.call` idiom): perry doesn't expose `Object.prototype` as a real object, so the chain `Object.prototype.X` reads as undefined and `.call` throws `TypeError: Cannot read properties of undefined (reading 'call')`. v0.5.612-era code already special-cased `Object.prototype.toString.call(x)` → `js_object_to_string(x)` at HIR lowering; this release adds the parallel arm for `Object.prototype.hasOwnProperty.call(o, k)` → `js_object_has_own(o, k)`. Refs #420 because drizzle's `is(value, type)` in `entity.js` uses this exact idiom on every entityKind check — without the rewrite, `new SQL([…])` (called inside `eq()`, `gt()`, every comparison helper) crashes with the `.call` throw before drizzle's actual logic runs. Plus a runtime fix: `js_object_has_own` previously coerced the key to a string and returned false for Symbol keys; now dispatches Symbol-typed keys through a new `js_object_has_own_symbol` helper in `crates/perry-runtime/src/symbol.rs` that consults the `SYMBOL_PROPERTIES` side table directly. **Partial unblock**: the `.call` crash is gone, but drizzle's `is(value, type)` still returns false for `is(stringChunk, Table)` because `Table.[entityKind]` is a static field with a Symbol-typed COMPUTED key (`static [entityKind] = "Table"`), and perry's HIR lowering at `lower_decl.rs::lower_class_from_ast` explicitly skips static computed-key fields. drizzle's `is` then throws `Class "<unknown>" doesn't look like a Drizzle entity`. test_query.ts moves from a SIGSEGV to a clean drizzle-thrown exception — meaningful structural improvement but doesn't unblock the full path yet. Implementing static computed-key Symbol fields on classes (write `(class_id, sym) → value` to a side table at module init, consult from hasOwn for class-ref receivers) is the natural next step. The string-key form of `Object.prototype.hasOwnProperty.call` (the more common idiom in npm packages — lodash, jquery, every safe-property-checker library) IS fully unblocked by this release.
- **v0.5.617** — Refs #486 (hono middleware followup — cross-module classes from re-exports inside compile packages): `import { C } from 'lib3'` where `lib3/index.js` does `export { C } from './c.js'` produced an empty object on `new C()` because `lib3/c.js` never entered `ctx.native_modules`. The relative-import branch of `resolve_import` (`crates/perry/src/commands/compile/resolve.rs`) classified `./c.js` as `ModuleKind::Interpreted` whenever `is_in_compile_package` returned false — and that helper checks only the literal `node_modules/<pkg>/` substring. `file:./` deps and symlinked package roots both canonicalize to paths *outside* any `node_modules/` segment, so even though `compile_package_dirs["lib3"]` was already populated by the bare-specifier resolution of `'lib3'`, the relative import inside the package looked external. Fix mirrors the membership check that `collect_modules.rs` already uses for the entry-file classification — accept either the substring match OR `canonical.starts_with(dir)` for any value in `compile_package_dirs` (excluding nested `node_modules/`). End-to-end: minimal repro `lib3/index.js` re-exporting `class C` from `lib3/c.js` now constructs C instances with the constructor body actually running and `c.test()` dispatching to `C.prototype.test`. Hono logger middleware (issue #486) now progresses past the previous `(number).replace is not a function` failure that was rooted in this same missing-class-metadata path. Five sanity tests (test_inheritance, test_gap_class_advanced, test_gap_closures, test_gap_async_advanced, test_edge_promises) all pass.
- **v0.5.616** — Refs #420 (drizzle-orm continued — squashed batch covering 7 followup fixes that together get drizzle's `pgTable` schema construction byte-for-byte matching Node and column-builder dispatch through cross-module 3-level inheritance chains): **(a)** `crates/perry-stdlib/src/common/dispatch.rs`'s v0.5.608 Web Fetch handle dispatch made unconditional calls to `crate::fetch::*`, but `pub mod fetch;` is gated on `feature = "http-client"` — when auto-optimize compiled perry-stdlib for drizzle (which doesn't use http-client), 4× E0433 errors appeared and the build silently fell back to prebuilt libs. Wrap the four calls in `#[cfg(feature = "http-client")] { ... }`. **(b)** `compile.rs::imported_vars` lookup at the cross-module Call site keyed `(resolved_path_str, exported_name)` against `exported_var_names`, but the latter is keyed by ORIGIN path. drizzle exports `pgTable` from `pg-core/table.js` and re-exports through `pg-core/index.js`; user code imports from `drizzle-orm/pg-core` which resolves to `index.js` — the lookup `(index.js, "pgTable")` missed and the cross-module call took the direct-getter-call path which treated the zero-arg getter's return value AS the call result. Fix walks `all_module_exports` to find the ORIGIN path before the lookup. Filtered the synthetic `__computed_field_<lo>_<hi>` names out of `ImportedClass.field_names` and `field_types` at all 6 population sites (12 filters) so they don't surface as enumerable own props on cross-module instances. **(c)** `class_field_global_index` in `type_analysis.rs` walked `class.fields.len()` and `class.fields.iter().position(...)` UNFILTERED, but the inline-slot layout (`packed_keys`) skips computed-key fields per v0.5.614/v0.5.615 — for any class mixing computed-key + regular fields, the slot index used for `this.field = X` writes shifted past where readers look, surfacing as `instance.field === number 0`. Filter `f.key_expr.is_some()` in both the parent-count walk and own-fields position loop. **(d)** Cross-module super() chain through ctor-less ancestors. Per JS spec, `class Mid extends Base {}` (no ctor) gets a default `constructor(...args) { super(...args); }`. perry's `Expr::SuperCall` only inlined the IMMEDIATE parent's constructor body — drizzle's `PgSerialBuilder extends PgColumnBuilder extends ColumnBuilder` (only ColumnBuilder defines `this.config = {…}`) silently produced `instance.config === number 0`. Fix walks past empty-bodied ancestors and through cross-module ctors with `param_count==0` until an ancestor with a real ctor is found, then forwards args. `compile_method` standalone-ctor emission adopts the closest ancestor-with-ctor's params as the synthesized signature so the LLVM function has slots for the args; emits a call to the parent's standalone ctor with all args forwarded. **(e)** `lower_new` for a class without own ctor walked the parent chain looking for an inlineable body; when the chain ran through ctor-less imported stubs (drizzle's `class PgSerial extends PgColumn extends Column` where PgColumn is imported with `constructor: None` on the consumer side and Column has the real body), the fallback at `imported_class_ctors.get(class_name)` missed because `class_name` is the LOCAL class. Fix walks past empty-bodied ancestors AND through cross-module ctors with `param_count==0` until an ancestor with a real cross-module ctor is found, then dispatches there. **(f)** Inherited methods in dynamic-dispatch table. The `obj.method()` dispatch tower for `obj: any` only registered classes that DIRECTLY declared the method — subclasses inheriting weren't represented. Walk every `class_id`'s parent chain to find the first ancestor that declares the method, registering `(start_cid → ancestor's fn_name)` so inherited dispatch works. **(g)** Runtime `js_native_call_method`'s vtable lookup walks parent chain. For receivers whose class isn't in the importing module's `class_ids` (return values of not-by-name-imported functions), the codegen-side dispatch tower has nothing to match. Fix walks `get_parent_class_id` chain up to 32 levels in both vtable-lookup sites of `object.rs`. End-to-end: drizzle's `serial("id").primaryKey()` now returns the same instance with `config.primaryKey: true`; probe7's `users.id` shows full real column data ({name:'id', primary:true, dataType:'number', columnType:'PgSerial', config:{...}}) matching Node byte-for-byte. The minimal 3-level cross-module repro (`Sub extends Mid extends Base` with Mid in a separate file) also matches Node. test_query.ts still SIGSEGVs because `eq(users.name, "alice")` uses drizzle's `sql\`...\`` tagged template — which itself works in single-file but the function-property assignment pattern `((sql2) => { sql2.empty = empty })(sql)` isn't supported (perry function values have no associated property storage); the `sql.empty()` call returns `number 0` cross-module. Function-property assignment is a separate feature gap, not the cross-module class chain — tracked as next iteration.
- **v0.5.615** — Refs #420 (drizzle-orm followup — cross-module static class fields): when `class Sub extends Base` is in a different file from `Base`, child-class declarations like `[Base.Symbol.X] = init` produced `undefined` because (1) HIR lowering at `expr_member.rs:323` only routes `obj.prop` through `Expr::StaticFieldGet` when `ctx.has_static_field(obj_name, prop)` returns true, and `class_statics` is per-module — imported classes' static fields aren't tracked, so cross-module `Base.Symbol` lowered as `PropertyGet { ExternFuncRef("Base"), "Symbol" }` instead. (2) Even when the lowering DID emit `StaticFieldGet`, the codegen lookup against `ctx.static_field_globals` missed because that map is populated only from `hir.classes` (this module's classes) — imported classes' static fields had no entry, falling back to `0.0`. (3) Source-side static-field globals were `internal` linkage, invisible to other modules' IR even after a declaration. Three coordinated fixes: **(a)** flip `add_internal_global` to `add_global` (external linkage) for the source module's static-field globals. **(b)** Add `static_field_names: Vec<String>` to `ImportedClass` (populate at all 6 sites in `compile.rs`); declare external globals on the consumer side and register in `static_field_globals`. **(c)** `Expr::PropertyGet` codegen — when receiver is `ExternFuncRef("Base")` and `(Base, prop)` is in `static_field_globals`, route to the global load.
- **v0.5.614** — Refs #420 (drizzle-orm compile-end-to-end shake-out): two coordinated fixes for class declarations whose static initializers depend on top-level consts AND for computed-key class fields. (a) Computed-key class fields like `[Parent.Symbol.X] = init` and `[SymA] = []` were previously **silently dropped** at AST→HIR lowering — `lower_decl.rs` matched only `PropName::Ident` / `PropName::Str` and `continue`d on `PropName::Computed`. drizzle's `Table` and `PgTable` use this shape ~6 times (`[Table.Symbol.ExtraConfigBuilder]`, `[Table.Symbol.ExtraConfigColumns]`, `[TableName]`, `[Schema]`, `[Columns]`, …); without the field declarations reaching codegen, the constructor preamble had nothing to emit and `instance[Symbol] === undefined` for every Symbol-keyed field. Fix: extend `ClassField` with `key_expr: Option<Expr>` (the lowered key expression for computed-key fields; `name` becomes a synthetic placeholder `__computed_field_<lo>_<hi>` for HIR identity), update `lower_class_prop` to handle `PropName::Computed` (lowers the key expression), remove the skip in `lower_class_from_ast`, and update `apply_field_initializers_recursive` in `lower_call.rs` to emit `Expr::IndexSet { This, key_expr, init }` instead of the string-keyed `PropertySet` for computed-key fields. The IndexSet path already dispatches Symbol-keyed writes through `js_object_set_symbol_property`. (b) Static field initializers that reference top-level consts (drizzle's `static Symbol = { Schema, Columns, ... }` where `const Schema = Symbol.for("...")` is declared earlier in the same module) were initialized to `undefined` because `init_static_fields` in `codegen.rs` runs ALL static fields at the very top of `main`, before any user `Let` bindings have run — so the static-field-init lowered `lower_expr(Schema)` to a load of `@perry_global_*` whose value was still 0.0. Fix: at `lower.rs::ast::Decl::Class` lowering (both export and non-export arms), inject `Stmt::Expr(Expr::StaticFieldSet { class_name, field_name, value })` into `module.init` for each static field with an init, AT THE POSITION the class declaration appears in source order. The upfront `init_static_fields` pass stays — it's now harmless duplicate work that the inline statements correctly overwrite once their referenced consts are bound. The two fixes together unblock drizzle's `pgTable("users", { id: serial(...).primaryKey(), name: text(...) })` schema construction so columns actually attach to the returned table object. End-to-end smoke verified with the issue-#420 minimal repro: `c[SymCol]` returned `"col-default"` matching Node (was `undefined` pre-fix). Drizzle's full query-builder still has additional blockers (next iteration: `Object.assign(target, src)` doesn't mutate target — separate spec violation).
- **v0.5.613** — **Closes #421** (hono compiles + runs end-to-end via `compilePackages`): `await app.fetch(req)` returns a Response with `status: 200` and `body: 'hello'` for the canonical `app.get('/', c => c.text('hello'))` repro. Three remaining bugs fixed this release: (1) `console.log(<Web Fetch handle>)` and `<handle> instanceof Promise` segfaulted because `format_jsvalue` and `js_instanceof` dereferenced the small registry-id payload as an ObjectHeader pointer (reading `obj - 8` for the GC type tag landed on unmapped memory). Both now skip handle-shaped pointers (`< 0x100000` after POINTER_TAG strip). (2) `dispatch_request_property` returned `Some(undefined)` for unknown properties on a known-Request handle, but Web Fetch handle id namespaces are disjoint between registries (Request id 1 ≠ Response id 1), so `response.status` on a Response handle whose id collided with a Request id resolved through Request first and shadowed the legitimate Response read. All four `dispatch_*_property` arms now return `None` for unknown properties so the dispatcher falls through. (3) Method dispatch (`response.text()` / `response.json()` / `headers.get(k)` / etc.) on any-typed Web Fetch handles wasn't wired into `js_handle_method_dispatch` at all — `await res.text()` returned NaN. Added `dispatch_response_method` / `dispatch_blob_method` / `dispatch_headers_method` in fetch.rs (each with the same membership + name-gate discipline as the property dispatchers) and wired them in. End-to-end: hono's full request → handler → Response → text() chain now works for the canonical 4-line program; further hono surface (middleware, routes with params, mergePath chain, JSON responses, custom headers) is beyond this release's scope but the structural blockers are gone. Parity: 210 pass / 3 known fails / 13 skipped (98.6%, +1 vs main; the 3 fails are pre-existing `known_failures.json` entries — `test_edge_destructuring`, `test_edge_objects_records`, `test_gap_object_methods`).
- **v0.5.612** — Refs #421 (two more hono internals fixes): (1) v0.5.607's any-typed string-method dispatch tower returned `JSValue::int32(i).bits()` (NaN-boxed INT32_TAG) for `indexOf`/`lastIndexOf`, but INT32_TAG values are NaN — comparisons like `idx < url.length` always returned false because NaN comparisons are false. Hono's `getPath()` does `for (; i < url.length; i++)` after `i = url.indexOf("/", colonIdx + 4)` — the loop never iterated, getPath returned empty string, the route never matched, dispatch fell through to compose, "Context is not finalized" fired. Fix: return `i as f64` directly (matches typed-path's `sitofp` from `lower_string_method.rs`). Same pattern for `lastIndexOf`. Also added `replace`/`replaceAll` arms to the same dispatch tower (string + RegExp pattern; function-replacement deferred). (2) Closures called with fewer args than declared got garbage in missing slots — calling `c.text('hello')` (1 arg) on a `(text, arg, headers) => …` arrow stored as a class field went through `js_native_call_method` field-scan → `js_native_call_value(..., args_len=1)` which transmuted func_ptr to a 1-arg signature; the closure body's args 2 and 3 read from random stack registers. Hono's `c.text`'s `headers` slot evaluated to a tiny denormal float, fast-path condition `!headers` was false, slow-path `setDefaultContentType(TEXT_PLAIN, headers)` ran and the resulting header object was processed through `responseHeaders.set(k, v)` which then hit the issue #510 catch-all `(number).set is not a function`. Fix: new `CLOSURE_ARITY_REGISTRY` thread-local in `crates/perry-runtime/src/closure.rs` mapping non-rest closures' func_ptr → declared arity, populated at module init via new `js_register_closure_arity` extern (parallel to the existing closure-rest registry). `js_native_call_value` consults the registry; when the closure's declared arity exceeds `args_len`, pads trailing slots with TAG_UNDEFINED and dispatches to the matching higher-arity `js_closure_callN` (1-8). Codegen `crates/perry-codegen/src/codegen.rs` builds a `closure_arities: HashMap<u32, u32>` alongside `closure_rest_params` and `emit_string_pool` registers each entry. End-to-end: hono's `app.fetch(req)` runs through `c.text` → `new Response('hello')` correctly returning a Response (was hitting "Context is not finalized" → `(number).set` chain pre-fix). Remaining hono blocker is a SIGSEGV deeper in the fetch dispatch chain — separate, distinct from the arity / int-tag / handle-NaN-boxing fixes. Parity: 209 pass / 4 known fails / 13 skipped (98.1%, no regressions vs main).
- **v0.5.611** — Closes #531: `ops.push(await runOp('name', N, async () => ...))` silently elided the inner closure's body — `compute_max_func_id` / `compute_max_local_id` in `crates/perry-transform/src/generator.rs` had no `Expr::ArrayPush` / `Expr::ArrayPushSpread` arms, so the user closure buried inside `ArrayPush.value: Await(Call { args: [..., Closure { func_id: K, ... }] })` was invisible to both scanners. The generator transform's `next_func_id` started below K, and the synthesized `__async_iter` `next`/`return`/`throw` closures got func_ids that collided with K. Codegen emits one LLVM function per func_id, so one definition wins and the other closure invokes it through a mismatched-shape capture array — calling `js_box_set` with a NULL or stale pointer (the bench's `LocalSet(__gen_done, true)` writes to whatever slot ended up in the capture position). Symptom in the @perryts/mongodb bench: `findOne_by_id_movie` and `find_movies_*` op closures' bodies never executed, with 3× `[PERRY WARN] js_box_set/get: invalid box pointer 0x0` warnings during their warmup loops. Fix: add `Expr::ArrayPush { value, .. }` and `Expr::ArrayPushSpread { source, .. }` arms to both scanners (mirrors the existing #154 / #393 / #212 arms for ArrayForEach/NativeMethodCall/etc.). Verified: bench's 9 ops now all execute correctly with 0 warnings, fingerprints match (find_movies = count 50, was count 1). Parity: 209 pass / 4 known fails / 13 skipped (98.1%, no regressions vs main).
- **v0.5.610** — Refs #421 (followup to v0.5.608): `recv.method(...args)` on any-typed receivers silently no-op'd — `Expr::CallSpread`'s closure-callee fallback evaluated `recv.method` as a property read which returned `undefined` for class-prototype methods, and `js_closure_call_apply_with_spread` then no-op'd. Hono's SmartRouter.match's inner `router.add(...routes[i])` hit this — inner routers never received the route entries, so match returned empty `[[],[]]` even when routes were registered. Fix: new `recv.method(...args)` arm in `Expr::CallSpread` (`crates/perry-codegen/src/expr.rs`) detects PropertyGet callees and routes through the new `js_native_call_method_apply` runtime helper which materialises a JS args array into a temp f64 buffer and forwards to `js_native_call_method`. Plus `replace`/`replaceAll` added to the any-typed string method dispatch tower (`crates/perry-runtime/src/object.rs`) — pattern can be string or RegExp; replacement must be string. Detect RegExp pattern via NaN-box POINTER_TAG check. Function replacements deferred (need closure dispatch). Hono's `await app.fetch(req)` now runs through the dispatch chain past route matching; remaining errors are deeper closure-box / context-finalization issues in hono internals (separate bug classes — pre-existing). Parity: 209 pass / 4 known fails / 13 skipped (98.1%, no regressions vs main).
- **v0.5.609** — Closes #528 (followup to #515/#518): chained-call array-method fold leaked through the `recv_is_class` `_ => false` catch-all — `this.col().find({})` got rewritten as `Expr::ArrayFind(this.col(), {})` and `js_array_find` read garbage from the user object's header. New `Call(inner)` arm in `recv_is_class` only allows the fold when the inner call's method is a known array-producing builtin; otherwise bails to runtime dispatch.
- **v0.5.608** — Refs #421: Phase 1 of handle-NaN-boxing unification — Web Fetch (Request/Response/Headers/Blob) handles now NaN-boxed at the FFI boundary; 25 fetch FFI fns migrated, 4 untyped property dispatch helpers added. Unblocks hono `request.url` in untyped position.
- **v0.5.607** — Closes #529 (followup to #515): `obj["method"](args)` / `obj["prop"]` on a class instance returned `undefined` — fold computed `MemberProp` with non-numeric string key to `Expr::PropertyGet` so dispatch hits the vtable; mirror fold in assignment form.
- **v0.5.606** — Closes #526: escape JS reserved words used as method names in generated `.d.ts` (axios.delete now emits `function _delete; export { _delete as delete }`).
- **v0.5.605** — Closes #515 (round 2): tighten `with`-method fold gates for object-literal / chained-method-call / typed-array receivers; add layered codegen + runtime fallbacks.
- **v0.5.604** — Closes #525: unify R005/#463 unimplemented-API message format for the 2-deep native-module call form (`m.method()`).
- **v0.5.603** — Refs #519/#421: register Request/Response/Headers typed-parameter annotations as native instances at HIR lowering so codegen's static dispatch fires.
- **v0.5.602** — Closes #509: parallel-codegen race producing colliding `.o` files — temp path nonce now uses an atomic counter alongside pid+nanos.
- **v0.5.601** — Closes #519: bind `this` for `instance.fn(args)` style cross-instance method calls via thread-local `IMPLICIT_THIS`; fill string-receiver `match`/`matchAll`/`search` arms.
- **v0.5.600** — Closes #461: pre-register `const f = (...) => …` arrow declarations as locals before lowering their initializer, so nested closures referencing `f` find it via `lookup_local`.
- **v0.5.600** — Closes #513: API manifest now enumerates every `NATIVE_MODULES` entry + every well-known binding (~250 entries backfilled) so the unimplemented-API gate flips strict for every supported module.
- **v0.5.599** — Closes #518: async class method's `await m.toArray()` returned `[]` because scalar-replacement of non-escaping object literals dropped the receiver-as-`this` for `js_native_call_method`.
- **v0.5.598** — Closes #512: API manifest now carries real param/return type signatures (`bcrypt.hash(password: string, …)`) instead of `(...args: any[]): any` fallback.
- **v0.5.597** — Closes #511 (followup to #462): `x.foo()` on undefined/null now throws `TypeError` and exits 1 (was silent no-op exit 0).
- **v0.5.596** — Refs #519: vtable `this` now NaN-boxed with POINTER_TAG before f64 cast so dispatched method bodies see a real instance pointer.
- **v0.5.595** — Closes #515: a class method named `with` with ≥2 params was getting folded into `Expr::ArrayWith` — gate the fold catch-all on `!recv_is_class`.
- **v0.5.594** — Closes #514: `s[0]` / `s.at(-1)` on a `(s: any)` parameter holding a string now correctly indexes via `js_dyn_index_get`; runtime tag-aware dispatch added for any-typed string methods.
- **v0.5.593** — Closes #510 (followup to #462): calling a method on a primitive whose name doesn't resolve now throws `TypeError: <kind>.<prop> is not a function` (was silent no-op).
- **v0.5.592** — Closes #471: `Record<number, T>` writes via `obj[i] = v` corrupted the heap — replace inline `obj+8+idx*8` store with type-dispatching `js_object_set_index_polymorphic`.
- **v0.5.591** — Refs #421/#420: cross-module `Call { callee: ExternFuncRef }` for an imported VARIABLE binding now fetches the closure value via the zero-arg getter and dispatches through `js_closure_callN`.
- **v0.5.590** — Closes #490: cross-platform system-tray icon API (`trayCreate`/`traySetIcon`/`traySetTooltip`/`trayAttachMenu`/`trayOnClick`/`trayDestroy`) on macOS / Windows / Linux + iOS/tvOS/visionOS/watchOS/Android stubs.
- **v0.5.589** — Closes #493: closure rest-param bundling now happens at runtime via a `func_ptr → fixed_arity` registry, fixing dynamic-dispatch call sites where codegen can't statically bundle.
- **v0.5.588** — Closes #507: perry-ext-net `Handle::current()` panic — auto-optimize now rebuilds tokio-using ext bindings into the same target dir as perry-stdlib so cargo unifies tokio compilations.
- **v0.5.587** — Closes #458/#491: WAV recording (`audioSetOutputFilename`/`audioStartRecording`/`audioStopRecording`) implemented on every UI backend (macOS/iOS/tvOS/visionOS/watchOS/Android/Windows; Linux+Web shipped in #458).
- **v0.5.586** — Closes #485: cross-module subclass field initializers + arrow class fields installed in parent ctor body now correctly inherit (unblocks hono `app.fetch` chain).
- **v0.5.585** — Refs #139: floating-point fast-math (`reassoc + contract` FMF) is now opt-in via `--fast-math` / `PERRY_FAST_MATH=1` / `package.json`. Default OFF means bit-exact f64 with Node on most code.
- **v0.5.584** — Closes #484: rest-parameter bundling for class method dispatch — parallel `method_has_rest` map populated alongside `method_param_counts`, threaded into the call site.


Older entries → CHANGELOG.md.
