# node:url granular parity suite

Focused Node.js parity cases for Perry's `node:url` compatibility layer.

These cases cover WHATWG URL/URLSearchParams behavior, module-level helpers, legacy URL APIs, and import shapes. They avoid host-specific path expectations where possible.
